# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-12 by CLAUDE_CODE (Opus 4.7 — Phase 8 Batch 2 Tier-1 seed-rotation tech debt cleared on branch fix/seed-password-from-env, squash-merge pending.)

PHASE:        Phase 8 Batch 2 — MAP FEATURE GROUP COMPLETE + Tier-1 seed-rotation tech debt cleared. Phase 8 Batch 2 backlog now reduced to: Notification.patrolId FK, real-time SSE (Tier 3), exports, alert history log.

LAST_DONE:    Seed-password rotation. Branch `fix/seed-password-from-env` (squash-merge pending). `packages/db/prisma/seed.ts` rewritten: removed hardcoded `WEBMASTER_PASSWORD` constant and `"DemoAdmin2024!SecurePass"` literal, added `requireEnv()` helper that throws on missing env, both user upserts now use `update: { passwordHash }` so re-seeding rotates. Added `WEBMASTER_PASSWORD` + `DEMO_SITE_ADMIN_PASSWORD` to .env.dev / .env.staging / .env.prod (unique 22-char openssl values per env, never reused) + .env.example placeholders. CREDENTIALS.md First Admin Account section rewritten as two per-env tables (Webmaster + Demo Site Admin). Rotation verified: `bcrypt.compare(process.env.WEBMASTER_PASSWORD, user.passwordHash)` returns true for both accounts after re-seed. Typecheck clean (6 packages), lint clean (5 packages), 32/32 tests pass. Lifts observation 292/391 — no longer a 🔴 gotcha; recorded as 🟢 change in lessons.md.

              MAP FEATURE GROUP COMPLETE (PRODUCT.md L191 "Geospatial Operator View" fully satisfied):
              - Tile basemap (CARTO, centered Banda Sea [127.5, -2.5] zoom 6) — sub-session 1.2b/1.2c
              - Subjects markers (emerald-500 circles, gray-400 stale) — commit f041215
              - Events markers (45°-rotated priority-colored squares) — commit f041215
              - Patrol tracks (selectable via shadcn Select dropdown, mapcn MapRoute line) — commit e1e2d8a
              - Patrol-area polygons (fill + outline per colorHex) — commit 7250039

              Opus-direct execution for seed rotation — Tier 1 task, ~5 files, well under thrashing threshold; no subagent dispatch needed.

NEXT:         Phase 8 Batch 2 remaining backlog (seed rotation now shipped):
              (next-1) Spec deferral #3 — Notification.patrolId FK migration + UI wiring (PRODUCT.md L187). Migration + router update + notification-center click-through. Tier 2.
              (next-2) Real-time notifications (WebSocket/SSE) — Tier 3, mandatory split per memory-governance.md §1.
              (next-3) PDF/CSV exports — Tier 2 per entity.
              (next-4) Spec deferral #1 — Alert history log (now meaningful since engine fires on real events).

              Open spec deferrals unchanged: (a) Alert history log PRODUCT.md L182, (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, (c) Notification.patrolId FK click-through PRODUCT.md L187.

BLOCKERS:     none. All Docker services healthy. CI lint gate passes on main. Worker container running cleanly.
GIT_BRANCH:   fix/seed-password-from-env (squash-merge to main pending; will be 5th commit ahead of origin/main after merge — e1e2d8a + 1b37dfe + 7250039 + b8ea313 + this seed-rotation squash. Push pending.)
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
TOKEN_ESTIMATE:    ~25K consumed (orientation + 5 source/env file edits + 1 governance write cycle)
CHECKPOINT_TYPE:   full
FILES_TOUCHED:     packages/db/prisma/seed.ts (modified — env-sourced + rotation upsert), .env.dev / .env.staging / .env.prod (modified — added WEBMASTER_PASSWORD + DEMO_SITE_ADMIN_PASSWORD), .env.example (modified — placeholders), CREDENTIALS.md (modified — First Admin Account rewritten as per-env tables), .cline/memory/lessons.md (appended 🟢 change entry), docs/CHANGELOG_AI.md (appended entry), docs/IMPLEMENTATION_MAP.md (header + Next Step updated), .cline/STATE.md (rewritten — this file).
TIER_CLASSIFICATION: 1 — lightweight (1 source file + 4 env files + CREDENTIALS.md + 3 governance docs; single-purpose tech debt cleanup)

MODELS:
  planning:   claude-opus-4-7 (Architect — V31 primary)
  execution:  claude-opus-4-7 (Opus-direct — Tier 1 task)
  governance: claude-opus-4-7 (this session)
