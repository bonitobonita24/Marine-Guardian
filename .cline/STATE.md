# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-30 14:40 GMT+8 by CLAUDE_CODE (Opus 4.7 architect + Sonnet 4.6 executor)

PHASE:        Phase 7 Feature Update — Super Admin Panel backlog item 4
              FULLY CLOSED (feature c20190e + Tier 1 follow-up #1
              "requireRouteAuth impersonation cookie support" aa8fbef)
              on origin/main. PRODUCT.md §210 line 216 "Manage button to
              enter tenant context" verified end-to-end on dev container.
              SSE notifications + CSV/PDF exports now work for
              impersonating super_admins.

LAST_DONE:    Item 4 follow-up #1 shipped + pushed:
                aa8fbef  feat(route-auth): honor impersonation cookie
                         for super_admin SSE+exports
                Branch:  feat/route-auth-impersonation
                         (squash-merged + deleted)
                Files:   apps/web/src/server/lib/route-auth.ts  37→78L
                         apps/web/src/server/lib/route-auth.test.ts NEW 156L
                           (9 vitest cases — RED→GREEN cycle)
                         apps/web/src/test-setup/next-headers-mock.ts NEW 22L
                         apps/web/vitest.config.ts +1L (setupFiles entry)
                Net:     +220 / -8 across 4 files

              Behavior change (mirrors protectedProcedure in trpc.ts):
                super_admin + sessionTenantId === '' + valid
                mg-impersonate-tenant cookie → cookie value becomes
                ctx.tenantId. Non-super_admin roles ignore the cookie
                (defense-in-depth). RouteAuthContext gained
                isPlatformImpersonating: boolean.

              Verification (Opus-side):
                pnpm typecheck    → 0 errors
                pnpm vitest --run → 661/661 pass (was 652)
                                    9 new route-auth cases
                                    69 test files total

              ⚠ Global next/headers vitest mock added — future route
                tests calling requireRouteAuth() directly inherit
                no-cookie default via test-setup/next-headers-mock.ts.
                Override per-file via vi.mock("next/headers", ...) if a
                test needs to simulate a present cookie.

              Recent commits on main:
                aa8fbef  feat(route-auth): impersonation cookie support ← THIS
                2851eb9  Refactor code structure for improved readability
                206c0da  chore(state): session checkpoint — Item 4 QA
                272f197  docs(governance): Item 4 Visual QA results
                c9212d5  chore(state): session checkpoint — Item 4 shipped
                50aee47  docs(governance): Item 4 super_admin switching
                c20190e  feat(admin): super_admin tenant context switching

KNOWN FOLLOW-UPS (not blocking — items 5/7/8 remain on backlog):

  1. 🟢 Banner not mounted on /admin/* routes. Currently only in
     (dashboard)/layout. UX gap when super_admin on /admin/tenants
     while impersonating — no visual reminder of current tenant. Minor
     UX polish, low priority.

  2. 🟢 Audit-while-impersonating: super_admin's actions on tenant
     resources during impersonation logged with userId=super_admin and
     tenantId=impersonatedTenantId via L5 audit. Forensic trail intact
     but does not flag "this action was performed by an impersonating
     platform admin". Adding an `impersonatedBy` field to audit_logs or
     augmenting audit writes with a context flag would close the gap.
     NOT in §210 scope.

  3. 🟢 Governance docs (CHANGELOG_AI + IMPLEMENTATION_MAP + lessons.md)
     lag aa8fbef by one commit. Batch into next ship (Item 5 onward)
     to amortize Sonnet dispatch overhead.

NEXT:         Backlog items 5, 7, 8 remain. Recommended order:
                5 (schedule conflict detection) — Item 4 unblocks this
                  because conflict detection runs in tenant context
                7 (Patrols+Subjects data table polish)
                8 (sidebar role-aware) — lowest priority

              Tier 1 follow-up after items 5/7/8:
                /admin/users page UI on top of platformUser.* router
                (mirrors /admin/tenants pattern with Manage column too).

BLOCKERS:     none
GIT_BRANCH:   main (origin/main now at aa8fbef — pushed 2026-05-30 14:40)
PORTS:        APP=45204 (CORRECTED from prior stale 43000)
              DB=45194 PGBOUNCER=45195 VALKEY=45196
              MINIO_API=45197 MINIO_CONSOLE=45198
              MAILHOG_SMTP=45199 MAILHOG_UI=45200
              (per docker port mapping; APP container internal 3000)
MODELS:
  planning:   claude-opus-4-7 (architect)
  execution:  claude-sonnet-4-6 (V32 zero-Opus-execution)
  governance: claude-sonnet-4-6 (same dispatch as execution)

DEV WORKFLOW NOTE (locked 2026-05-30):
  Dev compose uses production multi-stage Dockerfile — NO source mount.
  Source changes do NOT reach the container until rebuild + recreate.
  Standard QA pre-flight after a feature ship:
    docker compose -f deploy/compose/dev/docker-compose.app.yml build app
    cd deploy/compose/dev
    set -a; source ../../../.env.dev; set +a
    docker compose -f docker-compose.app.yml up -d --no-build --force-recreate app
  Container Ready logs appear in ~200ms after recreate. Then navigate
  to http://localhost:45204 for QA.
