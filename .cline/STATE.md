# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full Auto — Milestone C build)

PHASE:        Phase 7 — MILESTONE C BUILD COMPLETE (custom-role permission-matrix layer)
LAST_DONE:    Built the data-driven custom-role RBAC layer (fleet Tenant-RBAC standard §4),
              below the 3 fixed tiers. LOCAL feature branch feat/tenant-rbac-custom-role-matrix
              @ 82b89c5 (NOT pushed). 23 files, +2359 lines, 8 dispatched Sonnet tasks, all
              PM-verified against the FULL hard gate — GREEN:
                web typecheck 0 · db typecheck 0 · next build OK · 1781/1781 web tests ·
                turbo lint 5/5. (check-product-sync N/A — not wired in this repo.)
              What shipped:
                - schema: CustomRole + RolePermission (strict view/write/update/delete) +
                  nullable User.customRoleId FK (enum UNCHANGED). Migration
                  20260711090000_add_custom_role_permissions (CREATE TABLE x2 + ADD COLUMN + FKs).
                  NOT yet applied to any live DB (deploy owner-gated).
                - apps/web/src/lib/rbac/feature-registry.ts (canonical, client-safe, per-nav-href;
                  reserved non-grantable: users/settings/billing/profile) + has-permission.ts
                  (DB-backed resolver, deny-by-default, hard-clamped <= tenant_admin ceiling).
                - auth: JWT/session carry customRoleId + coarse customRolePermissions; ctx.customRoleId.
                - 3 enforcement surfaces: matrixProcedure(feature,action) tRPC factory (rbac.ts);
                  customRole router (list/getById/create/update/delete/assign/unassign, gated to
                  tenant_superadmin + platform tenant_manager, audited) registered in routers/index.ts;
                  middleware.ts coarse view-gate; sidebar.tsx nav view-filtering. Enum-role behavior
                  100% unchanged (verified by existing suites still green).
                - UI: tenant_superadmin-only Role Builder under /settings/roles (checkbox matrix,
                  shadcn only) + role-list + entry link from Settings page.
              Design [HOW] decisions locked (logged in DECISIONS_LOG.md): per-href feature
              granularity; custom-role users anchored role=tenant_admin + customRoleId (zero enum
              change); read-only features expose only "view"; Billing/User-Mgmt hard-reserved;
              cross-tenant = tenant_superadmin builds in own tenant, tenant_manager via impersonation;
              middleware coarse-JWT gate + authoritative DB matrixProcedure for fine CRUD.
NEXT (STEP 1 — TEST, un-gated): apply migration 20260711090000 to LOCAL dev DB, rebuild/run dev app
              off this branch, then Visual QA the Role Builder E2E as tenant_superadmin (create a
              custom role, assign to a user, confirm the assigned user sees only granted nav +
              is FORBIDDEN on ungranted tRPC actions). The 3 UI files (page/list/form) have no
              render test yet — Visual QA closes that gap. (Rule 16 major-update Visual QA.)
NEXT (STEP 2 — DEPLOY, AUTHORIZED once tested): the loop handoff records the owner's EXPLICIT word
              — owner: "build the C and make it push too once tested." So deploy of Milestone C IS
              authorized, CONDITIONAL on STEP-1 testing passing green. This satisfies the
              tenant-rbac-standard "explicit owner word" requirement + the branch-only HARD POLICY
              (approval granted). When STEP 1 is green: merge feat/tenant-rbac-custom-role-matrix ->
              main -> push (CI builds image) -> staging data-first gate (staging-refresh-and-deploy.sh:
              prod-copy -> pull image -> migrate) -> verify -> prod (backup + pull + prisma migrate
              deploy) -> demo (push-to-demo.sh: migrate, NEVER reseed) -> rebuild local dev off main
              (dev-leads-every-env). Tag v1.1.0 (additive MINOR) at prod promotion. Do NOT deploy
              until STEP-1 Visual QA is green.
OWNER-GATED (still, do NOT auto-do): Demo credential cleanup (stale webmaster@marine-guardian.local
              cross-seed + admin@demo.com super_admin-with-tenant_id) — the credential reseed. Distinct
              from the C deploy; re-surface, do not act. See project_marine_guardian_cred_override_verification_0710.
BLOCKERS:     none. Test then deploy per owner's "push once tested." Only the demo cred RESEED is gated.
GIT_BRANCH:   feat/tenant-rbac-custom-role-matrix @ 82b89c5 (LOCAL, unpushed). main clean at 6354e50.
RELEASE:      v1.0.0 shipped (Milestone 1). Custom-role layer will be a MINOR bump (v1.1.0) when
              released — new additive feature, no breaking change. Tag at deploy time, not now.
ENVS:         prod=mg.powerbyte.app  staging=mg-staging.powerbyte.app  demo=mg-demo.powerbyte.app
              tenant slug = `ph` on all envs; sole owner admin@mail.com; platform mgr = tenantadmin@/webmaster@
MODELS:
  planning:   claude-code (Opus)
  execution:  claude-sonnet via Agent(spec-executor)
  governance: gemini-2.5-flash-lite
