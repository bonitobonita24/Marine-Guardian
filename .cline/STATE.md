# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full Auto — Milestone C matrix-enforcement FIX complete)

PHASE:        Phase 7 — MILESTONE C SECURITY FIX DONE + FULLY TESTED → DEPLOY HELD (owner-gated)
LAST_DONE:    STEP 2 (FIX, un-gated [HOW]) COMPLETE + VERIFIED. Committed LOCAL main 8c80186 (NOT pushed).

              ✅ WHAT GOT DONE THIS SESSION (the enforcement-gap fix that held prod):
                - matrixProcedure redesigned (server/trpc/middleware/rbac.ts): now
                  matrixProcedure(base, feature, action) — chains the base enum gate FIRST, THEN
                  the DB matrix check via hasPermission. ZERO enum regression (hasPermission returns
                  true when customRoleId==null → every fixed role incl. platform tenant_manager with
                  empty tenantId passes through unchanged; NO independent tenant-throw added).
                - Wired ALL 117 grantable-feature endpoints across 22 routers, each with the SAME
                  base gate it already used (events/patrols/patrol-areas/patrol-schedule/notifications/
                  fuel/alerts/subjects/dashboard/map/exports/sync). Actions a feature doesn't expose
                  (exports/alerts write/delete) hard-clamp to denied for custom roles (deny-by-default).
                  Reserved surfaces (users/settings/billing/dsr/breach/reportTemplate/platform*) left
                  at enum ceiling — never matrix-gated.
                - New __tests__/matrix-enforcement.test.ts (6 cases: deny-ungranted / deny-view-false /
                  allow-granted / enum-passthrough / hard-clamp-unexposed-action / write-deny-on-view-only).
                - Committed local main 8c80186 (Tier-1, NOT pushed). Rebuilt dev off 8c80186; app+worker
                  HEALTHY; /api/health 200, /ph/login 200 (full wired router tree loads + serves clean).

              ✅ VERIFICATION (high bar — evidence over assertion):
                - Full gate GREEN: typecheck 7/7 · turbo lint 0 (--max-warnings 0) · web tests 1787/1787
                  (1781 + 6 new) · next build OK · product-sync OK.
                - ADVERSARIAL AUDIT (independent Explore reviewer, refute-mandate): 117/117 endpoints
                  wrapped, 0 holes, 0 enum regression, 0 reserved-router mis-wrap, 0 reserved-key misuse.
                - Session→ctx path statically confirmed end-to-end (config.ts reads user.customRoleId →
                  JWT token → session.user.customRoleId → ctx.customRoleId → matrixProcedure → hasPermission).
                - Dev DB migration 20260711090000 confirmed live (custom_roles + role_permissions present).
                - NOTE: credentialed browser assign-role→FORBIDDEN E2E deliberately SKIPPED to avoid pulling
                  a vault password into agent context; substituted by the 6 tRPC-layer enforcement unit tests
                  (which ARE the assign-role→FORBIDDEN-on-ungranted-action proof at the API layer) + static
                  session-wiring verification + prior session's live Role-Builder DB persistence E2E.

NEXT (STEP 3 — DEPLOY): HELD for EXPLICIT owner go. Rationale: deploy-discipline HARD HOLD (staging/prod
              require explicit owner word per tier; prod is NEVER automatic) + the outward-facing, hard-to-
              reverse nature of a production AUTH deploy + the standing guardrail that a prior-recorded
              "push once tested" is NOT live consent. Un-gated work is EXHAUSTED. When owner says go:
              path = data-first staging (deploy/staging-refresh-and-deploy.sh, rebuild image from 8c80186 →
              verify mg-staging) → manual prod promotion → tag v1.1.0. (Push main→origin is the staging line.)

DEFERRED [WHAT] (owner-gated, unchanged): demo super_admin credential reseed (mg-demo carries stale
              webmaster@marine-guardian.local + admin@demo.com — see cred_override memory).

BLOCKERS:     None technical. Milestone C is code-complete + fully tested locally. ONLY the staging/prod
              deploy remains, gated on explicit owner authorization.
GIT_BRANCH:   main @ 8c80186 (LOCAL commit, NOT pushed; contains the enforcement fix on top of dormant C + v1.1.0)
PORTS:        dev app 45204
