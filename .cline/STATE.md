# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-19 01:30 GMT+8 by CLAUDE_CODE (Opus 4.7 — Dashboard 404 root cause FIXED, uncommitted on fix/auth-pin-version)

PHASE:        Auth hotfix branch (fix/auth-pin-version) — Dashboard-404-on-authenticated-access ROOT CAUSE FOUND AND FIXED at runtime, NOT YET COMMITTED. Earlier middleware fix (localePrefix "as-needed" → "never") was insufficient; actual fix was removing the next-intl createMiddleware call entirely. See [[feedback_nextintl_middleware_breaks_static_prerender]] in memory.

RESUME_NEXT_SESSION:
  1. Branch: fix/auth-pin-version. 11 modified files, all related to auth fix chain (bcryptjs migration, next-auth pin, login page refactor, middleware fix, auth config instrumentation). Untracked: packages/db/scripts/verify-webmaster-login.mjs (useful diagnostic — keep).
  2. VERIFIED at runtime in rebuilt container (image dev-latest, May 19 1:25am GMT+8):
     - Unauth /dashboard → 307 → /login?callbackUrl=%2Fdashboard ✅
     - Login POST → 302, session-token cookie issued ✅
     - Authenticated /dashboard /events /users /patrols /map /settings /subjects → ALL HTTP 200 ✅
     - pnpm typecheck clean ✅  pnpm lint clean ✅
  3. NOT YET DONE before commit:
     - Full pnpm test run (211 tests were green pre-this-session; verify still green)
     - Browser visual smoke test (login flow → dashboard render → sidebar/header → one tRPC query loads)
     - Commit message (suggest: "fix(auth): pin next-auth beta.25, migrate to bcryptjs, remove next-intl middleware causing dashboard 404")
     - Squash-merge fix/auth-pin-version → main → delete branch
  4. After auth hotfix lands: resume Phase 8 Batch 3 Sub-batch 3.1 (Users UI) per [[project_marine_guardian_phase8_batch3]] — was on feat/user-management-table (commit 6b0ba78) awaiting visual check.

ROOT_CAUSE_NOTES (for memory + DECISIONS_LOG when sync'd):
  Symptom: webmaster login succeeded server-side (logs showed authorize OK, jwt callback OK, session token issued), but GET /dashboard with valid cookie returned HTTP 404 body of 8,835 bytes — exactly matching _not-found.html size. Other (dashboard) group routes had identical failure.
  Build artifacts WERE correct: /app/apps/web/.next/server/app/dashboard.html (26 KB) contained real <h1>Command Center</h1>, route module at (dashboard)/dashboard/page.js existed, app-paths-manifest mapped correctly.
  Diagnosis: next-intl createMiddleware (with localePrefix:"never") sets response state at runtime that makes Next.js 15 standalone bypass the static prerender file and fall through to _not-found.
  Fix: Removed createMiddleware import + call from apps/web/src/middleware.ts. Auth gate logic preserved. Locale still resolves via apps/web/src/lib/i18n/request.ts (hardcoded "en"). Reintroduce next-intl middleware ONLY when routes are restructured under app/[locale]/.

LAST_DONE:    Phase 8 Batch 3 Sub-batch 1 (Event Detail Modal) shipped via squash-merge to main (commit 3a3b7ae, pushed to origin). Three sub-task commits consolidated:
              - 1.1 (9fd10c3): tRPC backend — event.update + event.addAccompanyingRanger + event.removeAccompanyingRanger procedures on apps/web/src/server/trpc/routers/event.ts. All tenant-scoped. event.update uses .strict() schema with NOT_FOUND on count=0. addAccompanyingRanger has XOR refinement (registeredUserId XOR freetextName). removeAccompanyingRanger verifies tenant ownership before delete. Tests +12.
              - 1.2 (dcfe3bd): UI components — apps/web/src/components/events/{event-detail-modal,accompanying-rangers-input,event-timeline}.tsx (+2 test files). Modal uses shadcn Dialog with title/priority/details(offender/vessel/registration/address/action)/notes-textarea/coordinates/rangers/timeline. AccompanyingRangersInput is dual-mode chip UI (registered user search via user.list + freetext add). Events page wires modal via Maximize2 icon button on each KanbanCard (onPointerDown+onClick stopPropagation so dnd-kit drag still works). Tests +11.
              - 1.3 (71d33cc): EarthRanger push-back — apps/web/src/server/lib/earthranger-push.ts (self-contained PATCH helper, NOT imported from packages/jobs to keep web bundle lean). Wired into event.update mutation as best-effort try/catch; never fails local mutation. Maps {title, priority, eventDetails} → ER {title, priority, event_details}. Tests +8 (6 push-helper + 2 router integration covering happy path + 502 graceful degradation).
              Test deltas across the sub-batch: 140 → 171 (+31). All gates: typecheck ✅ lint ✅ test 171/171 ✅ build ✅. Two-stage review (Rule 25) passed both stages. Also shipped earlier same session: SSE-3 (commit 5b3ef4d — closes Phase 8 Batch 2).

NEXT:         Sub-batch 3 — User Management UI (`/admin/users`). PRODUCT.md scope (lines ~191-198): CRUD users within tenant, assign roles (Command Center Operator / Field Coordinator / Site Admin), activate/deactivate, last login display. Tier 2 estimate (~60K tokens, sub-divide into 3.1 backend user mutations + 3.2 list/CRUD UI + 3.3 role-edit + activate dialogs). Pre-flight: check if user router already exists (apps/web/src/server/trpc/routers/user.ts present from earlier work — survey before scoping). After Sub-batch 3 squash-merges: Batch 3 COMPLETE. Then Batch 4 candidates: Patrol Area Planning, Monthly Accomplishment View, Audit Log Viewer, Settings page.

UNBLOCKED_BY_SUB_BATCH_2:
  - Sub-batch 1 deferred "Reporter → ranger profile link" on EventDetailModal — /rangers/[id] route now exists for deep-linking. Trivial one-Edit follow-up when ready.

LOCKED_DECISIONS_SUB_BATCH_2 (next governance sync should write these to DECISIONS_LOG.md):
  - Reported-event attribution: STRING MATCH Event.reportedByName === KnownRanger.name (case-insensitive). No schema migration.
  - Patrol-led attribution: STRING MATCH PatrolSegment.leaderErId === KnownRanger.erSubjectId. No schema migration.
  - URL param [id] = KnownRanger.id (canonical ranger entity).
  - Sidebar: NO entry for Rangers — deep-link only.
  - Patrol de-dupe: leader + accompanying on same patrol counted ONCE; "patrol-led" wins in activity timeline.
  - Profile deviation: role + assignedArea NOT surfaced (schema lacks fields). Schema migration deferred to focused Phase 7 follow-up if needed.

BLOCKERS:     none

GIT_BRANCH:   main (clean, in sync with origin/main)
GIT_HEAD:     98566d3 feat(rangers): Ranger Detail page with profile, KPIs, event summary, activity timeline (Phase 8 Batch 3 Sub-batch 2)
GIT_HEAD_PREV: bb07060 chore(state): checkpoint after Phase 8 Batch 3 Sub-batch 1 ship

REMOTE_STALE:  origin/feat/event-kanban still exists on GitHub. User has not authorized remote delete. Run `git push origin --delete feat/event-kanban` when ready, then `git fetch --prune`.

DEFERRED_FROM_SUB_BATCH_1 (documented in commit 3a3b7ae body — clean paths, no schema changes needed):
  - Rich text editor for event notes — V1 ships textarea, notesJson schema accepts rich payload later. Recommendation: install Kibo UI editor in Phase 7 as a focused 1-file follow-up.
  - Single-point mini-map for event location — coordinates render as text for V1. Needs a thin mapcn primitive adapter (~30 LOC). Pulled out of this batch to keep scope bounded.
  - Reporter → ranger profile link — modal can't deep-link to ranger detail because Ranger Detail page doesn't exist yet. Will become trivial after Sub-batch 2 ships.

TOKEN_ESTIMATE:      ~140K consumed across full Sub-batch 2 session (architect decomposition + Sonnet 2.1 dispatch+thrash + Opus 2.1 salvage+rewrite + Opus 2.2a + Opus 2.2b + two-stage review + squash-merge orchestration + governance updates). Within Opus 4.7 200K budget but approaching SAFE-zone ceiling — Sub-batch 3 should start in fresh session.
CHECKPOINT_TYPE:     full (per memory-governance.md §2 — 12 files shipped to main + governance updates)
FILES_TOUCHED_SUB_BATCH_2: [
  "apps/web/src/server/trpc/routers/ranger.ts",
  "apps/web/src/server/trpc/routers/__tests__/ranger.test.ts",
  "apps/web/src/server/trpc/routers/index.ts",
  "apps/web/src/app/(dashboard)/rangers/[id]/page.tsx",
  "apps/web/src/components/rangers/ranger-profile-header.tsx",
  "apps/web/src/components/rangers/ranger-kpi-cards.tsx",
  "apps/web/src/components/rangers/ranger-event-summary.tsx",
  "apps/web/src/components/rangers/ranger-activity-timeline.tsx",
  "apps/web/src/components/rangers/__tests__/ranger-profile-header.test.tsx",
  "apps/web/src/components/rangers/__tests__/ranger-kpi-cards.test.tsx",
  "apps/web/src/components/rangers/__tests__/ranger-event-summary.test.tsx",
  "apps/web/src/components/rangers/__tests__/ranger-activity-timeline.test.tsx",
  ".cline/STATE.md"
]
TIER_CLASSIFICATION: Sub-batch 2 was Tier 2 (depth=2, score 56, threshold 40). Architect split into 3 sub-tasks: 2.1 backend (T1, ~20K), 2.2a page shell + 2 components (T2, ~22K), 2.2b 2 components + page wiring (T2, ~22K). 2.1 had one Sonnet thrash → Opus executor salvage. 2.2a + 2.2b ran cleanly as Opus from the start per [[feedback-marine-guardian-opus-executor-default]]. Net tests added: +40 (171 → 211).

PORTS:        APP=45204 DB=42731 CACHE=42733 (unchanged — verify via cat .env.dev | grep _PORT)

MODELS:
  planning:    claude-code (V31 primary — Opus 4.7 architected the Tiered Decomposition split)
  execution:   claude-opus-4-7 (Sub-tasks 1.1 completion + 1.2 + 1.3 + merge — Opus direct execution after Sonnet thrash on 1.1). Framework default for execution is Sonnet 4.6 via Agent(model: "sonnet"); Opus escalation justified per memory-governance.md §2.5b.
  governance:  gemini-2.5-flash-lite (cheapest, non-critical writes)
