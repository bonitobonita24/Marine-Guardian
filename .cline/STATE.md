# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-12 by CLAUDE_CODE (Opus 4.7 — Phase 8 Batch 2 Item 2 map patrol-area polygons shipped, squash-merged to main as 7250039. MAP FEATURE GROUP COMPLETE.)

PHASE:        Phase 8 Batch 2 — MAP FEATURE GROUP COMPLETE. Alerts engine + Interactive Map (basemap + subjects + events + tracks + polygons) all shipped. Phase 8 Batch 2 backlog now reduced to non-map items: seed password rotation, Notification.patrolId FK, real-time SSE (Tier 3), exports, alert history log.

LAST_DONE:    Map patrol-area polygon overlays. Squash commit on main: `7250039 feat(map): render patrol-area polygons as colored overlays`. New component `apps/web/src/components/map/MapPolygon.tsx` (119 lines) — client component using mapcn `useMap()` to imperatively addSource (geojson) + addLayer (fill + line) per area, with full useEffect lifecycle (setup, data update on geojson change, paint update on color/opacity change, cleanup wrapped in try/catch). Modified `apps/web/src/components/map/InteractiveMap.tsx` to add `trpc.map.patrolAreas.list.useQuery({ activeOnly: true })` and render `<MapPolygon>` per area BEFORE `<MapRoute>` (z-order: track line on top of polygons). Prisma Json → GeoJSON.Polygon | GeoJSON.MultiPolygon cast through `unknown`. Typecheck clean, lint clean (after removing react-hooks/exhaustive-deps disable comment for an unconfigured rule and moving an unnecessary type assertion inline), 32/32 tests pass.

              MAP FEATURE GROUP COMPLETE (PRODUCT.md L191 "Geospatial Operator View" fully satisfied):
              - Tile basemap (CARTO, centered Banda Sea [127.5, -2.5] zoom 6) — sub-session 1.2b/1.2c
              - Subjects markers (emerald-500 circles, gray-400 stale) — commit f041215
              - Events markers (45°-rotated priority-colored squares) — commit f041215
              - Patrol tracks (selectable via shadcn Select dropdown, mapcn MapRoute line) — commit e1e2d8a
              - Patrol-area polygons (fill + outline per colorHex) — commit 7250039

              Opus-direct execution for this item — Sonnet bypassed per the thrashing lesson reproduced earlier in this session (Sonnet workspace ≤15K real in this project, not 30K theoretical).

NEXT:         Phase 8 Batch 2 remaining backlog (no map work left):
              (next-1) `fix/seed-password-from-env` (Tier 1, ~3K) — read WEBMASTER_PASSWORD from env, write generated value to CREDENTIALS.md, set update: { passwordHash } on upsert. Lifts the 🔴 gotcha from lessons.md.
              (next-2) Spec deferral #3 — Notification.patrolId FK migration + UI wiring (PRODUCT.md L187). Migration + router update + notification-center click-through.
              (next-3) Real-time notifications (WebSocket/SSE) — Tier 3, mandatory split per memory-governance.md §1.
              (next-4) PDF/CSV exports — Tier 2 per entity.
              (next-5) Spec deferral #1 — Alert history log (now meaningful since engine fires on real events).

              Open spec deferrals unchanged: (a) Alert history log PRODUCT.md L182, (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, (c) Notification.patrolId FK click-through PRODUCT.md L187.

BLOCKERS:     none. All Docker services healthy. CI lint gate passes on main. Worker container running cleanly.
GIT_BRANCH:   main (4 commits ahead of origin/main pre-push: e1e2d8a + 1b37dfe + 7250039 + this governance commit. Push pending.)
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
TOKEN_ESTIMATE:    Opus session ~110K consumed (resume orientation + Sonnet dispatch attempt for patrol tracks + Opus-direct patrol tracks + Opus-direct polygons + 2 governance write cycles)
CHECKPOINT_TYPE:   full
FILES_TOUCHED:     apps/web/src/components/map/PatrolSelector.tsx (new — patrol tracks), apps/web/src/components/map/MapPolygon.tsx (new — polygons), apps/web/src/components/map/InteractiveMap.tsx (modified twice — tracks then polygons), docs/CHANGELOG_AI.md (2 entries added), docs/IMPLEMENTATION_MAP.md (header + map subsection + Next Step rewritten), .cline/STATE.md (rewritten — this file).
TIER_CLASSIFICATION: 2 — moderate (2 features, ~4 implementation files + 3 governance docs)

MODELS:
  planning:   claude-opus-4-7 (Architect — V31 primary)
  execution:  claude-opus-4-7 (Opus-direct for both items; Sonnet bypassed after the patrol-tracks subagent thrash earlier this session)
  governance: claude-opus-4-7 (this session)
