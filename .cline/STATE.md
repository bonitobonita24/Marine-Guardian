# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-29 11:16 GMT+8 by CLAUDE_CODE (Opus 4.7 architect + Sonnet executor)

PHASE:        Phase 7 Feature Update — Patrol Schedule (Gantt) module COMPLETE.
              All sub-batches 7.1a/b/c/d shipped to origin/main. Module satisfies
              PRODUCT.md lines 102-109 (Patrol Schedule with Gantt chart).

LAST_DONE:    7.1d shipped — commit a031a36 on origin/main fixes all 3 Visual QA
              bugs from 2026-05-29 morning QA session:
              - Bug A: Gantt viewport now syncs with period toolbar (scrollToDate
                prop added to GanttProvider, reusing existing getOffset helper).
              - Bug B: Sidebar renders ONE row per ranger (rangerName label) instead
                of one row per schedule (was patrol area name).
              - Bug C: Vendor header literals "Issues"/"Duration" → "Rangers"/"Span".
              Verified end-to-end via Playwright Visual QA on dev container port 45204:
              login site_admin → create Jun 2-5 schedule → all 3 fixes confirmed visually
              → refresh preserves state → 0 console errors.
              Branch feat/patrol-schedule-7-1d squash-merged + deleted local + remote.

NEXT:         Next session opens fresh. Pick from deferred backlog (priority order):
              1. Bug #6 (site-wide): super_admin (webmaster, tenantId=null) hits 403/401
                 on tenant pages. Need layout-level role gate redirect in
                 apps/web/src/app/(dashboard)/layout.tsx — redirect tenantId=null users
                 to a super-admin-only landing or limit them to /users/tenants/etc.
                 Affects ALL tenant pages, not just patrol-schedule.
              2. Bug #7 (site-wide): Seed expansion. Currently 10+ entity types have
                 zero rows on fresh login (PatrolSchedule, Event, Patrol, Subject,
                 FuelEntry, Observation, Alert, AreaBoundary, ReportExport, etc.).
                 Every menu shows empty state — bad first-impression UX.
              3. Super Admin Panel (PRODUCT.md line 210) — cross-tenant ops, narrow scope.
              4. Schedule conflict detection — overlapping ranger assignments, v2 enhancement.
              5. 5.1d Area A re-derive on areaName change — still BLOCKED (ER sync doesn't
                 emit area_name).

              Recommended next: Bug #6 first (small, well-scoped layout fix, unblocks
              super_admin testing); Bug #7 second (medium scope, makes the app demo-ready).

BLOCKERS:     none for backlog items 1-4. (Item 5 still blocked on upstream ER sync.)

GIT_BRANCH:   main (HEAD = a031a36)
PORTS:        APP=45204 (dev) — see .env.dev for full set
MODELS:
  planning:   claude-opus-4-7 (V32 architect — STATE.md write only)
  execution:  claude-sonnet-4-6 (V32 executor — all source writes)
  governance: gemini-2.5-flash-lite (per inputs.yml routing)

LINES_TOUCHED:    ~93 lines net across 4 files this session (gantt-view.tsx full rewrite +
                  page.tsx 5-line prop wire + kibo gantt 32-line vendor extension + next-tasks.md
                  scope update)
CHECKPOINT_TYPE:  full
FILES_TOUCHED:    [
  "apps/web/src/components/kibo-ui/gantt/index.tsx",
  "apps/web/src/app/(dashboard)/patrol-schedule/_components/gantt-view.tsx",
  "apps/web/src/app/(dashboard)/patrol-schedule/page.tsx",
  ".cline/next-tasks.md"
]
TIER_CLASSIFICATION: 1 — lightweight (single Sonnet dispatch, ~440 total lines in scope, 4 surgical edits)

V32 NOTES:    Zero Opus Execution honored. Opus dispatched one Sonnet task with inlined
              edit content; Sonnet returned DONE after 11 tool uses. Verification (typecheck,
              lint, build, git diff inspection) ran on Opus side via ctx_execute per
              V32.1 mitigation — kept tool logs out of Sonnet's context. No thrash.

DEV CONTAINER STATE:
              App image bonitobonita24/marine-guardian:dev-latest was rebuilt with the
              new code at 2026-05-29 ~10:34 and is currently running healthy on port 45204.
              Browser was closed after QA. Test schedule "Ranger Alice · Demo Patrol Zone
              Alpha · Jun 2-5" is in the dev DB — delete via the Patrol Schedule UI or
              leave for the next session's smoke test.
