# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-07-11 by CLAUDE_CODE (Full Auto — Milestone C SHIPPED to production)

PHASE:        Phase 7 — MILESTONE C FULLY DEPLOYED to staging + production (v1.1.0)
LAST_DONE:    Custom-role permission-matrix RBAC is now tRPC-ENFORCED and LIVE ON PROD.

              ✅ FIX (committed + pushed, origin/main @ cd1784f; deployed image sha-cd1784f):
                - matrixProcedure(base, feature, action): chains base enum gate FIRST, then DB matrix
                  check via hasPermission. ZERO enum regression (customRoleId==null → passthrough; no
                  independent tenant-throw). Wired ALL 117 grantable-feature endpoints across 22 routers.
                  Unexposed actions hard-clamp to denied for custom roles. Reserved surfaces untouched.
                - New __tests__/matrix-enforcement.test.ts (6 cases).
                - Commits: 8c80186 (fix) + cd1784f (state). Full gate green (typecheck 7/7, lint 0,
                  1787 tests, next build) + adversarial 117/117 audit (0 holes, 0 regression).

              ✅ DEPLOY (owner authorized "both" = staging + go live, this session — live consent):
                - CI run 29128202949 built image sha-cd1784f / staging-latest / latest (success).
                - STAGING (data-first gate, deploy/staging-refresh-and-deploy.sh): backed up staging →
                  copied PROD data → pulled image → migrated → up → /api/health 200 + /ph/login 200 +
                  custom_roles/role_permissions/users.custom_role_id all present. Migration 20260711090000
                  rehearsed against real prod data = GREEN.
                - PRODUCTION: backed up prod DB (/root/mg-prod-backup-pre-milestoneC-*.sql.gz, 25M) →
                  migrate deploy applied 20260711090000 to prod → pinned APP_IMAGE_TAG=sha-cd1784f →
                  recreated app+worker on the fix image → /api/health 200 + /login 200 + app HEALTHY +
                  custom-role tables/column live. mg.powerbyte.app SERVING the fix.
                - Tagged v1.1.0 (annotated) on cd1784f, pushed to origin.

              dev/staging/prod are code-identical (cd1784f = 8c80186 + a doc-only STATE commit); dev was
              rebuilt off the fix and is the reference build.

NEXT (un-gated): none — Milestone C is complete, tested, and shipped to prod.

DEFERRED [WHAT] (owner-gated, unchanged):
  1. DEMO stack (mg-demo) is NOT updated with Milestone C — demo is a separate deliberate-push env
     ("push to demo" needed). Owner said "both" (staging + prod), not demo. Holds until owner says.
  2. demo super_admin credential reseed (mg-demo carries stale webmaster@marine-guardian.local +
     admin@demo.com — see cred_override memory).

BLOCKERS:     none.
GIT_BRANCH:   main @ cd1784f (pushed) · tag v1.1.0 (pushed) · prod pinned to image sha-cd1784f
PORTS:        dev app 45204
