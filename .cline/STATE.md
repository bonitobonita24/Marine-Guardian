# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-13 by CLAUDE_CODE (Opus 4.7 direct execution — Phase 8 Batch 2 Item 4 SS-4 Notifications + Alert History Export FULLY SHIPPED: squash-merged to main (commit ab5a782), STATE chore (commit 0b0d421), pushed to origin/main (fast-forward 0070eeb..0b0d421), feat/exports-audit-views branch deleted. Item 4 (PDF/CSV Exports) is COMPLETE.)

PHASE:        Phase 8 Batch 2 Item 4 SS-4 FULLY SHIPPED — Notifications + Alert History Export squash-merged to main (commit ab5a782), STATE chore (commit 0b0d421), pushed to origin/main, feature branch deleted. Two final per-entity export endpoints live on main: /api/exports/notifications (tenant+user-scoped, isRead + notificationType filters, 9-column NotificationRow with flattened relation columns) and /api/exports/alert-history (tenant-scoped, alertRuleId filter, 8-column AlertHistoryRow with both immutable snapshot fields and current relation values falling back to "(deleted)").

              **ITEM 4 (PDF/CSV EXPORTS) IS COMPLETE.** All 5 entity export endpoints live with identical shape (manual auth + upload rate-limit 20/min + 10001-take overflow → HTTP 413 + sha256 filterHash + CSV/PDF branches + DATA_EXPORT audit):
              - /api/exports/events (SS-1)
              - /api/exports/patrols (SS-2)
              - /api/exports/alert-rules (SS-3)
              - /api/exports/notifications (SS-4)
              - /api/exports/alert-history (SS-4)

              Single source of truth pattern: each entity's router exports its list filter Zod schema (eventListFilters, patrolListFilters, alertRuleListFilters, notificationListFilters, alertHistoryListFilters) — consumed by both the tRPC list procedure and the Route Handler.

              Prior shipped this session: SS-3 Alert Rules Export (commit 1e03188 + 5252df2 + 0070eeb), SS-2 Patrols Export (52869ae + 72adb33 + e735686), SS-1 Events Export (0171309 + 3ed3382 + 1c783cd), SS-0 Export Foundation (4caa449), Alert History Log (7da0cab), Notification.patrolId FK (895fa84).

LAST_DONE:    Phase 8 Batch 2 Item 4 SS-4 — Notifications + Alert History Export feature work fully shipped to origin/main via the SS-1/SS-2/SS-3 cadence (3 commits: feat squash-merge ab5a782 + STATE chore 0b0d421 + this final-ship chore).

              Acceptance gate (all GREEN through ship):
              - typecheck 6/6 packages clean
              - lint 5/5 packages clean (zero fixups — SS-3's lint patterns for required-FK relation includes carried forward verbatim)
              - tests: 84/84 in apps/web (was 72 — added 6 notifications + 6 alert-history route handler tests) + 7/7 in packages/jobs (cached, unchanged) = 91 total
              - `pnpm --filter @marine-guardian/web build` — succeeds. Next.js registers all 5 export Dynamic Routes (/api/exports/{events,patrols,alert-rules,notifications,alert-history}) at 159 B / 103 kB each.
              - TDD order intact for both routes: each test file written first → RED confirmed (module-not-found on ../route) → implementation → 6/6 GREEN on first run.
              - Final-ship workflow: feat commit on branch → squash-merge to main → STATE chore post-merge → git push origin main (fast-forward 0070eeb..0b0d421) → git branch -D feat/exports-audit-views (force-delete because squash-merge produces different SHA than branch tip — same pattern as SS-2 and SS-3 final-ship) → this final-ship STATE chore.

              Files in this session: 10 changed, +929/-15.
              - 4 new: notifications/route.ts + notifications/__tests__/route.test.ts + alert-history/route.ts + alert-history/__tests__/route.test.ts
              - 4 modified: notification.ts router (extracted notificationListFilters), alertHistory.ts router (extracted alertHistoryListFilters), /notifications page.tsx (export buttons passing typeFilter), /alerts/history page.tsx (export buttons in flex group prepended to Back link)
              - 2 governance: docs/CHANGELOG_AI.md (SS-4 entry prepended), docs/IMPLEMENTATION_MAP.md (SS-4 flipped to [x], Next Step rewritten for completion, Shipped list updated)

              EXECUTION NOTES:
              Per the documented hook-injection thrashing lesson, stayed Opus-direct throughout. Hook injection still firing this session (vercel-plugin "vercel-functions"/"next-cache-components"/"nextjs"/"next-forge" suggestions on Read+Write+Edit; context-mode observation pointers on every Read; claude-mem timeline pointers; PostToolUse-validate suggestions on both new route files for "observability instrumentation" — rejected, matches SS-1/SS-2/SS-3 instrumentation surface i.e. none). Opus 4.7 absorbed all of them cleanly. Tier 2 estimate (~22K) held — two routes + tests + UI + governance + ship in one session without thrashing.

              Plan deviations: none of substance. Notifications: 9-column row matching SS-3's lean count. AlertHistory: 8-column row intentionally surfacing BOTH immutable snapshot fields (ruleNameSnapshot, eventTitleSnapshot) AND current relation values (alertRuleCurrent/eventCurrent, "(deleted)" fallback for SetNull-deleted FKs) — honest representation since alertHistory rows outlive their FK targets and consumers need both perspectives.

NEXT:         Phase 8 Batch 2 backlog after Item 4 completion — ONE remaining item:
              **Real-time notifications (WebSocket/SSE)** — Tier 3, MANDATORY split into ≥3 sub-sessions per memory-governance.md §1. Final feature group remaining in Phase 8 Batch 2.
              Sub-session split (proposed, to be confirmed when started):
              - SSE-1: Server-side SSE endpoint + tRPC subscription procedure + heartbeat/reconnect protocol
              - SSE-2: Client-side EventSource wrapper + React hook + integration with notification store
              - SSE-3: Auth + tenant scoping on SSE stream + reconnection resilience + load tests

              Open spec deferral: filter type drift (PRODUCT.md L189 enum mismatch) — 1-line human edit, not agent-side.

BLOCKERS:     none
GIT_BRANCH:   main (now at 0b0d421 + this final-ship STATE chore; in sync with origin/main; feat/exports-audit-views deleted)
PORTS:        dev 50620-50690 (base 50620 — unchanged from previous sessions)
MODELS:
  planning:   claude-code (Opus 4.7 — V31 primary)
  execution:  claude-opus-4-7 via Claude Code (Opus-direct due to documented hook-injection thrashing; reassess for next sub-session if hook overhead resolves)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
