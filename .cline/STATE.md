# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-21 19:35 GMT+8 by CLAUDE_CODE (Opus 4.7 — Phase 8 Batch 5 Sub-batch 5.3d SHIPPED to main via squash-merge as cd9ec36. 🎯 **Phase 8 Batch 5 Item 3 COMPLETE** (4 of 4 sub-batches: 5.3a + 5.3b + 5.3c + 5.3d). 🎯 **Phase 8 Batch 5 COMPLETE** (all 3 Items: AreaBoundary derivation + PatrolTrack materialization + ReportExport pdf-render pipeline). Continues 9-consecutive-clean-main-session-ship streak (5.1d-B → 5.1e → 5.2a → 5.2b → 5.2c → 5.3a → 5.3b → 5.3c → 5.3d). Push to origin authorization pending — 4 commits ahead of origin/main.)

PHASE:        Phase 8 Batch 5 Sub-batch 5.3d SHIPPED — /(dashboard)/exports admin list page (Client Component, coordinator+ gated) + status-badge.tsx (colored chip per status) + export-row.tsx (live polling refetchInterval=3000 while in-flight, disables for terminal states) + retry-button.tsx (admin-only) + reportExport.retry adminProcedure (resets row state + re-enqueues pdf-render + EXPORT_RETRY AuditLog) + Generate Report CTA on patrols/page.tsx (coordinator+ Dialog with reportType + paperSize selectors). 🎯 **Phase 8 Batch 5 Item 3 (ReportExport pdf-render pipeline) COMPLETE** — 4 of 4 sub-batches end-to-end (5.3a Puppeteer infrastructure 5837821 ✅ + 5.3b BullMQ queue/processor/worker + reportExport.create wiring 53c115e ✅ + 5.3c packages/storage + download Route Handler ad5a8d5 ✅ + 5.3d admin UI + retry mutation cd9ec36 ✅). 🎯 **Phase 8 Batch 5 COMPLETE** — all 3 Items done (Item 1 AreaBoundary derivation algorithm + Item 2 PatrolTrack Materialization job + Item 3 ReportExport pdf-render pipeline). End-to-end pipeline operable from the UI: coordinator clicks Generate Report on /(dashboard)/patrols → reportExport.create inserts queued row + BullMQ enqueue + EXPORT_REQUESTED AuditLog → Puppeteer renders + MinIO upload → status=ready → user sees ready badge on /(dashboard)/exports (live-polled every 3s) → user clicks Download → /api/exports/reports/{id}/download Route Handler streams the PDF + writes EXPORT_DOWNLOAD AuditLog. Admin-only Retry recovers stuck/failed exports by resetting state + re-enqueuing (jobId dedupe per 5.3b precedent).

LAST_DONE:    Sub-batch 5.3d SHIPPED to main via squash-merge as cd9ec36. NEW PAGE apps/web/src/app/(dashboard)/exports/page.tsx (Client Component, 196 LOC). NEW COMPONENTS exports/{status-badge,export-row,retry-button}.tsx (64 + 144 + 116 LOC) + patrols/generate-report-button.tsx (210 LOC). NEW MUTATION reportExport.retry adminProcedure (+44 LOC in reportExport.ts) — findFirst+update+enqueuePdfRender+EXPORT_RETRY AuditLog in sequential awaits. NEW SHARED SCHEMA retryReportExportInputSchema (+9 LOC in packages/shared/src/schemas/report-export.ts). Modified: patrols/page.tsx (+2 LOC — wired GenerateReportButton alongside RebuildTracksButton + Export CSV/PDF) + reportExport.test.ts (+155 LOC, 7 new retry cases). Tests: page.test.tsx (155 LOC, 5 cases) + export-row.test.tsx (200 LOC, 7 cases) + retry-button.test.tsx (113 LOC, 4 cases) + generate-report-button.test.tsx (131 LOC, 4 cases) + reportExport.test.ts retry section (155 LOC, 7 cases) = 27 net new vitest cases. Verification (pre-merge on feat/5.3d-exports-admin-ui-retry AND post-merge on main): web lint 0/0 strict --max-warnings 0, web typecheck 0 errors, web tests 307→334 (+27 — 4 new component test files + reportExport router additions); jobs tests 122/122 unchanged (5.3d is web-only on the implementation side; shared schema is pure types-only addition); storage tests 12/12 unchanged; shared typecheck 0. Monorepo total 441→468 (+27 net new for 5.3d). 3 minor lint errors caught + fixed inline before commit: (1) @typescript-eslint/no-unnecessary-type-assertion on `pollQuery.data?.status as ExportStatus | undefined` in export-row.tsx — Prisma narrowed already, cast removed; (2) same rule on `data.items as ExportRowItem[]` in exports/page.tsx — structurally compatible, cast removed; (3) @typescript-eslint/strict-boolean-expressions on `stubs.downloadEnabled ?` ternary in export-row.test.tsx — added `=== true` explicit narrowing. Decision: page is Client Component not Server Component (STATE.md brief said "Server Component" but trpc.useQuery requires "use client" — matches users/page.tsx precedent which the brief itself referenced). Main-session Opus 4.7 implementation per STATE.md NEXT-field brief — continues 5.1d-B/5.1e/5.2a/5.2b/5.2c/5.3a/5.3b/5.3c precedent (9 consecutive clean main-session ships). 5.3d consumed ~45K main-session tokens (estimate was ~40K — overrun driven by 3 lint fixes + verification of all 4 component test files + governance pass). Within main-session SAFE zone. No new lessons.md entries (no surprising gotchas — all 3 lint errors resolved by precedented fixes). Governance writes committed alongside this STATE.md rewrite.

NEXT:         Open a FRESH Claude Code session and say "Start Batch 6" (or "Show me Batch 6 candidates"). 🎯 **Phase 8 Batch 5 is COMPLETE** — all 3 Items shipped (AreaBoundary derivation + PatrolTrack materialization + ReportExport pdf-render pipeline). The pdf-render pipeline is end-to-end operable: coordinators can generate reports, admins can retry failed ones, downloads stream from MinIO via the Route Handler. **Batch 6+ candidates from the POST-ITEM-1 DEFERRED ledger:**
              1. **Coverage Report content** for /print-render/[tenant]/coverage/[exportId] — 3-page funder template per v2 §771 (currently a stub page; the pipeline machinery is complete but the page renders empty so PDFs come out blank). Recommended FIRST Batch 6 sub-batch since it unblocks user-visible value from the Item 3 pipeline immediately.
              2. **Per Area Report content** for /print-render/[tenant]/per-area/[exportId] — same pipeline target, different content.
              3. **Area Boundary Management UI** (full /admin/area-boundaries CRUD page) — currently the 5.1e RebuildAreaBoundariesButton lives on a stub page; build the actual admin CRUD.
              4. **Sync Health page** — operational visibility of er-sync run status, last successful sync, last error per tenant.
              5. **FuelEntry UI page + receipt photo upload to MinIO** — 4.1c shipped the FuelEntry router scaffold but no UI; needs list/create/edit + storage integration for receipt photos.
              6. **FuelEntry analytics aggregation** — SUM(liters) per area per range + L/km KPI math per v2 §182.
              **Or: Batch 6 BREAKING MIGRATIONS** (if user wants to clear the v2 backlog before more features):
              - Event.priority Int→enum (critical|high|medium|low) + downstream UI updates
              - Event.state `new_event`→`new` rename + downstream refs
              - Patrol.state enum expansion (open|scheduled|active|done|cancelled|draft)
              - AlertRule restructure (kind/condition_json/recipients_json/channels/cooldown_minutes)
              - Tenant sync-engine fields (active_check_interval_seconds + 14 others)
              - User.fullName → User.name rename (mechanical refactor)
              - SyncLog enum additions (subject_groups, tracks)
              - Subject.region/sex column extraction
              **Or: Batch 7+ items**:
              - AuditLog impersonation fields (acting_user_id/impersonated_as_tenant_id/severity/user_agent)
              - NotificationRecipient retention policy enforcement (1y per v2 §645)
              ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

PRIOR_5_3_C_DETAIL: Sub-batch 5.3c SHIPPED 2026-05-21 as squash-merge ad5a8d5 + governance 9c69fc2. NEW PACKAGE @marine-guardian/storage (packages/storage/{package.json, tsconfig.json, vitest.config.ts, src/index.ts 167 LOC, src/__tests__/storage.test.ts 215 LOC with 12 cases). New Route Handler apps/web/src/app/api/exports/reports/[id]/download/route.ts (135 LOC) + tests (~225 LOC, 9 cases). Processor stub replaced with real uploadPdf call. row.filePath now stores KEY only (env-derived bucket pairs back at read time). Decision locks: @aws-sdk/client-s3 over `minio` Node client, /api/exports/reports/{id}/download URL shape, key-only filePath convention, storage feature flag flip. inputs.yml storage.enabled false→true. 24 net new tests, monorepo 417→441.

PRIOR_5_3_B_DETAIL: Sub-batch 5.3b SHIPPED 2026-05-21 as squash-merge 53c115e + governance 39a1596 (origin-pushed prior session). pdf-render BullMQ queue (jobId pattern `pdf-render:${exportId}`) + processor + worker + reportExport.create enqueue wiring + EXPORT_REQUESTED AuditLog. 25 net new tests, monorepo 392→417.

PRIOR_5_3_A_DETAIL: Sub-batch 5.3a SHIPPED earlier 2026-05-21 as squash-merge 5837821 + governance 3898e0a (origin-pushed prior session). Puppeteer PDF renderer Docker service + service-token guard + pdf-renderer-client.ts + /print-render route stub + middleware service-token guard. 4 locked decisions in DECISIONS_LOG.

PRIOR_5_2_C_DETAIL: Sub-batch 5.2c SHIPPED 2026-05-21 as squash-merge 234b9ff. 🎯 Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job) COMPLETE. patrol.rebuildTracks adminProcedure + RebuildTracksButton UI. 7 new vitest cases.

PRIOR_5_1_E_DETAIL: Sub-batch 5.1e SHIPPED 2026-05-20 (squash-merge 88744ed). 🎯 Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm) COMPLETE for scope under remit. All 5 sub-batches shipped (5.1a + 5.1b + 5.1c + 5.1d Area B + 5.1e). 5.1d Area A (er-sync inline re-derive) DEFERRED.

DEFERRED:     5.1d Area A (er-sync inline re-derive on areaName change per v2 L546). Precondition unmet: ErEvent/ErPatrol payload types do not currently include area_name. Same precondition applies to er-sync.processor inline enqueuePatrolTrackMaterialize after each patrol upsert in syncPatrols. PARALLEL DEFERRAL — precursor sub-batch must bootstrap er-sync.processor.test.ts harness before either inline enqueue (5.2b path + 5.1d Area A path) can ship cleanly.

═══════════════════════════════════════════════════════════════════
🎯 PHASE 8 BATCH 5 — ITEM 3 PROGRESS LEDGER (ReportExport pdf-render pipeline) — COMPLETE
═══════════════════════════════════════════════════════════════════

  5.3a — Puppeteer PDF renderer Docker service + service-token guard         ✅ SHIPPED 2026-05-21 (squash-merge 5837821)
  5.3b — BullMQ pdf-render queue + processor + worker + reportExport.create  ✅ SHIPPED 2026-05-21 (squash-merge 53c115e)
  5.3c — packages/storage MinIO surface + download Route Handler             ✅ SHIPPED 2026-05-21 (squash-merge ad5a8d5)
  5.3d — /(dashboard)/exports admin UI + status polling + retry mutation     ✅ SHIPPED 2026-05-21 (squash-merge cd9ec36)

  🎯 Item 3 COMPLETE — 4 of 4 sub-batches end-to-end.

═══════════════════════════════════════════════════════════════════
🎯 PHASE 8 BATCH 5 — ITEM 2 PROGRESS LEDGER (PatrolTrack Materialization job) — COMPLETE
═══════════════════════════════════════════════════════════════════

  5.2a ✅ (d2a1dd1) · 5.2b ✅ (db50f6c) · 5.2c ✅ (234b9ff)

═══════════════════════════════════════════════════════════════════
🎯 PHASE 8 BATCH 5 — ITEM 1 PROGRESS LEDGER (AreaBoundary derivation algorithm) — COMPLETE
═══════════════════════════════════════════════════════════════════

  5.1a ✅ (6686042) · 5.1b ✅ (c4a14a9) · 5.1c ✅ (3b4145f) · 5.1d-B ✅ (068d32f) · 5.1e ✅ (88744ed)
  Area A deferred per DEFERRED field above.

═══════════════════════════════════════════════════════════════════
🎯 BATCH 4 SHIPMENT LEDGER — v2 Foundation Tables COMPLETE (carried over)
═══════════════════════════════════════════════════════════════════

  4.1a ✅ (56fb3fa) · 4.1b ✅ (cfe9195) · 4.1c ✅ (e972d82) · 4.1d ✅ (d32e618) · 4.1e ✅ (6687112)

═══════════════════════════════════════════════════════════════════
🎯 BATCH 5 SHIPMENT LEDGER — ALL 3 ITEMS COMPLETE 🎉
═══════════════════════════════════════════════════════════════════

  Item 1 (5.1a-e) ✅ — AreaBoundary derivation algorithm
  Item 2 (5.2a-c) ✅ — PatrolTrack Materialization job
  Item 3 (5.3a-d) ✅ — ReportExport pdf-render pipeline END-TO-END

═══════════════════════════════════════════════════════════════════
POST-BATCH-5 DEFERRED WORK — Batch 6+ candidates
═══════════════════════════════════════════════════════════════════

  USER-VISIBLE FEATURES (recommended next — unlocks immediate value from Batch 5 pipeline):
  - Coverage Report (3-page funder template) — content for /print-render/[tenant]/coverage/[exportId] (currently stub)
  - Per Area Report at /[tenant]/reports/area — content for /print-render/[tenant]/per-area/[exportId]
  - Area Boundary Management UI (full /admin/area-boundaries CRUD page — 5.1e button is on a stub page)
  - Sync Health page
  - FuelEntry UI page + receipt photo upload to MinIO
  - FuelEntry analytics aggregation (SUM(liters) per area per range, L/km KPI math per spec §182)

  BREAKING MIGRATIONS (Batch 6+):
  - Event.priority Int→enum (critical|high|medium|low) + UI updates
  - Event.state `new_event`→`new` rename + downstream refs
  - Patrol.state enum expansion (open|scheduled|active|done|cancelled|draft)
  - AlertRule restructure (kind/condition_json/recipients_json/channels/cooldown_minutes)
  - Tenant sync-engine fields (active_check_interval_seconds + 14 others)
  - User.fullName → User.name rename (mechanical refactor, opportunistic)
  - SyncLog enum additions (subject_groups, tracks)
  - Subject.region/sex column extraction

  BATCH 7+:
  - AuditLog impersonation fields (acting_user_id/impersonated_as_tenant_id/severity/user_agent)
  - NotificationRecipient retention policy enforcement (1y per spec L645)

  ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

═══════════════════════════════════════════════════════════════════

BLOCKERS:     none

GIT_BRANCH:   main (5.3d squash-merge complete at cd9ec36; governance commit pending alongside this STATE rewrite. After governance commit: push to origin/main authorization pending — 4 commits ahead of origin/main post-5.3c (ad5a8d5 5.3c impl + 9c69fc2 5.3c gov + cd9ec36 5.3d impl + pending 5.3d governance). Working tree apart from .specstory/statistics.json drift will be clean post-commit.)
GIT_HEAD:     cd9ec36 feat(web): exports admin UI + status polling + admin retry mutation + Generate Report CTA — Phase 8 Batch 5 Sub-batch 5.3d

REMOTE_STALE: origin/feat/event-kanban still exists on GitHub. User has not authorized remote delete. Run `git push origin --delete feat/event-kanban` when ready, then `git fetch --prune`. origin/main was current up through 39a1596 (the 5.3b governance commit) prior to this 5.3c + 5.3d arc; about to advance by 3 implementation commits + 2 governance commits on push.

PLAYWRIGHT_READY: Playwright 1.58.2 + system Chrome at /opt/google/chrome/chrome already installed. MCP tools loaded. Browser-driving auth gate remains — agent cannot self-authenticate (CREDENTIALS.md is gitignored + agent-prohibited per H1/H4). Visual checks delegated to human.

TOKEN_ESTIMATE:      This session — ~45K cumulative across orientation + 5.3d implementation (4 new components + 1 modified router + 1 modified page + 1 shared schema add + 4 new test files) + 3 lint fixes + verification pass + 6-section governance pass. Within main-session SAFE zone (~120K). Pattern continues: when Batch 6 runs in a fresh session, the STATE.md NEXT field (above) has the full candidate list pre-cached so orientation cost drops to ~3-5K.
CHECKPOINT_TYPE:     full (5.3d implementation complete + governance docs updated; squash-merge + branch cleanup complete; push to origin pending)
TIER_CLASSIFICATION: 5.3d = Tier 2 MODERATE per memory-governance.md §1 — score (9 × 2.5) + (2 × 5) + (2 × 3) = 38.5; 13 files touched (9 new + 4 modified); 2 modules (apps/web/src/app + apps/web/src/server/trpc); single main-session per 8-ship precedent (5.1d-B → 5.3c). Item 3 overall = Tier 3 HEAVY (4 sub-batches, ~40 files total, score >100). Properly decomposed per memory-governance.md §1 Tiered Decomposition. 4 of 4 sub-batches shipped.

PORTS:        APP=45204 DB=42731 CACHE=42733 PDF_RENDERER=internal-only (no host port — Docker network only)

MODELS:
  planning:    claude-code (V31 primary — Opus 4.7 plans decomposition + writes dispatch briefs + verifies)
  execution:   claude-opus-4-7 (main-session per 5.1d-B/5.1e/5.2a/5.2b/5.2c/5.3a/5.3b/5.3c/5.3d precedent — 9 consecutive clean main-session ships; dispatch-mechanism issue from 5.1d still unresolved but no longer relevant for tasks fitting within main-session SAFE zone)
  governance:  gemini-2.5-flash-lite (cheapest, non-critical writes — not currently used; CLAUDE_CODE handles all governance inline)
