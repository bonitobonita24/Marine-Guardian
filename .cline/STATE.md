# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-30 04:55 GMT+8 by CLAUDE_CODE (Opus 4.7 architect + Sonnet executor)

PHASE:        Phase 7 Feature Update — Super Admin Panel backlog item 6
              (EventType FK seed polish) SHIPPED to origin/main. Tier 1
              single-file polish — links 4 seeded Events to 5 seeded
              EventTypes via existing eventTypeId FK.

LAST_DONE:    seed.ts now captures every upserted EventType row id into
              an eventTypeIdByValue Map<string,string>. EventDef gained
              an eventTypeValue field. Each of the 4 eventDefs declares
              its mapping (event-001→illegal_fishing,
              event-002→wildlife_sighting, event-003→vessel_intrusion,
              event-004→equipment_damage). Both create and update sides
              of the event upsert now set eventTypeId via map lookup
              with ?? null fallback — re-seed backfills existing rows.

              Commits on origin/main this session:
                5e487c3  feat(seed): link Events to EventTypes via eventTypeId FK
                         (PRODUCT.md §210 backlog item 6)

              File touched (1, net +7 lines):
                packages/db/prisma/seed.ts
                  — lines 112-127: capture eventType row.id into map
                  — lines 287-313: EventDef.eventTypeValue + populate
                    eventTypeId on create + update

              Verification (Opus-side via ctx_execute):
                apps/web tsc --noEmit                       → 0 errors
                apps/web vitest run                         → 621/621 pass
                pnpm db:seed (re-ran against dev container) → success
                prisma query on events                      → all 4 events
                  have non-null eventTypeId, mappings correct, 0 unlinked

NEXT:         Backlog items 3, 4, 5, 7, 8 remain. Recommended order
              (cheapest first): 3 (platform.users.* router, Tier 2) →
              4 (tenant-switching) → 5 (schedule conflict detection) →
              7 (Patrols+Subjects data table) → 8 (sidebar role-aware).
BLOCKERS:     none
GIT_BRANCH:   main
PORTS:        APP=43000 DB=45194 (per .env.dev)
MODELS:
  planning:   claude-opus-4-7 (architect)
  execution:  claude-sonnet-4-6 (V32 zero-Opus-execution)
  governance: claude-sonnet-4-6 (same dispatch as execution)
