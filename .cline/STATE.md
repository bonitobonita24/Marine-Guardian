# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-21 21:25 GMT+8 by CLAUDE_CODE (Opus 4.7 — Phase 8 Batch 6 Sub-batch 6.1b SHIPPED to main via squash-merge as a8e44d9. Continues the 5.1d-B → 5.3d → 6.1a clean main-session ship streak (now 11 consecutive). 6.1a pushed to origin upfront this session per user authorization; 6.1b push pending — 2 commits ahead of origin/main post-6.1a-push.)

PHASE:        Phase 8 Batch 6 Sub-batch 6.1b SHIPPED — Coverage Report Page 2 (Area Boundary Summary) for /print-render/[tenantSlug]/coverage/[exportId]. Page 2 renders enabled-boundary table with Total/By-Track/By-Name columns + Outside-enabled tally + total row; Leaflet+OpenStreetMap map overlay with filled cyan polygons + dark patrol tracks + dashed outlines for ArcGIS-referenced boundaries; horizontal Recharts BarChart sorted DESC by patrol count; static "How patrols are attributed" methodology callout. CSS page-break-before: always forces a new physical page. Page 3 (Area Covered with coverage_km/coverage_hrs clipping algorithm) ships in 6.1c.

              NEW MODULE packages/shared/src/lib/area-attribution/ (6 src files + 1 test file with 20 vitest cases): types.ts (PatrolForAttribution + PatrolAttribution + AreaPatrolCount + AttributionSource), nearest-start-area.ts (pure wrapper over findNearestBoundary returning full boundary object), feature-matches-area.ts (pure wrapper over matchByName), attribute-patrol-to-area.ts (composite: nearest-first, name-fallback per v2 §771 — INVERTED from area-derivation/deriveArea), count-patrols-by-area.ts (aggregator with separate Outside-enabled tally), index.ts barrel.

              EXTENDED apps/web/src/server/coverage-report/get-coverage-report-data.ts (~159 LOC added): new CoverageReportArea + CoverageReportAttribution interfaces; trackLineString field on CoverageReportPatrolRow (full polyline for map overlay); extractTrackPolyline helper for LineString/MultiLineString head-to-tail flatten; prisma.areaBoundary.findMany scoped to tenant + isEnabled=true; attributePatrolToArea + countPatrolsByArea wiring populates enabledAreas[] + attributions[] + patrolCountsByArea[] + unattributedPatrolCount on the returned payload.

              NEW Page 2 RSC apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page-2-area-boundary-summary.tsx (~250 LOC + 8 vitest cases via renderToStaticMarkup with client-island mocks): section frame with page-break-before:always, header, 95mm map container, 5fr/4fr grid with boundary table (Area / Patrols / By Track / By Name + Outside row + Total tfoot) on left and chart column on right, variance-info callout below.

              NEW client islands apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/components/: area-coverage-map.tsx (~230 LOC — Leaflet MapContainer + TileLayer + Polygon per enabled area + Polyline per patrol track + AutoFitBounds + MapReadySignal flipping window.__renderReady after tile load + 2-frame paint flush, 8s safety timeout matches Puppeteer wait), patrol-area-bar-chart.tsx (~130 LOC — Recharts horizontal BarChart with Cell-per-row colors, sorted DESC by count + alphabetical tiebreak, Outside-enabled bar appended in gray, empty-state for zero boundaries), variance-info-callout.tsx (~40 LOC — static styled methodology footnote, no dialog because PDFs).

              EXTENDED deploy/pdf-renderer/src/server.js (+11 LOC): after existing networkidle0 wait, awaits page.waitForFunction(window.__renderReady === true) with 8s timeout + swallow-catch so pages without maps proceed immediately.

              DECISION LOCKED docs/DECISIONS_LOG.md: Coverage Report Page 2 Map Render Strategy = Leaflet + OpenStreetMap tiles + Puppeteer __renderReady wait flag — REJECTS static SVG composition. Decision governs all future report maps (6.1c Page 3, Per Area Report, future Quarterly Report). Trade-off accepted: tile network dependency + 0.5-2s added render time per PDF.

              DEPENDENCY ADDS apps/web: leaflet ^1.9.4 + react-leaflet ^5.0.0 (React 19 compatible — v4 had peer dep mismatch with React 19.2.5) + @types/leaflet ^1.9.21.

LAST_DONE:    Sub-batch 6.1b SHIPPED to main via squash-merge as a8e44d9. 21 files / +1974 -2 LOC. NEW: 6 area-attribution module files, 1 area-attribution test (20 cases), 1 page-2 RSC, 3 page-2 client/RSC components, 1 page-2 test (8 cases). MODIFIED: get-coverage-report-data.ts (+159 LOC + 10 new test cases — extractTrackPolyline suite +6 / Page 2 attribution suite +4), coverage-report.tsx (Page 2 mount), coverage-report.test.tsx (vi.mock for client islands + buildData fixture extended), apps/web/package.json + pnpm-lock.yaml (leaflet deps), deploy/pdf-renderer/src/server.js (__renderReady wait), docs/DECISIONS_LOG.md (map strategy lock), packages/shared/package.json (exports entry). 3 inline lint corrections during verification (no-unsafe-assignment on `pair[0]/pair[1]` indices needing `as unknown[]` cast — same precedent as 6.1a; no-unnecessary-type-conversion on String(b.source); no-unnecessary-type-assertion on `b.geometryType as ...`). 1 react-leaflet major version bump (v4 → v5) to escape React 19 peer dep mismatch. Pre-merge AND post-merge verification: web typecheck 0 / web lint 0 strict --max-warnings 0 / shared typecheck 0 / shared lint 0 / web 376 tests pass (358→376 = +18 net new) / shared 108 tests pass (88→108 = +20 net new) / jobs 122 unchanged / storage 12 unchanged. Monorepo total +38 net new vitest cases for 6.1b. Main-session Opus 4.7 implementation per the 6.1a precedent — now 11 consecutive clean main-session ships. 6.1b consumed ~70K main-session tokens (estimate was ~50K — overrun driven by Leaflet client-island implementation complexity + the react-leaflet v4→v5 peer dep pivot + 3 lint correction rounds). Slightly over the SAFE zone but the architect session completed without context thrash.

NEXT:         **Push 6.1b commits to origin/main first** (2 commits ahead: a8e44d9 implementation + the impending governance commit). Then open a FRESH Claude Code session and say "Start Batch 6 Sub-batch 6.1c" (or "Show me 6.1c candidates"). **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) is IN PROGRESS** — 2 of 3 sub-batches shipped (Page 1 — Patrol Index, Page 2 — Area Boundary Summary). Remaining work for the Coverage Report:

              **Sub-batch 6.1c — Page 3 (Area Covered) — THE ANALYTICAL PAYOFF:**
              - Coverage table sorted by coverage_km DESC, columns Boundary Name / Coverage Patrols / Coverage KMS / Coverage HRS, with "Est." badge for rows where hrs_estimated_count > 0
              - Footer note for "Est." rows + missing_tracks count when > 0
              - Bar chart (area-covered-chart) — top boundaries by km (reuse 6.1b PatrolAreaBarChart component as starting point, swap data shape)
              - Empty state: "No coverage in monitored boundaries for this period."
              - **Coverage computation algorithm** (the analytical payoff): for each PatrolTrack LineString, clip against each enabled AreaBoundary polygon → accumulate coverage_km per boundary + coverage_hrs (per-point timestamps when hasTimestamps=true, else pro-rated from totalHours with hrs_estimated_count++)
              - Decision lock needed: turf.js dependency vs pure-math line-polygon clipping. Recommend turf.js (well-tested, MIT, ~200KB but tree-shakeable). Add as packages/shared dependency since the algorithm is reusable across Per Area Report + future reports. Alternative: add only @turf/intersect + @turf/length + @turf/boolean-point-in-polygon (3 narrow modules) instead of full turf bundle.
              - 6.1b component reuse: AreaCoverageMap is potentially reusable on Page 3 with a coverage-overlay variant (paint clipped track segments in green to show actual coverage vs total). Consider extracting common map primitives if Page 3 uses a map; otherwise Page 3 is table+chart only (table is the primary funder-facing artifact).
              - Estimated scope: ~12-15 files, ~55-65K main-session tokens. Tier 2 MODERATE-HIGH. Likely splits into 6.1c-i (algorithm + tests) + 6.1c-ii (Page 3 RSC + table + chart + assemble).

              **Or: other Batch 6+ candidates from POST-BATCH-5 DEFERRED ledger (preserved below):**
              - Per Area Report content for /print-render/[tenant]/per-area/[exportId]
              - Area Boundary Management UI (full /admin/area-boundaries CRUD page)
              - Sync Health page
              - FuelEntry UI + receipt photo upload to MinIO
              - FuelEntry analytics aggregation (SUM(liters) per area per range + L/km KPI math per spec §182)

              **Or: Batch 6 BREAKING MIGRATIONS** (if user wants to clear the v2 backlog before more features):
              - Event.priority Int→enum (critical|high|medium|low) + downstream UI updates
              - Event.state `new_event`→`new` rename + downstream refs
              - Patrol.state enum expansion (open|scheduled|active|done|cancelled|draft)
              - AlertRule restructure (kind/condition_json/recipients_json/channels/cooldown_minutes)
              - Tenant sync-engine fields (active_check_interval_seconds + 14 others)
              - User.fullName → User.name rename (mechanical refactor)
              - SyncLog enum additions (subject_groups, tracks)
              - Subject.region/sex column extraction

              **Or: Batch 7+ items**:
              - AuditLog impersonation fields (acting_user_id/impersonated_as_tenant_id/severity/user_agent)
              - NotificationRecipient retention policy enforcement (1y per v2 §645)
              ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

PRIOR_6_1_A_DETAIL: Sub-batch 6.1a SHIPPED 2026-05-21 as squash-merge 7de30cf + governance f817d72. Coverage Report Page 1 (Patrol Index) + packages/shared/src/lib/coverage-period/ (8 pure functions). 6.1a consumed ~50K main-session tokens. shared 51→88 (+37), web 334→358 (+24). 5 inline lint/test corrections during verification + dir rename coverage/→coverage-report/.

PRIOR_5_3_D_DETAIL: Sub-batch 5.3d SHIPPED 2026-05-21 as squash-merge cd9ec36 + governance 665afd9. 🎯 Phase 8 Batch 5 Item 3 COMPLETE. 🎯 Phase 8 Batch 5 COMPLETE (all 3 Items: AreaBoundary derivation + PatrolTrack materialization + ReportExport pdf-render pipeline). /(dashboard)/exports admin list page (Client Component, coordinator+ gated) + status-badge.tsx + export-row.tsx (live polling refetchInterval=3000) + retry-button.tsx (admin-only) + reportExport.retry adminProcedure + Generate Report CTA on patrols/page.tsx. End-to-end pipeline operable from the UI.

PRIOR_5_3_C_DETAIL: Sub-batch 5.3c SHIPPED 2026-05-21 as squash-merge ad5a8d5 + governance 9c69fc2. @marine-guardian/storage package (S3/MinIO surface, 12 vitest cases). Download Route Handler /api/exports/reports/[id]/download with auth + AuditLog. Processor stub replaced with real uploadPdf call. row.filePath stores KEY only.

PRIOR_5_3_B_DETAIL: Sub-batch 5.3b SHIPPED 2026-05-21 as squash-merge 53c115e + governance 39a1596. pdf-render BullMQ queue + processor + worker + reportExport.create enqueue wiring + EXPORT_REQUESTED AuditLog. 25 net new tests.

PRIOR_5_3_A_DETAIL: Sub-batch 5.3a SHIPPED earlier 2026-05-21 as squash-merge 5837821 + governance 3898e0a. Puppeteer PDF renderer Docker service + service-token guard + pdf-renderer-client.ts + /print-render route stub + middleware service-token guard.

PRIOR_5_2_C_DETAIL: Sub-batch 5.2c SHIPPED 2026-05-21 as squash-merge 234b9ff. 🎯 Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job) COMPLETE. patrol.rebuildTracks adminProcedure + RebuildTracksButton UI.

PRIOR_5_1_E_DETAIL: Sub-batch 5.1e SHIPPED 2026-05-20 (squash-merge 88744ed). 🎯 Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm) COMPLETE for scope under remit. 5.1d Area A (er-sync inline re-derive) DEFERRED.
