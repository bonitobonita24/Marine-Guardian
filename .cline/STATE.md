# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-11 by CLAUDE_CODE (Opus 4.7 — Architect-Execute takeover after 2× Sonnet thrash; alerts evaluation engine processor + tests shipped)

PHASE:        Phase 8 Batch 2 — Alert Rule Evaluation Engine processor COMPLETE
LAST_DONE:    Phase 8 Batch 2 alerts.processor.ts implementation (evaluateAlerts function) + 5 vitest cases (tenant validation, no-rules, match+recipient, match+no-recipient, transaction-failure-no-partial-commit). Squash-merged to main as commit 0e62d62. UserRole enum mismatch resolved (admin → site_admin/super_admin fallback). vitest matcher lint errors resolved via scoped eslint-disable block. processAlert deprecated re-export preserved for start-workers.ts compat — alertsWorker registration unchanged. Two-stage review PASS. 5/5 tests in 132ms, lint exit 0, typecheck exit 0.

              History note: 2× Sonnet subagent dispatches thrashed at autocompact-3-in-3-turns despite progressively tighter scope (60K → 25K Sonnet workspace targets). Auto-loaded baseline (CLAUDE.md compact + 5 contextual rules files + Vercel plugin hooks injecting per-prompt skill directives + MCP server reminders + skills lists + memory injection) appears to consume more than budgeted. Sonnet 1 wrote 6.6KB test file then thrashed before processor (12 tool uses, 525s). Sonnet 2 wrote 143-line processor then thrashed before tests/lint/commit (15 tool uses, 378s). Opus took over the small remaining work per memory-governance.md §4 BLOCKED clause (two consecutive BLOCKEDs → Opus implements).

NEXT:         Phase 8 Batch 2 next item. Recommended order (from this thrashing-recovery session):
              (1) **Wire alerts enqueue from event.create** — single small Tier 2 task closing the deferred integration. Without this, engine is built but does not fire on real events. Scope: inject alertsQueue into apps/web/src/server/trpc/routers/event.ts, after successful event.create enqueue { tenantId, eventId } with try/catch (mutation must still succeed if enqueue fails), one integration test asserting enqueue is called. Task file to write: .cline/tasks/phase8-batch2-alerts-enqueue.md.
              (2) Interactive map (Leaflet/mapcn on /map page) — Tier 2 single session.
              (3) Spec deferral #3 — Notification.patrolId FK migration + UI wiring.
              (4) Real-time notifications (WebSocket/SSE) — Tier 3, mandatory split into 3 sub-sessions per §1.
              (5) PDF/CSV exports — Tier 2 per entity, one entity per session.
              (6) Spec deferral #1 — Alert history log (now meaningful since engine fires).

              Working-tree advisory: large unrelated framework churn remains uncommitted from prior sessions (.ai_prompt/*, CLAUDE.md, .claude/rules/*, deploy-v31.sh, AI/Master_Prompt_v31.md, .specstory/statistics.json, plus untracked user-management dialog files at apps/web/src/app/(dashboard)/users/ and apps/web/src/server/trpc/routers/__tests__/user.test.ts, plus 2 new task files at .cline/tasks/phase8-batch2-alerts-engine.md and .cline/tasks/phase8-batch2-alerts-engine-resume.md). NONE of this is part of the alerts engine commit. Decide separately whether to commit, stash, or discard before starting next feature work — do NOT git add -A on next session.

              Open spec deferrals from prior sessions remain unchanged (CHANGELOG_AI 2026-05-08): (a) Alert history log PRODUCT.md L182, (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, (c) Notification.patrolId FK click-through PRODUCT.md L187.

BLOCKERS:     none
GIT_BRANCH:   main (feat/alerts-evaluation-engine squash-merged + deleted)
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
TOKEN_ESTIMATE:    Opus session ~80K consumed (2× Sonnet dispatch + Opus takeover + governance writes)
CHECKPOINT_TYPE:   full
FILES_TOUCHED:     packages/jobs/src/processors/alerts.processor.ts, packages/jobs/src/__tests__/alerts.processor.test.ts, docs/CHANGELOG_AI.md, docs/IMPLEMENTATION_MAP.md, .cline/STATE.md
TIER_CLASSIFICATION: 2 — moderate (originally; after Sonnet 1 wrote tests, remaining work was Tier 1)

MODELS:
  planning:   claude-opus-4-7 (Architect — V31 primary)
  execution:  claude-sonnet-4-6 (Executor — Architect-Execute Model §4; this task: 2× Sonnet thrash → Opus takeover)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
