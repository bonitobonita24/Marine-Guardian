# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-11 by CLAUDE_CODE (Opus 4.7 — sub-session 1.2b committed: InteractiveMap client wrapper around mapcn primitive)

PHASE:        Phase 8 Batch 2 — Interactive Map feature, sub-session 1.2b COMPLETE (1.2c pending)
LAST_DONE:    Sub-session 1.2b shipped. Created `apps/web/src/components/map/InteractiveMap.tsx` — 25-line `"use client"` wrapper around the mapcn `Map` + `MapControls` primitive. Centered on Banda Sea `[127.5, -2.5]` at zoom 6 (Marine Guardian's primary operating area). Coordinate convention `[lon, lat]` follows the DECISIONS_LOG lock from 1.2a. No data layers, no tRPC calls, no `useMap`/marker logic. Props surface: `className?: string` only. Committed as `c2d4823` on `feat/interactive-map` (branch now 4 commits ahead of main: 78cd87a router → e2d28d1 plan → 4582cf0 mapcn install → c2d4823 wrapper).

              INHERITED BLOCKAGE FLAGGED FOR 1.2c (DO NOT MERGE TO MAIN UNTIL RESOLVED):
              `apps/web/src/components/ui/map.tsx` (the 1844-line mapcn vendor primitive from 1.2a commit 4582cf0) ships with 64 ESLint errors + 4 TS18048 typecheck errors under our strict config. Errors live ENTIRELY inside the vendor file — InteractiveMap.tsx itself is clean. Sub-session 1.2a did not gate on lint/typecheck before committing the registry pull, so this blockage was deferred forward.
              Recommended fix for 1.2c: file-level suppression header at the top of `apps/web/src/components/ui/map.tsx`:
                ```ts
                /* eslint-disable -- mapcn registry-managed vendor file, see DECISIONS_LOG */
                // @ts-nocheck — mapcn registry primitive, suppressed pending upstream strict-mode fix
                ```
              Mirrors the pattern obs 82 established for `map.test.ts` in sub-session 1.1. Vendor file is auto-regenerated by `npx shadcn@latest add @mapcn/map` so inline fixes would be clobbered on next pull. Add `// see DECISIONS_LOG` entry explaining the suppression.

NEXT:         Sub-session 1.2c — page route at `apps/web/src/app/(authed)/map/page.tsx` (or whichever group layout matches existing pages). Tasks for 1.2c:
              (1) FIRST: apply file-level eslint-disable + @ts-nocheck to `apps/web/src/components/ui/map.tsx` (vendor suppression — see blockage above).
              (2) Verify: `pnpm --filter @marine-guardian/web lint` exit 0, `pnpm --filter @marine-guardian/web typecheck` exit 0.
              (3) Create `app/.../map/page.tsx` Server Component that renders `<InteractiveMap />` inside a full-viewport container. No tRPC yet. ~20-40 lines.
              (4) Visual QA: pnpm dev → load /map → confirm CARTO tiles render at Banda Sea, MapControls work, no console errors.
              (5) Append CHANGELOG_AI + IMPLEMENTATION_MAP entries for the full 1.2 feature (a+b+c).
              (6) Two-stage review (Rule 25) → squash-merge `feat/interactive-map` → delete branch → STATE.md reset.

              After 1.2c merges to main, Phase 8 Batch 2 backlog (unchanged from prior STATE.md):
              (next-1) Spec deferral #3 — Notification.patrolId FK migration + UI wiring (PRODUCT.md L187).
              (next-2) Real-time notifications (WebSocket/SSE) — Tier 3, mandatory split per memory-governance.md §1.
              (next-3) PDF/CSV exports — Tier 2 per entity.
              (next-4) Spec deferral #1 — Alert history log.

              Open spec deferrals unchanged (CHANGELOG_AI 2026-05-08): (a) Alert history log PRODUCT.md L182, (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, (c) Notification.patrolId FK click-through PRODUCT.md L187.

BLOCKERS:     1.2a vendor file (`apps/web/src/components/ui/map.tsx`) has 64 ESLint + 4 TS errors that BLOCK any squash-merge to main. Fix in 1.2c step (1) via file-level suppressions before the page-route work.
GIT_BRANCH:   feat/interactive-map (4 commits ahead of main: router + plan + mapcn install + InteractiveMap wrapper)
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
TOKEN_ESTIMATE:    Opus session ~15K consumed (signature scan + small file write + commit + STATE.md edit)
CHECKPOINT_TYPE:   full
FILES_TOUCHED:     apps/web/src/components/map/InteractiveMap.tsx (new), .cline/STATE.md
TIER_CLASSIFICATION: 1 — lightweight (1 new file, 25 lines, 1 module, no dependencies)

MODELS:
  planning:   claude-opus-4-7 (Architect — V31 primary)
  execution:  claude-opus-4-7 (Tier 1 task, dispatched-as-Sonnet plan but Opus executed in-conversation per `start 1.2b` direct trigger; under 30K budget regardless)
  governance: claude-opus-4-7 (this session — STATE.md edit only; CHANGELOG/IMPLEMENTATION_MAP deferred to 1.2c final merge)
