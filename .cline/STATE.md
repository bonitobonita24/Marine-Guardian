# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-17 by CLAUDE_CODE (Opus 4.7 — squash-merge feat/sse-foundation → main complete; SSE-1 + SSE-2 shipped to origin in commit 3710472)

PHASE:        Phase 8 Batch 2 SSE-1 + SSE-2 SHIPPED to main (commit 3710472, pushed to origin/main). Realtime notification foundation live: server-side Valkey pub/sub (publisher + subscriber + alerts.processor integration + /api/stream/notifications Route Handler) + client-side SSE pipeline (EventSource wrapper + Zustand store + REST poller fallback + useNotificationStream hook with exponential backoff). 117/117 tests pass. Build clean. Branch feat/sse-foundation deleted locally (never pushed to origin).

LAST_DONE:    Squash-merged feat/sse-foundation (3 commits: a426c95 SSE-1, 0cf0b49 RED stubs, f64d88b SSE-2 GREEN) into main as single atomic commit 3710472 with full conventional-commit message. Pushed origin main (3aee3df..3710472). Deleted local feat branch. Separately committed b0b36c8 chore(scan) recording 2026-05-17 re-scan that verified all 3 Foundation Bundle plugins (context-mode, claude-mem, code-review-graph) already active. Two commits total on main this session.

NEXT:         SSE-3 — UI consumers + push notification preferences. Third and final SSE sub-session per the locked Tier 3 split. Create fresh branch feat/sse-events-and-hardening in a NEW Claude Code session with clean context budget. Work scope: (1) wire useNotificationStream into apps/web layout — display notification badge in header with unreadCount, (2) build /notifications page consuming notification-store with mark-as-read interactions, (3) replace sidebar refetchInterval polling with SSE-driven invalidation, (4) add user notification preferences (which alert types stream vs. digest-only), (5) hardening: rate-limit the /api/stream/notifications endpoint, add server-sent heartbeat to keep proxies happy, add metrics for connection count + reconnect rate.

BLOCKERS:     none

GIT_BRANCH:   main (origin/main synced)
GIT_HEAD:     b0b36c8 chore(scan): record 2026-05-17 re-scan
GIT_PREV:     3710472 feat(realtime): SSE-1 + SSE-2 (squash-merge)

STALE_BRANCHES_TO_CLEAN: feat/user-management, scaffold/part-7 (both local-only, unrelated to current work, candidates for branch -D cleanup in a future session)

TOKEN_ESTIMATE:      ~45K consumed this session (well within 60K SAFE zone per feedback_context_thrashing — context-mode + claude-mem + code-review-graph protections were active throughout)
CHECKPOINT_TYPE:     full (per memory-governance.md §2 — git operations modified files)
FILES_TOUCHED:       [".claude/scan-results.json", ".cline/STATE.md"]
TIER_CLASSIFICATION: Tier 1 (≤4 files, 1 module — git merge orchestration only, no source code changes)

PORTS:        APP=45204 DB=42731 CACHE=42733 (unchanged — verify via cat .env.dev | grep _PORT)

MODELS:
  planning:   claude-code (V31 primary)
  execution:  claude-opus-4-7 via Claude Code (this session — Opus direct execution; framework default Sonnet 4.6 via subagent dispatch when memory-governance.md §4 Architect-Execute Model applies)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
