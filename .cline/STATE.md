# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-29 19:52 GMT+8 by CLAUDE_CODE (Opus 4.7 architect + Sonnet executor)

PHASE:        Phase 7 Feature Update — Bug #7 (seed expansion for demo-ready UX) SHIPPED.
              Backlog continues with Super Admin Panel (PRODUCT.md §210) and schedule
              conflict detection.

LAST_DONE:    Bug #7 shipped — commit a2c15dd on origin/main expands packages/db/prisma/seed.ts
              from 171 → 460 lines with idempotent demo data scoped to the demo-site tenant.
              Added: 5 Subjects (rangers Alpha/Bravo/Charlie/Delta/Echo, subjectType="ranger"),
              2 AreaBoundaries (North Reef Zone, South Mangrove Zone), 4 Patrols (varied
              state+type), 5 PatrolSchedules (2 past, 1 today, 2 future), 4 Events (varied
              priority+state), 5 Observations linked to ranger Subjects, 3 FuelEntries
              (165L / ₱11,550 across 2 areas), 2 AlertRules (high-priority + SOS),
              2 ReportExports (status=ready with fake filePath).
              Idempotency: models with @@unique constraints use upsert; the rest use
              findFirst-then-create. Verified by 2x consecutive `pnpm db:seed` runs —
              counts stable. Visual QA on dev container port 45204:
                ✅ Dashboard counters populate (Active Events 3, Patrols 1, Month 4)
                ✅ Patrol Schedule Gantt renders 9 ranger labels, 0 console errors
                ✅ Events page lists all 4 titles with state breakdown
                ✅ Fuel analytics computes 7.399 L/km, per-area breakdown
                ⚠ Patrols/Subjects pages still show "data table pending" stubs —
                  data IS in DB, UI tables just aren't built (out of Bug #7 scope)
                ⚠ Dashboard event rows show "Unknown type" (eventTypeId not linked
                  in seed per spec — minor polish item for next demo iteration)
              Branch feat/bug-7-seed-expansion squash-merged to main (a2c15dd),
              pushed to origin, branch deleted local + remote.
              V32 dispatch metrics: 1 Sonnet executor call (Tier 1, 460 LoC final,
              well under 500-line cap). Schema scout dispatch hit Explore agent
              prompt-length cap twice — Opus read schema.prisma (704 lines, ~3.5K
              tokens) directly instead. Acceptable deviation from V32 R5 — within
              Opus context budget, no thrash.

NEXT:         Backlog (priority order):
              1. Super Admin Panel (PRODUCT.md §210) — extend the /admin stub from
                 Bug #6 with cross-tenant ops (tenant list, user provisioning,
                 platform-level metrics). The /admin route + sign-out scaffold
                 already ships; this fills in the actual platform admin features.
              2. Schedule conflict detection — overlapping ranger assignments on
                 PatrolSchedule. v2 enhancement on top of Patrol Schedule (7.1d).
              3. EventType FK linkage in seed — minor polish: seeded events should
                 reference EventTypes by `value` so dashboard rows stop showing
                 "Unknown type". ~10 LoC addition to seed.ts.
              4. Patrols + Subjects data table UI — replace "data table pending"
                 stubs with shadcn DataTable rendering the now-seeded rows.
              5. Sidebar role-aware item hiding — only relevant if Super Admin
                 Panel grows sidebar nav.

BLOCKERS:     none

GIT_BRANCH:   main (a2c15dd, in sync with origin/main)

PORTS:        APP=45204 (dev container — healthy)

MODELS:
  planning:   claude-opus-4-7 (V32 architect — Opus does NOT execute file writes
              except STATE.md per R1)
  execution:  claude-sonnet-4-6 (V32 executor — all Edit/Write dispatched here)
  governance: gemini-2.5-flash-lite (CHANGELOG_AI, agent-log — cheapest)

LINES_TOUCHED: ~294 lines created/modified this session
CHECKPOINT_TYPE: full
FILES_TOUCHED: ["packages/db/prisma/seed.ts"]
TIER_CLASSIFICATION: 1 — lightweight
