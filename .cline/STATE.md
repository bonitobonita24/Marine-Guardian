# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-23 4:05pm GMT+8 by CLAUDE_CODE (Opus 4.7 main-session — Task 3 follow-up defect cleanup basket shipped. 4 defects flagged in prior STATE.md NEXT (b, c, a, d) addressed in one session per user direction. Pdf renderer healthcheck fixed; orphan containers finding documented (false positive — yelli is a parallel active project); APP_ENV→bucket mapping landed end-to-end with bucket migrated + smoke test PASS; demo-site admin dev credential rotated via Scenario 34 rotation script with full smoke test PASS post-rotation.)

PHASE:        TASK 3 FOLLOW-UP DEFECT CLEANUP BASKET COMPLETE — All 4 small defects (b, c-finding, a, d) flagged in prior STATE.md addressed in one session. Source changes: 3 files modified + 1 new rotation script. Runtime changes: pdf_renderer container recreated with new healthcheck (now healthy); jobs dist rebuilt; app image rebuilt; worker+app containers recreated; MinIO bucket `marine-guardian-development-exports` migrated to `marine-guardian-dev-exports` (1 PDF moved, old bucket removed); dev DB demo-site admin password rotated via seed re-run.

              MODIFIED 3 source files + 1 NEW:
              - deploy/compose/dev/docker-compose.pdf-renderer.yml — replaced `wget` healthcheck (binary not in node:22-slim) with `node -e` HTTP probe. Container reaches (healthy) state 30s after recreate.
              - packages/storage/src/index.ts — added `APP_ENV_TO_BUCKET_ENV: Record<string,string>` mapping {development→dev, staging→staging, production→prod}. `getExportsBucketName` now translates APP_ENV → bucket env segment at the storage boundary; throws on unknown APP_ENV (silent fallthrough to "dev" would risk prod data in dev bucket). Module JSDoc preamble updated with the mapping table. Matches DECISIONS_LOG §142.
              - packages/storage/src/__tests__/storage.test.ts — beforeEach APP_ENV switched from misleading "test" to realistic "development". +3 new tests on getExportsBucketName: maps development → dev-exports, maps production → prod-exports, throws on unknown APP_ENV value. Storage test count 12 → 15.
              - scripts/rotate-demo-site-admin-password.sh NEW — reusable credential rotation script. Per Scenario 34: openssl-generates a 22-char password, updates .env.{env} via sed, updates CREDENTIALS.md row via structural awk slicing (NEVER cats the file or echoes the value), applies bcrypt hash via `pnpm db:seed` (dev) or emits SQL UPDATE statement to /tmp/rotate-sql-{env}.sql (staging/prod). Dev mode executed in this session; staging/prod modes await user execution + server-side SQL apply.

              RUNTIME WORK (not committed — operational state):
              - pdf_renderer container recreated with new healthcheck (no image rebuild needed — healthcheck is container-level).
              - MinIO: created marine-guardian-dev-exports bucket, mc-mv migrated the 1 Task 3 smoke PDF (cmphvyrsh0001v101dr2iqbmk.pdf, 187 KiB), removed marine-guardian-development-exports bucket.
              - pnpm --filter @marine-guardian/jobs build — rebuilt dist/start-workers.mjs with new APP_ENV mapping (verified bundle contains only `marine-guardian-dev-exports`, no `marine-guardian-development-exports`).
              - docker compose build app — rebuilt bonitobonita24/marine-guardian:dev-latest image (includes new jobs dist).
              - docker compose up -d --force-recreate worker app — both containers recreated, app reached (healthy) in 30s.
              - bash scripts/rotate-demo-site-admin-password.sh dev — generated new 22-char password, updated .env.dev DEMO_SITE_ADMIN_PASSWORD line, updated CREDENTIALS.md "### Demo Site Admin" dev row col 3, ran pnpm db:seed which upserted new bcrypt hash on admin@demo-site.local.
              - Post-rotation smoke test (bash scripts/smoke-tests/run-per-area-smoke.sh) → PASS — exportId=cmpi25btt0007v101tps3n87f, 191828 bytes, 3 pages, downloadUrl works, written to marine-guardian-dev-exports/${tenantId}/2026/05/.

              VERIFICATION:
              - pnpm typecheck (storage + web) — clean.
              - pnpm lint (web) — clean.
              - pnpm vitest run — storage 15/15 (was 12, +3), jobs 122/122, web 463/463. Total +3 across monorepo for defect (a).
              - docker ps — all marine-guardian_dev_* containers healthy including pdf_renderer (was reporting unhealthy for 2+ hours before defect b fix).
              - mc ls local/ — only marine-guardian-dev-exports bucket exists (old marine-guardian-development-exports bucket removed). 2 PDFs inside (original Task 3 smoke + post-rotation smoke).
              - Smoke test post-everything PASS — confirms new credentials work AND new bucket name works AND new healthcheck doesn't false-alarm AND no regressions.

LAST_DONE:    Task 3 follow-up defect cleanup basket — 4 defects (b pdf_renderer healthcheck, c orphan finding documented, a APP_ENV→bucket mapping with bucket migrated + image rebuilt, d demo-site admin credential rotation via new Scenario 34 rotation script). 24 consecutive clean main-session changesets (this commit pending will be #25).

NEXT:         **🔒 LOCKED QUEUE for next session — Area Boundary Management UI.** Load `.cline/next-tasks.md` FIRST. Work A.1 (List + Delete + relocate rebuild button) then A.2 (Create + Edit dialogs). Don't suggest other work until both shipped. Design decisions confirmed by user 2026-05-23 ~4:20pm: expand existing /patrol-areas page (not separate /admin/area-boundaries), raw GeoJSON textarea for create/edit (defer map drawing to future session), two commits within next session.

              Both sub-batches are Tier 1-2 (5 files each, ~40-50K main-session tokens). Closes 5.1e stub deferral + provides UI foundation for future map preview + map drawing + 5.1d Area A.

              **Outstanding from this session (deferred — needs user follow-through outside dev sessions):**
              - **Staging+prod demo-site admin rotation** (defect d follow-through). Run `bash scripts/rotate-demo-site-admin-password.sh staging` and `... prod` on this dev machine — script generates new passwords, updates CREDENTIALS.md rows + .env.{env} files locally, and writes SQL UPDATE statements to /tmp/rotate-sql-staging.sql + /tmp/rotate-sql-prod.sql. Then SCP/sync the updated .env.{env} files to the corresponding servers + run `psql "$DATABASE_URL" -f rotate-sql-{env}.sql` on each. Verify post-apply via a login attempt before deleting the SQL files.

              **Carry-forward candidates (AFTER locked queue closes):**
              - **Map preview + map drawing for area boundaries** — natural follow-ups to A.1+A.2. Leaflet/react-leaflet 5.x. Read-only render is Tier 1-2; drawing editor is Tier 2.
              - **Other Batch 6+ feature candidates**: Sync Health page, FuelEntry UI + receipt photo upload (MinIO live), FuelEntry ER ingest, real-BullMQ-or-ioredis-mock integration test in packages/jobs.
              - **Batch 6+ breaking migrations** (unchanged list): Event.priority Int→enum, Event.state rename, Patrol.state enum expansion, AlertRule restructure, Tenant sync-engine fields, User.fullName rename, SyncLog enum additions, Subject column extraction.
              - **Batch 7+**: AuditLog impersonation fields, NotificationRecipient retention policy enforcement.
              - ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

BLOCKERS:     none — Area Boundary Management UI locked queue ready for next session.
GIT_BRANCH:   main (queue-file commit pending; will land as commit #25)
PORTS:        unchanged (MinIO 45197 + 45198, app 45204, postgres 45194, valkey 45196, pgadmin 45201, mailhog 45199 + 45200, pgbouncer 45195, pdf_renderer internal-only on 4000)
MODELS:
  planning:   claude-code (Phase 2 — V31 primary; main-session Opus 4.7 for Phase 8 sub-batches + Phase 7-style defect fixes + Locked Task Queue execution + post-queue defect cleanup)
  execution:  claude-opus-4-7 main-session (per the 6.x established ship pattern — 24 consecutive clean main-session changesets) OR claude-sonnet-4-6 via Agent dispatch for genuinely parallel work
  governance: gemini-2.5-flash-lite (non-critical doc writes — cheapest)
