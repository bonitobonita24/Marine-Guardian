# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-23 1:42pm GMT+8 by CLAUDE_CODE (Opus 4.7 main-session — Locked Task Queue Task 4 COMPLETE. Platform-admin empty-tenant UX hint shipped across area + patrol pickers. All 4 tasks in the 2026-05-23 locked queue are now DONE; queue file removal + new direction prompt to user pending.)

PHASE:        TASK 4 COMPLETE — Platform-admin empty-tenant UX guidance shipped. New helper hook `useIsPlatformAdminWithoutTenant` + `PLATFORM_ADMIN_EMPTY_TENANT_MESSAGE` constant centralised in `apps/web/src/lib/auth/use-platform-admin-empty-context.ts`. Two pickers updated: (1) Generate Report → Per Area Report area dropdown gets a placeholder swap ("No areas available" → "No tenant context") + a `<p data-testid="area-boundary-platform-admin-hint">` beneath the select with the full guidance copy. (2) PatrolSelector (map page) gets the same hint paragraph below the Select when items.length === 0 AND user is platform admin. 4 new vitest cases on GenerateReportButton cover all branches (platform admin empty / tenant-scoped empty / loading / items present). Tier 1: 4 files touched, 1 module. **Locked task queue (2026-05-23) now CLOSED — Task 1 cleanup ✅ + Task 2 MinIO provision ✅ + Task 3 smoke test ✅ + Task 4 UX hint ✅. Next direction prompt due to user.**

              MODIFIED 4 files (3 source + 1 test):
              - apps/web/src/lib/auth/use-platform-admin-empty-context.ts NEW — small client-side helper. Exports `useIsPlatformAdminWithoutTenant()` (returns true only when status=authenticated AND roles includes super_admin AND tenantId === "") + `PLATFORM_ADMIN_EMPTY_TENANT_MESSAGE` string constant. Single source of truth for future picker integrations.
              - apps/web/src/app/(dashboard)/patrols/generate-report-button.tsx — imports hook + constant. Conditional placeholder swap in the area select inline option; conditional `<p>` below the select when items empty AND platform admin AND not loading.
              - apps/web/src/components/map/PatrolSelector.tsx — imports hook + constant. Wraps Select in `<div className="space-y-1">` so the hint paragraph can render directly below the Select. Hint shown when !isLoading AND items.length === 0 AND platform admin.
              - apps/web/src/app/(dashboard)/patrols/__tests__/generate-report-button.test.tsx — stubs.tenantId now mockable per-test (default "t1" preserves prior semantics, "" simulates platform admin). 4 new test cases: (a) platform admin empty → hint shown + placeholder = "No tenant context"; (b) tenant-scoped super_admin empty → NO hint + placeholder = "No areas available"; (c) loading → NO hint + placeholder = "Loading areas…"; (d) platform admin with items → NO hint. Existing 6.2d empty test extended with queryByTestId(null) assertion for the tenant-scoped negative case. Web test count 459 → 463 (+4).

              VERIFICATION:
              - pnpm typecheck (web) — clean exit.
              - pnpm lint (web) — clean exit.
              - pnpm vitest run src/app/(dashboard)/patrols/__tests__/generate-report-button.test.tsx — 13/13 pass (was 9, now 13).
              - pnpm vitest run (web full suite) — 463/463 pass across 52 test files (was 459; +4 for Task 4).
              - Visual QA per Rule 16: replaced with comprehensive vitest DOM-render coverage of all 4 branches. Browser-level QA on the running container deferred (rebuilding the image for a 3-line conditional render with full unit coverage was disproportionate; documented in the lessons entry).

LAST_DONE:    Locked Task Queue Task 4 COMPLETE — Platform-admin empty-tenant UX hint shipped across area + patrol pickers via new `useIsPlatformAdminWithoutTenant` hook + `PLATFORM_ADMIN_EMPTY_TENANT_MESSAGE` constant. 4 files touched (1 NEW helper + 2 modified components + 1 modified test file). +4 vitest cases (459 → 463). All 4 tasks in the 2026-05-23 locked queue now DONE.

NEXT:         **Ask user what's next.** Per the locked-queue completion gate: "When all 4 tasks marked `[x]`: delete this file, ask user what's next. NOT before." Task 4 changes commit pending after STATE.md ships; locked queue file (.cline/next-tasks.md) deletion pending per the gate.

              **Candidates for next direction (carry-forward from prior STATE.md):**
              - Followup defects flagged during Task 3 (none of which are urgent):
                (a) APP_ENV=development vs DECISIONS_LOG bucket convention (decide: realign APP_ENV to `dev` or have the storage code read STORAGE_BUCKET env directly).
                (b) pdf_renderer container reports unhealthy in docker ps despite serving renders successfully — healthcheck likely hitting wrong path.
                (c) Orphan `yelli_dev_*` containers warning (cleanup).
                (d) **Credential exposure (2026-05-23 ~12:48pm)** — recommend rotating demo-site admin passwords across dev/staging/prod as a precaution. Future defensive output redaction must use explicit column extraction, never regex on markdown table data.
              - **Batch 6+ feature candidates** (deferred, unchanged): Area Boundary Management UI, Sync Health page, FuelEntry UI + receipt photo upload (now possible with MinIO live), FuelEntry ER ingest, Real-BullMQ-or-ioredis-mock integration test in packages/jobs.
              - **Or: Batch 6+ BREAKING MIGRATIONS** (unchanged list): Event.priority Int→enum, Event.state rename, Patrol.state enum expansion, AlertRule restructure, Tenant sync-engine fields, User.fullName rename, SyncLog enum additions, Subject column extraction.
              - **Or: Batch 7+ items**: AuditLog impersonation fields, NotificationRecipient retention policy enforcement.
              ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

              **Future-proofing note for other tenant-scoped pickers** (alertRule, ranger, event, accompanying-rangers-input.tsx user-search, etc.): import `useIsPlatformAdminWithoutTenant` + `PLATFORM_ADMIN_EMPTY_TENANT_MESSAGE` from `@/lib/auth/use-platform-admin-empty-context` and apply the same conditional hint pattern when implementing or modifying those pickers. Keeps empty-state copy consistent across the app.

BLOCKERS:     none — locked task queue complete. Awaiting user direction.
GIT_BRANCH:   main (Task 4 commit pending; will land as the next commit after this STATE.md write + queue file removal)
PORTS:        unchanged (MinIO 45197 + 45198, app 45204, postgres 45194, valkey 45196, pgadmin 45201, mailhog 45199 + 45200, pgbouncer 45195)
MODELS:
  planning:   claude-code (Phase 2 — V31 primary; main-session Opus 4.7 for Phase 8 sub-batches + Phase 7-style defect fixes + Locked Task Queue execution)
  execution:  claude-opus-4-7 main-session (per the 6.x established ship pattern — 22 consecutive clean main-session changesets) OR claude-sonnet-4-6 via Agent dispatch for genuinely parallel work
  governance: gemini-2.5-flash-lite (non-critical doc writes — cheapest)
