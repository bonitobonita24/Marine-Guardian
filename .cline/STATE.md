# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-05-31 20:58 GMT+8 by CLAUDE_CODE (Opus 4.7 architect + Sonnet 4.6 executor — Option C OVERRIDE_CONFLICT audit)

PHASE:        Phase 7 Feature Update — §210 Super Admin Panel backlog
              FULLY CLOSED (8/8). Post-backlog Options D + C shipped.

              Today's full session ship log:

                a0d4d1f feat(admin-tenants): Item 8 — atomic
                        createTenantWithAdmin + dialog admin section
                ac19df9 chore(state+governance): Item 8 ship checkpoint
                81a6674 chore(state): §210 backlog CLOSED
                260439b chore(lint): clear 27 ESLint errors
                e9514e5 fix(admin-tenants): Item 8 Visual QA defects
                bcc6a42 chore(state+governance): Items 7+8 Visual QA pass
                b17ab37 chore(state): Items 7+8 Visual QA + lint debt cleared
                a6002d9 refactor: code structure
                19e4d4a chore(governance): Option D — Phase 7 lint gate
                e318ad1 chore(state): Option D checkpoint
                5dfa8f5 feat(patrol-schedule): Option C —
                        PATROL_SCHEDULE:OVERRIDE_CONFLICT audit row
                7853f18 refactor(patrol-schedule): wrap audit in $transaction
                        ⚠ SECURITY REGRESSION — used platformPrisma
                        (bypasses L6 tenant guard on tenant-scoped path)
                7a11af8 Revert "refactor(patrol-schedule): wrap ..."
                        ← restores 5dfa8f5 content (HEAD = 5dfa8f5 content)

              🎯 PRODUCT.md §210 SUPER ADMIN PANEL BACKLOG (8/8 SHIPPED):
                ✅ Item 1 — /admin landing (e5cd29d)
                ✅ Item 2 — /admin/tenants column completion (aedbcf7)
                ✅ Item 3 — platformUser router
                ✅ Item 4 — Tenant context switching (c20190e + aa8fbef)
                ✅ Item 5 — Schedule conflict detection (4aa25b1)
                ✅ Item 6 — Super_admin tenant page redirect (5e487c3)
                ✅ Item 7 — /admin/users page (8a30879)
                ✅ Item 8 — Initial Site Admin in tenant create (a0d4d1f)

              🎯 POST-BACKLOG HARDENING:
                ✅ Option D — Phase 7 lint gate hardening (19e4d4a)
                ✅ Option C — OVERRIDE_CONFLICT audit row (5dfa8f5)
                   + 7a11af8 revert of 7853f18 regression

LAST_DONE:    Executed Option C per prior STATE.md NEXT recommendation.
              Two-step ship with one revert mid-flight:

              STEP 1 — feat 5dfa8f5 (Sonnet dispatch 1):
                apps/web/src/server/trpc/routers/patrolSchedule.ts (+53/-1)
                  - Added `writeAuditLog` to the @marine-guardian/db
                    import (existing `prisma` import on L6).
                  - `create` mutation: when `input.overrideConflicts ===
                    true`, after the existing conflict-check block,
                    do the create then write an AuditLog row with
                    action="PATROL_SCHEDULE:OVERRIDE_CONFLICT",
                    entityType="PatrolSchedule", entityId=created.id,
                    changesJson={rangerUserId, scheduledStart,
                    scheduledEnd}. Non-override path unchanged.
                  - `update` mutation: same pattern — when override
                    flag set, do the update then write audit row with
                    the resolved data field as changesJson (Dates
                    converted to ISO strings).
                apps/web/src/server/trpc/routers/__tests__/patrolSchedule.test.ts (+46/-1)
                  - Mocked writeAuditLog at module level.
                  - 2 existing override tests now assert
                    `writeAuditLog` was called with the correct
                    action string.
                  - 2 new tests assert NO audit write on non-override
                    success paths (create + update).
                docs/CHANGELOG_AI.md (+9/-0) — entry prepended.

                HARD GATE results: typecheck 0 errors → 707/707 vitest
                passing → `pnpm --filter @marine-guardian/web build`
                clean. All three Phase 7 Step 19 commands exit 0.

                Tradeoff: writeAuditLog called sequentially after
                mutation (non-atomic). Argument cast as
                `prisma as unknown as any` because the tenant-guarded
                extended client doesn't trivially satisfy the helper's
                `PrismaClient | TransactionClient` signature.

              STEP 2 — refactor 7853f18 (Sonnet dispatch 2):
                Intended to wrap both override branches in
                `prisma.$transaction(async (tx) => ...)` to make the
                create+audit atomic and remove the `as any` cast.
                Sonnet instead switched to `platformPrisma.$transaction`
                — citing platform.ts L281 as precedent.

              STEP 3 — REVERT 7a11af8 (Opus, via git revert):
                platformPrisma is the BARE PrismaClient with NO
                extensions (packages/db/src/client.ts L23):
                  `export const platformPrisma = new PrismaClient();`
                while `prisma` is `.$extends(encryption).$extends(
                tenantGuardExtension)`.

                Using platformPrisma inside coordinatorProcedure
                (tenant-scoped, ctx.tenantId session-derived) BYPASSES
                the L6 Prisma guardrail extension on the override-
                mutation path only. This violates:
                  - Rule 7 L6 — "always active" tenant guardrails
                  - security.md "SUPERADMIN AND PLATFORM-LEVEL ROLES":
                    "Superadmin queries that bypass tenant scoping
                    MUST use a dedicated Prisma client instance
                    WITHOUT the L6 tenant-guard extension ...
                    Regular tenant-scoped routers continue using the
                    guarded prisma instance."

                platform.ts L281 is the legitimate use of
                platformPrisma: it CREATES a brand-new tenant + admin
                from a superadmin context (tenantId=null on the
                audit row). patrolSchedule mutations are tenant-scoped
                operations on existing tenants — they MUST use
                tenant-guarded prisma.

                Reverted via `git revert --no-edit 7853f18`. Working
                tree at 7a11af8 is byte-identical to 5dfa8f5
                (`git diff 5dfa8f5..HEAD --stat` = empty).

              FINAL STATE: 5dfa8f5 content on main, with revert
              commit 7a11af8 on top. Tests + lint + build all pass.
              Option C functional — every override-flag mutation now
              writes a PATROL_SCHEDULE:OVERRIDE_CONFLICT audit row
              with the correct tenant guard intact.

              KNOWN RESIDUAL CODE SMELL (acceptable):
                `writeAuditLog(prisma as unknown as any, {...})` —
                two occurrences in patrolSchedule.ts override paths
                (L138, L221 region). Not a security issue. Could be
                tightened to `as unknown as PrismaClient` if revisited.

              KNOWN RESIDUAL BEHAVIOR (acceptable):
                Non-atomic create→audit on override path. If the audit
                INSERT fails after the schedule INSERT succeeds, the
                override happens without audit trail. Same DB
                connection / immediate sequential write makes this
                vanishingly unlikely in practice. A future polish dispatch
                could wrap in `prisma.$transaction` (the tenant-guarded
                instance — NOT platformPrisma) if the cast can be
                resolved cleanly; needs investigation of why Sonnet
                couldn't make the tx type match writeAuditLog's signature
                without escaping to platformPrisma.

NEXT:         Option C shipped. POST-§210 BACKLOG (Bonito picks one,
              no priority order locked):

              A) Remaining Visual QAs on earlier §210 items:
                   - Item 5 standalone Visual QA — assignment dialog
                     conflict UI + override path with NEW audit row
                     visible in Activity Log (needs seeded overlapping
                     schedules in dev DB to exercise)
                   - Item 2 standalone Visual QA confirmation

              B) Item 7 follow-up — extend platformUser.updateRole
                 router to accept tenantId. Add Tenant Select back to
                 EditUserRoleDialog. Estimate ~80L. Tier 1.

              C-followup) Polish 5dfa8f5's `as unknown as any` cast:
                 either (a) tighten to `as unknown as PrismaClient`
                 (minimal change) OR (b) properly wrap in
                 `prisma.$transaction(async (tx) => ...)` using the
                 tenant-guarded instance — needs investigation of why
                 the extended client's tx doesn't satisfy
                 writeAuditLog's signature. Estimate ~15L Tier 1.

              E) Fresh PRODUCT.md section — Bonito's choice:
                 - v2/PRODUCT.md adoption (Batches 5-7 breaking
                   migrations)
                 - Phase 8 Batch 5 Item 2 (PatrolTrack Materialization)
                 - AreaBoundary derivation algorithm
                 - Coverage Report 4th page
                 NOT prioritized — defers to Bonito.

              F) Dev tenant cleanup — "QA Test Reef" + "QA Admin"
                 still in dev DB from earlier Visual QA; optional
                 housekeeping via /admin/tenants Deactivate.

              Recommended next-session opener:
                Bonito chooses one of A, B, C-followup, E, F.
                Suggested defaults:
                  - "Execute Option A: Item 5 Visual QA on override
                    audit" — closes the loop on Option C by
                    confirming the audit row appears in /admin
                    Activity Log after a real override action.
                  - "Execute Option C-followup: polish cast" — tiny
                    cleanup of the `as any` smell from 5dfa8f5.
                  - "Execute Option B: extend updateRole with tenantId"
                    — restores feature parity with EditUserRoleDialog
                    pre-Item 7 stub.

BLOCKERS:     none

GIT_BRANCH:   main. 4 unpushed commits on main:
                5dfa8f5 feat(patrol-schedule) Option C audit
                7853f18 refactor — REVERTED in 7a11af8
                7a11af8 Revert "refactor ..."
              (Plus earlier unpushed e318ad1, 19e4d4a from prior
              session.) Bonito to push when ready.

PORTS:        APP=45204 (dev), DB=5432 (dev container), Valkey=6379

MODELS:
  planning:   claude-code (Opus 4.7 — V32 architect)
  execution:  claude-sonnet-4-6 via Claude Code (V32 executor — all
              file writes except this STATE.md V32 R1 exception
              + the Opus-invoked `git revert` for 7a11af8)
  governance: gemini-2.5-flash-lite (not routed today; Sonnet 4.6
              handled CHANGELOG writes)

LINES_TOUCHED: ~108 lines net across patrolSchedule.ts (+53) and
               patrolSchedule.test.ts (+46) and CHANGELOG_AI.md (+9).
               7853f18 + 7a11af8 cancel out (net 0). STATE.md
               rewrite (this file) ~200 lines — Opus, not counted
               toward Sonnet budget.
CHECKPOINT_TYPE: full
FILES_TOUCHED: ["apps/web/src/server/trpc/routers/patrolSchedule.ts",
                "apps/web/src/server/trpc/routers/__tests__/patrolSchedule.test.ts",
                "docs/CHANGELOG_AI.md",
                ".cline/STATE.md"]
TIER_CLASSIFICATION: 1 — lightweight (3 files, ~108L, 1 module).
                     Note: 2 Sonnet dispatches required (dispatch 1
                     succeeded, dispatch 2 introduced regression
                     reverted by Opus). V32 R4 honored — no third
                     redispatch; remaining cleanup options listed in
                     NEXT for Bonito to pick.
