# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-19 19:10 GMT+8 by CLAUDE_CODE (Opus 4.7 — Sub-batch 4.1a SHIPPED to main)

PHASE:        Phase 8 Batch 4 IN PROGRESS. Sub-batch 4.1a (AreaBoundary scaffold) SHIPPED to main. 4 sub-batches remain (4.1b/c/d/e).

LAST_DONE:    Sub-batch 4.1a AreaBoundary scaffold SHIPPED to main via squash-merge from feat/area-boundary-scaffold. Branch deleted. Origin/main updated. Schema: BoundarySource + GeometryType enums + AreaBoundary model with tenant + creator + 2 indexes + back-relations on Tenant/User. Migration: 20260519183900_add_area_boundary (additive — 2 new enums, 1 new table, 2 indexes, 2 FK constraints, no breaking changes). Shared schemas: area-boundary.ts (createAreaBoundarySchema, updateAreaBoundarySchema). Router: areaBoundary.ts (list/getById tenantProcedure, create/update/delete adminProcedure) mounted on appRouter. Tests: areaBoundary.test.ts (6 cases). Verification: pnpm typecheck 0 errors, pnpm lint 0 warnings, vitest 232/232.

NEXT:         Open a FRESH Claude Code session and say "Start Sub-batch 4.1b" to begin PatrolTrack scaffold. See BATCH_4_PLAN below for remaining sub-batch breakdown.

═══════════════════════════════════════════════════════════════════
BATCH 4 PLAN — v2 Foundation Tables (additive, non-breaking)
═══════════════════════════════════════════════════════════════════

GOAL: Add the 5 new v2 tables + their attribution FKs to Event/Patrol.
      Defer breaking changes to later batches.

OPEN QUESTIONS RESOLVED (locked unless user objects):
  1. AlertHistory disposition → KEEP. v2 missed it in entity list, but functionality is load-bearing (immutable audit of which alerts fired against which events). Add to PRODUCT.md when swap happens.
  2. Event.priority Int→enum → DEFER. Needs coordinated UI rollout + downstream code updates. Out of Batch 4 scope.
  3. Event.state `new_event`→`new` rename → DEFER. Same reason. Out of Batch 4 scope.
  4. Subject.region/sex → Pull from existing `additional_json` at sync time when Subject sync refactor lands. Defer column extraction. Out of Batch 4 scope.

SUB-BATCHES (each fresh Claude Code session, ≤80K tokens):

  Sub-batch 4.1a — AreaBoundary scaffold ✅ SHIPPED to main 2026-05-19 (squash-merged from feat/area-boundary-scaffold, branch deleted)
    Files shipped: schema.prisma (BoundarySource + GeometryType enums + AreaBoundary model + back-relations on Tenant/User),
                   packages/db/prisma/migrations/20260519183900_add_area_boundary/migration.sql,
                   packages/shared/src/schemas/enums.ts (boundarySourceSchema, geometryTypeSchema),
                   packages/shared/src/schemas/area-boundary.ts,
                   packages/shared/src/schemas/index.ts (export added),
                   apps/web/src/server/trpc/routers/areaBoundary.ts,
                   apps/web/src/server/trpc/routers/index.ts (mount added),
                   apps/web/src/server/trpc/routers/__tests__/areaBoundary.test.ts (6 cases)
    Verification: typecheck 0 / lint 0 / vitest 232 (was 226)
    Tier: 2 — Opus executor — actual ~27K tokens

  Sub-batch 4.1b — PatrolTrack scaffold
    Files: schema.prisma edit, migration, routers/patrolTrack.ts, schemas/patrolTrack.ts, tests (2), sync-engine hook stub (1)
    Scope: Prisma model (one-track-per-patrol unique FK) + migration + tRPC read-only scaffold + stub for Patrol Track Materialization job
    Estimated: ~7 files, ~30K tokens
    Tier: 2 (Opus executor)

  Sub-batch 4.1c — FuelEntry + ReportExport scaffolds
    Files: schema.prisma edit, 2 migrations, 2 routers, 2 schemas, tests (3)
    Scope: Both tables are CC-native, no ER sync. CRUD routers for FuelEntry; create/poll/download routers for ReportExport.
    Estimated: ~9 files, ~35K tokens
    Tier: 2 (Opus executor)

  Sub-batch 4.1d — NotificationRecipient split (HAS DATA MIGRATION)
    Files: schema.prisma edit (remove fields from Notification + add NotificationRecipient), migration with backfill, notification router rewrite, Notification Center UI update, tests (3)
    Scope: Move userId/isRead/notificationType OFF Notification, ONTO NotificationRecipient. Backfill: 1 NotificationRecipient row per existing Notification. Update router to join. Update UI to filter by current user via NotificationRecipient.
    Estimated: ~7 files, ~40K tokens
    Tier: 3 (Opus executor — only sub-batch with real data migration risk)

  Sub-batch 4.1e — Event/Patrol attribution columns + Tenant ArcGIS fields
    Files: schema.prisma edit, migration, router updates (event.ts + patrol.ts), tests (2)
    Scope: Add `area_boundary_id` (FK, nullable), `area_derived_at` (DateTime, nullable) to Event + Patrol. Add `arcgis_boundary_url` + `arcgis_boundary_outfields` (both nullable) to Tenant. No derivation job yet — fields stay NULL until Batch 5+ wires the derivation logic.
    Estimated: ~6 files, ~25K tokens
    Tier: 2 (Opus executor)

ORDERING: 4.1a → 4.1b → 4.1c → 4.1d → 4.1e (any order works; only 4.1d has real risk).

POST-BATCH-4 DEFERRED WORK (future batches):
  - Event.priority Int→enum migration + UI updates (Batch 5+)
  - Event.state `new_event`→`new` rename + downstream refs (Batch 5+)
  - Patrol.state enum expansion (open|scheduled|active|done|cancelled|draft) (Batch 5+)
  - AlertRule restructure (kind/condition_json/recipients_json/channels/cooldown_minutes) (Batch 6+)
  - Tenant sync-engine fields (active_check_interval_seconds + 13 others) — Batch 6+
  - AuditLog impersonation fields (acting_user_id/impersonated_as_tenant_id/severity/user_agent) — Batch 7+
  - User.fullName → User.name rename (mechanical refactor, opportunistic)
  - SyncLog enum additions (subject_groups, tracks) — pull alongside related sync work
  - Subject.region/sex column extraction — pull from additional_json when Subject sync refactor lands
  - AreaBoundary derivation algorithm + re-derive BullMQ job + manual rebuild button — Batch 5
  - PatrolTrack Materialization job (per-patrol GPS track fetch from ER) — Batch 5

ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

═══════════════════════════════════════════════════════════════════

BLOCKERS:     none

GIT_BRANCH:   main (clean, pushed to origin)
GIT_HEAD:     af94a98 feat(users): user management data table — Phase 8 Batch 3 Sub-batch 3.1

REMOTE_STALE: origin/feat/event-kanban still exists on GitHub. User has not authorized remote delete. Run `git push origin --delete feat/event-kanban` when ready, then `git fetch --prune`.

PLAYWRIGHT_READY: Playwright 1.58.2 + system Chrome at /opt/google/chrome/chrome already installed. MCP tools loaded (mcp__plugin_playwright_playwright__browser_*). No `sudo npx playwright install chrome` needed. Browser-driving auth gate remains — agent cannot self-authenticate (CREDENTIALS.md is gitignored + agent-prohibited per H1/H4). Visual checks delegated to human.

TOKEN_ESTIMATE:      This session — ~40K including v2 diff + Batch 4 plan. Under 80K SAFE zone.
CHECKPOINT_TYPE:     full (Batch 4 plan locked; ready for first sub-batch session)
TIER_CLASSIFICATION: Batch 4 = Tier 3 (5 sub-batches, 35+ files, score >40). Properly decomposed into 5 Tier-2 sub-batches.

PORTS:        APP=45204 DB=42731 CACHE=42733 (unchanged — verify via cat .env.dev | grep _PORT)

MODELS:
  planning:    claude-code (V31 primary — Opus 4.7 plans the decomposition)
  execution:   claude-opus-4-7 (per [[feedback_marine_guardian_opus_executor_default]] — Opus executor on all dispatched Marine-Guardian work)
  governance:  gemini-2.5-flash-lite (cheapest, non-critical writes)
