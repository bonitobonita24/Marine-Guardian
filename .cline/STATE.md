# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-12 by CLAUDE_CODE (Opus 4.7 direct execution — Phase 8 Batch 2 Item 3: Alert History Log shipped on branch feat/alert-history-log, squash-merge pending user confirmation. Sonnet bypassed up-front per documented hook-injection thrashing pattern; ~10-file task estimated at Tier-2/3 boundary scored within Opus 100K ceiling per §2.5b.)

PHASE:        Phase 8 Batch 2 Item 3 COMPLETE — Alert History Log shipped on feat/alert-history-log (squash-merge pending). PRODUCT.md L182 ("Alert history log") spec deferral #1 cleared. Phase 8 Batch 2 backlog now reduced to 2 items.

LAST_DONE:    Alert History Log. Branch `feat/alert-history-log` (squash-merge pending).
              - `packages/db/prisma/schema.prisma`: +AlertHistory model (id, tenantId, alertRuleId?, eventId?, firedAt, matchedPriority, recipientCount, ruleNameSnapshot, eventTitleSnapshot + 4 indexes including composite [tenantId, firedAt]); +backlinks `alertHistory AlertHistory[]` on Tenant/Event/AlertRule.
              - `packages/db/prisma/migrations/20260512044045_add_alert_history/`: Prisma-generated migration.sql (CREATE TABLE alert_history + 4 indexes + 3 FK constraints — tenant RESTRICT, alertRule SET NULL, event SET NULL) + down.sql (reversible: drops constraints/indexes/table in safe order).
              - `packages/jobs/src/processors/alerts.processor.ts`: added `alertHistory` to typedTx shape; one `typedTx.alertHistory.create(...)` call per matching rule INSIDE the existing $transaction, AFTER the recipient loop — captures matchedPriority, recipientCount, ruleNameSnapshot, eventTitleSnapshot so the audit trail survives later rule/event deletion.
              - `packages/jobs/src/__tests__/alerts.processor.test.ts`: +`alertHistory: { create: vi.fn() }` in mockTx via vi.hoisted; +2 new test cases — (f) one history row per matching rule with snapshot fields populated (NOT per recipient — verified with 2-recipient fixture asserting notification.create×2 and alertHistory.create×1), (g) multiple matching rules write multiple history rows. RED→GREEN order confirmed (initial run showed 2 failed / 5 passed; post-implementation 7/7 pass).
              - `apps/web/src/server/trpc/routers/alertHistory.ts`: new router with `list` endpoint — cursor pagination, optional alertRuleId filter, tenant-scoped via tenantProcedure, includes nested alertRule + event for display.
              - `apps/web/src/server/trpc/routers/__tests__/alertHistory.test.ts`: 5 router tests — tenant scoping, alertRuleId filter, pagination with nextCursor, no-cursor when within limit, UNAUTHORIZED when tenant missing.
              - `apps/web/src/server/trpc/routers/index.ts`: registered alertHistoryRouter.
              - `apps/web/src/app/(dashboard)/alerts/history/page.tsx`: new client page — table view (Fired At / Rule / Event / Priority badge / Recipients column) with cursor-based prev/next pagination; italic "(deleted)" indicator when alertRule or event was later deleted using the snapshot fields; Event column links to /events/[id] when event still exists.
              - `apps/web/src/app/(dashboard)/alerts/page.tsx`: added "View History" outlined Button as Link to /alerts/history in the header beside the "New Rule" Dialog trigger.
              - `apps/web/src/components/ui/table.tsx`: shadcn Table primitive installed via `pnpm dlx shadcn@latest add table` (first use of Table in this repo).
              Migration applied to dev DB. Typecheck 6/6 clean, lint 5/5 clean, tests 57/57 pass (was 50/50 — added 2 processor tests + 5 router tests). 4 migrations total in repo now.

              EXECUTION NOTES:
              Per memory-governance §2.5b and the documented hook-injection thrashing lesson from earlier this session, Sonnet was bypassed up-front for this task. The scope assessment showed ~10 code files + 4 governance writes (score 49 on Tier 3 formula, just over the 40 threshold), but every Read call inside a subagent triggers vercel-plugin auto-suggesters (~1.5K per Read on `prisma/schema.prisma` and `apps/web/**` patterns) plus claude-mem prior-observation pointers (~500 per Read), so even tightly-scoped Tier-2 tasks blow Sonnet's 30K budget in practice. Opus-direct executed cleanly within ~50K total session tokens — well within the 100K ceiling and well under the 200K context window. No retries, no thrashing signals. Hook injection is still firing (~10× this session) but Opus 4.7 absorbs it without quality degradation.

              ALERT SYSTEM SPEC NOW FULLY SATISFIED:
              - Configurable alert rules per tenant (PRODUCT.md L171) — ✅ shipped 2026-05-10
              - Rule condition + channels (PRODUCT.md L172) — ✅ shipped
              - Active/disabled toggle (PRODUCT.md L173) — ✅ shipped
              - Edit and delete rules (PRODUCT.md L174) — ✅ shipped
              - Alert history log (PRODUCT.md L182) — ✅ THIS SESSION
              The Alert System section of PRODUCT.md has no remaining unimplemented bullets.

              UNTOUCHED (kept out of scope):
              - Notification.patrolId derivation from Event.patrolId — still null on all alert-generated notifications. Future task can wire this either via Event schema (+patrolId FK on Event) or via rule.scope="patrol" matching. Scaffolding in place from prior session's migration (20260512024505).
              - Filter type drift (PRODUCT.md L189) — still a 1-line spec edit pending from user.
              - PDF/CSV exports — next backlog item.
              - Real-time SSE — last backlog item; Tier 3, mandatory split per §1.

NEXT:         Phase 8 Batch 2 Item 4 — PDF/CSV Exports (PREPPED FOR NEXT SESSION).
              **Plan file:** `.cline/tasks/phase8-batch2-item4-exports.md` (full prep doc — locked decisions, tech choices, 5 sub-session breakdown with file manifests, test strategy, execution model).
              **Locked decisions** (DO NOT re-ask):
                - Access: all authenticated roles can export (tenant scoping still applies)
                - Filter behavior: respect current UI filter state (URL query → reuse list-endpoint Zod schemas)
                - Audit: write AuditLog DATA_EXPORT row per export (consistent with L5)
                - CSV: hand-rolled toCsv() helper, no library, RFC 4180 + UTF-8 BOM for Excel
                - PDF: @react-pdf/renderer (JSX composition, MIT, server renderToBuffer)
                - Endpoints: Next.js Route Handlers `/api/exports/{entity}/route.ts` with manual auth
                - Row cap: 10,000 per export (HTTP 413 above)
                - Rate limit: rateLimiters.upload tier (20/min)
              **Sub-sessions (in order):**
                SS-0 Foundation (~25K, ~7 files): route-auth helper + toCsv + ExportPdfDocument + writeExportAudit + filename builder + tests. Install @react-pdf/renderer. Blocks SS-1..4. Branch: feat/exports-foundation.
                SS-1 Events (~18K, ~4 files): /api/exports/events/route.ts + tests + UI buttons on /events page. Branch: feat/exports-events.
                SS-2 Patrols (~18K, ~4 files): same pattern. Branch: feat/exports-patrols.
                SS-3 Alert Rules (~18K, ~4 files): same pattern + special channel/condition formatting. Branch: feat/exports-alert-rules.
                SS-4 Notifications + Alert History combined (~22K, ~6 files): both audit-style views, share session. Branch: feat/exports-audit-views.
              **Execution model:** Opus-direct recommended for SS-0 (boundary case) and SS-4 (combined size). Sonnet-eligible for SS-1..3 IF hook-injection issue resolves by execution time; otherwise Opus-direct per §2.5b — try SS-1 with Sonnet first, observe, decide for SS-2/3.
              **After Item 4 ships:** only 1 Phase 8 Batch 2 backlog item remains — Real-time SSE (Tier 3, MANDATORY split per memory-governance.md §1).

              Open spec deferrals updated: (a) ~~Alert history log PRODUCT.md L182~~ — CLEARED this session; (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, STILL OPEN; (c) ~~Notification.patrolId FK click-through PRODUCT.md L187~~ — cleared earlier today. Only one spec deferral remains open repo-wide.

              Open spec deferrals updated: (a) ~~Alert history log PRODUCT.md L182~~ — CLEARED this session; (b) filter type drift PRODUCT.md L189 — 1-line PRODUCT.md edit by human, STILL OPEN; (c) ~~Notification.patrolId FK click-through PRODUCT.md L187~~ — cleared earlier today. Only one spec deferral remains open repo-wide.

BLOCKERS:     none. All Docker services healthy. Migration applied cleanly to dev DB. Worker container running cleanly. Lint + typecheck + tests all green on feat/alert-history-log.
GIT_BRANCH:   feat/alert-history-log (squash-merge to main pending user confirmation; will be 1 commit ahead of origin/main after merge).
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
TOKEN_ESTIMATE:    ~50K consumed this session (governance pre-flight + schema/migration + RED/GREEN test cycle + router + UI + validation + governance writes). Within the 60K SAFE feedback-memory threshold for Opus-direct execution; well under 80K SAFE zone and the 100K Opus ceiling per §2.5b. No autocompact thrashing observed despite ~10 hook-injection events.
CHECKPOINT_TYPE:   full
FILES_TOUCHED:     ["packages/db/prisma/schema.prisma","packages/db/prisma/migrations/20260512044045_add_alert_history/migration.sql","packages/db/prisma/migrations/20260512044045_add_alert_history/down.sql","packages/jobs/src/processors/alerts.processor.ts","packages/jobs/src/__tests__/alerts.processor.test.ts","apps/web/src/server/trpc/routers/alertHistory.ts","apps/web/src/server/trpc/routers/__tests__/alertHistory.test.ts","apps/web/src/server/trpc/routers/index.ts","apps/web/src/app/(dashboard)/alerts/history/page.tsx","apps/web/src/app/(dashboard)/alerts/page.tsx","apps/web/src/components/ui/table.tsx","docs/CHANGELOG_AI.md","docs/IMPLEMENTATION_MAP.md",".cline/memory/lessons.md",".cline/STATE.md"]
TIER_CLASSIFICATION: "3 — heavy (score 49, threshold 40), executed Opus-direct in one session per §2.5b given documented Sonnet thrashing pattern from hook injection"
MODELS:
  planning:   claude-code (Opus 4.7)
  execution:  claude-opus-4-7 direct (Sonnet bypassed per documented thrashing lesson)
  governance: claude-opus-4-7 (inline writes — Gemini Flash-Lite not used this session)
