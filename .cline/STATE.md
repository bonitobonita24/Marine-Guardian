# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-22 00:12 GMT+8 by CLAUDE_CODE (Opus 4.7 — Phase 8 Batch 6 Sub-batch 6.1c-ii SHIPPED to main as e75b9cb. 🎯🎯🎯 **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) COMPLETE.** Continues the 5.1d-B → 5.3d → 6.1a → 6.1b → 6.1c-i clean main-session ship streak (now 13 consecutive). Push pending: 6.1c-ii impl + impending governance commit = 2 commits to push to origin/main.)

PHASE:        Phase 8 Batch 6 Sub-batch 6.1c-ii SHIPPED — Coverage Report Page 3 (Area Covered) RSC + UI + data-loader extension. Closes Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template). The full funder-grade PDF deliverable now renders end-to-end: ReportExport pdf-render pipeline from Batch 5 Item 3 → Puppeteer → /print-render/[tenantSlug]/coverage/[exportId] → 3-page A4/Letter/Legal landscape HTML → MinIO upload → user downloads PDF. Page 1 is the patrol index (6.1a), Page 2 is the area boundary summary with map + chart + variance callout (6.1b), Page 3 is the per-boundary coverage table with coverage_km + pro-rated coverage_hrs + Est badge + missing-tracks footer (this sub-batch). LineString boundaries appear on Page 2 as dashed reference outlines but are excluded from Page 3 area coverage (clipping is meaningless against an unbounded coastline).

              NEW Page 3 RSC apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page-3-area-covered.tsx (~235 LOC): page-break-before:always section frame, header "Page 3 — Area Covered", 5fr/4fr grid (coverage table left + Recharts bar chart right), table columns Boundary Name / Coverage Patrols / Coverage KMS / Coverage HRS with per-row "Est." amber badge on hrsEstimatedCount > 0, totals tfoot row, methodology footer notes + missingTracksCount callout (singular "patrol has" / plural "patrols have" / omitted when 0), empty-state when sorted.length === 0. NEW Client island components/area-covered-chart.tsx (~135 LOC) — Recharts horizontal BarChart on coverageKm domain, teal-600 bars (Page 2 uses cyan-500), "Coverage KM" axis label, 1-decimal tick formatter + 2-decimal tooltip with "km" suffix, top-N filter with stable name-ASC tiebreak, empty-state when no bars have coverage. Tests: 12 net new vitest cases in __tests__/page-3-area-covered.test.tsx via renderToStaticMarkup with AreaCoveredChart mocked (section frame, page-break-before:always, empty state, ranking, alphabetical tiebreak, Est badge presence/absence, footer Est emphasis, missing-tracks plural/singular/omitted, totals row, chart-column mount).

              EXTENDED get-coverage-report-data.ts (+18 LOC): new fields `areaCoverage: BoundaryCoverage[]` (sorted by coverageKm DESC then areaName ASC) and `missingTracksCount: number` (patrols with totalHours > 0 but no trackLineString) on the CoverageReportData contract. Consumes accumulateCoverageByBoundary() from packages/shared/src/lib/coverage-clip (6.1c-i library — locked decision Coverage Clip Library Choice = narrow @turf/* modules). Mapping: patrols → {id, trackLineString, totalHours} (PatrolForCoverage shape); boundariesForAttribution already in AreaBoundaryForDerivation shape — the single-sourced boundary projection across area-derivation + area-attribution + coverage-clip. Polygon-only filter happens inside the aggregator. Tests: +4 net new in apps/web/src/server/coverage-report/__tests__/get-coverage-report-data.test.ts new "Page 3 area coverage" describe (per-Polygon accumulation from a crossing track; missingTracksCount=1 for patrols with hours but no track and =0 for draft rows with neither; zero-row seeding for enabled Polygons with no coverage; LineString boundaries excluded from areaCoverage but included in enabledAreas).

              MOUNTED coverage-report.tsx (+5 LOC) — Page3AreaCovered rendered after Page2AreaBoundarySummary, passing areaCoverage + missingTracksCount.

LAST_DONE:    Sub-batch 6.1c-ii SHIPPED to main as e75b9cb. 7 files / +934 -5 LOC. NEW: page-3-area-covered.tsx (~235 LOC RSC), components/area-covered-chart.tsx (~135 LOC Client), __tests__/page-3-area-covered.test.tsx (12 cases). MODIFIED: get-coverage-report-data.ts (areaCoverage + missingTracksCount fields + accumulator wiring), __tests__/get-coverage-report-data.test.ts (+4 Page 3 cases + assertion on falls-back test for new fields), coverage-report.tsx (+5 mount), __tests__/coverage-report.test.tsx (AreaCoveredChart mock + areaCoverage/missingTracksCount fixture defaults). One inline lint correction during verification: removed unused `hasAnyCoverage` const after refactor (no-unused-vars strict). Pre-commit verification on main: shared typecheck 0 / shared lint 0 strict --max-warnings 0 / shared 137 tests pass (unchanged) / web typecheck 0 / web lint 0 strict / web 392 tests pass (376→392 = +16 net new: +4 data-loader Page 3 + +12 Page 3 RSC) / jobs 122 unchanged / storage 12 unchanged. Monorepo total 647→663 (+16 net new for 6.1c-ii). Main-session Opus 4.7 implementation per the 6.1c-i precedent (commit directly on main, no feature branch) — now 13 consecutive clean main-session ships. 6.1c-ii consumed ~30K main-session tokens (estimate was 30-35K — landed clean in the middle of the SAFE zone).

NEXT:         **Push pending: 2 commits to push to origin/main** — e75b9cb (6.1c-ii impl) + impending governance commit for STATE.md + IMPLEMENTATION_MAP.md updates. Both commits land on main directly (per 6.1c-i precedent; no feature branch).

              Then open a FRESH Claude Code session and pick the next item from the POST-BATCH-5+BATCH-6-ITEM-1 DEFERRED ledger. **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) is COMPLETE** — 3 of 3 sub-batches shipped (Page 1 — Patrol Index from 6.1a; Page 2 — Area Boundary Summary from 6.1b; Page 3 algorithm from 6.1c-i; Page 3 UI from 6.1c-ii). The Coverage Report deliverable is end-to-end operable.

              **Or: other Batch 6+ candidates from POST-BATCH-5 DEFERRED ledger (preserved below):**
              - Per Area Report content for /print-render/[tenant]/per-area/[exportId]
              - Area Boundary Management UI (full /admin/area-boundaries CRUD page)
              - Sync Health page
              - FuelEntry UI + receipt photo upload to MinIO
              - FuelEntry analytics aggregation (SUM(liters) per area per range + L/km KPI math per spec §182)

              **Or: Batch 6 BREAKING MIGRATIONS** (if user wants to clear the v2 backlog before more features):
              - Event.priority Int→enum (critical|high|medium|low) + downstream UI updates
              - Event.state `new_event`→`new` rename + downstream refs
              - Patrol.state enum expansion (open|scheduled|active|done|cancelled|draft)
              - AlertRule restructure (kind/condition_json/recipients_json/channels/cooldown_minutes)
              - Tenant sync-engine fields (active_check_interval_seconds + 14 others)
              - User.fullName → User.name rename (mechanical refactor)
              - SyncLog enum additions (subject_groups, tracks)
              - Subject.region/sex column extraction

              **Or: Batch 7+ items**:
              - AuditLog impersonation fields (acting_user_id/impersonated_as_tenant_id/severity/user_agent)
              - NotificationRecipient retention policy enforcement (1y per v2 §645)
              ONCE ALL DEFERRED WORK SHIPS: swap docs/v2/PRODUCT.md → canonical docs/PRODUCT.md (mechanical swap, no code changes).

PRIOR_6_1_C_I_DETAIL: Sub-batch 6.1c-i SHIPPED 2026-05-21 as commit 452fe30 + governance 45becfe. packages/shared/src/lib/coverage-clip/ (5 src + 1 test file, 29 vitest cases). clipTrackToBoundary (turf.lineSplit + midpoint booleanPointInPolygon + length sum + empty-FeatureCollection fallback), computeCoverageHours (pro-rate), accumulateCoverageByBoundary (aggregator). Decision lock: Coverage Clip Library Choice = narrow @turf/* modules (boolean-point-in-polygon + helpers + length + line-split ^7.1.0). shared 108→137. 6.1c-i consumed ~40K main-session tokens.

PRIOR_6_1_B_DETAIL: Sub-batch 6.1b SHIPPED 2026-05-21 as squash-merge a8e44d9 + governance 1bce881. Coverage Report Page 2 (Area Boundary Summary) + packages/shared/src/lib/area-attribution/ (5 pure functions + 20 vitest cases). 6.1b consumed ~70K main-session tokens. shared 88→108, web 358→376. 3 inline lint corrections + 1 react-leaflet major version bump (v4→v5 for React 19 compat).

PRIOR_6_1_A_DETAIL: Sub-batch 6.1a SHIPPED 2026-05-21 as squash-merge 7de30cf + governance f817d72. Coverage Report Page 1 (Patrol Index) + packages/shared/src/lib/coverage-period/ (8 pure functions). 6.1a consumed ~50K main-session tokens. shared 51→88 (+37), web 334→358 (+24). 5 inline lint/test corrections during verification + dir rename coverage/→coverage-report/.

PRIOR_5_3_D_DETAIL: Sub-batch 5.3d SHIPPED 2026-05-21 as squash-merge cd9ec36 + governance 665afd9. 🎯 Phase 8 Batch 5 Item 3 COMPLETE. 🎯 Phase 8 Batch 5 COMPLETE (all 3 Items: AreaBoundary derivation + PatrolTrack materialization + ReportExport pdf-render pipeline). /(dashboard)/exports admin list page (Client Component, coordinator+ gated) + status-badge.tsx + export-row.tsx (live polling refetchInterval=3000) + retry-button.tsx (admin-only) + reportExport.retry adminProcedure + Generate Report CTA on patrols/page.tsx. End-to-end pipeline operable from the UI.

PRIOR_5_3_C_DETAIL: Sub-batch 5.3c SHIPPED 2026-05-21 as squash-merge ad5a8d5 + governance 9c69fc2. @marine-guardian/storage package (S3/MinIO surface, 12 vitest cases). Download Route Handler /api/exports/reports/[id]/download with auth + AuditLog. Processor stub replaced with real uploadPdf call. row.filePath stores KEY only.

PRIOR_5_3_B_DETAIL: Sub-batch 5.3b SHIPPED 2026-05-21 as squash-merge 53c115e + governance 39a1596. pdf-render BullMQ queue + processor + worker + reportExport.create enqueue wiring + EXPORT_REQUESTED AuditLog. 25 net new tests.

PRIOR_5_3_A_DETAIL: Sub-batch 5.3a SHIPPED earlier 2026-05-21 as squash-merge 5837821 + governance 3898e0a. Puppeteer PDF renderer Docker service + service-token guard + pdf-renderer-client.ts + /print-render route stub + middleware service-token guard.

PRIOR_5_2_C_DETAIL: Sub-batch 5.2c SHIPPED 2026-05-21 as squash-merge 234b9ff. 🎯 Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job) COMPLETE. patrol.rebuildTracks adminProcedure + RebuildTracksButton UI.

PRIOR_5_1_E_DETAIL: Sub-batch 5.1e SHIPPED 2026-05-20 (squash-merge 88744ed). 🎯 Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm) COMPLETE for scope under remit. 5.1d Area A (er-sync inline re-derive) DEFERRED.
