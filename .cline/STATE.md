# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-03T(Phase5-complete) by CLAUDE_CODE

PHASE:        Phase 5 complete — PAUSED
LAST_DONE:    Phase 5 validation complete. All 9 commands exit 0:
              install / validate-inputs / check-env / check-product-sync /
              lint / typecheck / test / build / audit (no HIGH CVEs).
              Fixes merged via fix/phase5-validation → main (squash):
              edge auth config for middleware, Suspense on login page,
              bcrypt serverExternalPackages, node: scheme webpack handler,
              pnpm overrides for tar CVE chain (HIGH × 6, now clean).
NEXT:         Phase 6 — say "Start Phase 6" in a NEW Claude Code session.
              Start Docker services: bash deploy/compose/start.sh dev up -d
              Then: pnpm db:migrate && pnpm db:seed
              Then: Visual QA per Rule 16 at http://localhost:45204
BLOCKERS:     none
GIT_BRANCH:   main (fix/phase5-validation merged and deleted — clean working tree)
PORTS:        APP=45204 DB=45194 PGBOUNCER=45195 CACHE=45196 MINIO=45197 PGADMIN=45201
MODELS:
  planning:   claude-code (Phase 2 — V31 primary)
  execution:  claude-sonnet-4-6 via Claude Code (V31 primary; Cline deprecated)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
