# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-07-10 by CLAUDE_CODE (Full Auto — milestone-1 deploy)

PHASE:        Phase 7 — MILESTONE 1 COMPLETE (3-tier RBAC + v1.0.0 shipped to all envs)
LAST_DONE:    Shipped 3-tier tenant RBAC + v1.0.0 white-label to prod + staging + demo.
              main FF-merged @ dd1e48e, annotated tag v1.0.0 pushed. Migration
              20260710093000_tenant_rbac_3tier (enum RENAME x3 + one_tenant_superadmin_per_tenant
              index) applied to ALL 3 live DBs. Pre-flight caught + fixed a guaranteed index
              collision (2 site_admin in tenant `ph` on every env) by demoting the duplicate
              admin@demo-site.local -> administrator, keeping admin@mail.com as sole owner
              (prod+demo normalized directly, backed up; staging inherited via prod-copy refresh).
              All 3 envs verified: roles correct, index present, /api/health=200, /ph/login=200.
              Local dev already had RBAC from prior session; footer (B) NOT yet in dev container.
NEXT:         BUILD MILESTONE C — custom-role permission-matrix layer (fleet standard section 4:
              feature registry + role_permissions strict-CRUD + hasPermission() resolver at 3
              surfaces + tenant_superadmin-only role-builder UI). TDD + full gate, then deploy to
              all 3 envs (same data-first path). Also rebuild local dev off main (footer + latest).
BLOCKERS:     none (deploy authorization given; custom-role layer is [HOW], build it).
OWNER-GATED:  Demo credential cleanup (stale webmaster@marine-guardian.local cross-seed +
              admin@demo.com super_admin-with-tenant_id) — the credential reseed, still owner-gated.
GIT_BRANCH:   main (feat/tenant-rbac-3tier merged; safe to delete)
RELEASE:      v1.0.0 (first official release) — git tag is source of truth
ENVS:         prod=mg.powerbyte.app  staging=mg-staging.powerbyte.app  demo=mg-demo.powerbyte.app
              tenant slug = `ph` on all envs; sole owner admin@mail.com; platform mgr = tenantadmin@/webmaster@
MODELS:
  planning:   claude-code (Opus)
  execution:  claude-sonnet via Agent(spec-executor)
  governance: gemini-2.5-flash-lite
