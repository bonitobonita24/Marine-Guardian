# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually. (File path kept in .cline/ for historical continuity; Cline deprecated V31.)
# Updated: 2026-05-17 by CLAUDE_CODE (Opus 4.7 — SSE-3 plan locked, Tier 3 split decomposed into 3 sub-task scope docs, ready for fresh-session execution)

PHASE:        Phase 8 Batch 2 — SSE-1 + SSE-2 SHIPPED (commit 3710472). SSE-3 PLANNED via Opus Architect role (memory-governance.md §4): decomposed into 3 Sonnet-sized sub-tasks (.cline/tasks/sse-3a.md, sse-3b.md, sse-3c.md). All sub-tasks share single branch feat/sse-events-and-hardening — single squash-merge at end of 3c.

LAST_DONE:    SSE-3 reconnaissance + plan. Found 2 STATE.md NEXT items already done (notifications/page.tsx exists; heartbeat already implemented in route.ts lines 57+110-124) — pruned from scope. Found 1 item undeclared in PRODUCT.md ("notification preferences: stream vs digest") — DEFERRED per Rule 10/29 (cannot infer). Final SSE-3 scope = (3a) UI consumers: RealtimeProvider mount + NotificationBell in header + realtime invalidation on /notifications page; (3b) replace sidebar 30s refetchInterval with SSE-driven invalidation; (3c) rate-limit Route Handler with rateLimiters.api per userId + process-local connection metrics module.

NEXT:         Execute SSE-3a in a NEW Claude Code session (clean context budget). Read .cline/tasks/sse-3a.md as the self-contained task brief. After 3a commits to feat/sse-events-and-hardening: open NEW session for 3b. After 3b: open NEW session for 3c. After 3c: human reviews 3-commit branch then squash-merges to main + pushes origin. DEFERRED: notification preferences (stream-vs-digest) — requires PRODUCT.md spec edit first, then separate Phase 7 Feature Update.

BLOCKERS:     none

GIT_BRANCH:   main (origin/main synced)
GIT_HEAD:     b0b36c8 chore(scan): record 2026-05-17 re-scan
GIT_PREV:     3710472 feat(realtime): SSE-1 + SSE-2 (squash-merge)

STALE_BRANCHES_TO_CLEAN: feat/user-management, scaffold/part-7 (both local-only, unrelated to current work, candidates for branch -D cleanup in a future session)

TOKEN_ESTIMATE:      ~55K consumed this Opus Architect session (reconnaissance + plan + 3 task docs + STATE update — within 60K SAFE zone)
CHECKPOINT_TYPE:     full (per memory-governance.md §2 — task scope docs + STATE.md written)
FILES_TOUCHED:       [".cline/tasks/sse-3a.md", ".cline/tasks/sse-3b.md", ".cline/tasks/sse-3c.md", ".cline/STATE.md"]
TIER_CLASSIFICATION: Architect-role session (Opus 4.7) — no source code modified; planning artefacts only. Each downstream sub-task is Tier 1-2 with ≤30K Sonnet budget per memory-governance.md §1 Step 2.5.

PORTS:        APP=45204 DB=42731 CACHE=42733 (unchanged — verify via cat .env.dev | grep _PORT)

MODELS:
  planning:   claude-code (V31 primary)
  execution:  claude-opus-4-7 via Claude Code (this session — Opus direct execution; framework default Sonnet 4.6 via subagent dispatch when memory-governance.md §4 Architect-Execute Model applies)
  governance: gemini-2.5-flash-lite (cheapest, non-critical writes)
