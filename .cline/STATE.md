# Project State — Marine Guardian Command Center
# Auto-generated by Claude Code after every task. Never edit manually.
# Updated: 2026-07-13 by CLAUDE_CODE (Full Auto — MUNICIPAL-WATERS EQUIDISTANCE FIX, LOCAL)
#
# LATEST (2026-07-13, branch feat/report-portrait-colors-maps, 15 commits, ALL LOCAL — owner-gated deploy):
#   Root-caused + fixed: adjacent municipal WATER polygons overlap (buffer(15km)−all land, "imaginary
#   line" 2026-06-29) → containingWaterMunicipality picked FIRST-in-arbitrary-order not nearest coast →
#   244/257 Baco events sat in BOTH Baco+Calapan water; 210 (incl ALL 15 wildlife sightings) belonged to
#   Calapan. FIX A (506d83a): resolve overlap by nearest coastline (PH RA 7160/RA 8550 median-line
#   equidistance). +2 TDD tests, gate GREEN (typecheck/lint/product-sync/web build/shared 223). Backfilled
#   dev: 718 events (Baco 257→21, Calapan 243→451, wildlife all→Calapan) + 863 patrols (Baco 209→52,
#   Calapan 589→723) — both via nearest-coast. Visual-QA confirmed (Baco LE 35→0, Monitoring 63→12).
#   Dev app+worker REBUILT (live processor now uses fix). FIX B (regen non-overlapping water geometry) =
#   DEFERRED/owner-gated (PENDING_DECISIONS 2026-07-13; reverses "imaginary line"). See memory
#   project_marine_guardian_municipal_waters_equidistance_fix_0713. NOTHING DEPLOYED.
#
# (prior) 2026-07-12 by CLAUDE_CODE (Batch 2: MPA-zone-by-province + Rangers-on-Duty count + hide-idle rename — shipped all 3 envs)

PHASE:        Phase 7 — branch feat/report-portrait-colors-maps @ 01fd136 (9 commits, ALL LOCAL). "Patrol Review" layout on report_map PDF: Total Patrols 3-col table + vertical flow + Patrol Tracks Heatmap folded onto the SAME patrol page (1 page, "Page 3 of 8"). Both dashboard dialogs Playwright-verified. Events "missing from report map" investigated → NOT a bug (report splits LE/Monitoring per page; events on Monitoring page 2; owner: leave as-is). Gate green (1801 tests/lint/build), PDF-verified. ONLY remaining = OWNER-GATED deploy (staging→prod). Batch 2 fixes previously SHIPPED all 3 envs (prod sha-640f0f4).

✅ DONE THIS SESSION — BATCH 2 (2026-07-12, owner-reported 3 issues):
  1. MPA Zone dropdown now filters by Province (was: ignored province, showed Apo Reef under
     Oriental Mindoro). report-filter-bar.tsx visibleZones. commit d398614.
  2. "Rangers on Duty" KPI now counts segment leaders ∪ accompanying rangers (was 0 while roster
     showed 14 "on patrol"). Shared server/lib/ranger-on-duty.ts used by KPI + roster (no more drift).
     Verified =14 on dev. commit 640f0f4.
  3. "Hide idle on map" → "Hide inactive rangers on map" (rename only; behavior is roster-status by
     owner's choice — "stale" GPS ≠ roster "idle"). commit 640f0f4.
  Deployed: staging (staging-latest) + prod (re-pinned sha-a2fa2d5→sha-640f0f4, backup
  .env.bak-pre-640f0f4) + dev rebuilt. Health 200 all 3.
  4. CC LIVE PATROL TRACKS were gone + "RECENT PATROLS: 0" vs ACTIVE PATROLS 55 — both filtered patrols
     by startTime∈48h; all 55 open patrols started 2026-05-29 (seed) → empty. Fix: CC map trackMode
     "inRange"→"active" (map.patrolTracks.active = recent patrols-with-tracks; EarthRanger tracks live
     on COMPLETED patrols, open are header-only — owner decision 2026-06-24); dashboard.activePatrols
     → currently-OPEN patrols (matches KPI). Verified dev: 41 track polylines + 15 recent-patrol rows
     (both were 0). commit 20afa30, deployed all 3 (prod sha-20afa30). Note: `active` tracks not
     muni-filtered (poss. follow-up); 55 stale open seed patrols = data-hygiene item.
  5. STALE-SESSION SELF-HEAL (commit fefa157, deployed all 3, prod sha-fefa157): a broken empty-slug
     session hitting /ph was sent to root /login (tenant acct can't sign in there → "Invalid credentials",
     looked like "/ph redirects to root domain"). Now cleared + sent to the TENANT login /{slug}/login
     (cookie deleted so no authed→dashboard bounce loop). Owner unblocked via hard-refresh; self-heal
     prevents recurrence. Prod fresh-login verified working (tenantSlug=ph, /ph→/ph/dashboard 200).

  ⭐ PENDING — DIAGNOSED, owner-requested, NOT yet implemented: remove "Marine Entry" from ALERTS &
     ESCALATIONS. Root cause FOUND: "Marine Entry" events = **Skylight Entry Alert** (eventType
     value=entry_alert_rep, display="Skylight Entry Alert", category=analyzer_event). Codebase ALREADY
     excludes Skylight from recentEvents/eventBreakdown/lastIncident/activeEvents via
     `NOT: { eventType: { display: { contains: "skylight", mode: "insensitive" } } }` (dashboard.ts) —
     the alerts panel just never got it. FIX (ready to build): apply the same Skylight exclusion to
     `alertHistory.list` (WHERE + via event→eventType relation) AND `dashboard.alertStats`
     (UNACKNOWLEDGED KPI). Mirror existing Skylight tests. Shared helper `isSkylightOrAnalyzerEvent`
     exists (apps/web/src/lib/event-label.ts). Note: alertHistory snapshots title only; filter via the
     live event.eventType.display (deleted-event alerts kept).
  ⭐ NEXT (owner-specced, DEFERRED): Rangers-on-Duty tile → clickable DRILLDOWN DIALOG — left: main
     rangers (segment leaders) each with nested accompanying rangers; right: FULL current-patrol track
     polyline per ranger (PatrolTrack.trackGeojson, like Interactive Report Map). See memory
     project_marine_guardian_report_map_kpi_fixes_0712.md.

✅ DONE THIS SESSION (2026-07-12, owner "yes do all"):
  - DIAGNOSIS: owner's "login not working" = WRONG URL, not a bug. Root /login is PLATFORM-only
    (config.ts:83 rejects non-null tenantId). Tenant accts (tenant_superadmin webmaster@…,
    tenant_admin admin@admin.com) MUST use /ph/login; only platform tenant_manager (tenantadmin@…)
    uses root /login. Verified EVERY canonical acct authenticates end-to-end on dev+staging+prod
    (real vault pw via /api/auth/csrf → /api/auth/callback/credentials).
  - BUG FIXED (commit a2fa2d5 on main, pushed): middleware built `/${tenantSlug}/dashboard` = "//dashboard"
    for authed empty-slug JWTs → browsers resolve protocol-relative to host "dashboard" (the
    https://dashboard/ NXDOMAIN). Guard added: empty-slug non-platform authed user → /login. +2 tests.
    Full pre-merge gate green (turbo lint · 1789 web tests · Next build · typecheck).
  - DEPLOYED all 3 envs: staging (staging-latest=a2fa2d5), PROD re-pinned sha-cd1784f→sha-a2fa2d5
    (/etc/komodo/stacks/marine-guardian/.env, backup .env.bak-pre-a2fa2d5; compose needs FULL -f set —
    app has cross-file depends_on), dev rebuilt off main. All health 200 + logins verified post-deploy.
  - STALE SESSIONS PURGED: security_version +1 on prod(7)/staging(7)/dev(7) — invalidates old JWTs
    (the 0711 direct-SQL reconciliation hadn't bumped it). Reversible by decrement.
  - Memory: project_marine_guardian_login_url_and_dashboard_redirect_fix_0712.md ;
    global lesson: middleware.protocol-relative-slash-slash-redirect.

✅ DONE PRIOR SESSION (2026-07-11, owner "do it all" then "yes reconcile it"):
  0. PUSHED main → origin — the 2 local doc-only commits (803a538 + e13ef11) are now on origin/main
     (c049d44..e13ef11). Then committed 63fff86 (push-to-demo login-hint fix) LOCALLY.
  1. MILESTONE C → DEMO stack (mg-demo.powerbyte.app): promoted prod image sha-cd1784f → demo-latest
     (demo == prod); pdf promoted from `latest` (pdf is NOT SHA-tagged; prod pins PDF_IMAGE_TAG=latest);
     recreated app/worker/pdf-renderer; migration 20260711090000_add_custom_role_permissions applied
     clean; health 200. Migrate-only, curated demo data preserved.
  2. DEMO cred reseed: admin@demo.com → canonical vault password (verified via bcrypt round-trip +
     live browser login at /ph/login → /ph/dashboard). Deactivated stale cross-seeded
     webmaster@marine-guardian.local. tenantadmin@powerbyteitsolutions.com remains platform mgr.
  3. FULL CREDENTIAL AUDIT (all 4 envs vs vault universal-login-credentials.enc.yaml): 6/6 canonical
     password hashes PASS. dev + demo fully canonical-compliant.
  4. STAGING + PROD TENANT RBAC RECONCILED to canonical (owner-approved; staging-first then prod;
     DB backups taken: /root/mg-{staging,prod}-backup-pre-rbac-reconcile-*.sql.gz):
       - webmaster@powerbyteitsolutions.com rebound → tenant_superadmin of tenant `ph`, activated.
       - admin@admin.com created → tenant_admin of `ph`, active, canonical pw.
       - admin@mail.com demoted tenant_superadmin→tenant_admin (frees one-owner index), kept inactive.
       - Client accounts (JosephD@bluealliance.earth, admin@demo-site.local) left inactive as-is.
       - Live-verified: staging webmaster@ + admin@admin.com → /ph/dashboard; PROD webmaster@ →
         mg.powerbyte.app/ph/dashboard (header role tenant_superadmin, footer v1.1.0 + white-label).
     Direct DB writes only (no reseed); a future staging refresh-from-prod carries prod's reconciled state.

LAST_DONE:    Custom-role permission-matrix RBAC (Milestone C, v1.1.0) LIVE on dev/staging/prod/demo.
              matrixProcedure wired into all 117 grantable endpoints; prod pinned sha-cd1784f, healthy.
              All login credentials canonical + reconciled + browser-verified across every environment.

NEXT (un-gated): none — nothing left to build/fix. Milestone C complete & shipped to all 4 envs;
              credentials audited/reconciled/verified everywhere.

OWNER-GATED [WHAT] (re-surface next session; do NOT act without explicit word):
  A. PUSH main → origin — main is 1 commit ahead (63fff86, push-to-demo login-hint fix, script/doc only).
     Gated purely because it is a push (Tier-1 default = local only). Trivial + safe, no deploy impact.
  (The former gated items 0/1/2 — push main, demo deploy, demo cred reseed — are all DONE this session.)

  OPTIONAL / not requested — prod tenant identity: tenant `ph` is still named "Demo Site" and its real
  Blue Alliance client accounts (admin@mail.com, JosephD@) are inactive. Only surface if owner wants to
  formalize the real prod tenant/owner — otherwise leave dormant. Not an action item.

BLOCKERS:     none.
GIT_BRANCH:   main @ 63fff86 (LOCAL — 1 ahead of origin/main e13ef11, UNPUSHED) · tag v1.1.0 (pushed)
              · prod pinned to image sha-cd1784f, healthy. Working tree clean.
PORTS:        dev app 45204
