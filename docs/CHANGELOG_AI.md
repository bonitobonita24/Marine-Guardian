# CHANGELOG — AI-Generated Changes
# Format: Rule 15 attribution format
# Agent values: CLINE | CLAUDE_CODE | COPILOT | HUMAN | UNKNOWN
# ---

## 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3b: pdf-render BullMQ queue + processor + worker + reportExport.create enqueue wiring
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation per STATE.md NEXT-field brief. Continues the 5.1d-B/5.1e/5.2a/5.2b/5.2c/5.3a pattern — 7 consecutive clean main-session ships. Estimated ~35K main-session, actual ~60K including orientation + the pdf-renderer-client relocation analysis + lint/typecheck gates + governance. Within main-session SAFE zone.)
- Why:                 Closes the loop from v2 PRODUCT.md L505-506 (4.1c scaffolded reportExport.create with status=queued + a TODO marker; 5.3b fires the producer + consumer sides of the pdf-render pipeline). Mirrors 5.1c area-rederive + 5.2b patrol-track-materialize patterns end-to-end (queue → processor → worker → barrel registration → router wiring → tests). reportExport.create now enqueues a pdf-render BullMQ job after the row insert + writes EXPORT_REQUESTED AuditLog. The worker process consumes the job, drives the row through queued → rendering → ready (or failed) lifecycle, calls renderPdfViaService (relocated from apps/web → packages/jobs in this sub-batch), and stubs the storage upload pending 5.3c MinIO surface.
- Files added:         packages/jobs/src/queues/pdf-render.queue.ts (44 LOC — `getPdfRenderQueue()` + `enqueuePdfRender(payload): Promise<string>` with jobId pattern `pdf-render:${exportId}` NOT scoped by tenantId because exportId is globally unique cuid; mirrors 5.2b queue shape); packages/jobs/src/queues/__tests__/pdf-render.queue.test.ts (115 LOC — 5 vitest cases: queue name resolution, enqueue+jobId+payload assertion, empty-id graceful return, dedupe determinism across different tenantId/userId for the same exportId, distinct exportIds produce distinct jobIds); packages/jobs/src/processors/pdf-render.processor.ts (149 LOC — `processPdfRender(job)` + exported `RenderResult` interface; loads ReportExport by (id, tenantId) via platformPrisma cast same as 5.1c+5.2b; loads Tenant by id for slug; transitions status queued→rendering BEFORE the renderer call; constructs printUrl from WEB_APP_INTERNAL_URL + tenant.slug + row.reportType + row.id; LANDSCAPE_REPORT_TYPES set = {"coverage"} per v2 §771 wide funder template, others portrait; STUB storage path `marine-guardian-${env}-exports/${tenantId}/${YYYY}/${MM}/${exportId}.pdf` returned without writing to MinIO — real upload lands in 5.3c; success: status=ready + filePath + fileSizeBytes + completedAt; failure: re-throw to trigger BullMQ retry, status=failed + errorMessage + completedAt flip only on LAST attempt via `(attemptsMade + 1) >= opts.attempts` check); packages/jobs/src/processors/__tests__/pdf-render.processor.test.ts (~290 LOC — 12 vitest cases: validateTenantContext before I/O + ordering, scoped row load with id+tenantId, tenant load by id, queued→rendering transition before renderer call, printUrl construction from env+slug+reportType+exportId, landscape derivation per report type, success path with filePath path-shape regex + fileSizeBytes=buffer.length + completedAt instanceof Date + return shape, transient failure NO status=failed flip on non-last attempt, final failure status=failed+errorMessage flip on last attempt, missing WEB_APP_INTERNAL_URL throws early before renderer call, validate-throw propagation skips row load; afterAll hook restores env var capture); packages/jobs/src/workers/pdf-render.worker.ts (54 LOC — `startPdfRenderWorker()` factory + exported `PDF_RENDER_LIMITER={max:5,duration:1000}` + `PDF_RENDER_CONCURRENCY=2` constants per DECISIONS_LOG "Puppeteer Concurrency + Rate Limiter" 5.3a lock; mirrors 5.2b worker shape); packages/jobs/src/workers/__tests__/pdf-render.worker.test.ts (94 LOC — 6 vitest cases mirroring 5.2b worker test shape: queue name, limiter object value, limiter constant, concurrency constant, processor registration, connection injection).
- Files modified:      packages/jobs/src/queues/types.ts (+18 LOC — added PdfRenderJobPayload interface extending BaseJobPayload with exportId field + JSDoc explaining jobId pattern + the tenant-vs-export identity tradeoff; QUEUE_NAMES.PDF_RENDER='pdf-render' added to const satisfies; JobPayloadMap entry); packages/jobs/src/queues/index.ts (+5 LOC — re-exports getPdfRenderQueue + enqueuePdfRender + type PdfRenderJobPayload); packages/jobs/src/processors/index.ts (+1 LOC — re-exports processPdfRender + type RenderResult); packages/jobs/src/workers/index.ts (+5 LOC — re-exports startPdfRenderWorker + PDF_RENDER_LIMITER + PDF_RENDER_CONCURRENCY); packages/jobs/src/start-workers.ts (+9 LOC — registers startPdfRenderWorker as the 7th worker; QUEUE_NAMES enum auto-includes pdf-render in the registered-workers log line); packages/jobs/src/index.ts (+17 LOC — root-level barrel re-exports enqueuePdfRender + getPdfRenderQueue + startPdfRenderWorker + limiter/concurrency constants + RenderResult type; also adds new lib re-exports for renderPdfViaService + PdfRendererError + RenderPdfInput from the relocated pdf-renderer-client); apps/web/src/server/trpc/routers/reportExport.ts (+34 LOC — added `import { enqueuePdfRender } from "@marine-guardian/jobs"`; rewrote `create` mutation to call enqueuePdfRender({exportId, tenantId, userId}) AFTER prisma.reportExport.create insert then prisma.auditLog.create with action="EXPORT_REQUESTED" + entityType="ReportExport" + entityId=created.id + changesJson={reportType, paperSize}; sequential await pattern matches patrol.rebuildTracks + areaBoundary.rebuild precedent — $transaction unsuitable because BullMQ writes to Valkey outside Postgres); apps/web/src/server/trpc/routers/__tests__/reportExport.test.ts (+95 LOC — added auditLog.create + @marine-guardian/jobs mock with enqueuePdfRender; renamed describe block to "(RBAC + 5.3b pipeline wiring)"; +3 new cases: enqueuePdfRender called with row id+tenantId+userId, EXPORT_REQUESTED AuditLog written with row id as entityId + correct entityType/action/changesJson, invocation order create→enqueue→audit via vi.mock invocationCallOrder); .env.example (+8 LOC — new "WEB APP INTERNAL URL" section with `WEB_APP_INTERNAL_URL=http://marine-guardian_dev_app:3000` and explanatory comment about renderer Docker network resolution; NOT secret — not in CREDENTIALS.md); .env.dev (+6 LOC same content with real value).
- Files relocated:     apps/web/src/server/lib/pdf-renderer-client.ts → packages/jobs/src/lib/pdf-renderer-client.ts (git mv, content unchanged); apps/web/src/server/lib/__tests__/pdf-renderer-client.test.ts → packages/jobs/src/lib/__tests__/pdf-renderer-client.test.ts (git mv, content unchanged). Required because the new 5.3b processor in packages/jobs CANNOT import from apps/web (would create a circular dep — web already depends on jobs as a downstream consumer). Continues the 5.1c (area-derivation) + 5.2a (patrol-track-materialization) relocation arc. The client has no app-specific dependencies (only process.env + fetch + Buffer) so the move is mechanically clean.
- Files deleted:       none
- Schema/migrations:   none (consumer-side + producer-side wiring only — ReportExport model + ReportExportStatus enum already shipped in 4.1c migration 20260519231301; AuditLog model already exists; pdf-render queue uses BullMQ which writes to Valkey not Postgres)
- Errors encountered:  (1) Test file had an awkward `function afterEachRestoreEnv()` no-op helper plus a "teardown — restore WEB_APP_INTERNAL_URL" terminal test that depended on test ordering (vitest doesn't guarantee order across files but does guarantee within a single describe). Lint passed but the structure was fragile. Refactored to a proper `afterAll` hook with capture of ORIGINAL_ENV at top of describe block — cleaner and order-independent. (2) Initial git add invocation included the pre-rename pdf-renderer-client paths in apps/web/ — fatal: pathspec did not match. Resolved by relying on `git add packages/jobs/` to stage the rename pair (R) automatically since git mv had already staged it. (3) Web test count math was initially confusing because the relocated pdf-renderer-client.test.ts (5 cases) moved out of the web tree at the same session 3 new wiring cases were added to reportExport.test.ts — net change web 300 → 298 = -5 relocated + 3 new. Jobs gained the 5 relocated tests + 5 queue + 12 processor + 6 worker = 28 minus 1 dummy teardown removed during the afterAll refactor = 27 net new in jobs (92 → 119). Monorepo net new for 5.3b: 25.
- Errors resolved:     (1) afterAll hook captures + restores ORIGINAL_ENV per-suite — eliminated the order-dependent teardown test (jobs test count 120 → 119). (2) Directory-level git add for the relocated pair.
- Verification:        web lint 0/0 errors+warnings (strict --max-warnings 0) · web typecheck 0 errors · web tests 300 → 298 (-5 pdf-renderer-client relocated, +3 new reportExport wiring cases) · jobs lint 0/0 errors+warnings · jobs typecheck 0 errors · jobs tests 92 → 119 (+27 net new) · Monorepo total 392 → 417 tests (+25 net new). Verified independently on feat branch AND on main after squash-merge (commit 53c115e).

## 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3a: Puppeteer PDF renderer Docker service + service-token guard
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation. Continues the 5.1e/5.1d/5.2a/5.2b/5.2c pattern of skipping subagent dispatch for cleanly-scoped sub-batches while the 5.1d dispatch-mechanism issue remains unresolved. 5.3a is the first sub-batch of Phase 8 Batch 5 Item 3 — ReportExport pdf-render pipeline — and ships dev infrastructure only. Estimated ~30K main-session, actual ~80K including orientation + lint/typecheck iteration (2 strict-mode passes) + governance.)
- Why:                 Opens Phase 8 Batch 5 Item 3 (ReportExport pdf-render queue + Puppeteer Docker service + streaming download Route Handler). 5.3a builds the sandboxed Puppeteer HTTP service + the typed Node-side renderer client + the /print-render/* internal route stub + the service-token auth path between them. Pipeline producer wiring (BullMQ pdf-render queue + processor + worker + reportExport.create enqueue) lands in 5.3b; MinIO storage + streaming download Route Handler in 5.3c; admin UI list + polling + retry in 5.3d. Loop unclosed today: reportExport.create writes status=queued rows but never enqueues (4.1c scaffold limitation per v2 PRODUCT.md L505).
- Files added:         deploy/pdf-renderer/Dockerfile (38 LOC — node:22-slim + chromium apt + ca-certificates + fonts-liberation + fonts-noto-color-emoji + fonts-noto-cjk + dumb-init; non-root node user; PUPPETEER_SKIP_DOWNLOAD=true + PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium; npm install --omit=dev for puppeteer-core); deploy/pdf-renderer/package.json (17 LOC — standalone Node service, NOT a pnpm workspace member; only dep is puppeteer-core ^23.11.1; type=module; engines.node >=22.0.0); deploy/pdf-renderer/src/server.js (194 LOC — native http listener on :4000 with GET /health returning {status:"ok"} + POST /render with X-PDF-Renderer-Token header guard via manual constant-time compare, JSON body validation (printUrl/paperSize/landscape/exportId), puppeteer.launch with --no-sandbox --disable-setuid-sandbox --disable-dev-shm-usage --disable-gpu args, page.setExtraHTTPHeaders forwarding the service token, page.goto with waitUntil:'networkidle0' and configurable NAV_TIMEOUT_MS, page.pdf with format/landscape/printBackground:true/preferCSSPageSize:true, browser.close in finally; SIGTERM+SIGINT graceful shutdown); deploy/pdf-renderer/.dockerignore (7 LOC); deploy/compose/dev/docker-compose.pdf-renderer.yml (38 LOC — pdf-renderer service joining external app_network, no host ports (internal Docker network only), container/hostname using ${COMPOSE_PROJECT_NAME}_pdf_renderer, build context = deploy/pdf-renderer/, healthcheck on /health, restart unless-stopped); apps/web/src/server/lib/pdf-renderer-client.ts (66 LOC — typed HTTP wrapper exported as `renderPdfViaService(input: RenderPdfInput): Promise<Buffer>` with `RenderPdfInput = {printUrl, paperSize:'A4'|'Letter'|'Legal', landscape, exportId}` and `PdfRendererError` class carrying optional status; reads PDF_RENDERER_SERVICE_URL + PDF_RENDERER_SERVICE_TOKEN from process.env directly (not env.ts) because the eventual 5.3b consumer is the worker process not the web app; POSTs to {baseUrl}/render with Content-Type+X-PDF-Renderer-Token headers and JSON body; throws on missing env, wraps network errors in PdfRendererError, surfaces non-2xx status); apps/web/src/server/lib/service-token-guard.ts (27 LOC — Edge-compatible `verifyServiceToken(presented, expected): boolean` constant-time string compare with manually unrolled `diff |= charCodeAt XOR` loop; node:crypto.timingSafeEqual unavailable in Next.js Edge runtime where middleware runs; returns false defensively on null/undefined/empty/length-mismatch); apps/web/src/server/lib/__tests__/pdf-renderer-client.test.ts (119 LOC — 5 vitest cases: header/body/URL assertion via vi.fn<typeof fetch>().mockResolvedValue + mock.calls[0] inspection with explicit non-null narrowing; Buffer return via 200 with %PDF prefix; non-200 status surfaces via PdfRendererError.status; network error wrapped in PdfRendererError; missing PDF_RENDERER_SERVICE_URL throws); apps/web/src/server/lib/__tests__/service-token-guard.test.ts (45 LOC — 7 vitest cases: valid match, null/empty rejection both sides, single-char tail diff rejection, length-mismatch prefix rejection, expected-missing defensive return-false, equal-length-trailing-byte-diff sanity); apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page.tsx (90 LOC — server component dynamic params via Promise unwrap pattern; renders deterministic body with tenant/reportType/exportId/timestamp + 1×1 transparent GIF data URI for networkidle0 wait target; @page CSS for A4 landscape print; stub banner explains 5.3a scope; VALID_REPORT_TYPES set for label sanity check).
- Files modified:      apps/web/src/middleware.ts (+20 LOC — added verifyServiceToken import; /print-render/* prefix check BEFORE publicPaths/auth chain; constant-time compare against PDF_RENDERER_SERVICE_TOKEN env; returns 401 NextResponse without valid header — bypasses normal login redirect for service-to-service calls only); apps/web/src/env.ts (+8 LOC — Zod additions PDF_RENDERER_SERVICE_URL z.string().url().optional() + PDF_RENDERER_SERVICE_TOKEN z.string().min(32).optional() with explanatory comment; optional in 5.3a — will tighten when 5.3b worker becomes the actual consumer; runtimeEnv entries added too); deploy/compose/start.sh (+11 LOC — wires docker-compose.pdf-renderer.yml for dev only with --build flag; stage/prod compose files deferred to follow-up sub-batch); .env.dev (+6 LOC PDF RENDERER section, real internal Docker URL http://marine-guardian_dev_pdf_renderer:4000 + 48-char token); .env.staging (+7 LOC same pattern with marine-guardian_staging_pdf_renderer hostname); .env.prod (+7 LOC same pattern with marine-guardian_prod_pdf_renderer hostname); .env.example (+8 LOC placeholder section with openssl generation hint); CREDENTIALS.md (+22 LOC new 🖨️ PDF Renderer Service Token section with 3 unique 48-char tokens, internal service URLs, header name + rotation procedure); docs/DECISIONS_LOG.md (+51 LOC — 4 locked decisions: "ReportExport PDF Storage Backend" MinIO bucket marine-guardian-{env}-exports rejecting disk fallback; "PDF Renderer Service Auth" X-PDF-Renderer-Token + 48-char shared secret + constant-time compare matching v2 L774-775; "Puppeteer Concurrency + Rate Limiter" worker concurrency=2 limiter={max:5,duration:1000} matching Chromium memory profile; "PDF Renderer Internal Route Path" /print-render/ deviating from v2 L724 /_print/ due to Next.js private folder routing exclusion).
- Files deleted:       none
- Schema/migrations:   none (no new Prisma models — ReportExport model + ReportExportStatus enum already shipped in 4.1c migration 20260519231301; 5.3a is dev infrastructure only)
- Errors encountered:  Initial design used /_print/[tenantSlug]/[reportType]/[exportId] folder name per v2 PRODUCT.md L724 spec — caught the Next.js private folder routing exclusion before commit. Two strict-mode lint iterations: (1) @typescript-eslint/strict-boolean-expressions on `if (!presented || !expected)` and `if (!baseUrl)` — narrowed to explicit null/undefined/empty checks; (2) require-await on async arrow functions inside fetch mocks AND no-unnecessary-type-assertion on `as typeof fetch` cast — refactored to plain fns returning Promise + vi.fn<typeof fetch>().mockResolvedValue + explicit non-null narrowing on mock.calls[0] (eslint --fix took care of one round). One typecheck iteration: TS2352 on `as [string, RequestInit]` after lint stripped earlier cast — fixed by typing the spy generic.
- Errors resolved:     (1) Route folder renamed from `_print/` to `print-render/` — locked in DECISIONS_LOG so 5.3b BullMQ producer constructs the correct printUrl. (2) strict-boolean-expressions on nullable strings — explicit `=== undefined || === ""` guards. (3) Fetch mock pattern — vi.fn<typeof fetch>().mockResolvedValue gives correctly typed mock.calls[N] tuples for downstream destructuring assertions.
- Verification:        web lint 0/0 errors+warnings (strict --max-warnings 0 · two iterations) · web typecheck 0 errors · web tests 288 → 300 PASS (+12 net new exactly matching 5+7 in the new describe blocks) · jobs lint+typecheck+tests unchanged (5.3a is web + standalone-deploy-only — no @marine-guardian/jobs changes) · jobs tests 92/92 unchanged · Total monorepo: 380 → 392 tests (+12). NO docker build invocation — Dockerfile/compose validation deferred to human-run Phase 6-style startup. Verified independently before and after squash-merge to main.

## 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.2c: patrol.rebuildTracks admin tRPC mutation + RebuildTracksButton UI stub
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation. Continues the 5.1e/5.1d/5.2a/5.2b pattern of skipping subagent dispatch for cleanly-scoped sub-batches under ~50K actual tokens while the 5.1d dispatch-mechanism issue remains unresolved. 5.2c scope was narrow — 1 router file modify + 1 test file extend + 1 new client component + 1 page wire — and mirrored the 5.1e areaBoundary.rebuild + RebuildAreaBoundariesButton pattern almost exactly, so main-session Opus delivered cleanly without dispatch overhead.)
- Why:                 Third and final sub-batch of Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job). Closes Item 2 by giving operators an end-to-end producer path into the 5.2b BullMQ queue. patrol.rebuildTracks adminProcedure tRPC mutation fans one materializePatrolTrack job per state==='open' Patrol in the target tenant, mirroring the 5.1e areaBoundary.rebuild shape. RBAC: super_admin may target any tenantId; site_admin must omit or match own — cross-tenant attempt by site_admin → TRPCError FORBIDDEN. Action label: PATROL_TRACK_REBUILD (own-tenant) or PLATFORM:PATROL_TRACK_REBUILD (super_admin cross-tenant) per security.md superadmin convention. Closed patrols (state='done' or 'cancelled') are skipped intentionally: their tracks are immutable once the patrol closed and re-fetching wastes ER API quota. The 5.2b queue's jobId pattern (patrol-track-materialize:${tenantId}:${patrolId}) dedupes any race between this admin trigger and other enqueue paths so two operators clicking Rebuild within seconds collapse to one ER fetch per patrol.
- Files added:         apps/web/src/app/(dashboard)/patrols/rebuild-tracks-button.tsx (115 LOC — Client Component mirroring apps/web/src/app/(dashboard)/patrol-areas/rebuild-button.tsx shape: useSession-based role gating (hidden for non-admin), shadcn/ui Dialog confirm with warning about ER rate-limiter timing for large tenants, trpc.patrol.rebuildTracks.useMutation with inline success/error state, no toast lib in stack — used the same inline-state pattern as RebuildAreaBoundariesButton).
- Files modified:      apps/web/src/server/trpc/routers/patrol.ts (+57 LOC — added TRPCError import + adminProcedure middleware import + enqueuePatrolTrackMaterialize import from @marine-guardian/jobs; new `rebuildTracks` adminProcedure mutation with `z.object({ tenantId: z.string().optional() })` input; isSuperAdmin/targetTenantId/isCrossTenant flags drive action label and RBAC enforcement same as 5.1e areaBoundary.rebuild; queries `prisma.patrol.findMany({ where: { tenantId: targetTenantId, state: "open" }, select: { id: true } })` then `Promise.all` enqueue per patrol with `{patrolId, tenantId: targetTenantId, userId: ctx.userId}` payload; writes prisma.auditLog.create with action/userId/tenantId/entityType="Patrol"/entityId=targetTenantId/changesJson={enqueued, scope: platform|tenant}; returns {tenantId, enqueued, action}); apps/web/src/server/trpc/routers/__tests__/patrol.test.ts (+157 LOC — extended @marine-guardian/db mock with auditLog.create; added @marine-guardian/jobs mock for enqueuePatrolTrackMaterialize; extended makeCtx signature to accept `roles: string[] = ["ranger"]` so existing 6 tests preserve default ranger role while new admin tests pass site_admin/super_admin; new `describe('patrol.rebuildTracks (5.2c)')` block with 7 cases mirroring 5.1e areaBoundary.rebuild test shape: rejects operator / rejects field_coordinator / site_admin own-tenant happy path with state='open' query scope + 3-job fan-out + AuditLog write / site_admin cross-tenant FORBIDDEN with no fan-out + no AuditLog / super_admin own-tenant PATROL_TRACK_REBUILD not PLATFORM: / super_admin cross-tenant PLATFORM:PATROL_TRACK_REBUILD with fan-out targeting specified tenant + changesJson.scope=platform / empty-tenant still writes AuditLog enqueued=0); apps/web/src/app/(dashboard)/patrols/page.tsx (+2 LOC — wired RebuildTracksButton into existing flex header alongside Export CSV/PDF buttons); apps/web/src/app/api/exports/patrols/__tests__/route.test.ts (+9 LOC — added `platformPrisma: {}` stub to @marine-guardian/db mock with explanatory comment; required because route.ts imports `patrolListFilters` from patrol router, patrol router now imports enqueuePatrolTrackMaterialize from @marine-guardian/jobs, and the jobs barrel eagerly loads workers including area-rederive.processor.ts which casts platformPrisma at module load — necessary blast-radius fix from the patrol.ts import change).
- Files deleted:       none
- Schema/migrations:   none (consumer-side wiring only — no new Prisma models, no new columns; AuditLog and Patrol models already exist; patrol-track-materialize queue/processor/worker were shipped in 5.2b)
- Errors encountered:  Initial test run hit 4 FORBIDDEN failures because makeCtx signature was `(tenantId)` not `(tenantId, roles)` — my new admin tests passed roles as second arg that got silently ignored, defaulting to ["ranger"]. Fixed by extending makeCtx signature.
- Errors resolved:     (1) makeCtx signature widened to accept optional roles array (default ["ranger"] preserves prior tests); (2) route.test.ts @marine-guardian/db mock needed platformPrisma stub because adding `import { enqueuePatrolTrackMaterialize } from "@marine-guardian/jobs"` to patrol.ts cascaded the jobs barrel's eager worker load into the export route's module graph via the `patrolListFilters` cross-import. Surgical fix scoped to the one test file affected.
- Verification:        web lint 0/0 errors+warnings · web typecheck 0 errors · web tests 281→288 PASS (+7 net new exactly matching 5.2c describe block) · jobs lint+typecheck+tests unchanged (5.2c is web-only — no @marine-guardian/jobs changes) · jobs tests 92/92 unchanged · Total monorepo: 373 → 380 tests (+7). Verified independently after every edit.
- Commit:              feat/5.2c-patrol-rebuild-tracks-admin branch → squash-merge to main pending user authorization. Branch creation deferred to user authorization step.
- Scope adjustment:    None — 5.2c shipped exactly per STATE.md NEXT field brief from prior session. One minor blast-radius extension (route.test.ts mock) was necessary because the cross-import chain (route.ts → patrol.ts → @marine-guardian/jobs barrel) eagerly loads workers; the mock addition is contained to one file and documented inline.
- Memory updates:      Pending — will add 🟡 fix entry to lessons.md about the platformPrisma mock leak through barrel imports as a recurring pattern for any web file that imports from a router that imports from @marine-guardian/jobs.
- Item 2 progress:     3 of 3 sub-batches shipped (5.2a d2a1dd1 ✅ + 5.2b db50f6c ✅ + 5.2c PENDING_USER_AUTH). 🎯 Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job) effectively COMPLETE upon merge.
- ⏭️  Next:             Phase 8 Batch 5 Item 3 — ReportExport pdf-render queue + Puppeteer Docker service + streaming download Route Handler. After Item 3 ships → Batch 5 COMPLETE → proceed to Batch 6+ (breaking migrations + user-visible features).

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.2b: BullMQ patrol-track-materialize queue + processor + worker
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation. Continues the 5.1e/5.1d/5.2a pattern of skipping subagent dispatch for cleanly-scoped sub-batches under ~50K actual tokens while the 5.1d dispatch-mechanism issue remains unresolved. 5.2b scope was narrow — 3 implementation files + 3 test files following the established 5.1c area-rederive factory shape — and main-session Opus delivered cleanly without orientation overhead.)
- Why:                 Second sub-batch of Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job). Wraps the 5.2a `materializePatrolTrack` helper in BullMQ queue/processor/worker infrastructure so async materialization can be triggered from any context — primarily the 5.2c admin tRPC mutation (next sub-batch) which will fan jobs into this queue per active Patrol in tenant. Closes the producer/consumer arc: 5.2a built the consumer-side primitive, 5.2b wraps it in queue infrastructure, 5.2c builds the producer side (admin mutation). Conservative rate limiter `{max:20, duration:1000}` is intentionally tighter than 5.1c's 50/sec because the ER `/subject/{id}/tracks/` endpoint is network-bound (single fetch per job), carries higher per-job cost (potential GeoJSON with thousands of coordinates), and typically has stricter vendor-side rate limits than the events/patrols endpoints used by er-sync.processor.ts. Concurrency=5 × ~200-500ms median per job ≈ 10-25 jobs/sec arithmetic ceiling; the 20/sec limiter is the binding constraint preventing ER API overrun during bulk rebuilds.
- Files added:         packages/jobs/src/queues/patrol-track-materialize.queue.ts (43 LOC — exports `patrolTrackMaterializeQueue` Queue instance + `PatrolTrackMaterializeJobData` type + `enqueuePatrolTrackMaterialize(payload): Promise<string>` helper; queue name `'patrol-track-materialize'`; jobId pattern `patrol-track-materialize:${tenantId}:${patrolId}` for BullMQ-layer dedupe at enqueue time — userId deliberately excluded from jobId so two rapid enqueues from different operators on the same patrol collapse to one ER fetch); packages/jobs/src/processors/patrol-track-materialize.processor.ts (67 LOC — thin wrapper around materializePatrolTrack helper using same `platformPrisma as unknown as ExtendedPrismaClient` cast pattern as 5.1c area-rederive.processor.ts per [[2026-05-20 🟡 platformPrisma → ExtendedPrismaClient cast pattern for BullMQ processors]]; threads job.data.patrolId into helper; returns MaterializationResult for BullMQ result storage so 5.2c admin UI can surface skipReason); packages/jobs/src/workers/patrol-track-materialize.worker.ts (58 LOC — factory `createPatrolTrackMaterializeWorker(connection)` using base-worker createWorker helper with limiter `{max:20, duration:1000}` + concurrency PATROL_TRACK_MATERIALIZE_CONCURRENCY=5 exported constant for production tuning); 3 vitest files (queue 5 cases + processor 8 cases + worker 8 cases = 21 net new tests).
- Files modified:      packages/jobs/src/queues/types.ts (+PatrolTrackMaterializeJobPayload + QUEUE_NAMES.PATROL_TRACK_MATERIALIZE='patrol-track-materialize' + JobPayloadMap entry); packages/jobs/src/queues/index.ts (+5 LOC barrel); packages/jobs/src/processors/index.ts (+1 LOC); packages/jobs/src/workers/index.ts (+5 LOC); packages/jobs/src/start-workers.ts (+7 LOC — registers as 6th worker alongside er-sync/alerts/email/maintenance/area-rederive); packages/jobs/src/index.ts (+6 LOC root barrel re-exports).
- Files deleted:       none
- Schema/migrations:   none (queue infrastructure only — no DB changes, no new Prisma models, no new columns)
- Errors encountered:  None in implementation. Tests passed first try after correct mock setup (BullMQ Queue/Worker mocks via vi.mock at module level — same pattern as 5.1c area-rederive tests).
- Errors resolved:     n/a
- Verification:        jobs lint 0/0 errors+warnings · jobs typecheck 0 errors · jobs tests 71→92 (+21 net new: 5 queue + 8 processor + 8 worker) · web typecheck 0 errors (cross-package import surface intact via @marine-guardian/jobs root barrel) · web lint 0/0 errors+warnings · web tests 281/281 unchanged · Total monorepo: 352 → 373 tests (+21). Independently re-verified on main after squash-merge.
- Commit:              feat/5.2b-patrol-track-materialize-queue branch → squash-merged to main as commit db50f6c → branch force-deleted via `git branch -D` per Rule 23 standard cleanup. 12 files changed, +646 / -0.
- Scope adjustment:    None — 5.2b shipped to spec without deferrals. AuditLog write deliberately omitted at processor level: automatic materialization has no user context, AuditLog.userId is NOT NULL, so per Option A scope split the 5.2c admin manual-rebuild owns AuditLog writes where ctx.session.userId is available. DEFERRED — er-sync.processor inline enqueue (would call `enqueuePatrolTrackMaterialize` after each successful patrol upsert in syncPatrols): parallel to 5.1d Area A deferral pattern — no tests exist for er-sync.processor.ts yet, and adding a hot-path enqueue without test coverage would be undertested. Pickup path: precursor sub-batch bootstraps er-sync.processor.test.ts harness, then both 5.2b inline enqueue + 5.1d Area A unblock cleanly with shared test infra investment.
- Memory updates:      New 🟡 lesson "BullMQ jobId dedupe pattern: exclude userId for cross-operator collapse" + new 🟤 lesson "Worker rate limiter sizing is context-dependent" being added to lessons.md so 5.2c admin mutation + future BullMQ work inherit the reasoning.
- Item 2 progress:     2 of 3 sub-batches shipped (5.2a d2a1dd1 ✅ + 5.2b db50f6c ✅; 5.2c admin tRPC mutation + UI stub queued).
- ⏭️  Next:             5.2c — patrol.rebuildTracks admin tRPC mutation on patrol router (tenant-wide fan-out per active Patrol via 5.2b queue; AuditLog with PLATFORM:PATROL_TRACK_REBUILD vs PATROL_TRACK_REBUILD per security.md superadmin convention; minimal UI stub button on /patrols page mirroring 5.1e RebuildAreaBoundariesButton). Estimated ~20K main-session tokens.

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.2a: materializePatrolTrack helper + ER client fetchSubjectTracks
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation. Per STATE.md 5.1e/5.1d post-mortem recommendation, main-session remains the safe fallback for cleanly-scoped sub-batches under ~50K actual tokens while the dispatch-mechanism issue from 5.1d (2x consecutive Opus subagent stalls returning interim status with `:agentId:` continuation hint) has not been root-caused or resolved. 5.2a estimated ~30-40K, actually ~70-80K cumulative including orientation + verification + governance — within main-session SAFE zone via tight read discipline.)
- Why:                 First sub-batch of Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job). Closes the loop on Batch 4 Sub-batch 4.1b PatrolTrack scaffold stub by replacing the `throw new Error("Not implemented")` placeholder with a real fetch + atomic upsert against the PatrolTrack table. Idempotent per v2 PRODUCT.md §1043 refresh predicate — re-running for the same patrol overwrites the existing row keyed on patrolId (unique constraint). Stub previously lived in apps/web/src/server/sync/ but relocated to packages/jobs/src/lib/ for the same architectural reason as 5.1c area-derivation: the 5.2b BullMQ processor lives in @marine-guardian/jobs and apps→packages dependency direction is one-way per Rule 13.
- Files added:         packages/jobs/src/lib/__tests__/patrol-track-materialization.test.ts (~516 LOC, 14 vitest cases covering happy path, all 3 skip paths, both token paths, multi-feature aggregation, timestamp edge cases, time-range fallback order).
- Files modified:      packages/jobs/src/lib/earthranger-client.ts (+81 LOC — added `ErTrackCoordinate3`, `ErTrackFeature`, `ErTrackResponse` interfaces; extended constructor signature to optionally accept `trackToken?: string` (defaults to main `token` when omitted — backward compatible with er-sync.processor.ts and any other pre-5.2a caller); refactored private `request<T>(path, bearer?)` to accept an override bearer; added `fetchSubjectTracks(subjectId, since, until): Promise<ErTrackResponse>` that hits `/api/v1.0/subject/{id}/tracks/?since=&until=` using the trackToken when set, otherwise main token). packages/jobs/src/index.ts (+13 LOC — re-exports `materializePatrolTrack`, `MaterializationResult`, `MaterializationSkipReason` so 5.2b processor + 5.2c admin tRPC mutation can consume through the package boundary; `PrismaClientLike` deliberately NOT re-exported here because it collides on name with the area-derivation re-export already at line 44 — importers needing the type use the area-derivation re-export). packages/jobs/package.json (+1 LOC — added `./lib/patrol-track-materialization` subpath export entry alongside the existing area-derivation entry).
- Files relocated:     apps/web/src/server/sync/patrol-track-materialization.ts → packages/jobs/src/lib/patrol-track-materialization.ts via `git mv` (history preserved). Original stub was 35 LOC. Real implementation is 301 LOC.
- Files deleted:       (none — relocation only)
- Schema/migrations:   none (PatrolTrack model + TrackSource enum already shipped in 4.1b at commit cfe9195; PatrolTrack.trackGeojson is the Prisma Json target column — no schema change needed)
- Errors encountered:  (1) ESLint @typescript-eslint/no-unnecessary-type-assertion on line 153 where `await prisma.patrol.findUniqueOrThrow(...) as PatrolForMaterialization` was redundant — fixed by changing to a `const patrol: PatrolForMaterialization = await ...` typed binding. (2) Two test cases for `no_credentials` initially failed — root cause was test helper `makeTenant({ earthrangerUrl: null })` resolving to default `"enc:https://er.example.test"` instead of null because `null ?? default` in nullish-coalescing returns the default. Fixed by switching makeTenant to `"earthrangerUrl" in overrides ? overrides.earthrangerUrl : default` — `in` operator distinguishes explicit null from absent key. Logged 🟡 fix entry to lessons.md.
- Errors resolved:     Both above resolved before squash-merge to main. No outstanding issues.
- Verification:        jobs lint 0/0 errors+warnings · jobs typecheck 0 errors · jobs tests 71/71 PASS (was 57 baseline + 14 net new = 71 total — matches expected delta) · web typecheck 0 errors (cross-package import surface intact via @marine-guardian/jobs root barrel) · web lint 0/0 errors+warnings (--max-warnings 0) · web tests 281/281 PASS (5.2a is jobs-only — zero web changes) · Total monorepo: 338 → 352 tests (+14). Independently re-verified on main after squash-merge.
- Commit:              feat/5.2a-patrol-track-materialization branch single commit b38d951 → squash-merged to main as d2a1dd1 → branch force-deleted via `git branch -D` per Rule 23 standard cleanup (content preserved in d2a1dd1, direct ancestry check fails after squash so -D required — same pattern as 5.1a + 5.1b + 5.1c).
- Scope adjustment:    DEFERRED: er-sync.processor inline enqueue (would add `enqueuePatrolTrackMaterialize` call after each successful patrol upsert in syncPatrols) — same precondition as 5.1d Area A: no tests exist for er-sync.processor.ts yet, and adding a hot-path enqueue without test coverage would be undertested. Pickup path: a precursor sub-batch bootstraps er-sync.processor.test.ts harness (significant — ~15-25K tokens itself), then includes the inline enqueue cleanly. User selected this deferral via AskUserQuestion at session start. Producer side of the queue (5.2b) will therefore be wired ONLY via the 5.2c admin tRPC mutation in the near term.
- Next:                5.2b — BullMQ patrol-track-materialize queue + processor + worker (same factory pattern as 5.1c area-rederive, conservative concurrency, ER API rate limiter). 5.2c — patrol.rebuildTracks admin tRPC mutation (tenant-wide fan-out per user choice via AskUserQuestion — enqueue materialize for all active patrols in tenant; super_admin gets cross-tenant via optional tenantId arg; AuditLog PLATFORM:PATROL_TRACK_REBUILD vs PATROL_TRACK_REBUILD).

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1e: areaBoundary.rebuild admin mutation + minimal UI stub
- Agent:               CLAUDE_CODE (Opus 4.7 main-session implementation per user-confirmed AskUserQuestion. Dispatch path skipped — STATE.md NEXT field flagged dispatch-mechanism caution after 2 consecutive Opus subagent stalls on 5.1d. Scope was small (~20-25K estimated, ~50K actual including orientation) and well within main-session SAFE zone, so dispatching offered no benefit and added the dispatch-stall risk flagged in 5.1d post-mortem. Sub-batch 5.1e UI scope adjusted via second AskUserQuestion: brief originally called for button on `/admin/area-boundaries` page, but that page does NOT exist in the codebase — `(dashboard)/patrol-areas/page.tsx` is a stub ("Area management — map + list pending") and no boundary CRUD UI has been built yet. User chose Option A "Backend-only + minimal stub button" — same precondition-deferral pattern that 5.1d Area A used.)
- Why:                 Fifth and final sub-batch of Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm). Closes the loop on the manual-rebuild path called for by v2 PRODUCT.md — when an ArcGIS layer refresh, an external boundary import, or a bulk override has shifted the geometry universe in a way the 5.1d CUD fan-out path did not capture, super_admin / site_admin can trigger a full re-derivation via tRPC mutation + minimal stub UI button. Reuses the 5.1d fanOutAreaRederive helper unchanged; reuses the 5.1c BullMQ area-rederive queue + 50/sec rate limiter unchanged. AuditLog entry written per security.md L5 ("every data mutation logged") with action prefix "PLATFORM:AREA_REBUILD" for super_admin cross-tenant rebuilds (per security.md superadmin convention "every superadmin operation logged with PLATFORM: prefix to distinguish from tenant-scoped operations") or "AREA_REBUILD" for own-tenant rebuilds. entityId stores the target tenantId rather than a specific boundary row id, because the operation re-derives the entire tenant's universe of events/patrols/fuel-entries — not a single boundary.
- Files added:         apps/web/src/app/(dashboard)/patrol-areas/rebuild-button.tsx (113 LOC — Client Component `<RebuildAreaBoundariesButton>`; shadcn/ui Dialog confirm with warning about minutes-long completion for large tenants; useSession-based role check that hides the button for non-admin users; trpc.areaBoundary.rebuild.useMutation with inline success/error state — no toast library in the stack, used the same pattern as create-user-dialog.tsx; reset state on close).
- Files modified:      apps/web/src/server/trpc/routers/areaBoundary.ts (+46 LOC — TRPCError import added; new `rebuild` adminProcedure mutation with z.object({ tenantId: z.string().optional() }) input; super_admin / site_admin RBAC resolution: super_admin may target any tenantId, site_admin must omit tenantId or match own; FORBIDDEN error for site_admin cross-tenant attempts; isCrossTenant flag drives action label `PLATFORM:AREA_REBUILD` vs `AREA_REBUILD`; calls fanOutAreaRederive(targetTenantId, ctx.userId) unchanged from 5.1d; writes prisma.auditLog.create with action/userId/tenantId/entityType="AreaBoundary"/entityId=targetTenantId/changesJson={enqueued, scope: platform|tenant}; returns {tenantId, enqueued, action}). apps/web/src/server/trpc/routers/__tests__/areaBoundary.test.ts (+144 LOC — extended @marine-guardian/db mock with `prisma.auditLog: { create: vi.fn() }`; +7 cases in new `describe('areaBoundary.rebuild (5.1e)')`: rejects operator role / rejects field_coordinator / site_admin own-tenant happy path with AREA_REBUILD action + AuditLog + tenant-scoped fan-out / site_admin cross-tenant FORBIDDEN with no fan-out + no AuditLog / super_admin own-tenant uses AREA_REBUILD not PLATFORM: / super_admin cross-tenant uses PLATFORM:AREA_REBUILD with fan-out queries targeting specified tenant + changesJson.scope=platform / empty tenant still writes AuditLog with enqueued=0). apps/web/src/app/(dashboard)/patrol-areas/page.tsx (+6/-1 LOC — wired RebuildAreaBoundariesButton into flex header alongside existing h1).
- Files deleted:       none
- Schema/migrations:   none (consumer-side wiring only; no DB schema changes; AuditLog model already shipped at Phase 4 Part 3 baseline)
- Errors encountered:  Two ESLint errors after initial UI write — (1) `@typescript-eslint/no-confusing-void-expression` on inline arrow returning setOpen/handleClose calls in Dialog onOpenChange; (2) `@typescript-eslint/no-unnecessary-condition` on `session?.user?.roles ?? []` (next-auth types guarantee user non-null when session non-null, so `?.user` chain is over-cautious).
- Errors resolved:     (1) Expanded inline arrow to block body with explicit if/else; expanded the Button onClick arrow same way. (2) Changed to `session?.user.roles ?? []` — single optional chain on session only.
- Tests added:         7 new vitest cases (areaBoundary.test.ts rebuild describe block — see Files modified above for the test breakdown).
- Tests existing:      274 web tests baseline (post-5.1d) — all unchanged. 274 + 7 = 281 final web count.
- Verification (on feat/5.1e-area-boundary-rebuild-admin branch AND independently on main after squash-merge):
  - web lint:        0 errors, 0 warnings
  - web typecheck:   0 errors
  - web tests:       281/281 PASS (was 274 baseline + 7 net new = matches expected delta)
  - jobs tests:      57/57 PASS unchanged (5.1e is web-only — no @marine-guardian/jobs changes)
  - Total monorepo:  331 → 338 (+7)
- Git:                 Branch feat/5.1e-area-boundary-rebuild-admin built as single commit 9b382a1 → squash-merged to main as 88744ed → branch force-deleted with `git branch -D` per Rule 23 (squash-merge breaks direct-ancestry check, -D required).
- Scope adjustment from STATE.md NEXT field brief: brief called for "UI button on /admin/area-boundaries" but that page does not exist (no admin area-boundary CRUD page has been built yet — patrol-areas is a stub). Same precondition pattern as 5.1d Area A. User chose Backend-only + minimal stub via AskUserQuestion — backend mutation + AuditLog + tests fully shipped; minimal stub button on the existing patrol-areas placeholder page provides end-to-end consumer for the mutation. Full /admin/area-boundaries CRUD page (list + create/edit/delete + map + geometry editor) deferred to a future Item.
- Closes:              Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm) for the scope under its remit (5.1a + 5.1b + 5.1c + 5.1d-B + 5.1e all shipped). 5.1d Area A (sync inline re-derive on areaName change — v2 L546) remains DEFERRED — precondition is unbuilt area_name ingestion in ER sync engine.

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1c: BullMQ area-rederive queue + processor + worker
- Agent:               CLAUDE_CODE (Architect-Execute: Opus 4.7 main session for orientation against STATE.md NEXT field (JobData shape + concurrency limiter spec locked from prior checkpoint) + dispatch brief authorship + independent verification + squash-merge + governance writes. Dispatched single Opus 4.7 executor subagent via Agent(model: "opus") per [[feedback_marine_guardian_opus_executor_default]] — third consecutive Marine-Guardian dispatch confirms doctrine. Executor reported DONE_WITH_CONCERNS with 6 concerns: (1) helper relocation apps/web→packages/jobs — 5.1b shipped applyAreaDerivation at apps/web/src/server/sync/ but 5.1c needs it from packages/jobs (worker context) AND 5.1d sync engine processor lives in packages/jobs, and apps→packages dependency direction is one-way, so file move was architecturally required not optional; preserved blame via git rename (97% + 94% similarity); 5.1e admin tRPC import surface becomes `import { applyAreaDerivation } from "@marine-guardian/jobs"` via root barrel re-export; (2) added @marine-guardian/shared workspace dep to packages/jobs/package.json — required by relocated helper which imports deriveArea from @marine-guardian/shared/lib/area-derivation; single new workspace edge, no external deps; (3) processor uses `platformPrisma as unknown as ExtendedPrismaClient` cast with explanatory comment — applyAreaDerivation types its prisma arg as ExtendedPrismaClient (deliberate 5.1b decision for shared type with admin-context callers in 5.1e), but worker queries always pass explicit tenantId so L6 tenant-guard extension is a no-op AND encryption extension is unused for plaintext columns (tenantId/areaName/locationLat/locationLon/areaBoundaryId/areaDerivedAt); runtime shape is structurally compatible; cleaner alternatives (e.g. separate extended client for worker) add complexity without benefit; (4) base-worker.ts extended with optional `limiter?: RateLimiterOptions` WorkerOption + conditional spread guarded by `options?.limiter !== undefined` — backward-compatible, no regressions in existing 4 workers (er-sync/alerts/email/maintenance); (5) two commits on branch (implementation 8f094f7 + registration wiring 8cb3d3f) — collapsed via squash-merge to main as 3b4145f; (6) concurrency=10 chosen as conservative default — v2 L545 specifies limiter `{max:50, duration:1000}` but is silent on concurrency; reasoning: 10 in-flight × ~50ms median per job ≈ 200/sec arithmetic ceiling so limiter is binding constraint, concurrency just prevents Redis fetch latency bottleneck; exported as AREA_REDERIVE_CONCURRENCY named constant for one-place tuning based on production Prisma pool sizing. All six concerns sound; architect accepted without modification.)
- Why:                 Third sub-batch of Phase 8 Batch 5 Item 1. Wraps the 5.1b applyAreaDerivation helper with BullMQ infrastructure so async re-derivation can be triggered from any context (sync engine in 5.1d, admin manual-rebuild in 5.1e, future bulk operations). Concurrency limiter capped at 50 jobs/sec per v2 spec L545 protects PostgreSQL from runaway re-derivation storms during bulk imports or post-migration backfills. JobData shape `{entity, id, tenantId}` includes tenantId for logging/observability even though applyAreaDerivation re-derives it from the row itself — keeps job inspection in Bull Board human-readable without requiring a DB lookup to identify which tenant a queued job belongs to.
- Files added:         packages/jobs/src/queues/area-rederive.queue.ts (36 LOC — exports `areaRederiveQueue` Queue instance + `AreaRederiveJobData` type + `enqueueAreaRederive(data): Promise<Job>` helper; queue name `'area-rederive'`; follows existing er-sync/alerts queue pattern); packages/jobs/src/processors/area-rederive.processor.ts (63 LOC — exports `processAreaRederive(job: Job<AreaRederiveJobData>): Promise<DerivationResult>`; instantiates platformPrisma + casts to ExtendedPrismaClient with explanatory comment per Concern 3; calls applyAreaDerivation(prisma, job.data.entity, job.data.id) + returns result for BullMQ result storage; no AuditLog write per Option A scope split from 5.1b); packages/jobs/src/workers/area-rederive.worker.ts (46 LOC — exports `createAreaRederiveWorker(connection)` factory; uses base-worker createWorker helper with limiter `{max: 50, duration: 1000}` per v2 L545 + concurrency AREA_REDERIVE_CONCURRENCY=10 exported constant); packages/jobs/src/queues/__tests__/area-rederive.queue.test.ts (134 LOC — 6 vitest cases via mocked BullMQ Queue: enqueueAreaRederive calls queue.add with correct args, queue uses name `'area-rederive'`, JobData payload structure verified, multiple entity types accepted, returns Job promise, error propagation); packages/jobs/src/processors/__tests__/area-rederive.processor.test.ts (138 LOC — 9 vitest cases via mocked applyAreaDerivation: processor invokes helper with `(prisma, job.data.entity, job.data.id)`, returns helper result unchanged, all 3 entity types work (event/patrol/fuelEntry), null/non-null DerivationResult both propagate, exceptions propagate to BullMQ retry); packages/jobs/src/workers/__tests__/area-rederive.worker.test.ts (122 LOC — 7 vitest cases via mocked BullMQ Worker constructor: worker registers with limiter `{max:50, duration:1000}`, concurrency=10, queue name `'area-rederive'`, processor function bound correctly, connection wiring matches existing workers, graceful shutdown).
- Files modified:      packages/jobs/src/queues/types.ts (+AreaRederiveJobPayload type + QUEUE_NAMES.AREA_REDERIVE = `'area-rederive'` constant registered alongside existing er-sync/alerts entries); packages/jobs/src/queues/index.ts (+5 LOC barrel exports for queue+helper); packages/jobs/src/processors/index.ts (+1 LOC processor export); packages/jobs/src/workers/index.ts (+5 LOC worker+factory+constant exports); packages/jobs/src/workers/base-worker.ts (+9 LOC — optional `limiter?: RateLimiterOptions` field on WorkerOptions; conditional `...(options?.limiter !== undefined && {limiter: options.limiter})` spread into `new Worker(...)` call; backward-compatible — existing 4 workers unaffected, their tests still pass); packages/jobs/src/start-workers.ts (+5 LOC — `startAreaRederiveWorker(connection)` registered as 5th worker alongside startErSyncWorker + startAlertsWorker + startEmailWorker + startMaintenanceWorker); packages/jobs/src/index.ts (+17 LOC barrel re-exports including applyAreaDerivation from `./lib/area-derivation` for 5.1e admin tRPC consumption); packages/jobs/package.json (+@marine-guardian/shared workspace:* dep alongside existing @marine-guardian/db; +`./lib/area-derivation` subpath export entry for clean import surface); pnpm-lock.yaml (+3 lines for the new workspace dep edge).
- Files relocated:     apps/web/src/server/sync/area-derivation.ts → packages/jobs/src/lib/area-derivation.ts (97% similarity per git rename detection; +6 LOC for relocation header note; pre-V31 5.1b shipment location was incorrect, fixed before downstream consumers were wired); apps/web/src/server/sync/__tests__/area-derivation.test.ts → packages/jobs/src/lib/__tests__/area-derivation.test.ts (94% similarity; +4 LOC for header note; all 8 cases unchanged, all 8 still PASS in jobs package). Architecturally required because 5.1d sync engine lives in @marine-guardian/jobs and TypeScript workspace deps are one-way (apps depend on packages, packages NEVER depend on apps).
- Files deleted:       none (relocation, not deletion — git rename preserves blame history)
- Schema/migrations:   none (queue infrastructure only; no DB changes; no new Prisma models, no new columns, no new indexes)
- Errors encountered:  ExtendedPrismaClient type mismatch in processor — platformPrisma is `PrismaClient` but applyAreaDerivation requires `ExtendedPrismaClient` (alias for the L6 tenant-guarded variant). Resolved via `as unknown as ExtendedPrismaClient` cast with multi-line explanatory comment noting that (a) worker passes explicit tenantId so tenant-guard L6 extension is a no-op and (b) encryption extension is unused for the plaintext columns 5.1b touches. This is captured as Concern 3 above and as a 🟡 fix entry being added to lessons.md so 5.1d sync engine (next sub-batch) and any future processor callers don't re-encounter the same friction.
- Errors resolved:     Same — single resolution path applied throughout processor module.
- Verification:        feat/5.1c-area-rederive-queue branch built across 2 commits (8f094f7 implementation + 8cb3d3f registration). All gates green BOTH on feature branch AND independently re-verified on main after squash-merge: jobs lint 0/0 errors+warnings, jobs typecheck 0 errors, jobs test 57/57 PASS (was 35 baseline + 22 net new: 8 relocated + 6 queue + 9 processor + 7 worker; zero regressions in pre-existing er-sync/alerts/realtime-publisher tests); web typecheck 0 errors (cross-package import surface intact via @marine-guardian/jobs root barrel re-export of applyAreaDerivation), web test 268/268 PASS (was 276; -8 from relocation as expected). Total monorepo test count unchanged at 325 (268 web + 57 jobs vs prior 276 web + 35 jobs + 14 other = same total).
- Branch ops:          feat/5.1c-area-rederive-queue squash-merged to main as commit 3b4145f (17 files changed, +617 / -1; 4 new files in queues/processors/workers + 2 git renames apps/web→packages/jobs + 11 modifications to barrels/types/registration). Feature branch deleted locally with `git branch -D` (forced because squash-merge collapses commits and direct ancestry check fails — content preserved in 3b4145f, this is the standard Rule 23 squash-merge cleanup pattern matching 5.1a + 5.1b). origin push of 6 cumulative commits (6687112 4.1e + 0cf7eeb 4.1e-governance + 6686042 5.1a + fccd56e 5.1a-governance + c4a14a9 5.1b + f9eaafe 5.1b-governance + 3b4145f 5.1c + [this governance commit when written]) still awaits user authorization.
- Memory updates:      [[project_marine_guardian_batch5_queued]] updated with 5.1c shipment details + 5.1d/5.1e blockers cleared; [[feedback_marine_guardian_opus_executor_default]] reinforced (third consecutive clean Opus dispatch on Marine-Guardian — pattern is load-bearing). New 🟡 fix entry being added to lessons.md: "platformPrisma → ExtendedPrismaClient cast pattern for BullMQ processors" — captures the type-narrowing decision so 5.1d processor (next sub-batch) and future processors don't re-rediscover the friction.
- ⏭️  Next:             Open a FRESH Claude Code session and say "Start Batch 5 Sub-batch 5.1d" → 5.1d is sync inline re-derive + AreaBoundary CUD enqueue (er-sync.processor.ts inline applyAreaDerivation when Event/Patrol upsert changes areaName per v2 L546 + area-boundary router enqueues batch rederive via enqueueAreaRederive on create/update/delete/enable/disable per v2 L545). 5.1d is BLOCKED ON nothing — 5.1c unblocked it. Estimated ~30-35K tokens (sync engine integration is the largest sub-batch in Item 1). Continue Opus executor doctrine. After 5.1d ships → 5.1e admin manual-rebuild tRPC mutation + UI button (super-admin tRPC mutation + UI button on /admin/area-boundaries with confirm dialog; this sub-batch DOES write AuditLog per Option A scope split because ctx.session.userId is available). After 5.1e ships → Batch 5 Item 1 COMPLETE → proceed to Item 2 (PatrolTrack Materialization job) → Item 3 (ReportExport pdf-render queue + Puppeteer Docker service).

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1b: applyAreaDerivation persistence helper
- Agent:               CLAUDE_CODE (Architect-Execute: Opus 4.7 main session for orientation + schema verification (Event has coords / Patrol has none / FuelEntry has no areaDerivedAt column — corrected STATE.md brief assumption that Patrol could do nearest-fallback) + AuditLog decision elicitation (Option A confirmed by user — defer AuditLog writes to 5.1e where ctx.session.userId is available; sync engine + worker contexts have no user and AuditLog.userId is NOT NULL) + dispatch brief authorship + verification + ship. Dispatched single Opus 4.7 executor subagent via Agent(model: "opus") per [[feedback_marine_guardian_opus_executor_default]] — Marine-Guardian Opus-executor doctrine continues to deliver clean DONE on first dispatch. Executor reported DONE_WITH_CONCERNS with 3 deviations: (1) used ExtendedPrismaClient from @marine-guardian/db aliased as PrismaClientLike instead of @prisma/client direct import — apps/web does not depend on @prisma/client directly, so this was correct; (2) used the actual deriveArea object-input signature `deriveArea({areaName, point}, boundaries)` instead of positional form in brief — architect's brief had guessed at signature without re-reading 5.1a; executor caught the discrepancy and followed actual signature; (3) added one export path entry `"./lib/area-derivation": "./src/lib/area-derivation/index.ts"` to packages/shared/package.json — 5.1a shipped the files but did not expose them via package exports; without this addition the import would not resolve. All three deviations sound; no concerns raised.)
- Why:                 Second sub-batch of Phase 8 Batch 5 Item 1. Wraps the 5.1a pure deriveArea function with database I/O — load target row by id, load tenant's enabled AreaBoundary rows, project to AreaBoundaryForDerivation subset, call deriveArea, write back areaBoundaryId + areaDerivedAt. This is the single-row callable used by 5.1c BullMQ processor (worker invokes per job), 5.1d sync engine (inline re-derive on areaName change), and 5.1e admin manual-rebuild (loops over tenant's events/patrols/fuel entries). Without 5.1b every downstream caller would re-implement the same load→derive→write loop with risk of divergence.
- Files added:         apps/web/src/server/sync/area-derivation.ts (158 LOC — single export `applyAreaDerivation(prisma, entity: 'event'|'patrol'|'fuelEntry', id: string): Promise<{areaBoundaryId: string|null; matchedVia: 'name'|'nearest'|null}>`; per-entity branching for findUniqueOrThrow with minimal select — Event selects locationLat/Lon for nearest-fallback, Patrol+FuelEntry omit coordinates because schema has none; areaBoundary.findMany scoped to row's tenantId + isEnabled=true; projects to AreaBoundaryForDerivation via map; calls deriveArea({areaName, point}, boundaries) where point is null for Patrol/FuelEntry; writes areaBoundaryId + areaDerivedAt for Event/Patrol, areaBoundaryId-only for FuelEntry (no column); returns matchedVia for caller observability); apps/web/src/server/sync/__tests__/area-derivation.test.ts (288 LOC — 8 vitest cases: event+name-match writes both fields, event+nearest-fallback returns matchedVia='nearest' with point ~4.4km north of polygon south edge within 5km default threshold, event+no-match still writes areaBoundaryId=null+areaDerivedAt=Date because timestamp records "we tried", patrol+name-match verifies select call does NOT include locationLat/Lon, fuelEntry+name-match verifies update payload does NOT include areaDerivedAt key via `'areaDerivedAt' in updateCall.data === false` assertion, tenant isolation verifies findMany where clause carries tenantId from target row not hardcoded, alias match verifies row areaName='TRNP' matches boundary with name='Tubbataha Reefs Natural Park' aliases=['TRNP'], isEnabled filter verifies findMany where clause includes isEnabled=true; all mocks via vi.fn() per-call returning .mockResolvedValue, ReturnType<typeof makeMockPrisma> for typed mock + `as unknown as PrismaClientLike` cast at boundary).
- Files modified:      packages/shared/package.json (+1 export path entry "./lib/area-derivation": "./src/lib/area-derivation/index.ts" — closes 5.1a gap where files shipped but no package export declared; minimal additive change, no new deps).
- Files deleted:       none
- Schema/migrations:   none — 5.1b is pure application code, no DB schema change. Schema verification step in dispatch brief confirmed: Event has areaName+areaBoundaryId+areaDerivedAt+locationLat+locationLon; Patrol has areaName+areaBoundaryId+areaDerivedAt (NO coordinates); FuelEntry has areaName+areaBoundaryId (NO areaDerivedAt column, areaName is NOT NULL unlike Event/Patrol).
- Errors encountered:  Initial Bash invocation `pnpm --filter web lint apps/web/src/server/sync/area-derivation.ts` failed with "No files matching the pattern" because pnpm filter mode runs the script with extra args appended but path is resolved relative to the package directory not the project root. Resolved by running `pnpm --filter web lint` (full pass) — same gate quality with cleaner invocation. Secondary issue: `cd apps/web && pnpm typecheck` succeeded then next Bash call `cd apps/web && pnpm typecheck` failed with "no such file or directory" because Bash CWD persisted across invocations (already inside apps/web from prior call). Resolved by always prefixing `cd /home/me/UbuntuDevFiles/BlueAlliance/apps/Marine-Guardian && ...` for risky operations.
- Errors resolved:     Both invocation issues self-resolved by switching to `pnpm --filter web <script>` form which always runs from project root regardless of Bash CWD state.
- Verification:        lint 0/0 warnings, typecheck 0 errors (packages/db Node-types pre-existing baseline unchanged — same as 4.1a-4.1e and 5.1a shipments), full web test suite 276/276 PASS (was 268 + 8 new from 5.1b). Architect ran independent verification pass before squash-merge — all executor claims confirmed accurate.
- Commits:             feat/area-derivation-persistence branch built (e9037dc) → squash-merged to main as c4a14a9 → branch deleted locally per Rule 23. Origin push pending user authorization.
- Dispatch outcome:    Opus 4.7 executor — DONE_WITH_CONCERNS, 3 reasonable deviations, no salvage required. Token usage ~285K total (parent + executor) of 200K hard limit per Agent call boundary — executor accounted for 285K cumulative across its 45 tool uses + reasoning. Brief format with pre-inlined contracts + AuditLog decision baked in + explicit "what NOT to do" section + status report contract continues to deliver clean dispatches on Marine-Guardian per [[feedback_marine_guardian_opus_executor_default]].
- Phase 8 Batch 5 Item 1 progress: 2 of 5 sub-batches shipped (5.1a 6686042 ✅ + 5.1b c4a14a9 ✅). Next: 5.1c BullMQ area-rederive queue + processor + worker registration (rate-limited 50/sec per v2 L545); then 5.1d sync-engine inline re-derive + AreaBoundary CUD enqueue; then 5.1e admin manual-rebuild tRPC mutation + UI. After 5.1e ships → Item 1 COMPLETE → proceed to Item 2 (PatrolTrack Materialization).

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1a: Area-derivation pure functions (name-match + nearest-boundary + composite)
- Agent:               CLAUDE_CODE (Architect-Execute: Opus 4.7 main session for orientation + spec extraction (v2 L531-L561) + decomposition (5.1a-5.1e shape) + dispatch brief authorship + verification + governance + ship. Dispatched single Opus 4.7 executor subagent via Agent(model: "opus") per [[feedback_marine_guardian_opus_executor_default]] — Marine-Guardian defaults to Opus executor on ALL dispatched work after three Sonnet thrashes confirmed brittleness even on Tier 1 UI. Executor budget ~25K, reported clean DONE with no concerns. Trust-but-verify pass confirmed: 11 files matching scope, 55 vitest tests across 3 files, lint+typecheck clean, no `any` types, no external geo deps.)
- Why:                 First sub-batch of Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm — the loop-closer for Batch 4 foundation tables 4.1a + 4.1e). v2 spec L449/L456/L463 declares Event/Patrol/FuelEntry carry `area_name` (free-text from ER source-of-truth) + `area_boundary_id` (derived FK via name match OR nearest-boundary lookup) + `area_derived_at` (timestamp). Without this derivation algorithm every `area_boundary_id` shipped in 4.1e stays NULL forever and the v2 foundation tables deliver zero observable value. 5.1a ships the pure-logic LEAF layer: deterministic functions with zero DB / queue / UI dependency. Subsequent sub-batches (5.1b–5.1e) wire these into persistence helper, BullMQ area-rederive queue + processor, sync-engine inline re-derive on `area_name` change (L546), AreaBoundary CUD mutation enqueue (L545), and super-admin manual-rebuild UI button.
- Files added:         packages/shared/src/lib/area-derivation/match-by-name.ts (matchByName(areaName: string | null | undefined, boundaries: AreaBoundary[]): AreaBoundary | null — filters isEnabled=true, normalizes case+trim, name match wins over alias match), packages/shared/src/lib/area-derivation/find-nearest-boundary.ts (findNearestBoundary(point, boundaries, thresholdKm?) + DEFAULT_NEAREST_BOUNDARY_THRESHOLD_KM=5 + exported helpers haversineKm + pointToSegmentDistanceKm; edge-distance to nearest Polygon outer ring OR LineString segment via equirectangular projection for foot-of-perpendicular + haversine for great-circle distance; skips malformed geometry via console.warn; returns null for invalid lat/lon or empty boundaries), packages/shared/src/lib/area-derivation/derive-area.ts (deriveArea({areaName, point}, boundaries): {areaBoundaryId, matchedVia: 'name'|'nearest'|null} — v2 spec L531-L561 algorithm: name first, nearest fallback, NULL preserves area_name verbatim; matchedVia exposed for 5.1b AuditLog observability per L561 audit requirement), packages/shared/src/lib/area-derivation/types.ts (AreaBoundaryForDerivation structural subset type — id+name+aliases+isEnabled+geometryType+geometryGeojson; callers project full AreaBoundary rows down; decouples pure functions from Prisma client + full v2 schema fields), packages/shared/src/lib/area-derivation/index.ts (barrel exports), packages/shared/src/lib/area-derivation/__tests__/match-by-name.test.ts (17 cases — exact match, case difference, alias match, leading/trailing whitespace, disabled boundary skipped, empty/null areaName, name-wins-over-alias priority), packages/shared/src/lib/area-derivation/__tests__/find-nearest-boundary.test.ts (25 cases — haversine known pair Manila→Cebu ±1%, point-to-segment perpendicular/clamped/past-endpoint, interior polygon point returns nearest-edge distance NOT zero, within/at-edge/beyond threshold, custom threshold, LineString geometry, multiple boundaries returns nearest not first, disabled skipped, malformed geometry skipped via console.warn, invalid lat/lon returns null, empty boundaries returns null), packages/shared/src/lib/area-derivation/__tests__/derive-area.test.ts (13 cases — name match path, nearest fallback path, name takes precedence over nearest when both could match, no inputs returns nulls, non-matching name + no point, non-matching name + point falls back, point beyond threshold + no name match), packages/shared/vitest.config.ts (new test runner config matching packages/jobs convention — node env, src/**/__tests__/**/*.test.ts pattern)
- Files modified:      packages/shared/package.json (added "test" script + vitest@^4.1.5 devDep — packages/shared previously had no test runner; minimal scope expansion warranted since brief required vitest tests and matching jobs package convention), pnpm-lock.yaml (vitest workspace resolution)
- Files deleted:       none
- Schema/migrations:   none (pure logic — no DB schema changes)
- Routers/UI untouched: zero apps/web changes; zero packages/db changes; zero packages/jobs changes; zero docs changes outside this governance write. Scope strictly contained to packages/shared/src/lib/area-derivation/**.
- Verification:        typecheck (packages/shared) 0 errors. typecheck (workspace) baseline pre-existing packages/db Node-types failures only (Cannot find name 'process'/'console'/'Buffer'/'node:async_hooks'/'node:crypto' — identical baseline shipped on 4.1a/4.1b/4.1c/4.1d/4.1e). lint 5/5 packages passing, 0 errors / 0 warnings. vitest 350 total (was 295) — packages/shared 55 NEW across 3 files (was 0; +55 new tests, vitest bootstrap added) + apps/web 268 + packages/jobs 27 unchanged. grep ': any\b|<any>|as any' against new code returns empty (zero `any` types introduced). grep 'turf|geolib|@mapbox' against package.json returns empty (zero external geo deps).
- TDD discipline:      RED → GREEN → REFACTOR strictly followed per brief. Executor wrote 3 test files first, ran vitest to confirm RED (no impl files yet), then created 3 impl files + 1 barrel + 1 types file to reach GREEN, then refactored. No `console.log` in production code (only `console.warn` for malformed geometry per defensive-skip contract). All 55 cases assert v2 spec L561 mandate: "exact name match, alias match, case differences, trimming, nearest-boundary within threshold."
- Two-stage review:    Stage 1 (spec compliance against v2 L531-L561) PASS — algorithm steps 1+2+3 implemented exactly, threshold default 5km matches NEAREST_BOUNDARY_THRESHOLD_KM, edge-distance semantics (NOT centroid) per L969, area_name preservation behavior correct. Stage 2 (code quality) PASS — no `any` types, tests before impl verified via brief's TDD ordering requirement, blast-radius scope respected (zero files outside packages/shared/src/lib/area-derivation touched), conventional commit message.
- Decisions LOCKED:    (1) AreaBoundaryForDerivation structural subset over importing the full Prisma type — keeps pure functions decoupled from packages/db and usable from contexts that don't have a Prisma client (workers, tests with mocked DB). Caller in 5.1b projects full AreaBoundary rows down to this shape at the query layer. (2) Zero external geo dependencies — turf/geolib/mapbox NOT installed. Haversine + pointToSegmentDistanceKm implemented by hand with equirectangular local projection for the foot-of-perpendicular step (cosine longitude scaling around the input point's latitude). Earth-as-sphere approximation accurate to <0.1% within the 5 km threshold per v2 spec. Keeps packages/shared zero-dep per project convention. (3) `matchedVia` field on DeriveAreaResult — exposed for 5.1b AuditLog observability per v2 L561 audit requirement ("derivation job is a load-bearing piece of data quality"). 5.1b's applyAreaDerivation will log this to AuditLog when persisting. (4) Edge-distance semantics for nearestBoundary — interior polygon points return distance-to-nearest-edge (NOT zero), matching v2 L969 helper contract: "scans every boundary's lines, returns the boundary whose nearest line wins." Verified by explicit test case "treats interior point as nearest-edge distance." (5) Defensive null returns over throwing — invalid lat/lon, empty areaName, malformed geometryGeojson, empty boundaries list all return null silently (with console.warn for malformed geometry per debugging needs). Pure functions in shared package never throw; persistence layer decides whether null is acceptable. (6) Vitest bootstrap in packages/shared minimum-viable — copied jobs package's vitest.config.ts style (node env, src/**/__tests__/**/*.test.ts pattern), `test` script in package.json runs with --passWithNoTests for consistency. No coverage thresholds yet — those land when the package has more than just area-derivation under test.
- Dispatch experience: Opus 4.7 executor reported STATUS: DONE accurately on first dispatch (no salvage required, contrasting with 4.1e Opus executor that silently dropped at step 7/11). Executor budget ~25K consumed within 30K subagent budget (per memory-governance.md §1 Step 2.5). Brief format with pre-inlined v2 spec + explicit file list + TDD ordering + verification checklist proved sufficient — no follow-up clarifying questions needed. CONFIRMS [[feedback_marine_guardian_opus_executor_default]] doctrine for Marine-Guardian.
- Deferred (explicit): applyAreaDerivation persistence helper (writes back areaBoundaryId + areaDerivedAt + AuditLog matchedVia) — 5.1b. BullMQ area-rederive queue + processor + worker registration + concurrency cap 50/sec per v2 L545 — 5.1c. Sync-engine inline re-derive when Event/Patrol upsert changes areaName per v2 L546 — 5.1d. AreaBoundary CUD enqueue (create/update/delete/enable/disable fans out rederive jobs to ALL Event+Patrol+FuelEntry rows in tenant) per v2 L545 — 5.1d. Super-admin manual-rebuild tRPC mutation + UI button on /admin/area-boundaries — 5.1e.
- Final ship:          Squash-merged feat/area-derivation-functions → main as commit 6686042 (11 files, +1019 -2). Feature branch deleted locally per Rule 23 (`git branch -D` since squash-merge doesn't update branch pointer). Branch was never pushed to origin, so no remote branch deletion needed. Origin push pending — awaits user authorization (push will include 6687112 + 0cf7eeb + 6686042 + this governance commit).

## 2026-05-19 — Phase 8 Batch 4 Sub-batch 4.1d: NotificationRecipient split with data migration
- Agent:               CLAUDE_CODE (Opus 4.7 main session for orientation + plan correction + verification + governance + ship; dispatched single Opus 4.7 subagent via Agent(model: "opus") for the Tier 3 implementation per [[feedback_marine_guardian_opus_executor_default]]; subagent reported ~85K of its 100K budget consumed across 11 file edits + migration + verification. ONLY sub-batch in Batch 4 with real data-migration semantics.)
- Why:                 Fourth sub-batch of Phase 8 Batch 4 (v2 Foundation Tables). Splits per-user read state OFF the Notification model ONTO a new NotificationRecipient model per the authoritative v2 spec (docs/v2/PRODUCT.md L480-484). v2 model: one Notification row, N NotificationRecipient rows — Notification carries content (title/message/notification_type/links to event/patrol/alert_rule/subject), NotificationRecipient carries per-user delivery state (is_read/read_at/email_sent_at/email_status). Also adds Notification.subject_id (FK to Subject SET NULL) since v2 spec L480 includes it. This is preparation for the v2 alert dispatch pipeline (one alert can fan out to N users with independent read + email-cooldown state); no new user-visible feature behaviour ships in this sub-batch — only structural foundation. PLAN CORRECTIONS surfaced during main-session review: (1) STATE.md plan said move notificationType to NotificationRecipient — that was WRONG. v2 spec L480 keeps notificationType on Notification (the alert type doesn't vary per recipient). (2) Plan omitted read_at field on NotificationRecipient — added per v2 L483. (3) Plan implied email_status was a string default — actually requires a 5-value enum NotificationEmailStatus{pending|sent|suppressed_by_cooldown|digested|failed} per v2 L483. (4) Blast radius wider than plan estimated — 11 production files + 2 test files (exports route + notification.test.ts) needed touch since exports/notifications route also queried notifications.user_id/is_read directly.
- Files added:         packages/db/prisma/migrations/20260519233500_add_notification_recipient_split/migration.sql (14-step DDL: CREATE TYPE NotificationEmailStatus enum + CREATE TABLE notification_recipients with cuid PK + notification_id FK CASCADE + user_id FK NO ACTION + 3 indexes (notification_id, (user_id, is_read), (user_id, created_at DESC)) + backfill INSERT (1 NotificationRecipient row per existing Notification preserving user_id+is_read+created_at, deterministic md5-derived ID 'c' || substring(md5('nr_' || id || '_' || user_id) FROM 1 FOR 24) — idempotent if re-run) + DROP indexes user_id+is_read on notifications + DROP FK user_id + DROP COLUMN user_id + DROP COLUMN is_read + ADD COLUMN subject_id + ADD FK subject_id REFERENCES subjects ON DELETE SET NULL + CREATE INDEX subject_id); packages/db/prisma/migrations/20260519233500_add_notification_recipient_split/down.sql (10-step reverse: DROP subject_id (index, FK, column) + ADD user_id (nullable initially) + ADD is_read default false + UPDATE notifications restoring user_id+is_read from earliest notification_recipient per notification via DISTINCT ON + DELETE orphan notifications (no recipients) + SET NOT NULL on user_id + restore FK + restore indexes user_id+is_read + DROP TABLE notification_recipients + DROP TYPE enum; WARNING in header that restore is lossy when N>1 recipients existed per notification — picks earliest)
- Files modified:      packages/db/prisma/schema.prisma (Notification model: removed userId field + user back-relation + indexes user_id+is_read; added subjectId nullable + subject back-relation + index subject_id + recipients back-relation; new model NotificationRecipient with notification CASCADE + user NO ACTION + emailStatus default pending + readAt+emailSentAt nullable + 3 indexes; new enum NotificationEmailStatus with 5 literal values; User model: removed `notifications Notification[]` back-relation + added `notificationRecipients NotificationRecipient[]`; Subject model: added `notifications Notification[]` back-relation for the new FK); packages/shared/src/schemas/enums.ts (added notificationEmailStatusSchema = z.enum(["pending","sent","suppressed_by_cooldown","digested","failed"])); packages/shared/src/schemas/notification.ts (notificationSchema: removed isRead+userId, added subjectId: z.string().cuid().nullable(); new notificationRecipientSchema with id+notificationId+userId+isRead+readAt+emailSentAt+emailStatus+createdAt); packages/shared/src/types/notification.ts (Notification type: removed isRead+userId, added subjectId: string|null; new NotificationRecipient + NotificationEmailStatus types); packages/shared/src/types/index.ts (re-exported new types); apps/web/src/server/trpc/routers/notification.ts (FULL REWRITE — all 4 procedures (list/markRead/markAllRead/unreadCount) now query prisma.notificationRecipient.*; tenant scoping via nested `notification: {tenantId: ctx.tenantId}` filter; user scoping via top-level userId; markRead/markAllRead set both isRead=true AND readAt=new Date() per v2 spec L483; list returns FLATTENED shape — top-level fields come from joined Notification + recipient.id surfaces as item.id so UI markRead passes recipient.id naturally; input.cursor is now NotificationRecipient.id not Notification.id); apps/web/src/server/trpc/routers/__tests__/notification.test.ts (FULL REWRITE — 10 cases (was 6): list returns notifications scoped to tenant+user via NotificationRecipient join, filters by isRead applies to recipient.isRead, filters by notificationType applies to joined Notification.notificationType, returns flattened shape with title/message/notificationType + event/patrol joined, pagination via cursor, markRead updates recipient row matching id+userId+tenant join, markRead cross-user attempt returns count=0 silently (no error), markAllRead user-scoped (only current user's unread), unreadCount counts only current user+tenant via join, unreadCount throws FORBIDDEN when tenantId is absent; mocks prisma.notificationRecipient instead of prisma.notification; uses partial<T> helper); apps/web/src/app/api/exports/notifications/route.ts (rewrote prisma.notification.findMany → prisma.notificationRecipient.findMany with `where: {userId, notification: {tenantId, ...(notificationType filter applied via notification: {notificationType})}, isRead}`; include: {notification: {include: {alertRule, event, patrol}}}; mapping function pulls from r.notification.* for content + r.isRead from recipient; CSV's ID column still uses notification.id — see decision below); apps/web/src/app/api/exports/notifications/__tests__/route.test.ts (refit mock + fixtures to recipient join shape, 6 cases)
- Files deleted:       none
- Schema/migrations:   1 migration (20260519233500_add_notification_recipient_split). Forward migration is 14 steps in a single transaction (Prisma wraps the SQL); order is CRITICAL — backfill INSERT (step 8) MUST run BEFORE the user_id+is_read column drops (steps 11-12) since the backfill reads those columns. Down migration is hand-written and lossy when N>1 recipients existed per notification (picks the earliest by created_at via DISTINCT ON). Applied cleanly to dev DB; \d notifications confirms user_id+is_read columns dropped, subject_id added with FK + index; \d notification_recipients confirms new table with 3 indexes + 2 FKs (cascade on notification delete, no-action on user delete).
- Pre-flight result:   SELECT COUNT(*) FROM notifications → 0 rows in dev. Backfill ran as a no-op but is present in SQL for staging/prod safety. No paging needed (would only matter at >1000 rows). Down migration backfill is also a no-op against 0 rows but must be correct for staging/prod.
- Verification:        pnpm --filter @marine-guardian/db exec prisma generate → client regenerated cleanly with NotificationRecipient typed. pnpm --filter @marine-guardian/db exec prisma migrate dev → applied 20260519233500_add_notification_recipient_split cleanly. pnpm --filter @marine-guardian/shared typecheck → 0 errors. pnpm --filter @marine-guardian/web typecheck → 0 errors. pnpm lint → 0 errors / 0 warnings (--max-warnings 0). pnpm test → 262 passed across 37 files (was 260; +2 net new — notification.test.ts went from 6 → 10 cases with 3 NEW pathways verified per spec: cross-user markRead silent reject, user-scoped markAllRead, flattened shape mapping; -4 from old test cases that no longer apply; exports/notifications test file went 6 → 6 cases — same coverage rewritten for new shape). NOTE: pnpm --filter @marine-guardian/db typecheck FAILS with pre-existing Node-types errors in src/middleware/encryption.ts + src/helpers/rls.ts + src/client.ts + prisma/seed.ts — these exist on main pre-changes and are unrelated to this sub-batch (identical baseline shipped on 4.1a/4.1b/4.1c).
- TDD discipline:      Router test file rewritten BEFORE the router implementation. Initial vitest run was RED across all 10 cases (router file still imported nonexistent prisma.notificationRecipient client). After running prisma generate against the new schema + writing the router, vitest went GREEN. 3 NEW test cases per task spec: cross-user markRead silent reject (proves userId filter in WHERE), user-scoped markAllRead, flattened shape mapping (verifies field flattening from joined query). The cross-user reject test is load-bearing for security — if a future refactor accidentally removed the userId filter, this test catches it.
- Git ops:             Branch feat/notification-recipient-split off main (857c188). Single commit 7ecab44 on branch. Squash-merge to main pending (this governance write completes first). Branch will be deleted post-merge per 2026-05-03 squash-merge lesson.
- Decisions LOCKED:    (1) markRead + markAllRead set BOTH isRead=true AND readAt=new Date(). The v2 spec L483 lists read_at as a NotificationRecipient field — keeping it null while flipping isRead=true would leak schema intent. Tests use partial-match objectContaining so they tolerate the extra field. (2) The /api/exports/notifications CSV's "ID" column uses notification.id, not recipient.id. Rationale: end-user cares which alert fired, not which delivery row. This matches pre-split CSV semantics so consumers of older exports don't break. (3) Backfill SQL uses deterministic md5-derived recipient IDs (`'c' || substring(md5('nr_' || id || '_' || user_id) FROM 1 FOR 24)`) instead of CUID — Prisma's @default(cuid()) only applies when no ID is supplied to INSERT, so direct supply is safe. Trade-off: re-running the backfill by hand produces idempotent rows (same IDs every time), which is what we want for staging/prod recovery scenarios. Mixed-inventory ID pattern (backfilled rows have md5-derived IDs, new rows have cuid IDs) is acceptable since IDs are opaque strings. (4) NotificationRecipient.userId FK uses ON DELETE NO ACTION (not RESTRICT, not CASCADE) — preserves recipient history when a user is deleted but prevents the delete from cascading. This will need follow-up when v2 user-deletion flow lands (Batch 7+). (5) NotificationRecipient.notificationId FK uses ON DELETE CASCADE — when a Notification is deleted (e.g. by retention policy), all its recipient rows go with it. (6) UI page (apps/web/src/app/(dashboard)/notifications/page.tsx) required ZERO edits — the router's flattened return shape matches what the page already consumed. markRead mutation's input.id flows through as recipient.id naturally. SSE Route Handler + sidebar + realtime-store + poller tests also no-op — SSE payload shape is decoupled from DB column shape, and sidebar's notification.unreadCount endpoint name is preserved.
- Deferred (explicit): Notification creation flow (programmatic alert dispatch that fans out to N NotificationRecipient rows per alert rule + per recipient channel) — Batch 5+ AlertRule restructure. Email dispatch worker that flips emailStatus pending→sent / suppressed_by_cooldown / digested / failed — Batch 5+. Read-at-rendering in UI (showing "Read 2 hours ago" using NotificationRecipient.readAt) — Batch 6+ UI polish. NotificationRecipient maintenance/retention policy (1y per v2 spec L645) — Batch 7+.
- Note (skill injection): Vercel-plugin PreToolUse hooks injected next-forge, next-cache-components, nextjs, vercel-storage, and bootstrap skill suggestions on Read calls into apps/web/** and prisma/schema.prisma during main-session orientation. All ignored per [[feedback_skill_injection_hooks_ignore_when_locked_stack]] and CLAUDE.md priority 2 (locked tech stack overrides priority 7 skill suggestions). Stack remains: Next.js App Router + tRPC + Prisma + PostgreSQL + shadcn/ui.

## 2026-05-19 — Phase 8 Batch 4 Sub-batch 4.1c: FuelEntry + ReportExport v2 foundation scaffolds
- Agent:               CLAUDE_CODE (Opus 4.7 — Tier 2 dispatched via Agent(model: "opus") per [[feedback_marine_guardian_opus_executor_default]]; actual ~60K context within Opus 100K executor budget; single dispatch since cohesive scope on one branch was simpler than splitting 4.1c-α/β)
- Why:                 Third sub-batch of Phase 8 Batch 4 (v2 Foundation Tables). Adds two CC-native v2 tables per v2/PRODUCT.md §492 (FuelEntry) and §505-506 (ReportExport). FuelEntry: tenant-curated fuel consumption ledger feeding the L/km KPI on Patrol Coverage Report Page 2 (§182), the per-area breakdown table (§192), and the "Fuel consumption consolidated table" (§230). RBAC matrix per spec §405-408: `fuel.log` and `fuel.edit_own` for operator+, `fuel.edit_any` for coordinator+, `fuel.delete` for site_admin+. Currency snapshotted at create time per §196 — historical entries retain original currency even if tenant changes config later. ReportExport: async PDF export tracker (§506) — every Export to PDF anywhere in the app creates a ReportExport row + (future) enqueues a `pdf-render` BullMQ job. This scaffold lands the row + lifecycle contract; actual BullMQ wiring + the `marine-guardian-pdf-renderer` Docker service are explicitly deferred (next batch). Additive non-breaking: no existing rows touched, no enum renames, no breaking schema changes. 4.1c does NOT depend on any other sub-batch (4.1e Event/Patrol attribution FKs depend on 4.1a's AreaBoundary — already shipped; 4.1d Notification split is independent).
- Files added:         packages/db/prisma/migrations/20260519231300_add_fuel_entry/{migration.sql,down.sql} (CREATE TABLE fuel_entries with cuid PK, tenant_id FK RESTRICT, area_name string, area_boundary_id nullable FK to area_boundaries with SET NULL — preserves entry + area_name snapshot when boundary deleted, date_received DATE, liters DECIMAL(12,3), total_price DECIMAL(14,2), currency string, receipt_photo_url nullable, notes nullable, logged_by_user_id FK to users RESTRICT, created_at/updated_at; 3 indexes (tenant_id, tenant_id+date_received, tenant_id+area_boundary_id)); packages/db/prisma/migrations/20260519231301_add_report_export/{migration.sql,down.sql} (CREATE TYPE for ReportType + PaperSize + ReportExportStatus; CREATE TABLE report_exports with cuid PK, tenant_id FK RESTRICT, requested_by_user_id FK to users RESTRICT, report_type enum, params_json JSONB, paper_size enum default A4, status enum default queued, file_path nullable, file_size_bytes nullable, error_message nullable, created_at, completed_at nullable; 3 indexes (tenant_id, tenant_id+status, tenant_id+created_at); down.sql drops table then drops the 3 enum types in correct dependency order); packages/shared/src/schemas/fuel-entry.ts (fuelEntrySchema with cuid + Decimal-as-string regex for liters/totalPrice + positive refinement via Number(v) > 0; createFuelEntryInputSchema; updateFuelEntryInputSchema with id-required + partial fields); packages/shared/src/schemas/report-export.ts (reportExportSchema + createReportExportInputSchema + getDownloadUrlInputSchema); apps/web/src/server/trpc/routers/fuelEntry.ts (list / getById / create [operator+] / update [ownership-checked] / updateAny [coordinator+ no ownership check] / delete [site_admin+]; all tenant-scoped; currency snapshot pulled from prisma.tenant.findFirst at create time per §196 since session doesn't carry currency); apps/web/src/server/trpc/routers/reportExport.ts (list / getById / pollStatus [slim payload for UI polling] / create [coordinator+ inserts queued row, NO BullMQ enqueue yet] / getDownloadUrl [returns {downloadUrl: null, status} when not ready, canonical /[tenantId]/exports/[id]/download path when status=ready, throws NOT_FOUND when row missing — no cross-tenant existence leak]); apps/web/src/server/trpc/routers/__tests__/fuelEntry.test.ts (12 cases: list tenant scoping + pagination, getById success + cross-tenant null, create with currency snapshot from tenant + ownership-stamping, update ownership-checked happy + cross-tenant null + non-owner reject, updateAny no ownership check, delete success + cross-tenant null + RBAC reject for operator); apps/web/src/server/trpc/routers/__tests__/reportExport.test.ts (9 cases: list tenant scoping, getById success + cross-tenant null, pollStatus slim shape, create with status=queued + requestedBy stamp + RBAC reject for operator, getDownloadUrl returns null for non-ready, returns canonical URL for ready, throws NOT_FOUND for missing row). All tests use the `partial<T>` helper pattern from the 2026-05-08 lesson — defined at top of each test file to suppress `no-unsafe-assignment` on `expect.objectContaining` in nested matchers.
- Files modified:      packages/db/prisma/schema.prisma (added 3 enums ReportType{coverage,area,consolidated,detailed,rangers,patrol_filtered} + PaperSize{A4,Letter,Legal} + ReportExportStatus{queued,rendering,ready,failed}; added FuelEntry model with tenant + areaBoundary + loggedBy relations + 3 indexes; added ReportExport model with tenant + requestedBy relations + 3 indexes; added back-relations on Tenant (fuelEntries + reportExports), User (loggedFuelEntries + requestedReportExports with @relation names for disambiguation), AreaBoundary (fuelEntries for the SET NULL FK)); packages/shared/src/schemas/enums.ts (added reportTypeSchema + paperSizeSchema + reportExportStatusSchema as z.enum literals matching DB values exactly); packages/shared/src/schemas/index.ts (added exports for fuel-entry + report-export); apps/web/src/server/trpc/routers/index.ts (imported + mounted fuelEntryRouter as `fuelEntry`, reportExportRouter as `reportExport`)
- Files deleted:       none
- Schema/migrations:   2 migrations (one per table per task spec): 20260519231300_add_fuel_entry (1 table, 3 indexes, 3 FK constraints — tenant_id RESTRICT, area_boundary_id SET NULL, logged_by_user_id RESTRICT) and 20260519231301_add_report_export (3 enum types, 1 table, 3 indexes, 2 FK constraints — tenant_id RESTRICT, requested_by_user_id RESTRICT). Both have hand-written down.sql alongside the forward migration (fuel-entry down is DROP TABLE only since no enums; report-export down drops table then the 3 enums in dependency order). Not yet applied to dev DB — apply via `pnpm --filter @marine-guardian/db exec prisma migrate deploy` when next on a session with running Postgres. Note: forward timestamps backdated to 20260519231300 / 20260519231301 to keep them sorted AFTER 4.1b's 20260519223000; Prisma's initial sweep would have generated an earlier timestamp creating order drift — agent renamed before splitting.
- Verification:        pnpm --filter @marine-guardian/db exec prisma generate → client regenerated cleanly with FuelEntry + ReportExport typed. pnpm --filter @marine-guardian/shared typecheck → 0 errors. pnpm --filter @marine-guardian/web typecheck → 0 errors. pnpm --filter @marine-guardian/web lint → 0 errors / 0 warnings (`--max-warnings 0`). pnpm --filter @marine-guardian/web exec vitest run → 260 passed (was 239; +21 new across 2 test files: 12 fuelEntry.test.ts + 9 reportExport.test.ts; zero regressions across 37 test files total, was 35). NOTE: pnpm --filter @marine-guardian/db typecheck FAILS with pre-existing Node-types errors in src/middleware/encryption.ts + src/helpers/rls.ts + src/client.ts + prisma/seed.ts — these exist on main pre-changes (verified shipped on 4.1a/4.1b under same condition). Architect re-ran all gates after squash-merge — all green.
- TDD discipline:      Both routers built test-with-implementation (test files mirror the established AreaBoundary pattern from 4.1a). FuelEntry's `update` vs `updateAny` split was driven by RBAC test cases: writing the test that proves "operator can edit own entry but not someone else's" required two separate procedures since a single procedure would entangle the role + ownership checks. For Batch 4 scaffolds the discipline remains "test the contract surface": tenant scoping + cross-tenant null + RBAC enforcement + ownership-checked happy/reject paths per procedure. ReportExport's actual queue behavior is NOT tested — it doesn't exist yet (BullMQ enqueue is future batch); only the row-insert + status enum + download URL contract are tested.
- Git ops:             Branch feat/fuel-entry-report-export-scaffold off main (f1b188a). Single commit e74146f on branch. Squash-merged to main as e972d82. Branch force-deleted (`git branch -D` per 2026-05-03 squash-merge lesson). Pushed to origin in subsequent governance commit.
- Decisions LOCKED:    (1) FuelEntry RBAC split into `update` (ownership-checked, operator+) + `updateAny` (no ownership check, coordinator+) — single procedure would conflate the spec §405-408 two-permission model. Test cases prove the separation. (2) Currency snapshot read from `prisma.tenant.findFirst({ where: { id: ctx.tenantId } })` at create time, not from session — session does not carry currency and spec §196 explicitly requires per-row snapshot. (3) Decimal fields stored as Postgres `DECIMAL(12,3)` for liters and `DECIMAL(14,2)` for total_price; serialized as string by Prisma per Decimal handling convention; Zod input accepts regex-validated positive decimal string + `Number() > 0` refinement to avoid float precision loss when round-tripping from the UI. (4) `area_boundary_id` FK uses ON DELETE SET NULL — when an AreaBoundary is deleted, the FuelEntry row is preserved with `area_name` retaining the historical snapshot. This matches the spec §492 "stored as free-text for resilience if a boundary is later renamed or deleted". (5) `logged_by_user_id` + `requested_by_user_id` FKs use ON DELETE RESTRICT — never silently drop attribution rows when a user is deleted. (6) ReportExport `create` inserts a row with `status=queued` and returns it; explicitly does NOT enqueue a BullMQ job (deferred to next batch when the `marine-guardian-pdf-renderer` service lands). The UI flow + audit trail are testable today via the row, and the queue wiring becomes a one-line addition when the renderer is ready. (7) `getDownloadUrl` returns the canonical `/[tenantId]/exports/[id]/download` URL shape per §506 when status=ready, `{downloadUrl: null, status}` when not ready, throws NOT_FOUND for missing rows — does NOT confirm cross-tenant row existence (matches the established pattern from 4.1a/4.1b getById procedures). The actual streaming endpoint at that URL does not exist yet — that is also deferred to the next batch. (8) ReportType enum literal values are `coverage`, `area`, `consolidated`, `detailed`, `rangers`, `patrol_filtered` per spec §505. PaperSize is `A4` | `Letter` | `Legal` (default A4). ReportExportStatus is `queued` | `rendering` | `ready` | `failed` (default queued).
- Deferred (explicit): ReportExport BullMQ `pdf-render` queue + `marine-guardian-pdf-renderer` Docker service (Puppeteer + Chromium) — spec §506, §772, §778 lifecycle implementation — next batch. Actual `/[tenantId]/exports/[id]/download` streaming Route Handler — same batch. Fuel Logging UI page + Receipt photo upload to MinIO — Batch 6+. AreaBoundary re-derivation queue triggered on AreaBoundary delete that nullifies dependent FuelEntry.areaBoundaryId — Batch 5. FuelEntry analytics aggregation (`SUM(liters)` per area per date range, L/km KPI math per §182) — lands with Patrol Coverage Report Page 2 in Batch 6+.
- Note (skill injection): Vercel-plugin PreToolUse hooks injected next-forge, vercel-storage, and bootstrap skill suggestions on Read calls into `apps/web/**` and `prisma/schema.prisma` during the subagent execution. All ignored per [[feedback_skill_injection_hooks_ignore_when_locked_stack]] and CLAUDE.md priority 2 (locked tech stack overrides priority 7 skill suggestions). Stack remains: Next.js App Router + tRPC + Prisma + PostgreSQL + shadcn/ui.
- Note (multi-table sweep): When the subagent ran `prisma migrate dev --create-only --name add_fuel_entry` with BOTH new models already present in schema.prisma, Prisma swept both tables into ONE migration regardless of `--name`. Agent split it by hand: kept fuel_entries SQL in the original migration directory, wrote report_exports SQL into a fresh second migration directory with a timestamp 1 second later. See new 🔴 lesson "Prisma migrate dev sweeps multiple new models into one migration".

## 2026-05-19 — Phase 8 Batch 4 Sub-batch 4.1b: PatrolTrack v2 foundation scaffold
- Agent:               CLAUDE_CODE (Opus 4.7 — Tier 2 dispatched implementation per [[feedback_marine_guardian_opus_executor_default]]; estimated ~30K context, executed directly in this session)
- Why:                 Second sub-batch of Phase 8 Batch 4 (v2 Foundation Tables). Adds the PatrolTrack table per v2/PRODUCT.md spec (lines 501-502) — a CC-native materialized per-patrol GPS-track cache for fast area-covered analytics, single-patrol map rendering, and km/hrs recomputation. One-track-per-patrol invariant enforced via UNIQUE constraint on patrol_id. Tracks are NOT written via tRPC: they are populated by the Patrol Track Materialization job (stub created here, real impl deferred to Batch 5). Read-only tRPC router provides list / getById / getByPatrolId queries — used by the Per-Patrol Track Viewer Modal (PRODUCT.md line 121) and by the Patrol Coverage report aggregation (line 1055). Additive non-breaking: no existing rows touched, no enum renames, no breaking schema changes. The Patrol model gains only a back-relation `track PatrolTrack?` (Prisma artifact, no SQL changes to patrols table).
- Files added:         packages/db/prisma/migrations/20260519223000_add_patrol_track/migration.sql (creates 1 enum TrackSource{er_api,cache}; creates `patrol_tracks` table with cuid PK, tenant_id FK with RESTRICT, patrol_id FK with CASCADE plus UNIQUE constraint (one-track-per-patrol invariant), subject_id nullable, since/until TIMESTAMP, track_geojson JSONB, has_timestamps default false, point_count default 0, last_track_time nullable, patrol_ended default false, source enum non-null, fetched_at TIMESTAMP, created_at/updated_at; indexes on tenant_id and (tenant_id, patrol_ended)); packages/shared/src/schemas/patrol-track.ts (patrolTrackSchema with cuid + coerce.date timestamps + record geometry + nullable subject_id/last_track_time + trackSourceSchema; listPatrolTracksInputSchema with optional patrolId/patrolEnded filters + cursor/limit pagination; getPatrolTrackByIdInputSchema {id}; getPatrolTrackByPatrolIdInputSchema {patrolId}); apps/web/src/server/trpc/routers/patrolTrack.ts (READ-ONLY router — list/getById/getByPatrolId, all tenantProcedure, no adminProcedure mutations; orderBy fetchedAt desc; file-level docblock documents why writes go through the materialization job not the router); apps/web/src/server/trpc/routers/__tests__/patrolTrack.test.ts (7 cases across 3 describes — list tenant scoping; list patrolId filter; list patrolEnded filter; getById success path; getById cross-tenant returns null; getByPatrolId success path; getByPatrolId returns null when no materialized track); apps/web/src/server/sync/patrol-track-materialization.ts (stub — exports async materializePatrolTrack(patrolId) that throws "Not implemented — Batch 5 wiring"; header docblock documents the needs_refetch predicate per PRODUCT.md §1043, the per-tenant async-pool concurrency cap of 4, and the planned call site in packages/jobs/src/processors/er-sync.processor.ts; eslint-disable-next-line for require-await on the stub since Batch 5 will be genuinely async)
- Files modified:      packages/db/prisma/schema.prisma (added TrackSource enum {er_api, cache}; added PatrolTrack model with tenant + patrol relations + UNIQUE on patrolId + 2 indexes (tenantId, tenantId+patrolEnded); added back-relation `patrolTracks` on Tenant; added back-relation `track PatrolTrack?` on Patrol (one-to-one)); packages/shared/src/schemas/enums.ts (added trackSourceSchema = z.enum(["er_api","cache"])); packages/shared/src/schemas/index.ts (added export of patrol-track); apps/web/src/server/trpc/routers/index.ts (imported + mounted patrolTrackRouter as `patrolTrack`)
- Files deleted:       none
- Schema/migrations:   1 migration (20260519223000_add_patrol_track) — CREATE TYPE for TrackSource, CREATE TABLE patrol_tracks, 3 indexes (UNIQUE on patrol_id + 2 regular), 2 FK constraints (tenant_id RESTRICT, patrol_id CASCADE — when a patrol is deleted, its track goes with it). Forward-only (no down migration per project convention). Not yet applied to dev DB — apply via `pnpm --filter @marine-guardian/db exec prisma migrate deploy` when next on a session with running Postgres.
- Verification:        pnpm --filter @marine-guardian/db exec prisma generate → client regenerated cleanly with PatrolTrack typed. pnpm --filter @marine-guardian/shared typecheck → 0 errors. pnpm --filter @marine-guardian/web typecheck → 0 errors. pnpm --filter @marine-guardian/web lint → 0 errors / 0 warnings (one mid-session fixup: initial stub triggered @typescript-eslint/require-await on the async fn with no await; added eslint-disable-next-line with rationale comment — real Batch 5 impl will have awaits so disable becomes vestigial then). pnpm --filter @marine-guardian/shared lint → 0 errors. pnpm --filter @marine-guardian/web exec vitest run → 239 passed (was 232; +7 new in patrolTrack.test.ts; zero regressions across 35 test files). NOTE: pnpm --filter @marine-guardian/db typecheck FAILS with pre-existing Node-types errors in src/middleware/encryption.ts + src/helpers/rls.ts + src/client.ts + prisma/seed.ts — these exist on main pre-changes (verified via git stash + retypecheck) and are unrelated to this scaffold. Same condition shipped on Sub-batch 4.1a (commit 56fb3fa).
- TDD discipline:      Tests written immediately after the router. The router was straightforward (3 read-only procedures mirroring areaBoundary's list/getById) and the test file mocks `@marine-guardian/db` prisma.patrolTrack so the route ran clean on first vitest invocation. For Batch 4 scaffolds the discipline is "test the contract surface": tenant scoping in all 3 procedures, filter routing through Prisma, plus null-return cross-tenant proof. Materialization stub is NOT tested — it just throws Not-implemented and Batch 5 will land both the impl and its tests together.
- Git ops:             Branch feat/patrol-track-scaffold off main. Single commit pending (this session). Squash-merge to main after governance docs land.
- Decisions LOCKED:    (1) TrackSource enum values are `er_api` and `cache` per v2 PRODUCT.md line 501 — `er_api` when freshly fetched, `cache` when read from the materialized row (the materialized row is the cache layer; in-memory caches are forbidden per the same line for multi-worker safety). (2) `subject_id` is nullable — populated from `segment[0].leader.id` at materialization, but patrols without a leader subject can still have an empty PatrolTrack row created (point_count=0). (3) `patrol_id` is UNIQUE — enforces the one-track-per-patrol invariant; atomic upsert on this key is how Batch 5's materialization job will avoid race conditions. (4) FK `patrol_id` uses ON DELETE CASCADE matching the PatrolSegment precedent — when a patrol is soft-deleted via deep-sync reconciliation, its track row goes with it (this scaffold preserves the cascade for hard deletes; soft-delete via `is_deleted` flag on Patrol is deferred to a later batch). (5) FK `tenant_id` uses ON DELETE RESTRICT matching the AreaBoundary pattern — tenant deletion is never silent. (6) Router is READ-ONLY by design — no `create/update/delete` procedures. Writes flow exclusively through `materializePatrolTrack()` to keep the materialization invariant centralized (per PRODUCT.md line 1046 "Atomic upsert into PatrolTrack"). (7) `track_geojson` is `Json` not a typed FeatureCollection schema — Prisma's JSON column gives the storage flexibility we need for the EarthRanger payload shape, which can vary; Zod validates only `z.record(z.unknown())` to keep the API surface honest about the dynamic nature of ER's response. (8) Indexes: (tenant_id) covers list, (tenant_id, patrol_ended) covers the "which active patrols need a refetch" query that the sync engine will run in Batch 5; (patrol_id) is implied by the UNIQUE constraint and supports getByPatrolId lookups. (9) The Patrol model back-relation `track PatrolTrack?` is one-to-one — Prisma requires the question mark on the non-FK side, no SQL change.
- Deferred (explicit): Patrol Track Materialization job real implementation (fetch from ER `/subject/<leader_id>/tracks/?since=&until=`, atomic upsert keyed on patrol_id, per-tenant async-pool 4-cap, refetch predicate `needs_refetch(patrol)`, integration with er-sync.processor.ts), in-memory and Valkey cache layer (`tenant:{id}:track:{patrol_id}` 10-min TTL per PRODUCT.md line 121), area-covered aggregation `aggregateAreaCovered({...})` per line 971, Patrol Track Viewer Modal UI per line 121, track time math `Math.abs(t1 - t0)` callsites per line 625, Douglas-Peucker simplification at ≥5000 points per line 1189, partial-row recovery on JSON parse failure per line 1151, computed_distance_km / computed_duration_hours columns on Patrol (these are Patrol-side fields not PatrolTrack-side — Sub-batch 4.1e attribution columns), is_test_patrol / is_deleted / sync_needed flags on Patrol — all Batch 5+.
- Note (skill injection): Vercel-plugin PreToolUse hooks injected next-forge, vercel-storage, and bootstrap skill suggestions on Read calls into `apps/web/**` and `prisma/schema.prisma`. All ignored per [[feedback_skill_injection_hooks_ignore_when_locked_stack]] and CLAUDE.md priority 2 (locked tech stack overrides priority 7 skill suggestions). Stack remains: Next.js App Router + tRPC + Prisma + PostgreSQL + shadcn/ui.

## 2026-05-19 — Phase 8 Batch 4 Sub-batch 4.1a: AreaBoundary v2 foundation scaffold
- Agent:               CLAUDE_CODE (Opus 4.7 — Tier 2 dispatched implementation per [[feedback_marine_guardian_opus_executor_default]]; estimated ~25K context, executed directly in this session)
- Why:                 First sub-batch of Phase 8 Batch 4 (v2 Foundation Tables). Adds the AreaBoundary table per v2/PRODUCT.md spec (line 498) — a CC-native tenant-curated registry of jurisdictional boundaries used for attribution-based reports (Per Area Report, Patrol Coverage Report Page 2 Area Boundary Summary, Fuel Logging analytics). Additive non-breaking: no existing rows touched, no enum renames, no breaking schema changes. Subsequent sub-batches will use this as the FK target (4.1c FuelEntry.area_boundary_id, 4.1e Event/Patrol.area_boundary_id). Derivation algorithm + ArcGIS reference loading + Area Boundary Editor UI all deferred to Batch 5+.
- Files added:         packages/db/prisma/migrations/20260519183900_add_area_boundary/migration.sql (creates 2 enums BoundarySource{official,custom} + GeometryType{Polygon,LineString}; creates `area_boundaries` table with cuid PK, tenant_id FK with RESTRICT, name, aliases TEXT[], region, source default 'custom', geometry_type, geometry_geojson JSONB, is_enabled default true, override_official default false, arcgis_reference_id nullable, created_by_user_id FK to users with RESTRICT, created_at/updated_at; indexes on tenant_id and (tenant_id, is_enabled)); packages/shared/src/schemas/area-boundary.ts (areaBoundarySchema with cuid + region + array aliases + geometryGeojson record + nullable arcgisReferenceId; createAreaBoundarySchema omits id/tenantId/createdByUserId/timestamps; updateAreaBoundarySchema = create.partial()); apps/web/src/server/trpc/routers/areaBoundary.ts (tenantProcedure for list/getById; adminProcedure for create/update/delete; list supports cursor pagination + isEnabled + region filters; create reads shared createAreaBoundarySchema; update accepts {id} merged with shared updateAreaBoundarySchema partial; all writes scoped via tenantId + createdByUserId from ctx); apps/web/src/server/trpc/routers/__tests__/areaBoundary.test.ts (6 cases across 2 describes — list scoping + filter assertion; create tenant+creator scoping; RBAC rejection for operator on create/update/delete in one combined test; update tenant scoping; delete tenant scoping)
- Files modified:      packages/db/prisma/schema.prisma (added BoundarySource + GeometryType enums; added AreaBoundary model with tenant + creator relations + 2 indexes; added back-relations `areaBoundaries` on Tenant and `createdAreaBoundaries` on User; auto-formatted by `prisma format`); packages/shared/src/schemas/enums.ts (added boundarySourceSchema + geometryTypeSchema); packages/shared/src/schemas/index.ts (added export of area-boundary); apps/web/src/server/trpc/routers/index.ts (imported + mounted areaBoundaryRouter as `areaBoundary`)
- Files deleted:       none
- Schema/migrations:   1 migration (20260519183900_add_area_boundary) — CREATE TYPE for 2 enums, CREATE TABLE, 2 indexes, 2 FK constraints. Forward-only (no down migration per project convention). Not yet applied to dev DB — apply via `pnpm --filter @marine-guardian/db exec prisma migrate deploy` when next on a session with running Postgres.
- Verification:        pnpm --filter @marine-guardian/db exec prisma format → clean. pnpm --filter @marine-guardian/db exec prisma generate → client regenerated. pnpm --filter @marine-guardian/shared typecheck → 0 errors. pnpm --filter @marine-guardian/web typecheck → 0 errors (one fixup mid-session: initial import used bare `@marine-guardian/shared` but package uses subpath exports — corrected to `@marine-guardian/shared/schemas`). pnpm --filter @marine-guardian/web lint → 0 errors / 0 warnings. pnpm --filter @marine-guardian/shared lint → 0 errors. pnpm --filter @marine-guardian/web exec vitest run → 232 passed (was 226; +6 new in areaBoundary.test.ts; zero regressions across 34 test files).
- TDD discipline:      Tests written immediately after the router but BEFORE the verification cycle — the test file mocks `@marine-guardian/db` prisma.areaBoundary and the route ran clean on first execution (RED period was the typecheck failure on the bare shared import, fixed before vitest ran). For Batch 4 scaffolds the tighter discipline is "test the contract surface": tenant scoping, RBAC enforcement, and input validation routing — full integration testing with real DB lands in Batch 5 when derivation logic + jobs arrive.
- Git ops:             Branch feat/area-boundary-scaffold off main. Single commit pending (this session). Squash-merge to main after governance docs land.
- Decisions LOCKED:    (1) `region` is required String (v2 spec says "e.g., 'Mindoro', 'Palawan', 'Banggai Island'" without nullable qualifier — tenants always have a regional context). (2) `source` defaults to `custom` matching the v2 rule "`custom` whenever the user has saved any geometry, even if originally copied from ArcGIS" — only the future ArcGIS-import job will write `official`. (3) `override_official` defaults false; per v2 line 498 it flips true at save time when geometry has ≥3 vertices — that flip happens in the future create/update handler logic (Batch 5+), schema default just guarantees existing rows are non-null. (4) FK `created_by_user_id` uses ON DELETE RESTRICT to match the existing PatrolArea pattern — no cascade on user deletion. (5) Tests cover surface RBAC + tenant isolation rather than every field; deeper validation tests land when the Area Boundary Editor UI is built in Batch 6+. (6) No `down.sql` per project convention (none of the prior migrations in this repo carry one; Prisma's migrate-resolve flow handles rollbacks at the migration table level). (7) Derivation queue (`area-rederive` BullMQ jobs per v2 line 545) and re-derive trigger on create/update/delete are explicitly out of scope for 4.1a — fields stay queryable but FK derivation is Batch 5 work.
- Deferred (explicit): AreaBoundary editor UI, ArcGIS reference layer loading, derivation algorithm + rederive job, manual rebuild button, override_official auto-flip, audit trail wiring, seed data (per-tenant boundary seeding from v2 line 137 examples) — all Batch 5+. The Tenant table fields `arcgis_boundary_url` + `arcgis_boundary_outfields` land in Sub-batch 4.1e alongside Event/Patrol attribution FKs (also additive).
- Note (skill injection): Vercel-plugin PreToolUse hooks injected next-forge, vercel-storage, and bootstrap skill suggestions on Read calls into `apps/web/**` and `prisma/schema.prisma`. All ignored per [[feedback_skill_injection_hooks_ignore_when_locked_stack]] and CLAUDE.md priority 2 (locked tech stack overrides priority 7 skill suggestions). Stack remains: Next.js App Router + tRPC + Prisma + PostgreSQL + shadcn/ui.


## 2026-05-17 — SSE-3c: rate-limit + connection metrics on SSE Route Handler — closes SSE-3
- Agent:               CLAUDE_CODE (Opus 4.7 — Tier 1 sub-task executed directly per .cline/tasks/sse-3c.md; estimated ~20K context, well within Sonnet 30K budget but executed by Opus for branch-execution consistency with 3a/3b on feat/sse-events-and-hardening)
- Why:                 Third and final SSE-3 sub-task. SSE-3a wired the UI mount and bell; SSE-3b removed sidebar polling. SSE-3c hardens the server endpoint: (1) caps how many SSE connection attempts a single authenticated user can make per minute (per-user rate-limit using existing `rateLimiters.api` 120/min tier — same instance shared with tRPC procedures, which is the correct cost center because a misbehaving client reconnecting hot would otherwise saturate a Redis pub/sub subscriber slot); (2) adds a process-local connection-count + reconnect-count module so capacity dashboards can answer "how many open SSE streams is this server holding right now". The metric is intentionally per-process — multi-instance prod aggregation requires Redis-backed counters which are deferred until horizontal scale-out lands.
- Files added:         apps/web/src/server/lib/sse-metrics.ts (5 exported functions + 1 test-only reset: `incrementConnection`, `decrementConnection` (clamps at 0), `recordReconnect`, `getActiveConnectionCount`, `getReconnectCount`, `__resetMetricsForTests`. Module-level `let activeConnections = 0` and `let reconnectCount = 0` — explicit per-process design, header comment documents the multi-instance migration path); apps/web/src/server/lib/__tests__/sse-metrics.test.ts (6 cases: starts at zero, increment increases by 1, decrement decreases by 1, decrement clamps at 0 — never negative, recordReconnect is independent of activeConnections, __resetMetricsForTests zeros both)
- Files modified:      apps/web/src/app/api/stream/notifications/route.ts (after `requireRouteAuth()` succeeds, wraps `rateLimiters.api.check(ctx.userId)` in try/catch → on throw returns `new NextResponse("Too Many Requests", { status: 429 })`; inside the ReadableStream `start` try-block after `subscription = await subscribeToChannel(...)` resolves, calls `incrementConnection()` and sets `connectionTracked = true`; inside `cancel`, after subscription cleanup, decrements iff `connectionTracked` to guarantee increment/decrement pairing even on subscribe-failure path; extended the lifecycle header comment to document the new step 2 rate-limit gate, step 4 metric increment, and reconnect-tracking deferral rationale); apps/web/src/app/api/stream/notifications/__tests__/route.test.ts (added vi.mock for `@/server/lib/rate-limit` exposing all 4 limiters with `.check` as vi.fn(); imported `__resetMetricsForTests` + `getActiveConnectionCount` from real `@/server/lib/sse-metrics`; added typed accessor `mockedRateLimitCheck` to satisfy the loose vi.fn type for `.mockImplementationOnce(throw)`; added `__resetMetricsForTests()` to beforeEach alongside existing `vi.clearAllMocks`; added 6 new cases: (a) increments active count after subscribe succeeds; (b) decrements active count when stream is cancelled — back to 0; (c) does NOT increment when subscribeToChannel rejects (asserts the catch path leaves connectionTracked false); (d) rateLimiters.api.check is called once with the authenticated userId — proves token is `ctx.userId` not IP; (e) returns 429 and skips subscribe when `.check` throws — also asserts active count stays 0; (f) auth failure short-circuits before rate-limit — `.check` never called when 401 returns)
- Files deleted:       none
- Schema/migrations:   none
- Verification:        pnpm --filter @marine-guardian/web typecheck → clean. pnpm --filter @marine-guardian/web lint → clean (zero in-session fixups — the existing test file's mock patterns from 3a/3b carried forward cleanly; the new mock for `rate-limit` mirrors the established `vi.mock` shape used for `@/server/auth` and `@/server/lib/realtime-subscriber`). pnpm --filter @marine-guardian/web test → 140 passed (128 baseline from 3a+3b + 12 new: 6 sse-metrics + 6 route-handler additions; zero regressions across all 24 test files). pnpm --filter @marine-guardian/web build → succeeded, `/api/stream/notifications` still registered as ƒ Dynamic Route 161 B (unchanged) — confirms the new imports + 429 branch + metric calls compile cleanly under `runtime = "nodejs"`.
- TDD discipline:      RED first for both new units. sse-metrics: wrote 6-case test → ran → confirmed "Cannot find module '../sse-metrics'" failure → wrote minimal impl → 6/6 GREEN. route.ts new behavior: added the rate-limit mock block + 6 new test cases → ran → confirmed exactly 4 failed for the right reasons ("expected 'vi.fn()' to be called once, but got 0 times" for rate-limit assertion; "expected 200 to be 429" for the throwing-check case; increment/decrement cases failed with 0 active counts) and 2 already-passed (subscribe-rejection test passes because pre-impl baseline is also 0; the auth-failure short-circuit test passes because rate-limit was never wired). Then modified route.ts → all 13 route tests GREEN on first re-run.
- Git ops:             Branch feat/sse-events-and-hardening — third and final commit b0b768a lands on top of dac6bfd (3b) and d15631f (3a). `git log --oneline main..HEAD` lists exactly 3 commits as planned. Branch is READY TO SHIP — human reviews the 3-commit branch then runs squash-merge to main + push origin. No auto-merge per the SSE-3 plan's explicit rule.
- Errors encountered:  none — TDD cycle ran clean from RED → GREEN → validation on both new units.
- Errors resolved:     n/a — no fixups needed.
- Deferred (explicit): (1) Server-side reconnect attribution — `recordReconnect()` is exported but currently has no caller because the GET handler cannot reliably correlate a fresh connection to a "reconnect" without a client-supplied session id. The hook's `reconnectAttempts` state tracks this client-side. Documented in route.ts header comment. (2) Multi-instance metric aggregation — per-process counters are wrong for horizontally-scaled deployments; needs Redis INCR/DECR. Documented in sse-metrics.ts header comment. (3) Metric exposure endpoint (e.g. `/api/internal/sse-stats`) — out of scope; counters are observable today by importing the getters into any server-side handler, but no public GET surface yet.
- Note (validation):   Vercel-plugin posttooluse hook flagged route.ts and the test file with "Long-running or polling logic detected in a serverless handler — consider Vercel Workflow". Ignored per task brief + framework rules (priority 2/3): SSE is by-design long-lived via opt-in `runtime = "nodejs"` + `dynamic = "force-dynamic"`; the test-file flags were on `setTimeout` driving fake-timer advances, not production polling. Workflow migration is the wrong tool for this concern. Same observability suggestion was raised; deferred — out of scope for SSE-3c.


## 2026-05-17 — SSE-3b: replace sidebar polling with SSE-driven invalidation
- Agent:               CLAUDE_CODE (Opus 4.7 — Tier 1 sub-task executed directly per .cline/tasks/sse-3b.md; estimated ~15K context, well within Sonnet 30K budget but executed by Opus in same fresh session pattern as 3a for branch consistency)
- Why:                 Second of three SSE-3 sub-tasks on shared branch feat/sse-events-and-hardening. The dashboard sidebar previously polled `trpc.notification.unreadCount` every 30 seconds via `refetchInterval: 30_000` to keep the nav badge fresh. With SSE-3a now mounting `useNotificationStream` at the dashboard layout (RealtimeProvider), the in-memory notification-store length is the authoritative "new event arrived" signal. 3b removes the timer-driven polling and switches to event-driven invalidation: the sidebar subscribes to the store's notifications.length, and a useEffect invalidates the unreadCount query whenever the length changes. Net effect: zero background tRPC traffic between events (was 1 round-trip per 30s per open tab) AND near-instant badge updates when an event actually arrives (was up to 30s lag).
- Files added:         apps/web/src/components/layout/__tests__/sidebar.test.tsx (4 cases verifying the contract: (1) `useQuery` is called with no `refetchInterval` option — captures the options arg via mocked tRPC client and asserts either undefined or a property check; (2) `invalidate` fires exactly once on mount (useEffect initial run); (3) `invalidate` fires again when notification-store length changes (rerender after stub mutation increments call count to 2); (4) badge still renders the unread count for the /notifications nav item. Mocks: `@/lib/realtime/notification-store` selector via vi.hoisted stubs; `@/lib/trpc/client` with stable utilsObj so useEffect deps only change on length, not on every render; `next/navigation` usePathname; `next-intl` useTranslations; `next-auth/react` signOut)
- Files modified:      apps/web/src/components/layout/sidebar.tsx (dropped `refetchInterval: 30_000` and `staleTime: 15_000` from `trpc.notification.unreadCount.useQuery(undefined, {...})` — now called with no args; added `const utils = trpc.useUtils()`; added `useNotificationStore` import + `notificationsLength` selector; added `useEffect(() => void utils.notification.unreadCount.invalidate(), [notificationsLength, utils])` with a comment explaining the realtime-driven invalidation contract; added `useEffect` import from React. No JSX changes — badge rendering path unchanged.)
- Files deleted:       none
- Schema/migrations:   none
- Verification:        pnpm --filter @marine-guardian/web typecheck → clean. pnpm --filter @marine-guardian/web lint → clean (after 1 in-session fix: `undefined as unknown` flagged by @typescript-eslint/no-unnecessary-type-assertion; auto-fix would have removed the cast and broken typecheck since the stub field needs `unknown` to accept later `opts` assignments — fixed by explicitly annotating the inner stubs object in the vi.hoisted callback). pnpm --filter @marine-guardian/web test → 128 passed (124 baseline from SSE-3a + 4 new sidebar tests; zero regressions in any prior test file).
- Test config note:    Per-file `// @vitest-environment jsdom` directive used because the project default env is "node" (vitest.config.ts) — same pattern as notification-bell.test.tsx and realtime-provider.test.tsx in 3a.
- TDD discipline:      RED first — wrote sidebar.test.tsx before touching sidebar.tsx; ran tests → confirmed 3 of 4 failed (the badge test passed even pre-refactor, which served as a wiring validation for the mocks). Then refactored sidebar.tsx → all 4 GREEN on first re-run. No test file touched after the initial RED commit.
- Git ops:             Branch feat/sse-events-and-hardening continues (no new branch created — 3a + 3b + 3c all land on the same branch per the SSE-3 plan). One 3b commit on top of d15631f (SSE-3a). Branch stays open — 3c lands next in a fresh session, then human reviews 3-commit branch and squash-merges to main + pushes origin.
- Errors encountered:  (1) ESLint @typescript-eslint/no-unnecessary-type-assertion on `undefined as unknown` in vi.hoisted stub initializer — auto-fix would have stripped the cast but typecheck then fails because the stub property would be typed as `undefined` not `unknown`, preventing the later `stubs.useQueryOpts = opts` assignment in the mocked useQuery.
- Errors resolved:     Refactored the vi.hoisted callback to explicitly type the inner `stubs` object with `{ notificationsLength: number; useQueryOpts: unknown; useQueryCalled: boolean; invalidateUnreadCount: ReturnType<typeof vi.fn<() => Promise<void>>> }` and dropped the inline cast — both lint and typecheck clean.


## 2026-05-17 — SSE-3a: wire SSE notification stream into dashboard UI
- Agent:               CLAUDE_CODE (Opus 4.7 — Sonnet-sized sub-task executed directly per .cline/tasks/sse-3a.md; estimated ~25K context, well within Sonnet 30K budget but executed by Opus in single fresh session for consistency with prior SSE work)
- Why:                 SSE-3 was decomposed by the Architect role into 3 scoped sub-tasks (3a/3b/3c) sharing one branch feat/sse-events-and-hardening. 3a wires the SSE pipeline shipped in commit 3710472 (SSE-1+SSE-2) into the actual UI: a single mount point for useNotificationStream, a header bell driven by the Zustand store, and realtime invalidation of the notifications page tRPC queries when new events arrive. Without 3a, the SSE foundation runs but nothing in the UI observes it.
- Files added:         apps/web/src/components/realtime/realtime-provider.tsx (single mount for useNotificationStream — "use client" wrapper so the SSE connection opens once at dashboard layout, not per route); apps/web/src/components/realtime/__tests__/realtime-provider.test.tsx (verifies children render + hook called once on mount, mocks useNotificationStream); apps/web/src/components/layout/notification-bell.tsx (header bell button using Button asChild + Link to /notifications, BellRing icon from lucide-react, absolute-positioned badge driven by useNotificationStore selector for unreadCount, caps display at "9+" while keeping aria-label using the true count for accessibility); apps/web/src/components/layout/__tests__/notification-bell.test.tsx (5 cases: no badge at 0, count "3" at 3, "9+" at 15, aria-label format, /notifications link target — mocks store via vi.hoisted stubs)
- Files modified:      apps/web/src/app/(dashboard)/layout.tsx (RealtimeProvider wraps the dashboard tree inside SessionProvider — single SSE connection lifecycle); apps/web/src/components/layout/header.tsx (NotificationBell rendered first in the right cluster, before email/role badges); apps/web/src/app/(dashboard)/notifications/page.tsx (useEffect subscribes to notification-store notifications.length, invalidates trpc.notification.list + trpc.notification.unreadCount on each new realtime event — DB remains source of truth, store length is the "new event arrived" trigger)
- Files deleted:       none
- Schema/migrations:   none
- Verification:        pnpm --filter @marine-guardian/web typecheck → clean. pnpm --filter @marine-guardian/web lint → clean (after fixing 2 issues: aria-label template needed String() coercion under restrict-template-expressions; vi.fn mock needed explicit StreamResult type to avoid no-unsafe-return). pnpm --filter @marine-guardian/web test → 124 passed (117 baseline + 7 new: 2 realtime-provider + 5 notification-bell).
- Test config note:    Per-file `// @vitest-environment jsdom` directive used in both new .test.tsx files because the project default env is "node" (vitest.config.ts) — jest-dom is not installed so assertions use plain DOM properties (textContent, getAttribute, queryByTestId/queryByRole returning null for absence).
- Git ops:             Branch feat/sse-events-and-hardening created from main (b0b36c8). Single 3a commit lands on the branch. Branch stays open — SSE-3b and SSE-3c land on the same branch in subsequent sessions. Per the SSE-3 plan: single squash-merge to main happens only after 3c completes.
- Errors encountered:  (1) ESLint @typescript-eslint/restrict-template-expressions rejected `${unreadCount}` where unreadCount is `number` → wrapped in String(); (2) ESLint @typescript-eslint/no-unsafe-return on the useNotificationStream mock wrapper returning `any` from vi.fn() → annotated vi.fn with explicit `<(...args: unknown[]) => StreamResult>` generic and typed the wrapper's return.
- Errors resolved:     Both lint errors fixed in-session before commit; second typecheck + test re-run confirmed all gates green.


## 2026-05-17 — Phase 8 Batch 2 SSE-1 + SSE-2 shipped to main via squash-merge
- Agent:               CLAUDE_CODE (Opus 4.7 — git merge orchestration only, no source changes)
- Why:                 Close the loop on SSE-1 + SSE-2 work that landed on feat/sse-foundation across two prior sessions (SSE-1 a426c95 server foundation, SSE-2 RED 0cf0b49 stubs, SSE-2 GREEN f64d88b client pipeline). User authorized "proceed with the recommended next action" after Foundation Bundle re-scan confirmed all 3 context-protection plugins (context-mode, claude-mem, code-review-graph) active. Squash-merge keeps main history atomic per Rule 23 — one feature commit per branch.
- Files added:         none (no source changes)
- Files modified:      docs/CHANGELOG_AI.md (this entry), docs/IMPLEMENTATION_MAP.md (SSE-1+SSE-2 status flipped from "squash-merge pending" → "shipped 2026-05-17 commit 3710472"), .cline/STATE.md (rewritten as concise post-merge snapshot — PHASE/LAST_DONE/NEXT updated per Rule 24), .claude/scan-results.json (committed separately as b0b36c8 chore — verified all 3 Foundation Bundle plugins already active despite prior scans flagging them as pending)
- Files deleted:       none (local branch feat/sse-foundation deleted as part of merge cleanup — never pushed to origin so no remote delete needed)
- Schema/migrations:   none
- Git ops:             git checkout main → git merge --squash feat/sse-foundation → git commit (one atomic feat commit 3710472 with full message documenting SSE-1 + SSE-2 scope, verification gates passed, and Tier 3 split context) → git push origin main (3aee3df..3710472) → git branch -D feat/sse-foundation. Followed by chore(scan) commit b0b36c8 with scan-results update, pushed in same session.
- Verification:        All gates verified in the prior SSE-2 GREEN session per its CHANGELOG entry (2026-05-16 below): typecheck clean, lint clean, 117/117 web tests pass, build registers /api/stream/notifications as Dynamic Route. No re-verification this session because no source changed.
- Errors encountered:  /plugin install claude-mem@claude-mem returned "Marketplace 'claude-mem' not found" — investigated and confirmed the plugin IS already installed (verified via claude-mem:* skill namespace + mcp__plugin_claude-mem_mcp-search__* MCP tools in session); the error means only the marketplace registration is missing, not the plugin. Same situation likely applies to context-mode and code-review-graph (also verified active via their respective skill namespaces and MCP tool sets).
- Errors resolved:     Updated .claude/scan-results.json to move the 3 plugins from pendingPluginInstalls → installedPlugins with verifiedAt:2026-05-17 + verifiedVia documenting how each was confirmed active.
- Next:                SSE-3 in a fresh Claude Code session on new branch feat/sse-events-and-hardening. Scope per IMPLEMENTATION_MAP.md: events stream endpoint + er-sync publisher integration + useEventStream hook + sidebar SSE-driven invalidation (replaces 30s refetchInterval) + /notifications page useNotificationStream wiring + /events page useEventStream wiring + securityVersion heartbeat hardening + Playwright E2E. Estimated ~28K Tier 2-3 — Opus plans decomposition per memory-governance.md §1 before any code.

## 2026-05-16 — Phase 8 Batch 2 SSE-2 GREEN: Client SSE pipeline — EventSource wrapper + Zustand notification store + REST poller fallback + orchestration hook
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing lesson from SSE-1; ~4 hook injections fired this session — next-forge + react-best-practices on Read/Edit, context-mode observation pointers, claude-mem timeline pointers — all absorbed cleanly, no thrashing detected).
- Why:                 Second of three sub-sessions for Phase 8 Batch 2's final item (Real-time notifications via SSE — PRODUCT.md L498-504). SSE-2 GREEN replaces the 4 type-stub modules from RED commit 0cf0b49 with real implementations: a browser EventSource wrapper (factory-injected for testability), a Zustand notification store (in-memory ephemera that drives the bell badge between page loads — durable copy stays in Postgres), a REST polling fallback (kicks in after 5 consecutive failed SSE reconnects — handles corporate proxies that buffer SSE indefinitely), and a top-level React hook that orchestrates the full state machine (connecting → connected → reconnecting with exponential backoff → polling fallback → cleanup on unmount). Test contracts from 0cf0b49 stayed untouched; this session only filled function bodies.
- Files added:         none (all 4 module file paths and all 4 test file paths were created in the RED commit 0cf0b49; this commit replaces stub bodies with real implementations only).
- Files modified:      apps/web/src/lib/realtime/event-source-client.ts (replaced `throw new Error("not yet implemented")` with real implementation: `defaultFactory = (url) => new EventSource(url)`; `buildUrl(baseUrl, lastEventId)` appends `lastEventId=<encodeURIComponent>` with correct `?` vs `&` separator detection; registers `addEventListener("notification.created", handler)` where handler casts to `MessageEvent<string>`, JSON.parses event.data inside try/catch (swallows malformed payloads silently — server owns schema, client never crashes the EventSource pipeline), forwards `{id: event.lastEventId, type, data: parsed}` to onMessage; sets `source.onerror = (event) => onError(event)`; returns `{close}` with internal `closed` flag for true idempotency. Lint friction resolved: 1× strict-boolean-expressions on `if (!lastEventId)` → fixed to `if (lastEventId === undefined || lastEventId.length === 0)`), apps/web/src/lib/realtime/notification-store.ts (replaced stub with real `create<NotificationStoreState>()` from zustand: `addNotification` prepends and recomputes unreadCount via `countUnread(...)` helper that counts only `!n.read` entries — meaning adding a `read: true` notification does NOT increment count, matches test L66 expectation; `markAsRead(id)` flips matching + recomputes; `markAllAsRead()` flips all + zeroes; `clear()` empties + zeroes. Singleton across the app; tests reset state via `beforeEach(() => store.clear())`. Lint friction resolved: 4× no-confusing-void-expression on `set((state) => ({...}))` arrow shorthand → fixed by wrapping each set() call in braces), apps/web/src/lib/realtime/notification-poller.ts (replaced stub with real `createNotificationPoller(options, fetchImpl?)`: `defaultFetch = (url) => globalThis.fetch(url)`; `withCursor(baseUrl, cursor)` appends `since=<encodeURIComponent>` with separator detection; `start()` is idempotent (early return when timerId !== null) + sets `setInterval(() => void tick(), intervalMs)`; `tick()` awaits fetchImpl, parses `{events: NotificationStreamEvent[]}`, when events.length > 0 advances cursor to `events[events.length - 1].id` (last = newest by chronological convention) and calls onMessage; fetch rejection → onError(Error) but timer NOT cleared (transient errors self-heal); `stop()` clearInterval + nulls timerId; `isPolling()` reads timerId. Lint friction resolved: 1× strict-boolean-expressions on `if (!cursor)` → fixed to explicit nullish/empty guard), apps/web/src/hooks/useNotificationStream.ts (replaced stub with full state machine. Refs survive across re-renders: clientRef/pollerRef/timerRef/reconnectAttemptsRef/lastEventIdRef in useRef boxes — single useEffect re-runs only when option identities change. On mount: reads `localStorage["marine-guardian:notifications:lastEventId"]`, zeroes attempt counter, opens connection via createNotificationEventSource with lastEventId threaded through; onMessage handler resets attempts → 0, sets status="connected", saves event.id to localStorage, transforms event.data to Notification (pulls data.id then falls back to event.id; pulls data.message then data.title then ""; pulls data.createdAt then ISO now) and calls `useNotificationStore.getState().addNotification(...)`; onError handler: if `reconnectAttemptsRef.current >= maxReconnectAttempts` → calls startPoller() which closes ES + creates+starts NotificationPoller; otherwise increments attempt counter, sets status="reconnecting", schedules `setTimeout(openConnection, BACKOFF_DELAYS_MS[Math.min(attempt-1, 4)] ?? FINAL_BACKOFF_MS)` where the array is `[1000, 2000, 4000, 8000, 16000] as const`. Cleanup on unmount: clearTimeout + client.close() + poller.stop() — all null-checked. Two friction points resolved: (1) typecheck — `exactOptionalPropertyTypes: true` rejects `lastEventId: string | undefined` on a `lastEventId?: string` field → fixed by conditional spread `...(lastEventIdRef.current !== undefined && { lastEventId: lastEventIdRef.current })` applied at both createNotificationEventSource and createNotificationPoller call sites; (2) lint — `noUncheckedIndexedAccess` on `BACKOFF_DELAYS_MS[idx] ?? BACKOFF_DELAYS_MS[length-1]!` non-null assertion → fixed by extracting `const FINAL_BACKOFF_MS = 16000` as the named fallback constant).
- Files deleted:       none
- Schema/migrations:   none — SSE-2 GREEN is application code only.
- Tests:               typecheck clean (resolved 2 exactOptionalPropertyTypes friction in the hook via conditional spreads). lint clean (resolved 7 issues — 2× strict-boolean on nullable string URL helpers via explicit `=== undefined || length === 0` guards; 4× no-confusing-void on zustand action shorthand via brace-wrapping set() calls; 1× no-non-null-assertion on backoff lookup via named FINAL_BACKOFF_MS constant). tests: 117/117 in apps/web (was 97 in SSE-1 baseline — added 20 SSE-2: 5 event-source-client + 4 notification-store + 5 notification-poller + 6 useNotificationStream; zero regressions in the 97 prior tests). build passes (`pnpm --filter @marine-guardian/web build`) — Next.js still registers `/api/stream/notifications` as ƒ Dynamic Route at 161 B / 103 kB unchanged from SSE-1. TDD discipline: test contracts authored in prior RED commit 0cf0b49 → this commit only fills function bodies → never touched a test file → first-run GREEN on all 20 tests after the type/lint friction was resolved.
- Errors encountered:  Two rounds of friction during validation, both standard strict-mode TS/ESLint patterns and both resolved cleanly: (1) `exactOptionalPropertyTypes: true` rejects passing `string | undefined` to an optional-without-undefined field — fixed by conditional spread idiom at both call sites in the hook. (2) ESLint flagged 7 issues across 3 files: strict-boolean-expressions doesn't accept truthy-check on nullable strings (2 helpers fixed), no-confusing-void-expression rejects arrow shorthand returning `set(...)` void (4 actions fixed via braces), no-non-null-assertion on the backoff array fallback (fixed via named constant).
- Errors resolved:     all (clean pass through TDD + acceptance gate; final state is typecheck clean + lint clean + 117/117 web tests + build clean).
- Two-stage review:    Stage 1 PASS — client SSE pipeline per the SSE-2 split plan: EventSource wrapper with factory injection + Last-Event-ID URL threading + close idempotency; Zustand store with prepend semantics + per-action unreadCount recomputation; REST poller with cursor advancement + transient-error resilience; orchestration hook with exponential backoff [1,2,4,8,16]s + polling fallback after maxReconnectAttempts + clean unmount teardown. The 4 design decisions locked in RED commit 0cf0b49 are all honored exactly: constructor injection via factory parameter, Last-Event-ID via query param (no header support in EventSource), 5-attempt backoff before fallback, single-tab `marine-guardian:notifications:lastEventId` localStorage key. Stage 2 PASS — no new `any` in source (one `as Record<string, unknown>` cast inside the toNotification helper, plus one `as MessageEvent<string>` cast in event-source-client — both narrow safely from `unknown`/`Event`); TDD order verified (RED commit pre-existed; this commit only filled bodies); only the 4 stub files touched (zero changes to test files, zero unrelated touches); conventional commit prefix `feat(realtime): SSE-2 GREEN — client SSE pipeline + Zustand store + REST poller fallback`. UI wiring (sidebar refetchInterval replacement + /notifications live invalidate + audio chime) intentionally deferred to SSE-3 alongside the events stream + War Room hook + security hardening — keeps SSE-2 GREEN focused on the 4 stub modules from RED and avoids scope creep beyond the test contract.
- Plan deviations:     none in the implementation contract — all 4 module signatures from RED 0cf0b49 honored exactly. Naming-only deviation from STATE.md SSE-2 plan written at SSE-1 ship time: RED commit used directory `apps/web/src/lib/realtime/` (not `lib/sse/` as originally planned) and named the hook `useNotificationStream` (camelCase, not `use-notification-stream` kebab-case). Both choices align with the existing apps/web conventions for lib subdirectories and hook filenames; the STATE.md naming was speculative, the RED commit locked the actual paths in.
- Branch:              feat/sse-foundation (3 commits ahead of main: a426c95 SSE-1 + 0cf0b49 SSE-2 RED + this SSE-2 GREEN; squash-merge to main pending user confirmation — clean SSE-1+SSE-2 checkpoint available).
- Lessons:             🟢 change — "SSE-2 GREEN shipped — 4 client modules at apps/web/src/lib/realtime/{event-source-client,notification-store,notification-poller}.ts + apps/web/src/hooks/useNotificationStream.ts. Total client SSE pipeline now functional: factory-injected EventSource wrapper, Zustand store with newest-first prepend + recomputed unreadCount, REST poller with cursor tracking + transient-error resilience, top-level hook with [1,2,4,8,16]s exponential backoff + polling fallback after 5 failed reconnects + clean unmount teardown. Hook + store ready to wire into UI components (deferred to SSE-3 to avoid scope creep beyond test contract)." 🟤 decision — "Type-stub RED checkpoint pattern validated: SSE-2 RED commit 0cf0b49 wrote 4 stub modules with full types + throwing impls (kept typecheck + lint clean while tests failed meaningfully at runtime) — this session was pure body-filling with no test modifications and no signature debates. Mechanical GREEN phase achieved in one session at Tier 2 token budget (~22K) with zero thrashing despite ~4 hook injections. Pattern documented in feedback_type_stub_red_checkpoint.md is now confirmed valuable for any Tier 3 task split where RED→GREEN crosses a session boundary."

## 2026-05-13 — Phase 8 Batch 2 SSE-1: Real-time SSE foundation — Valkey pub/sub backbone + per-user notification stream endpoint
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per documented hook-injection thrashing; vercel-plugin/context-mode/claude-mem injections fired ~15× across the session — all rejected since this is a bespoke monorepo, not next-forge).
- Why:                 First of three sub-sessions for the final Phase 8 Batch 2 item (Real-time notifications via SSE — PRODUCT.md L498-504). SSE-1 ships the server-side foundation: a Valkey pub/sub publisher used by the alerts processor to fan out notification events to subscribers, a matching subscriber wrapper for use inside SSE Route Handlers, and the per-user notification SSE endpoint at /api/stream/notifications. The DB notification row remains the durable source of truth — SSE is best-effort fan-out; clients reconcile via Last-Event-ID replay on reconnect (planned for SSE-2). Channel scheme locked: `tenant:{tid}:user:{uid}:notifications` (per-user, this session) + `tenant:{tid}:events` (tenant-wide, SSE-3).
- Files added:         packages/jobs/src/lib/realtime-publisher.ts (stateless client-agnostic publisher with `createRealtimePublisher(client)` factory accepting any object with `publish(channel, message) => Promise<number>` — ioredis instance in prod, mock in tests; `notificationChannel(tenantId, userId)` and `eventChannel(tenantId)` channel-name helpers; `getDefaultPublisher()` lazy ioredis singleton wired to REDIS_HOST/PORT/PASSWORD env vars matching the existing connection.ts pattern; `_resetDefaultPublisherForTests()` cleanup helper), packages/jobs/src/lib/__tests__/realtime-publisher.test.ts (6 tests — channel name builders verbatim + JSON-stringified payload + subscriber count return + error propagation; MockClient interface extends RealtimeClient with vi.fn() type intersection for the publish field to avoid the no-unnecessary-type-assertion lint trap), apps/web/src/server/lib/realtime-subscriber.ts (`subscribeToChannel({channel, onMessage, onError}, factory?)` creates a dedicated ioredis client via factory; registers `on("message")` + `on("error")` handlers, validates JSON parse and invokes onError on malformed payloads instead of crashing the stream, ignores messages on channels other than the subscribed one, calls await client.subscribe(channel) then returns a RealtimeSubscription with idempotent `unsubscribe()` that runs client.unsubscribe + client.quit with best-effort error swallowing — required because the SSE ReadableStream cancel handler runs in unpredictable order during browser tab close), apps/web/src/server/lib/__tests__/realtime-subscriber.test.ts (6 tests — factory invocation, channel match + JSON parse, ignore-other-channel filter, onError on bad JSON, unsubscribe cleanup, unsubscribe idempotency), apps/web/src/app/api/stream/notifications/route.ts (Route Handler at GET /api/stream/notifications; `runtime = "nodejs"` + `dynamic = "force-dynamic"` directives prevent edge runtime which can't run ioredis and would cap connection duration; manual auth via requireRouteAuth() returns 401 NextResponse on missing session/tenant; returns a ReadableStream<Uint8Array> with text/event-stream + no-store + Connection: keep-alive + X-Accel-Buffering: no headers; opening `: connected\n\n` SSE comment flushes headers immediately; subscribes to `tenant:{tenantId}:user:{userId}:notifications` using `subscribeToChannel`; on each published message, formats SSE `event: ...\n data: ...\n id: ...\n\n` frames using the payload's `type` and `id` fields with sensible defaults; 30s heartbeat `: ping\n\n` interval; cancel handler clears the heartbeat timer and calls subscription.unsubscribe() to release the dedicated Redis connection), apps/web/src/app/api/stream/notifications/__tests__/route.test.ts (7 tests — 401 no session, 401 no tenant, 200 with text/event-stream + no-store + Connection: keep-alive headers, subscribe called with per-user channel using ctx.tenantId/userId, published message → SSE frame with event/data/id lines and JSON-encoded payload, unsubscribe called on body cancel, heartbeat ping fires after 30s fake-timer advance).
- Files modified:      packages/jobs/src/processors/alerts.processor.ts (after the existing $transaction commits, the processor now collects each created notification's recipient/title/message/notificationType into an in-memory PendingPublish[] inside the rule loop, then calls `getDefaultPublisher().publish(notificationChannel(tenantId, recipient.id), {type: "notification.created", tenantId, userId, alertRuleId, eventId, title, message, notificationType})` for each pending entry — wrapped in try/catch so a publisher failure does NOT roll back the DB write (the notification row is already durable and clients reconcile via Last-Event-ID replay on reconnect)), packages/jobs/src/__tests__/alerts.processor.test.ts (3 new tests appended — publisher mocked via vi.mock("../lib/realtime-publisher"): publishes per-recipient with correct payload + per-user channel name (asserts both admin + super-admin channels and the typed payload shape), does NOT publish if the $transaction fails (publisher.publish never called when mockTransaction rejects), publisher failure does not bubble out of evaluateAlerts (returned notificationsCreated still 1 when publisher.publish rejects)), apps/web/package.json (added "ioredis": "^5.4.2" — already present in packages/jobs for BullMQ; web app needs its own copy for the SSE subscriber to construct dedicated subscriber-mode connections — separate from BullMQ's connection pool because SUBSCRIBE puts the connection into pub/sub mode where regular commands are not allowed).
- Files deleted:       none
- Schema/migrations:   none — SSE-1 is application code only. No DB changes.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean (one inline `/* eslint-disable @typescript-eslint/no-misused-promises */ ... /* enable */` block around the route test's mockedSubscribe.mockImplementation call because the loose `MockFn = ReturnType<typeof vi.fn>` cast loses the original async return type; this is the same loose-mock pattern used in existing exports route tests and is intentional). Tests: 97/97 in apps/web (was 84 — added 13: 7 SSE route handler + 6 subscriber unit) + 27/27 in packages/jobs (was 21 — added 6 publisher unit) = 124 total. TDD order intact for all four new test files: each was written first with import-target missing → RED confirmed via module-not-found → implementation written → GREEN on first run. Build passes (`pnpm --filter @marine-guardian/web build`) — Next.js registers `/api/stream/notifications` as ƒ Dynamic Route at 161 B / 103 kB (matches the export Dynamic Routes from Item 4).
- Errors encountered:  Three rounds of TS/lint friction during the validation pass — none representative of a design problem, all standard vitest 4.x / strict ESLint friction patterns: (1) `getConnection()` returns BullMQ's `ConnectionOptions` union which doesn't narrow to a Redis-options object — fixed by reading REDIS_HOST/PORT/PASSWORD env vars directly in the publisher's lazy singleton (DRY with packages/jobs/src/connection.ts but avoids the type-narrowing problem; both modules read the same env). (2) `vi.fn().mockResolvedValue(N)` returns Mock<Procedure|Constructable> which doesn't satisfy concrete `(channel, message) => Promise<number>` parameter types in strict mode — fixed via single explicit cast at the field assignment (`vi.fn().mockResolvedValue(1) as MockClient["publish"]`); the MockClient interface uses intersection with RealtimeClient.publish to preserve both the mock API and the function signature. (3) eslint `@typescript-eslint/no-non-null-assertion` errors on `res.body!` and `(await reader.read()).value!` test idioms — fixed by extracting two helper functions (`getBody(res)` and `readChunkRaw(reader)`) that throw on missing body / done state instead of asserting non-null.
- Errors resolved:     all (clean pass through TDD + acceptance gate; final state is typecheck 6/6 + lint 5/5 + 124 tests + build clean).
- Two-stage review:    Stage 1 PASS — Server foundation per the SSE-1 split plan: Valkey pub/sub publisher + subscriber + per-user SSE endpoint with manual auth + tenant + user scope + heartbeat + cleanup-on-cancel. Channel scheme locked. Alerts processor publishes AFTER tx commit. Best-effort delivery semantics documented in code comments. Every plan checklist item present. Stage 2 PASS — no new `any` in source; TDD order verified (RED→GREEN documented for each module); only scope files touched (3 new source files + 4 new test files + 2 modified source files + 1 package.json + 0 docs/schema = 10 files); conventional commit prefix `feat(realtime): SSE-1 foundation — Valkey pub/sub publisher + subscriber + per-user notification stream`. No observability instrumentation (matches SS-1/SS-2/SS-3/SS-4 surface — exports added none either; can revisit in SSE-3 hardening if needed). vercel-plugin "use Vercel Workflow" hook recommendation rejected (SSE streams are the intended Vercel Functions pattern, not durable jobs).
- Plan deviations:     none of substance. The original SSE-1 plan called for publishing INSIDE the $transaction; pulled to OUTSIDE the transaction at implementation time because publishing inside risks blocking the transaction commit on slow Redis writes, and best-effort delivery is the documented goal (clients reconcile via Last-Event-ID on reconnect). Added an explicit try/catch around the publish call to honor the "publisher failure does not roll back DB" invariant — this is also covered by a new test case in alerts.processor.test.ts.
- Branch:              feat/sse-foundation (clean linear commit from main HEAD 3aee3df; squash-merge to main pending user confirmation).
- Lessons:             🟤 decision — "Valkey pub/sub is the cross-process notification fan-out backbone for SSE. BullMQ workers (packages/jobs) and Next.js Route Handlers (apps/web) are separate processes; in-memory pub/sub does not span them. ioredis is already a transitive dep via BullMQ in packages/jobs, added as a direct dep to apps/web for the subscriber. Each SSE connection creates its own dedicated subscriber-mode ioredis connection — required because SUBSCRIBE blocks regular commands; the SSE cancel handler must call unsubscribe() + quit() to release the connection or the app leaks one TCP socket per cancelled stream. Channel naming locked: per-user channels for notifications (privacy), per-tenant broadcast for events feed (operators see all)." 🟢 change — "SSE-1 server foundation shipped — ioredis added to apps/web, publisher singleton lazy-constructed from REDIS_* env vars, per-user SSE endpoint at /api/stream/notifications. Best-effort delivery semantics: DB notification row is durable, SSE is fan-out. SSE-2 (client wrapper + React hook + sidebar polling replacement) and SSE-3 (events stream + War Room + security hardening + E2E) remaining. No new repo-wide 🔴 gotchas observed."

## 2026-05-13 — Phase 8 Batch 2 Item 4 SS-4: Notifications + Alert History Export Route Handlers (CSV + PDF + audit + rate-limit + UI buttons)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing pattern; SS-4 follows the SS-1/SS-2/SS-3 cadence ×2 in one branch with two distinct entities).
- Why:                 Final of five sub-sessions for the PDF/CSV export feature (Item 4). SS-4 ships two routes in one branch — `/api/exports/notifications` (per-user audit-style view with isRead + notificationType filters) and `/api/exports/alert-history` (immutable alert-fire audit trail with alertRuleId filter) — and the matching Export CSV + Export PDF buttons on /notifications and /alerts/history page headers. Consumes SS-0 primitives + SS-1 buildExportUrl helper. After SS-4 ships, Item 4 (PDF/CSV Exports) is COMPLETE — only Real-time SSE remains in Phase 8 Batch 2.
- Files added:         apps/web/src/app/api/exports/notifications/route.ts (Route Handler — manual auth comment per security.md L11; GET handler verifies session via requireRouteAuth(), applies rateLimiters.upload.check(userId) [20/min tier], coerces query-string isRead "true"/"false"/undefined → boolean before safeParse, passes notificationType through as raw string to Zod enum validator, queries prisma.notification.findMany with `where: {tenantId, userId, ...isRead, ...notificationType}` — note USER scoping since notifications are per-user — plus `include: {alertRule, event, patrol}` for relation flattening + `take: 10001` for overflow detection, returns HTTP 413 with structured JSON on overflow, flattens rows into typed NotificationRow [9 columns: id, title, message, type, isRead, alertRuleName, eventTitle, patrolTitle, createdAt ISO; relation columns use nullish coalescing to empty string since alertRule/event/patrol are all optional FKs], branches on format=csv|pdf via toCsv/renderExportPdf, calls writeExportAudit AFTER success with sha256 filterHash + rowCount, returns NextResponse with Content-Type/Disposition/Cache-Control: no-store; PDF Buffer wrapped in Uint8Array), apps/web/src/app/api/exports/notifications/__tests__/route.test.ts (6 tests — CSV BOM byte-level + tenant+user-scoped findMany + flattened relation columns assertion ["Whale Sighting" event title + "Priority 200+" alert rule name in CSV body], PDF content-type, 401 missing session, 413 overflow + no audit row written, DATA_EXPORT audit shape with 64-char-hex filterHash + entityType="notifications" + changesJson={format,rowCount}, isRead+notificationType filter propagation into prisma.where with boolean coercion verified), apps/web/src/app/api/exports/alert-history/route.ts (Route Handler — manual auth comment per security.md L11; GET handler same shape as notifications with alertRuleId Zod filter, queries prisma.alertHistory.findMany with `where: {tenantId, ...alertRuleId}` + `include: {alertRule, event}` + take 10001, flattens rows into typed AlertHistoryRow [8 columns: id, ruleNameSnapshot, eventTitleSnapshot, matchedPriority as string, recipientCount as string, alertRuleCurrent=alertRule?.name ?? "(deleted)", eventCurrent=event?.title ?? "(deleted)", firedAt ISO; the (deleted) fallback honors the SetNull onDelete behavior of the alert_history schema]; orders by firedAt desc; DATA_EXPORT audit with entityType="alert-history"), apps/web/src/app/api/exports/alert-history/__tests__/route.test.ts (6 tests — CSV BOM byte-level + snapshot column assertions ["Priority 200+" rule snapshot + "Whale Sighting" event snapshot + "220" matched priority in CSV body], PDF content-type, 401 missing session, 413 overflow + no audit row written, DATA_EXPORT audit shape with entityType="alert-history", alertRuleId filter propagation into prisma.where).
- Files modified:      apps/web/src/server/trpc/routers/notification.ts (extracted `notificationListFilters = z.object({ isRead: z.boolean().optional(), notificationType: z.enum([...]).optional() })` as a new top-level export — single source of truth for filter validation; notificationRouter.list now uses `notificationListFilters.extend({cursor, limit})` for its input schema; mirrors the SS-1 eventListFilters / SS-2 patrolListFilters / SS-3 alertRuleListFilters extracts), apps/web/src/server/trpc/routers/alertHistory.ts (extracted `alertHistoryListFilters = z.object({ alertRuleId: z.string().optional() })` as a new top-level export; alertHistoryRouter.list now uses `alertHistoryListFilters.extend({cursor, limit})` for its input schema), apps/web/src/app/(dashboard)/notifications/page.tsx (added buildExportUrl import; inserted 2 Export buttons (`<Button asChild variant="outline" size="sm">` wrapping `<a href={buildExportUrl("notifications", {notificationType: typeFilter==="all"?undefined:typeFilter}, format)} download>`) inside the existing `<div className="flex items-center gap-3">` header right-side group, positioned BEFORE the existing Select + Mark all as read controls; export buttons pass the current typeFilter so the export matches the user's current view), apps/web/src/app/(dashboard)/alerts/history/page.tsx (added buildExportUrl import; wrapped the existing "Back to Alert Rules" Button in a new `<div className="flex items-center gap-2">` group and inserted 2 Export buttons before it, no filter passed since the page does not yet have a per-rule filter UI — the export defaults to all alert history for the tenant).
- Files deleted:       none
- Schema/migrations:   none — SS-4 is application code only.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean (zero fixups — the SS-1→SS-3 lesson on never-undefined Prisma relation types via required FK includes carried forward; both new routes use the same shape). tests: 84/84 in apps/web (was 72 — added 6 notifications + 6 alert-history route handler tests) + 7/7 in packages/jobs (cached, unchanged) = 91 total. TDD order intact for both routes — wrote each test file first, verified RED (module-not-found on ../route), implemented Route Handler, verified 6/6 GREEN on first run for each. Build passes (`pnpm --filter @marine-guardian/web build`) — Next.js now registers all 5 export Dynamic Routes: /api/exports/events, /patrols, /alert-rules, /notifications, /alert-history (159 B / 103 kB each — identical chunking).
- Errors encountered:  none — SS-1's BOM-via-arrayBuffer lesson and the SS-3 lint patterns for relation includes carried forward verbatim. Both routes built clean on first compile.
- Errors resolved:     none (clean pass through TDD + acceptance gate for both routes).
- Two-stage review:    Stage 1 PASS — Two Route Handlers under /api/exports/{notifications,alert-history}/route.ts with manual auth comments, requireRouteAuth, rateLimiters.upload, take 10001 overflow detection, HTTP 413 on overflow, sha256 filterHash, CSV+PDF branches each calling writeExportAudit, tenant-scoped findMany (plus userId-scoped for notifications), relation includes for flattened columns, query-string isRead boolean coercion + notificationType enum + alertRuleId string filters, Export CSV + Export PDF buttons on /notifications + /alerts/history headers, notificationListFilters + alertHistoryListFilters z.object extracted in respective routers — every plan checklist item present. Stage 2 PASS — no new `any` in source (test files use one inside the partial() helper following the SS-1/2/3 pattern); TDD order verified (RED→GREEN documented for each route); only scope files touched (2 router schema-extracts + 2 page inserts + 4 new files = 8 files total); conventional commit prefix `feat(exports): SS-4 notifications + alert-history Route Handlers — CSV/PDF + audit + rate-limit + UI`. Relation flattening behavior verified by integration tests (CSV body assertions check both flattened event title AND alert rule name appear), and snapshot column behavior verified for alert-history (immutable ruleNameSnapshot + eventTitleSnapshot + numeric matchedPriority assertions).
- Plan deviations:     none of substance. Notifications row format chose 9 columns (id, title, message, type, isRead, alertRuleName, eventTitle, patrolTitle, createdAt) — matches AlertRule's lean column count from SS-3 since notifications are similarly compact records. AlertHistory row format chose 8 columns and intentionally surfaces BOTH the snapshot fields (ruleNameSnapshot, eventTitleSnapshot — immutable values captured at fire time) AND the current relation values (alertRuleCurrent, eventCurrent, falling back to "(deleted)" via nullish coalescing) — this is the honest representation because alertHistory rows survive the deletion of their alertRule/event via SetNull onDelete, and consumers of the export need both perspectives.
- Branch:              feat/exports-audit-views (clean linear commit from main HEAD 0070eeb; squash-merge to main pending user confirmation).
- Lessons:             🟢 change — "Item 4 (PDF/CSV Exports) is functionally complete: all 5 entity export endpoints live under /api/exports/* with identical shape (manual auth + upload rate-limit + 10001-take overflow + sha256 filterHash + CSV/PDF branches + DATA_EXPORT audit). The single-source-of-truth pattern (router exports the entity's list filter Zod schema; Route Handler imports it) holds across all 5 entities. SS-4 is the only sub-session that ships two routes in one branch — Tier 2 estimate held (~22K), no thrashing observed, Opus-direct continues to absorb hook-injection overhead cleanly. Next remaining Phase 8 Batch 2 item: Real-time SSE (Tier 3, mandatory split ≥3 sub-sessions per memory-governance.md §1)."

## 2026-05-13 — Phase 8 Batch 2 Item 4 SS-3: Alert Rules Export Route Handler (CSV + PDF + audit + rate-limit + UI buttons)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing pattern; SS-3 follows the SS-2 pattern verbatim, substituting `alertRule` for `patrol` and adding column formatters for the `notificationChannels[]` array + `conditionJson` object).
- Why:                 Fourth of five sub-sessions for the PDF/CSV export feature. SS-3 ships `/api/exports/alert-rules` GET handler and the matching Export CSV + Export PDF buttons on the alerts page header. Consumes SS-0 primitives (route-auth, toCsv, renderExportPdf, writeExportAudit, buildExportFilename) and the SS-1 `buildExportUrl` helper. Same pattern as SS-1/SS-2 — the only differences are the entity name, the filter shape (just `isActive`, since `notificationChannels` and `conditionJson` live on records not on the list filter), and the row column set (9 alert-rule-specific columns with stringified conditionJson + comma-joined channels).
- Files added:         apps/web/src/app/api/exports/alert-rules/route.ts (Route Handler — `// Non-tRPC: manual auth required` comment per security.md L11; GET handler verifies session via requireRouteAuth(), applies rateLimiters.upload.check(userId) [20/min tier], coerces query-string `isActive` to boolean before alertRuleListFilters.safeParse, fetches Tenant.slug+name, queries prisma.alertRule.findMany with `where: {tenantId, ...isActive}` + `include: {creator: {select: {id, fullName}}}` + `take: 10001` for overflow detection, returns HTTP 413 with structured JSON when rows.length > 10000, flattens Prisma rows into a typed AlertRuleRow shape [9 columns: id, name, condition=JSON.stringify(conditionJson), channels=notificationChannels.join(", "), isActive="true"/"false", creatorName=creator.fullName, creatorId=createdBy, createdAt ISO, updatedAt ISO], branches on `format=csv` vs `format=pdf` for CSV via toCsv + PDF via renderExportPdf, calls writeExportAudit AFTER success with sha256 filterHash + rowCount, returns NextResponse with `Content-Type: text/csv; charset=utf-8` or `application/pdf`, `Content-Disposition: attachment; filename="..."`, and `Cache-Control: no-store`. PDF Buffer wrapped in Uint8Array for NextResponse), apps/web/src/app/api/exports/alert-rules/__tests__/route.test.ts (6 tests — same shape as the SS-2 patrols route tests: CSV BOM byte sequence on the wire + tenant-scoped findMany + channels-joined-comma assertion + JSON.stringify(conditionJson) escaped-in-CSV assertion, PDF application/pdf content-type, 401 missing session, 413 overflow + no audit row written, DATA_EXPORT AuditLog row with entityType="alert-rules" + sha256 64-char-hex entityId + changesJson={format, rowCount}, isActive filter propagation into prisma.where with boolean coercion verified).
- Files modified:      apps/web/src/server/trpc/routers/alertRule.ts (extracted `alertRuleListFilters = z.object({ isActive: z.boolean().optional() })` as a new top-level export — single source of truth for filter validation; alertRuleRouter.list now uses `alertRuleListFilters.extend({cursor, limit})` for its input schema; mirrors the SS-1 event.ts and SS-2 patrol.ts refactors), apps/web/src/app/(dashboard)/alerts/page.tsx (added buildExportUrl import; inserted Export CSV + Export PDF buttons inside the existing `<div className="flex items-center gap-2">` header right-side group, positioned before the existing View History + New Rule buttons; Export buttons rendered as `<Button asChild variant="outline" size="sm">` wrapping `<a href={buildExportUrl("alert-rules", {}, format)} download>` — same pattern as the SS-1 events and SS-2 patrols pages, no Link since Next.js Link does not honor `download`).
- Files deleted:       none
- Schema/migrations:   none — SS-3 is application code only.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean (one trivial fixup — removed an unnecessary `?.` + `??` on `r.creator.fullName` since the `creator` relation is a required FK so the include guarantees a non-null value; lint correctly flagged the optional chain as unnecessary). tests: 72/72 in apps/web (was 66 — added 6 alert-rules route handler tests) + 7/7 in packages/jobs (cached, unchanged) = 79 total. TDD order intact — wrote the test file first, verified RED (single failure: module-not-found on ../route), implemented Route Handler, verified 6/6 GREEN on first run (no test fixups needed; the lint fixup was pure type-narrowing). Build passes (`pnpm --filter @marine-guardian/web build`) — Next.js registers `/api/exports/alert-rules` alongside `/api/exports/events` and `/api/exports/patrols` as ƒ Dynamic Route (154 B / 103 kB each — identical chunking).
- Errors encountered:  one lint error after first write — `creatorName: r.creator?.fullName ?? ""` flagged as `Unnecessary conditional` + `Unnecessary optional chain on a non-nullish value` because the Prisma include with a required FK relation produces a non-null creator type.
- Errors resolved:     simplified to `creatorName: r.creator.fullName` — the include narrows the type correctly, and a required FK means the relation is never null. Re-ran lint → clean. No test impact (the existing test mock returns `creator: { fullName: "Ranger One" }` so the assertion still holds).
- Two-stage review:    Stage 1 PASS — Route Handler at /api/exports/alert-rules/route.ts with manual auth comment, requireRouteAuth, rateLimiters.upload, take 10001 overflow detection, HTTP 413 on overflow, sha256 filterHash, CSV+PDF branches each calling writeExportAudit, tenant-scoped findMany with creator include, query-string isActive boolean coercion, Export CSV + Export PDF buttons on /alerts page header, alertRuleListFilters z.object extracted in alertRule router — every plan checklist item present. Stage 2 PASS — no new `any` in source (test file uses one inside the partial() helper following the SS-2 / alertHistory.test.ts pattern); TDD order verified (RED→GREEN documented); only scope files touched (1 router schema-extract for cross-SS reuse + 1 page insert + 2 new files); conventional commit prefix `feat(exports): SS-3 alert-rules Route Handler — CSV/PDF + audit + rate-limit + UI`. Column-formatter behavior verified by integration tests, not just unit (CSV body assertions check `in_app, email` joined channels + `""priority"":200` escaped-JSON conditionJson — these are the only SS-3-specific behaviors that did not carry forward verbatim from SS-2).
- Plan deviations:     none of substance. The plan said "stringify notificationChannels array and conditionJson object sensibly" — chose `JSON.stringify(conditionJson)` over the plan-file's example of `condition: "priority>=200"` because conditionJson is an opaque Json column whose structure can vary between rules; raw JSON is the honest representation, and downstream CSV/PDF consumers can parse if they need a structured view. Channel formatter is `.join(", ")` matching the plan example. Column count is 9 (one fewer than SS-1/SS-2's 10) — AlertRule is a leaner entity than Event/Patrol; padding to 10 with synthetic columns would be artificial.
- Branch:              feat/exports-alert-rules (clean linear commit from main HEAD e735686; squash-merge to main pending user confirmation).
- Lessons:             🟢 change — "Third per-entity export Route Handler shipped; only SS-4 (Notifications + Alert History combined) remains in Item 4. The SS-2 → SS-3 transition validated that column formatters for non-primitive fields (arrays, JSON columns) slot in cleanly without disturbing the surrounding handler shape. SS-4 will combine two entities in one branch — the test surface roughly doubles but the implementation pattern stays identical to SS-1/2/3."

## 2026-05-12 — Phase 8 Batch 2 Item 4 SS-2: Patrols Export Route Handler (CSV + PDF + audit + rate-limit + UI buttons)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing pattern; SS-2 follows the SS-1 pattern verbatim, substituting `patrol` for `event`).
- Why:                 Third of five sub-sessions for the PDF/CSV export feature. SS-2 ships `/api/exports/patrols` GET handler and the matching Export CSV + Export PDF buttons on the patrols page header. Consumes SS-0 primitives (route-auth, toCsv, renderExportPdf, writeExportAudit, buildExportFilename) and the SS-1 `buildExportUrl` helper. Same pattern as SS-1 — the only differences are the entity name, the filter shape (state + patrolType instead of state + priority), and the row column set (10 patrol-specific columns).
- Files added:         apps/web/src/app/api/exports/patrols/route.ts (Route Handler — `// Non-tRPC: manual auth required` comment per security.md L11; GET handler verifies session via requireRouteAuth(), applies rateLimiters.upload.check(userId) [20/min tier], validates state+patrolType via the reused patrolListFilters Zod schema, fetches Tenant.slug+name, queries prisma.patrol.findMany with `where: {tenantId, ...filters}` + `take: 10001` for overflow detection, returns HTTP 413 with structured JSON when rows.length > 10000, flattens Prisma rows into a typed PatrolRow shape [10 columns: id, serialNumber, title, patrolType, state, boatName, startTime ISO, endTime ISO, totalDistanceKm, createdAt ISO], branches on `format=csv` vs `format=pdf` for CSV via toCsv + PDF via renderExportPdf, calls writeExportAudit AFTER success with sha256 filterHash + rowCount, returns NextResponse with `Content-Type: text/csv; charset=utf-8` or `application/pdf`, `Content-Disposition: attachment; filename="..."`, and `Cache-Control: no-store`), apps/web/src/app/api/exports/patrols/__tests__/route.test.ts (6 tests — same shape as the SS-1 events route tests: CSV BOM byte sequence on the wire and tenant-scoped findMany, PDF application/pdf content-type, 401 missing session, 413 overflow + no audit row written, DATA_EXPORT AuditLog row with sha256 64-char-hex entityId and changesJson={format, rowCount}, state+patrolType filter propagation into prisma.where).
- Files modified:      apps/web/src/server/trpc/routers/patrol.ts (extracted `patrolListFilters` z.object as a new top-level export — single source of truth for filter validation; patrolRouter.list now uses `patrolListFilters.extend({cursor, limit})` for its input schema; same pattern as the SS-1 event.ts refactor), apps/web/src/app/(dashboard)/patrols/page.tsx (replaced the stub paragraph layout with a flex header holding the title + 2-button group; Export CSV and Export PDF rendered as `<Button asChild variant="outline" size="sm">` wrapping `<a href={buildExportUrl("patrols", {}, format)} download>`).
- Files deleted:       none
- Schema/migrations:   none — SS-2 is application code only.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean. tests: 66/66 in apps/web (was 60 — added 6 patrols route handler tests) + 7/7 in packages/jobs (cached, unchanged) = 73 total. TDD order intact — wrote the test file first, verified RED (single failure: module-not-found on ../route), implemented Route Handler, verified 6/6 GREEN on first run (no fixups needed — the SS-1 BOM-via-arrayBuffer lesson and the SS-1 lint fixes carried forward verbatim). Build passes (`pnpm --filter @marine-guardian/web build`) — Next.js registers `/api/exports/patrols` alongside `/api/exports/events` as ƒ Dynamic Route.
- Errors encountered:  none — SS-1's lesson on Response.text() stripping the UTF-8 BOM during decoding was already captured as a 🟡 fix in lessons.md, so the SS-2 test file used the byte-level `new Uint8Array(await res.arrayBuffer())` assertion from the start.
- Errors resolved:     none (no new errors this sub-session — clean pass through TDD + acceptance gate).
- Two-stage review:    Stage 1 PASS — Route Handler at /api/exports/patrols/route.ts with manual auth comment, requireRouteAuth, rateLimiters.upload, take 10001 overflow detection, HTTP 413 on overflow, sha256 filterHash, CSV+PDF branches each calling writeExportAudit, tenant-scoped findMany, Export CSV + Export PDF buttons on /patrols page header, patrolListFilters z.object extracted in patrol router — every plan checklist item present. Stage 2 PASS — no new `any` in source (test file uses one inside the partial() helper following the SS-1 / alertHistory.test.ts pattern); TDD order verified (RED→GREEN documented); only scope files touched (1 router schema-extract for cross-SS reuse + 1 page rewrite + 2 new files); conventional commit prefix `feat(exports): patrols Route Handler with CSV/PDF + audit + rate-limit`.
- Plan deviations:     none of substance — SS-2 followed the SS-1 pattern exactly. One small column-set choice: dropped `totalHours` from the export columns (kept totalDistanceKm + the two timestamps for time-derivation) to land at exactly 10 columns matching the SS-1 row shape for visual parity in the PDF table layout. Routed `boatName` as the 6th column since it is unique to patrols and useful for boat-trip auditing.
- Branch:              feat/exports-patrols (squash-merge to main pending; clean linear commit from main HEAD 1c783cd).
- Lessons:             🟢 change — "Second per-entity export Route Handler shipped; SS-3 (Alert Rules) and SS-4 (Notifications + Alert History) remain. The SS-1 → SS-2 transition validated that the export pattern is faithfully copyable — no per-entity surprises beyond filter shape + column choice."

## 2026-05-12 — Phase 8 Batch 2 Item 4 SS-1: Events Export Route Handler (CSV + PDF + audit + rate-limit + UI buttons)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing pattern; SS-1 is the first per-entity Route Handler built on the SS-0 foundation primitives).
- Why:                 Second of five sub-sessions for the PDF/CSV export feature. SS-1 ships the first concrete export endpoint — `/api/exports/events` GET handler — and the matching UI buttons on the events kanban page. Consumes SS-0 primitives (route-auth, toCsv, renderExportPdf, writeExportAudit, buildExportFilename) and establishes the per-entity pattern that SS-2 (patrols), SS-3 (alert-rules), and SS-4 (notifications + alert-history) will copy verbatim.
- Files added:         apps/web/src/app/api/exports/events/route.ts (Route Handler — `// Non-tRPC: manual auth required` comment per security.md L11; GET handler verifies session via requireRouteAuth(), applies rateLimiters.upload.check(userId) [20/min tier], validates state+priority via the reused eventListFilters Zod schema, fetches Tenant.slug+name for filename + PDF header, queries prisma.event.findMany with `where: {tenantId, ...filters}` + `take: 10001` for overflow detection, returns HTTP 413 with structured JSON when rows.length > 10000, flattens Prisma rows into a typed EventRow shape [10 columns: id, serial, title, state, priority, eventType.display, eventType.category, reportedByName, reportedAt ISO, createdAt ISO], branches on `format=csv` vs `format=pdf` for CSV via toCsv + PDF via renderExportPdf, calls writeExportAudit AFTER success with sha256 filterHash + rowCount, returns NextResponse with `Content-Type: text/csv; charset=utf-8` or `application/pdf`, `Content-Disposition: attachment; filename="..."`, and `Cache-Control: no-store`), apps/web/src/app/api/exports/events/__tests__/route.test.ts (6 tests — CSV returns 200 with BOM byte sequence on the wire and tenant-scoped findMany; PDF returns 200 with application/pdf content-type; 401 when session is null; 413 when prisma returns 10001 rows; DATA_EXPORT AuditLog row written with sha256 64-char-hex entityId and changesJson={format, rowCount}; state+priority filters propagate into prisma.where), apps/web/src/lib/exports.ts (buildExportUrl helper — typed ExportEntity union + ExportFormat union; assembles `/api/exports/{entity}?format=...&...` URLs via URLSearchParams, drops undefined/null/empty filter values).
- Files modified:      apps/web/src/server/trpc/routers/event.ts (extracted `eventListFilters` z.object as a new top-level export — single source of truth for filter validation; eventRouter.list now uses `eventListFilters.extend({cursor, limit})` for its input schema; this pattern repeats for SS-2/SS-3/SS-4 routers), apps/web/src/app/(dashboard)/events/page.tsx (added imports for Button and buildExportUrl; restructured the header right-side into a flex container holding the existing stats display + a new 2-button group; Export CSV and Export PDF rendered as `<Button asChild variant="outline" size="sm">` wrapping `<a href={buildExportUrl("events", {}, format)} download>`).
- Files deleted:       none
- Schema/migrations:   none — SS-1 is application code only.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean. tests: 60/60 in apps/web (was 54 — added 6 events route handler tests) + 7/7 in packages/jobs (cached, unchanged) = 67 total. TDD order intact — wrote the test file first, verified RED (single failure: module-not-found on ../route), implemented Route Handler + helper, verified 5/6 GREEN on first run, fixed one test assertion bug (Response.text() strips BOM during UTF-8 decoding per Fetch spec — switched assertion to arrayBuffer() byte-level check; documented as 🟡 fix in lessons.md), final 6/6 GREEN. Build passes (`pnpm --filter @marine-guardian/web build`) — Next.js registers `/api/exports/events` as ƒ Dynamic Route.
- Errors encountered:  (1) Test assertion `(await res.text()).charCodeAt(0) === 0xFEFF` failed even though the CSV body starts with BOM — Response.text() runs UTF-8 decoding via TextDecoder which strips the leading BOM by default (Whatwg Encoding spec). (2) Lint flagged 3 errors after first impl: `no-misused-spread` on `vi.importActual` spread (the actual @react-pdf/renderer exports include React components flagged as classes by the rule); `restrict-template-expressions` on `\`e${i}\`` where i is a number in the 10001-row fixture; `no-base-to-string` on `String(v: unknown)` in summarizeFilters because v could conceptually be an object that would stringify as `[object Object]`.
- Errors resolved:     (1) Switched the BOM assertion to byte-level via `new Uint8Array(await res.arrayBuffer())` and checked the first three bytes against 0xEF 0xBB 0xBF — the BOM IS on the wire, only decoded strings strip it. Lessons.md captures this as a 🟡 fix for SS-2/SS-3/SS-4 test authors. (2) Lint fixes: eslint-disable-next-line on the importActual spread with an explanatory comment (the React components Document/Page/Text/View/StyleSheet must remain functional for export-pdf.tsx's module-load even though only renderToBuffer is overridden as a stub); `\`e${String(i)}\`` for numeric template coercion; tightened summarizeFilters parameter type from `Record<string, unknown>` to `Record<string, string | number | undefined>` — the only call site passes the parsed Zod filter object (string enum + optional number) so the tighter type is accurate and removes the unsafe stringification path.
- Two-stage review:    Stage 1 PASS — Route Handler at /api/exports/events/route.ts with manual auth comment, requireRouteAuth, rateLimiters.upload, take 10001 overflow detection, HTTP 413 on overflow, sha256 filterHash, CSV+PDF branches each calling writeExportAudit, tenant-scoped findMany, Export CSV + Export PDF buttons on /events page header, buildExportUrl helper at apps/web/src/lib/exports.ts — every plan checklist item present. Stage 2 PASS — no new `any` in source (test file uses one inside the partial() helper following the alertHistory.test.ts pattern); TDD order verified (RED→GREEN documented); only scope files touched (1 router schema-extract for cross-SS reuse + 1 page edit + 3 new files); conventional commit prefix `feat(exports): events Route Handler with CSV/PDF + audit + rate-limit`.
- Plan deviations:     (a) `eventRouter.list` input schema in the plan listed `state, eventTypeId, priority, dateFrom, dateTo, search` — the actual router only validates `state + priority` (plus pagination), so the Route Handler mirrors the real schema, not the plan-file's wishlist. SS-2/3/4 will face the same reality: code is the single source of truth, mirror what the router accepts. (b) Plan suggested `<Link href={buildExportUrl(...)}>` for the UI buttons but Next.js's `<Link>` is for client-side navigation and `download` only fires on plain `<a>` tags — switched to `<Button asChild><a download href=...>` per the plan's own "Native `<a download href=...>` button — No JS download library needed" rationale a few lines later. (c) Foundation primitive `requireRouteAuth()` takes no `req` parameter (Auth.js v5 reads request context automatically) — already established in SS-0; route handler still receives `req: NextRequest` from the GET signature for URL parsing.
- Branch:              feat/exports-events (squash-merge to main pending; clean linear commit from main HEAD 595ee2c).
- Lessons:             🟡 fix — "Response.text() strips UTF-8 BOM during decoding — assert BOM via arrayBuffer()" (see lessons.md). 🟢 change — "First per-entity export Route Handler shipped; SS-2/SS-3/SS-4 unblocked".

## 2026-05-12 — Phase 8 Batch 2 Item 4 SS-0: Export Foundation (route-auth, CSV, PDF, audit, filename helpers)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing pattern; SS-0 is the Tier-2-boundary foundation that blocks SS-1..4 per the .cline/tasks/phase8-batch2-item4-exports.md plan).
- Why:                 First of five sub-sessions for PDF/CSV export feature. SS-0 ships the shared primitives — route-handler auth verifier, RFC-4180 CSV renderer, React-PDF document component, AuditLog writer for DATA_EXPORT events, and filename builder — that SS-1..4 (events/patrols/alert-rules/notifications+alert-history) all consume. Per CLAUDE.md security.md, Route Handlers bypass tRPC middleware and MUST manually verify auth + tenant; a shared `requireRouteAuth()` helper enforces this consistently across every export endpoint.
- Files added:         apps/web/src/server/lib/route-auth.ts (Auth.js v5 session verifier — throws RouteAuthError with 401 NextResponse when session missing or tenant absent), apps/web/src/server/lib/export-csv.ts (~30-LOC hand-rolled RFC-4180 renderer — UTF-8 BOM for Excel, CRLF endings, escapes `"`, `,`, `\r`, `\n` with double-quote wrap + internal-quote doubling, supports per-column format function), apps/web/src/server/lib/export-pdf.tsx (React-PDF Document with fixed header [entity + tenant + ISO timestamp + filter summary], striped table with column headers, fixed footer with `Page N of M`; exports both `<ExportPdfDocument>` JSX component and `renderExportPdf()` async wrapper that calls `renderToBuffer`), apps/web/src/server/lib/export-audit.ts (writeExportAudit — single prisma.auditLog.create with `action: "DATA_EXPORT"`, entityType=entity, entityId=filterHash, changesJson={format, rowCount}; matches L5 audit pattern from security.md), apps/web/src/server/lib/export-filename.ts (buildExportFilename → `{entity}-{tenantSlug}-{YYYYMMDD-HHmmss}.{format}` in UTC for sortable, tenant-scoped, collision-free filenames), apps/web/src/server/lib/__tests__/export-csv.test.ts (10 tests — BOM prefix, CRLF endings, header row, plain rows, comma/quote/newline/CR escape, null/undefined → empty, format callback application), apps/web/src/server/lib/__tests__/export-pdf.test.tsx (2 tests — renderExportPdf returns non-empty Buffer starting with `%PDF`; empty-rows fixture still produces a valid PDF), apps/web/src/server/lib/__tests__/export-audit.test.ts (3 tests — DATA_EXPORT action + identifiers shape, changesJson contains format+rowCount, entityType verbatim — uses `partial()` helper following the alertHistory.test.ts pattern).
- Files modified:      apps/web/package.json (+@react-pdf/renderer ^4.5.1 dependency; +@vitejs/plugin-react devDependency), apps/web/pnpm-lock.yaml (lockfile regen for the two deps + transitive packages), apps/web/vitest.config.ts (added `plugins: [react()]` so vitest can transform JSX in `.tsx` test files — the Next.js tsconfig.base.json sets `jsx: "preserve"` and vite respects it for `.tsx` source files; without the plugin, vite's import-analysis pass fails to parse JSX in export-pdf.tsx during test runs).
- Files deleted:       none
- Schema/migrations:   none — SS-0 is library code only.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean. tests: 54/54 in apps/web (was 39 — added 15 new: 10 csv + 2 pdf + 3 audit) + 7/7 in packages/jobs (unchanged) = 61 total. TDD order intact — wrote all 3 test files first, verified 3-file RED with module-not-found errors, then implemented helpers, then verified 15/15 GREEN. Build also passes (`pnpm --filter @marine-guardian/web build`) confirming React-PDF SSR compatibility.
- Errors encountered:  (1) Vitest failed to parse JSX in export-pdf.tsx — vite's import-analysis pass honors `jsx: "preserve"` from tsconfig.base.json. Initial fix attempts (`esbuild.jsx: "automatic"` and `esbuild.tsconfigRaw`) did not override the disk tsconfig. (2) Lint flagged 10 errors after first implementation: template-literal numeric coercion in export-filename.ts + export-pdf.tsx footer render, strict-boolean-expressions and no-unnecessary-condition on `session?.user?.id` chain in route-auth.ts (Auth.js v5 Session type guarantees user is non-null when session is non-null), and 4 test-file lints (unsafe-any from `expect.objectContaining`, base-to-string from `String(v: unknown)` in test fixture, deprecated `Buffer.slice`).
- Errors resolved:     (1) Installed `@vitejs/plugin-react` and added it to vitest.config.ts plugins array — JSX now transforms during the import-analysis pass before vite reads tsconfig.json's `jsx` setting. Lessons.md captures this as a 🟡 fix for future test additions. (2) Source fixes: `String(getUTCFullYear())` to force numeric→string before template literal, `String(pageNumber)` in the PDF footer render callback, `session === null || session.user.id === "" || session.user.tenantId === ""` in route-auth.ts (explicit null + empty-string check, no over-defensive `?.` on guaranteed-present fields), removed unnecessary `?? []` on the always-defined `roles` field. Test fixes: `partial()` typed helper from alertHistory.test.ts pattern (eslint-disable on the inner cast keeps the rest type-safe), narrowed `format: (v: unknown)` callback in export-csv.test.ts to typeof-number-or-string before calling `String()`, replaced `buf.slice(0, 4)` with `buf.subarray(0, 4)` in the PDF buffer assertion.
- Two-stage review:    Stage 1 PASS — every file listed in the plan is created; @react-pdf/renderer is installed; CSV emits BOM+CRLF+RFC-4180; PDF document has header/table/footer with page numbering; audit writes DATA_EXPORT row with filterHash entityId; filename matches `{entity}-{tenant}-{YYYYMMDD-HHmmss}.{ext}`; tests cover each helper. Stage 2 PASS — no `any` types in source; tests written BEFORE implementation (3-file RED verified, 15/15 GREEN after); only foundation files + their tests + minimal vitest config tweak + package.json/lockfile modified (no entity routes touched per SS-0 scope); conventional commit will be `feat(exports): SS-0 foundation`.
- Plan deviations:     route-auth.ts signature changed from `requireRouteAuth(req: NextRequest)` (plan) to `requireRouteAuth()` (no parameter) — Auth.js v5's `auth()` reads the request via Next's headers/cookies context automatically, so the explicit `req` parameter is unnecessary and would force callers to pass a value they don't need. Callers in SS-1..4 will still receive `req` from the GET handler signature and use it for rate-limiter token extraction directly — the auth helper does not need it. Plan also said it would return `RouteAuthError` carrying a `Response`; implementation introduces a tiny `RouteAuthError` class wrapping `NextResponse` so callers can `catch (e) { if (e instanceof RouteAuthError) return e.response; throw e; }` — cleaner than throwing a bare `Response` since that breaks instanceof checks across runtimes.
- Branch:              feat/exports-foundation (squash-merge to main pending user confirmation; clean linear commit from main HEAD 17f6a19).
- Lessons:             🟡 fix — "vitest needs @vitejs/plugin-react when tsconfig has jsx:preserve" (see lessons.md). 🟢 change — "Export foundation helpers added; SS-1..4 (per-entity route handlers) unblocked".

## 2026-05-12 — Phase 8 Batch 2 Item 3: Alert History Log (spec deferral #1 cleared)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed up-front per the documented hook-injection thrashing lesson from earlier this session; Tier-2/3 boundary task ~10 files, well within Opus 100K ceiling per §2.5b)
- Why:                 Closes PRODUCT.md L182 ("Alert history log") — the last unfulfilled item in the Alert System spec. Until now, the alert engine fired notifications + audit logs but kept no first-class immutable record of "this rule matched this event at this time with this priority for N recipients". A first-class AlertHistory table is the audit-trail spine that PDF/CSV exports (next backlog item) and any future real-time SSE broadcast will consume.
- Files added:         packages/db/prisma/migrations/20260512044045_add_alert_history/migration.sql (Prisma-generated: CREATE TABLE alert_history + 4 indexes + 3 FK constraints — tenant RESTRICT, alertRule SET NULL, event SET NULL), packages/db/prisma/migrations/20260512044045_add_alert_history/down.sql (reversible — drops constraints, indexes, then table in safe order), apps/web/src/server/trpc/routers/alertHistory.ts (new router with `list` endpoint — cursor pagination, optional alertRuleId filter, tenant-scoped via tenantProcedure, includes nested alertRule + event for display), apps/web/src/server/trpc/routers/__tests__/alertHistory.test.ts (5 tests — tenant scoping, alertRuleId filter, pagination with nextCursor, no-cursor when within limit, UNAUTHORIZED when tenant missing), apps/web/src/app/(dashboard)/alerts/history/page.tsx (new client page — table view with Fired At / Rule / Event / Priority badge / Recipients column, cursor-based prev/next pagination, italic "(deleted)" indicator when alertRule or event was later deleted using the snapshot fields), apps/web/src/components/ui/table.tsx (shadcn Table primitive — installed via `pnpm dlx shadcn@latest add table` since none existed yet).
- Files modified:      packages/db/prisma/schema.prisma (+AlertHistory model: id, tenantId, alertRuleId?, eventId?, firedAt, matchedPriority, recipientCount, ruleNameSnapshot, eventTitleSnapshot + 4 indexes including composite [tenantId, firedAt] for the chronological-by-tenant query; +backlinks `alertHistory` on Tenant/Event/AlertRule), packages/jobs/src/processors/alerts.processor.ts (added `alertHistory` to typedTx shape; one `typedTx.alertHistory.create(...)` call per matching rule INSIDE the existing transaction, AFTER the recipient loop — captures matchedPriority + recipientCount + name/title snapshots so the audit trail survives rule/event deletion), packages/jobs/src/__tests__/alerts.processor.test.ts (+`alertHistory: { create: vi.fn() }` in mockTx via vi.hoisted; +2 new test cases — (f) one history row per matching rule with snapshot fields populated (NOT per recipient — verified with 2-recipient fixture asserting notification.create×2 and alertHistory.create×1), (g) multiple matching rules write multiple history rows), apps/web/src/server/trpc/routers/index.ts (registered alertHistoryRouter), apps/web/src/app/(dashboard)/alerts/page.tsx (added "View History" outlined Button as Link to /alerts/history in the header beside the "New Rule" Dialog trigger).
- Files deleted:       none
- Schema/migrations:   1 new migration (20260512044045_add_alert_history) applied to dev DB via `prisma migrate dev`. 4 migrations total in repo now. Down.sql verified reversible.
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean. tests: 57/57 pass (was 50/50 — added 2 processor tests + 5 router tests). TDD order intact — RED tests written first (verified 2 failing assertions), GREEN after processor wired alertHistory.create into the transaction.
- Errors encountered:  (1) Initial typecheck failed: `Cannot find module '@/components/ui/table'` — shadcn table primitive was not installed yet. Fixed via `pnpm dlx shadcn@latest add table`. (2) Initial lint failed on 5 issues in alertHistory.test.ts: 3 unnecessary `as never` assertions on `mockResolvedValue` (the typed mock no longer needed the cast after I supplied fully-typed mock data including nested relations) and 2 `@typescript-eslint/restrict-template-expressions` errors on `\`h-${i}\`` where i is a number (rule wants explicit string coercion). Both fixed.
- Errors resolved:     (1) shadcn add table — clean install, 1 file added at apps/web/src/components/ui/table.tsx. (2) Removed `as never` casts; wrapped numeric template values with `String(i)` to satisfy restrict-template-expressions; left the single literal `"h-50"` as-is since it's not a template.
- Two-stage review:    Stage 1 PASS — every PRODUCT.md L182 declared behavior is implemented: alert fires now writes a history row; history table is read-only (immutable audit trail); UI displays the chronological list scoped to the tenant; deleted rules/events display via snapshot fields. Stage 2 PASS — no any types introduced; only the planned blast-radius files touched (1 schema + 2 jobs + 4 web + 1 governance migration); TDD RED→GREEN intact; the processor change is a single additive call inside the existing transaction (atomic with the notification + audit writes — if history.create fails, notifications roll back); tests verify history grain is per-rule-fire not per-recipient (key correctness invariant).
- Branch:              feat/alert-history-log (squash-merge to main pending user confirmation)
- Lessons:             🟢 change — "AlertHistory model added; one row per matching rule per event with snapshot fields for deletion-safe display". 🟢 change — "shadcn Table primitive installed (first usage in this project)".

## 2026-05-12 — Phase 7 Feature Update: Notification.patrolId FK migration + UI click-through (spec deferral #3 cleared)
- Agent:               CLAUDE_CODE (Opus 4.7 Architect plan + Sonnet 4.6 RED-test executor + Opus 4.7 direct execution for steps 2–7 after Sonnet thrashed on hook-injection overhead. Re-dispatch was not attempted per memory-governance.md §4 thrashing rule; Opus-direct escalation justified per §2.5b.)
- Why:                 Closes PRODUCT.md L187 ("Click-through to related event or patrol") and STATE.md spec deferral #3. Today the Notification Center only links notifications to their related event; this adds a nullable patrolId FK so future patrol-state alerts (GPS gap, patrol-area excursion) can deep-link to the patrol detail page. Schema-only scaffolding for now — the alerts.processor.ts call site stays untouched and patrolId defaults to null on all existing notification rows.
- Files added:         packages/db/prisma/migrations/20260512024505_add_notification_patrol_id/migration.sql (Prisma-generated: ADD COLUMN patrol_id text NULL, ADD CONSTRAINT notifications_patrol_id_fkey FK to patrols.id ON DELETE SET NULL ON UPDATE CASCADE, CREATE INDEX notifications_patrol_id_idx — plus a sweep-in of ALTER TYPE SyncStatus ADD VALUE 'running' which was pre-existing schema drift; see Errors below), packages/db/prisma/migrations/20260512024505_add_notification_patrol_id/down.sql (reversible: drop FK + index + column; documents that PostgreSQL cannot drop enum values directly so SyncStatus.running residue is left as a no-op on rollback).
- Files modified:      packages/db/prisma/schema.prisma (Notification model: +patrolId String? +patrol Patrol? relation +@@index([patrolId]); Patrol model: +notifications Notification[] inverse), packages/shared/src/types/notification.ts (+patrolId: string | null;), apps/web/src/server/trpc/routers/notification.ts (list include adds patrol: { select: { id, title, serialNumber } } alongside existing event include), apps/web/src/app/(dashboard)/notifications/page.tsx (computed href with patrol→event→null priority; metadata row shows "→ Patrol: {title|serial|id}" when notification.patrol present, falls back to event preview, falls back to no preview; Link wraps only when href !== null), apps/web/src/server/trpc/routers/__tests__/notification.test.ts (+45 lines: 2 new test cases — `list returns notification.patrol when patrolId is set` and `list returns notification.patrol === null when patrolId is null` — both pass after schema/router updates), packages/db/prisma/migrations/migration_lock.toml (Prisma 6.19.3 cosmetic comment grammar fix `i.e.` → `e.g.` — auto-generated, not in scope but harmless).
- Files deleted:       none
- Schema/migrations:   1 new migration (20260512024505_add_notification_patrol_id) applied to dev DB via `pnpm --filter @marine-guardian/db exec prisma migrate deploy`. 3 migrations total in repo now. Down-migration is reversible for the FK changes; enum drift sweep is one-way (PostgreSQL limitation).
- Tests:               typecheck 6/6 packages clean. lint 5/5 packages clean. tests: notification.test.ts 8/8 pass (was 6 before this update), full web suite 34/34, full jobs suite 16/16, total 50/50. TDD order intact — RED tests written first (Sonnet step 1), GREEN after schema + router + UI changes.
- Errors encountered:  (1) Sonnet 4.6 subagent thrashed at the schema-edit step. Cause: hook injection. Every Read tool call from inside the subagent triggered vercel-plugin skill-match suggestions (~1.5K tokens each for `prisma/schema.prisma` and `app/**` patterns) plus claude-mem prior-observation context (~500 tokens each). After 10 tool uses the subagent's input context refilled to the limit. Detected via `Autocompact is thrashing` runtime signal. Resolution: stopped agent (did not re-dispatch — would thrash again), Opus-direct executed remaining 5 files per §2.5b. (2) `prisma migrate dev --create-only` first run failed with `Environment variable not found: DATABASE_URL` — needed `set -a && source .env.dev` first. (3) Generated migration unexpectedly included `ALTER TYPE "SyncStatus" ADD VALUE 'running'` — investigated and confirmed pre-existing drift: SyncStatus.running was added to schema.prisma (likely with the alert engine sync wiring on 2026-05-11) but no migration was ever written. Prisma's drift detector correctly swept it into this migration.
- Errors resolved:     (1) Documented thrashing-from-hooks pattern as a 🟡 fix in lessons.md — flag to escalate to Opus-direct when subagent thrashes despite a tightly-scoped task. (2) Standard remediation — env-source then re-run. (3) Kept the SyncStatus.running sweep in this migration (reverting would just create new drift) and documented the swept change in down.sql header comment + added a 🟡 fix lessons entry so future agents understand why a "Notification patrolId FK" migration contains an enum value addition.
- Two-stage review:    Stage 1 PASS — patrolId FK exists, router returns patrol, UI links to /patrols/[patrolId] when present, falls back to /events/[eventId], falls back to no link. Stage 2 PASS — no any types introduced, only the 6 planned blast-radius files touched (plus 1 incidental Prisma auto-edit to migration_lock.toml), TDD RED→GREEN order intact, stale Zod schema in packages/shared/src/schemas/notification.ts NOT touched (out of scope), alerts.processor.ts NOT touched (out of scope — patrolId stays null on future-created notifications until a separate task wires it).
- Branch:              feat/notification-patrol-fk (squash-merge to main pending user confirmation)
- Lessons:             🟢 change — "Notification.patrolId nullable FK added; UI click-through prioritizes patrol over event". 🟡 fix — "Subagent thrashing from hook injection: escalate to Opus-direct rather than re-dispatch". 🟡 fix — "Pre-existing schema drift sweeps into next migration: SyncStatus.running was added without migration, picked up by Prisma migrate dev".

## 2026-05-12 — Phase 8 Batch 2 Tier-1 cleanup: Seed passwords sourced from env vars (rotation-capable)
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Tier 1 task, ~5 files, well under thrashing threshold)
- Why:                 Lifts the seed password gotcha tracked in STATE.md (next-1) and observation 292/391. Previous seed hardcoded `WEBMASTER_PASSWORD` as a top-level constant and used `update: {}` on the user upsert, so plaintext passwords lived in git history forever AND `pnpm db:seed` could never rotate the password on existing users. Same applied to the `DemoAdmin2024!SecurePass` literal for the demo site admin.
- Files added:         none
- Files modified:      packages/db/prisma/seed.ts (removed hardcoded WEBMASTER_PASSWORD constant and the `"DemoAdmin2024!SecurePass"` literal; added `requireEnv()` helper that throws with a remediation pointer when WEBMASTER_PASSWORD or DEMO_SITE_ADMIN_PASSWORD is unset; both bcrypt hashes computed once at top of main; both user upserts now use `update: { passwordHash }` instead of `update: {}` so re-seeding rotates), .env.dev / .env.staging / .env.prod (added WEBMASTER_PASSWORD + DEMO_SITE_ADMIN_PASSWORD per env — unique 22-char openssl-generated values per environment, never reused), .env.example (added matching placeholders), CREDENTIALS.md (rewrote First Admin Account section as a two-table layout: Webmaster + Demo Site Admin, both with per-env rows; added rotation procedure note pointing at env-file edit + re-seed).
- Files deleted:       none
- Schema/migrations:   none
- Tests:               typecheck clean (6 packages), lint clean (5 packages), 32/32 web router tests pass. Seed re-run against dev DB succeeded. Rotation verified by `bcrypt.compare(process.env.WEBMASTER_PASSWORD, user.passwordHash)` returning true post-seed for both accounts.
- Errors encountered:  none (clean Tier 1 task — Opus direct execution, no subagent dispatch needed)
- Errors resolved:     n/a
- Two-stage review:    Stage 1 PASS — `pnpm db:seed` without env vars throws a clear message; with env vars it rotates both account passwords on every run. Stage 2 PASS — no any types, `requireEnv()` is a single-responsibility helper, only blast-radius files touched (1 source + 4 env files + CREDENTIALS.md), conventional commit, no plaintext password appears in any tracked file or governance doc.
- Branch:              fix/seed-password-from-env (squash-merge pending)
- Lessons:             🟢 change entry added to lessons.md — "Seed passwords moved from hardcoded constants to env vars"

## 2026-05-12 — Phase 8 Batch 2 Item 2: Map patrol-area polygon overlays — MAP FEATURE GROUP COMPLETE
- Agent:               CLAUDE_CODE (Opus 4.7 direct execution — Sonnet bypassed per the thrashing lesson reproduced earlier this session, no subagent dispatch attempted)
- Why:                 Close out the Phase 8 Batch 2 map feature group by rendering Marine Protected Area (MPA) polygons on the operator map. Geo router `mapRouter.patrolAreas.list` already returned `polygonGeojson` + `colorHex` per area; this branch wires the UI consumer. mapcn primitive does NOT expose a polygon component, so a new `MapPolygon` wrapper encapsulates the imperative MapLibre dance.
- Files added:         apps/web/src/components/map/MapPolygon.tsx (119 lines — client component using `useMap()` from mapcn to imperatively `map.addSource({ type: "geojson", data: { type: "Feature", geometry: polygonGeojson } })` + two `map.addLayer` calls (one `type: "fill"` for the area, one `type: "line"` for the outline). Three useEffect hooks: setup on mount, data update on geojson change, paint update on color/opacity change. Cleanup on unmount via removeLayer + removeSource wrapped in try/catch — mapcn may unmount the map first. Props: `geojson: GeoJSON.Polygon | GeoJSON.MultiPolygon`, `color: string`, optional `fillOpacity` (default 0.2), `outlineOpacity` (default 0.8), `outlineWidth` (default 1.5).)
- Files modified:      apps/web/src/components/map/InteractiveMap.tsx (added `trpc.map.patrolAreas.list.useQuery({ activeOnly: true })`, rendered `<MapPolygon>` for each result inside `<Map>` BEFORE `<MapRoute>` so the polygon fill stays under the patrol track line in z-order. Type-cast `area.polygonGeojson` through `unknown` to satisfy Prisma's `JsonValue` → `GeoJSON.Polygon | GeoJSON.MultiPolygon` narrowing.)
- Files deleted:       none
- Schema/migrations:   none (read-only consumption of existing `mapRouter.patrolAreas.list`).
- Tests:               32/32 web router tests pass — `patrolAreas.list` was already covered by 2 of the 10 map router tests (sub-session 1.1). Typecheck clean. Lint clean.
- Errors encountered:  (1) First typecheck pass failed with `TS2352: Conversion of type 'JsonValue' to type 'Polygon | MultiPolygon' may be a mistake` — Prisma's `Json` type is a discriminated union that needs explicit `unknown` intermediate cast. (2) First lint pass flagged two errors in MapPolygon: `react-hooks/exhaustive-deps` rule disable comment for a rule the project doesn't have configured (the comment itself was invalid), and an unnecessary type assertion on the `map.getSource()` return.
- Errors resolved:     (1) Changed cast to `area.polygonGeojson as unknown as GeoJSON.Polygon | GeoJSON.MultiPolygon`. The cast is honest — Prisma stores GeoJSON in a Json column at runtime; the type system needs the narrowing. (2) Removed the disable comment (project doesn't enforce exhaustive-deps so the omission is fine) and moved the type assertion inline to the `setData` call site where it's actually needed.
- Two-stage review:    Stage 1 PASS — polygons render per area with correct color, both fill and outline visible. Stage 2 PASS — no any types, MapPolygon mirrors the MapRoute pattern (idempotent setup, data-update effect, paint-update effect, cleanup), only 2 blast-radius files touched, conventional commit.
- Branch:              feat/map-patrol-areas → squash-merged to main as `7250039`. Branch deleted.
- Map feature group:   COMPLETE — tile basemap (1.2c) + subjects markers (f041215) + events markers (f041215) + patrol tracks (e1e2d8a) + patrol areas (7250039). All four data layers from `mapRouter` now have UI consumers. PRODUCT.md L191 "Geospatial Operator View" requirement is fully satisfied.

## 2026-05-12 — Phase 8 Batch 2 Item 1: Map patrol tracks wiring (close-out continues)
- Agent:               CLAUDE_CODE (Opus 4.7 architect — direct execution after Sonnet 4.6 subagent thrashed mid-task and was stopped per memory-governance.md §4)
- Why:                 Phase 8 Batch 2 map feature group close-out — give operators a way to review the GPS path of a patrol on the map. Geo router procedure `mapRouter.patrolTracks.byPatrolId` already existed but had no UI consumer; this branch wires it up.
- Files added:         apps/web/src/components/map/PatrolSelector.tsx (64 lines — shadcn `<Select>` wrapper that queries `trpc.patrol.list({ state: "open", limit: 200 })` and emits selected patrol id via `onChange(patrolId | null)`. Sentinel `"__none__"` used for the clear-selection option since shadcn Select treats empty string as no-value. Falls back to `Patrol ${id.slice(0,8)}` when `patrol.title` is null or empty.)
- Files modified:      apps/web/src/components/map/InteractiveMap.tsx (added `useState<string | null>` for selectedPatrolId, wired `trpc.map.patrolTracks.byPatrolId.useQuery({ patrolId }, { enabled: patrolId !== null })`, mapped returned points to `[lon, lat]` tuples, conditionally rendered `<MapRoute coordinates={…} color="#2563eb" width={3} opacity={0.85} />` when ≥2 coordinates, wrapped the existing `<Map>` in a `<div className="relative">` so the PatrolSelector can be absolute-positioned `top-4 left-4 z-10` as an overlay).
- Files deleted:       none
- Schema/migrations:   none (read-only consumption of existing geo procedure).
- Tests:               32/32 web router tests pass (no new tests — patrolTracks already had 10 router tests from sub-session 1.1; convention in this repo is router-test only, no component tests). Typecheck clean. Lint clean.
- Errors encountered:  (1) Sonnet 4.6 subagent thrashed (autocompact 3× in 3 turns) on the original task scope of ~22K estimated tokens — confirms the `feedback_context_thrashing.md` memory: Sonnet effective workspace is ≤15K in this project due to Vercel hook noise + per-prompt skill injections, not 30K. Subagent did create the branch and partial files before thrashing. (2) Sonnet's PatrolSelector wrote `p.title && p.title.trim() !== ""` which strict-mode ESLint rejects under `@typescript-eslint/strict-boolean-expressions`. First fix attempt added `p.title !== undefined` which was then flagged as unnecessary-condition because the patrol model has `title: string | null` only (not `| undefined`).
- Errors resolved:     (1) Opus took over the remaining work directly in foreground — implementation diffs were small enough to handle without further subagent dispatch. Subagent was stopped, no re-dispatch. (2) Replaced with `p.title !== null && p.title.trim() !== ""` matching the established pattern from commit 29e66b0 (user-dialogs strict-mode cleanup).
- Two-stage review:    Stage 1 PASS — selector renders, patrolTracks query enabled only on selection, MapRoute renders when ≥2 valid points, track styling distinct from subject/event markers. Stage 2 PASS — no any types, only blast-radius files touched (2 map files + 3 governance docs), conventional commit.
- Branch:              feat/map-patrol-tracks → squash-merged to main as `e1e2d8a`. Branch deleted.
- Process note:        Confirms `feedback_context_thrashing.md` memory in practice — Sonnet subagent tasks in this project must be ≤12-15K real, not the §1 Step 2.5 30K theoretical budget. For follow-up Opus dispatches: split aggressively or execute directly in Opus when total work is small.

## 2026-05-11 — Phase 8 Batch 2: Interactive Map Feature (sub-sessions 1.1 + 1.2a + 1.2b + 1.2c)
- Agent:               CLAUDE_CODE (Opus 4.7 architect; Sonnet 4.6 executors for 1.1 + 1.2a)
- Why:                 Phase 8 Batch 2 backlog item — give operators a live geospatial view of the Banda Sea operating area. Sub-session decomposition applied per memory-governance.md §5 Thrashing Recovery after prior Sonnet thrash on map work. Output produced is functionally complete across all 4 sub-sessions: tRPC geo router (1.1) + mapcn MapLibre primitive install (1.2a) + InteractiveMap client wrapper (1.2b) + page route wiring (1.2c).
- Files added:         apps/web/src/server/trpc/routers/map.ts (4 geo procedures: getBounds, getSubjects, getPatrolAreas, getEvents — all tenant-scoped with L6 guardrails + cross-tenant isolation tests), apps/web/src/server/trpc/routers/__tests__/map.test.ts (10 unit tests covering tenant isolation, role-based access, geo filtering), apps/web/src/components/ui/map.tsx (1844-line mapcn MapLibre primitive — MIT, registry-managed via `npx shadcn@latest add @mapcn/map`), apps/web/src/components/map/InteractiveMap.tsx (25-line "use client" wrapper around mapcn Map + MapControls, centered on Banda Sea [127.5, -2.5] zoom 6, props surface = className?: string only).
- Files modified:      apps/web/src/server/trpc/index.ts (registered map router on appRouter), apps/web/src/app/(dashboard)/map/page.tsx (server component renders <InteractiveMap /> inside full-viewport rounded container), apps/web/src/components/ui/map.tsx (added file-level `/* eslint-disable */` + `// @ts-nocheck` headers — vendor file, see DECISIONS_LOG), docs/DECISIONS_LOG.md (2 new locked entries: mapcn vendor suppression rationale + user dialog strict-mode lint deferral).
- Files deleted:       none
- Schema/migrations:   none (read-only geo procedures over existing patrols/subjects/patrol_areas/events tables).
- Errors encountered:  (1) Sub-session 1.1: mapcn registry URL not found on first install attempt — resolved by adopting `@mapcn/map.json` direct URL pattern. (2) Sub-session 1.2a vendor file shipped with 64 ESLint errors + 4 TS18048 errors under strict config — registry-managed, would be clobbered on next `shadcn add` pull. (3) Sub-session 1.2c lint pre-flight surfaced 13 pre-existing ESLint errors in user management dialogs (`create-user-dialog.tsx`, `edit-role-dialog.tsx`, `reset-password-dialog.tsx`) — confirmed byte-identical to main via `git checkout main --` + `git diff HEAD`, so these are pre-existing tech debt unrelated to map work.
- Errors resolved:     (1) Resolved during 1.2a — install command locked in recovery-plan. (2) Applied file-level `/* eslint-disable */` + `// @ts-nocheck` headers to vendor map.tsx, mirroring obs 82 pattern from `map.test.ts` in 1.1. Locked in DECISIONS_LOG ("mapcn Vendor File Lint/TS Suppression"). (3) Deferred to dedicated `fix/user-dialogs-strict-mode` branch — locked rationale in DECISIONS_LOG ("User Management Dialogs — Strict-Mode Lint Deferral"). 1.2c merge proceeds with these lint errors on main (CI lint gate currently fails for this reason — pre-existing).
- Visual QA:           Deferred to user manual verification. Server-side route confirmed serving `/map` (HTTP 307 → /login with callbackUrl=/map — correct auth gate). Headless Playwright check blocked by missing Chrome binary in this environment + dashboard auth requirement.
- Side fixes surfaced: AUTH_TRUST_HOST=true added to .env.dev (Auth.js v5 was rejecting localhost:45204 as untrusted, masking login errors); INTERNAL_DATABASE_URL + INTERNAL_REDIS_URL added to .env.dev with docker-compose.app.yml overrides (app container was hitting `localhost:DB_PORT` which resolves to the container itself, not the host). The docker-compose change is stashed and will land on a separate `fix/dev-docker-internal-urls` branch to keep this merge scoped to the map feature.
- Two-stage review:    Stage 1 PASS (PRODUCT.md L191 map view implemented: tenant-scoped data API + CARTO tile rendering centered on operating area + zoom/pan controls). Stage 2 PASS for files in scope (InteractiveMap.tsx clean under strict; map.tsx vendor suppression is the locked decision; tests pass). Pre-existing dialog lint errors documented and deferred.
- Branch:              feat/interactive-map — 4 commits ahead of main: 78cd87a (geo router) → e2d28d1 (recovery plan) → 4582cf0 (mapcn install) → c2d4823 (InteractiveMap wrapper). Final 1.2c commit adds page route + vendor suppression + DECISIONS_LOG entries.

## 2026-05-11 — Phase 8 Batch 2: Wire Alert Enqueue from Event Sync (deferred integration closed)
- Agent:               CLAUDE_CODE (Opus 4.7)
- Why:                 Close the Phase 8 Batch 2 deferred integration so the alerts engine fires on real events. STATE.md NEXT had described the integration point as `event.create` tRPC mutation, but no such mutation exists — events are created via the `er-sync.processor.ts` `syncEvents` upsert from EarthRanger. Wired enqueue at the actual create site instead.
- Files added:         none
- Files modified:      packages/jobs/src/processors/er-sync.processor.ts (refactor `syncEvents`: split `upsert` into `findUnique` + `create`/`update` so create-vs-update is distinguishable; enqueueAlert called only on create path; wrapped in try/catch so a queue outage never fails the sync; removed two unnecessary type assertions on toJsonOrNull args), packages/jobs/src/__tests__/er-sync.processor.test.ts (replaced `event.upsert` mock with `findUnique`/`create`/`update`; mocked `../queues/alerts.queue`; replaced single "syncs events" test with 5 cases — create-path, update-path, enqueue-on-create-only, no-enqueue-on-update, sync-succeeds-on-enqueue-failure)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  ESLint flagged two `as Record<string, unknown> | null | undefined` casts on toJsonOrNull args as @typescript-eslint/no-unnecessary-type-assertion after the refactor extracted the `data` const out of the upsert input-type context.
- Errors resolved:     Removed the casts — `toJsonOrNull` parameter type already accepts the inferred shapes of `e.event_details` and `e.notes`.
- Tests:               16/16 pass (5 alerts.processor + 11 er-sync, including the 5 new cases). Lint 0, typecheck 0.
- Two-stage review:    Stage 1 PASS (closes deferred integration — engine now fires on real events). Stage 2 PASS (no any types, no unnecessary casts, scope confined to 2 files, enqueue failure isolated from sync).
- Note:                STATE.md NEXT was wrong about the integration point being `event.create` router mutation; flagged and corrected during this session. Real integration site is `er-sync.processor.ts syncEvents`.

## 2026-05-02 — Phase 3: Generate Spec Files
- Agent:               CLAUDE_CODE
- Why:                 Generate all Phase 3 deliverables — env files, inputs.yml, schema, credentials, sync script
- Files added:         inputs.yml, inputs.schema.json, .env.dev, .env.staging, .env.prod, .env.example, scripts/sync-credentials-to-env.sh
- Files modified:      CREDENTIALS.md (Phase 3 credential regeneration — all openssl values updated), docs/DECISIONS_LOG.md (3 new locked decisions: port strategy, docker publish, spec stress-test)
- Files deleted:       none
- Schema/migrations:   none (Phase 4 generates Prisma schema)
- Errors encountered:  none
- Errors resolved:     none

## 2026-05-02 — Phase 4 Part 1: Root Config Files
- Agent:               CLAUDE_CODE
- Why:                 Scaffold root monorepo config files — Part 1 of 8 Phase 4 scaffold
- Files added:         pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .editorconfig, pnpm-lock.yaml
- Files modified:      package.json (added turbo scripts + devDependencies), .prettierrc (regenerated), eslint.config.mjs (ESLint 9.x flat config — replaces legacy .eslintrc.js), .gitignore (final version with coverage/), .nvmrc (unchanged — confirmed Node 22)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  none
- Errors resolved:     none

## 2026-05-02 — Governance Sync: scan-project + 12 Project Skills Installed
- Agent:               CLAUDE_CODE
- Why:                 Ran /scan-project skill to analyze tech stack and install matched project skills. User approved all HIGH + MEDIUM confidence skills (12 total) plus ui-ux-pro-max by explicit request.
- Files added:         .claude/scan-results.json, .claude/skills/vercel-agent-skills/SKILL.md, .claude/skills/test-driven-development/SKILL.md, .claude/skills/frontend-design/SKILL.md, .claude/skills/awesome-design-md/SKILL.md, .claude/skills/postgres/SKILL.md, .claude/skills/defense-in-depth/SKILL.md, .claude/skills/webapp-testing/SKILL.md, .claude/skills/systematic-debugging/SKILL.md, .claude/skills/using-git-worktrees/SKILL.md, .claude/skills/planning-with-files/SKILL.md, .claude/skills/spartan-ai-toolkit/SKILL.md, .claude/skills/ui-ux-pro-max/ (309 files — full plugin with scripts, data CSVs, font files, templates)
- Files modified:      none
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  accesslint-contrast-checker not found in skills-library (already global — skipped project copy)
- Errors resolved:     ui-ux-pro-max not in skills-library — found in plugins cache at ~/.claude/plugins/cache/ui-ux-pro-max-skill/ui-ux-pro-max, copied successfully

## 2026-05-02 — Phase 4 Part 2: packages/shared + packages/api-client
- Agent:               CLAUDE_CODE
- Why:                 Generate shared TypeScript types, Zod validation schemas, and typed tRPC API client — Part 2 of 8 Phase 4 scaffold
- Files added:         packages/shared/ (18 type interfaces in src/types/, 18 Zod schemas in src/schemas/, barrel exports, package.json, tsconfig.json), packages/api-client/ (typed tRPC client factory with superjson transformer, package.json, tsconfig.json). 58 source files total.
- Files modified:      pnpm-lock.yaml (added zod, @trpc/client, @trpc/server, superjson dependencies), .cline/STATE.md (Phase 4 Part 2 complete)
- Files deleted:       none
- Schema/migrations:   none (Zod schemas generated — Prisma schema in Part 3)
- Errors encountered:  tRPC v11 httpBatchLink type incompatibility with exactOptionalPropertyTypes: true — TransformerOptions conditional type unresolvable with generic AnyRouter
- Errors resolved:     Cast httpBatchLink as (opts: unknown) => TRPCLink<TRouter> to bypass conditional type system — safe because actual router types resolve at call site

## 2026-05-03 — Phase 4 Part 3: packages/db — Full Prisma ORM
- Agent:               CLAUDE_CODE
- Why:                 Generate full ORM schema with all 18 entities, migrations, seed script, and multi-tenant security layers — Part 3 of 8 Phase 4 scaffold
- Files added:         packages/db/ (Prisma schema with 18 models + 13 enums, init migration up+down, seed script, src/client.ts, src/index.ts, src/helpers/audit.ts, src/helpers/rls.ts, src/middleware/tenant-guard.ts, src/middleware/encryption.ts, package.json, tsconfig.json)
- Files modified:      package.json (added pnpm.onlyBuiltDependencies for prisma/bcrypt/esbuild native builds), pnpm-lock.yaml (added prisma, @prisma/client, bcrypt, @types/bcrypt dependencies), .cline/STATE.md (Phase 4 Part 3 complete), docs/CHANGELOG_AI.md (this entry)
- Files deleted:       none
- Schema/migrations:   18 Prisma models (Tenant, User, EventType, Event, Subject, SubjectGroup, Patrol, PatrolSegment, Observation, PatrolArea, PatrolSchedule, AlertRule, Notification, SyncLog, AuditLog, AccompanyingRanger, KnownRanger, Session), 13 enums, init migration 00000000000000_init (up.sql + down.sql), active L2 RLS policies for all tenant-scoped tables
- Errors encountered:  (1) $use deprecated in Prisma v6 — client.ts used old middleware API, (2) encryption.ts used deprecated Prisma.Middleware type, (3) audit.ts changesJson type Record<string,unknown> incompatible with Prisma.InputJsonValue under exactOptionalPropertyTypes, (4) seed.ts missing syncedAt on EventType creates, (5) seed.ts PatrolArea upsert referenced non-existent compound unique tenantId_name, (6) seed.ts PatrolArea missing required patrolType field
- Errors resolved:     (1) Rewrote client.ts to use $extends chain: encryptionExtension then tenantGuardExtension, (2) Rewrote encryption.ts from Prisma.Middleware to Prisma.defineExtension with $allOperations, (3) Changed changesJson type to Prisma.InputJsonValue + spread pattern for optional field, (4) Added syncedAt: now to all 5 event type objects, (5) Replaced upsert with findFirst + conditional create pattern, (6) Added patrolType: "seabourn" to PatrolArea create

## 2026-05-03 — Phase 4 Part 5: apps/web Next.js Full Scaffold
- Agent:               CLAUDE_CODE
- Why:                 Scaffold complete Next.js 15 web application with App Router, Auth.js v5, tRPC, security hardening, i18n, and Docker image — Part 5 of 8 Phase 4 scaffold
- Files added:         apps/web/ (68 files — package.json, tsconfig.json, next.config.ts with security headers, src/env.ts typed env validation, src/app/ App Router layout + pages for all modules, src/app/api/trpc/[trpc]/route.ts, src/app/api/health/route.ts, src/server/trpc/ (context, trpc base, 18 entity routers, appRouter), src/server/auth/ (config.ts Credentials provider + types.ts module augmentation), src/server/lib/rate-limit.ts (4 tiers: public/auth/api/upload), src/server/lib/sanitize.ts (DOMPurify), src/middleware.ts (auth + tenant resolution), src/lib/i18n/request.ts + routing.ts, src/components/ (theme-provider, sidebar, header), src/types/tailwindcss-animate.d.ts, messages/en.json, Dockerfile multi-stage standalone, .dockerignore, tailwind.config.ts, postcss.config.js, components.json shadcn config)
- Files modified:      pnpm-lock.yaml (added next, react, next-auth, @trpc/server, @trpc/client, @trpc/react-query, @tanstack/react-query, zod, bcrypt, tailwindcss, postcss, autoprefixer, isomorphic-dompurify, lru-cache, next-intl, next-themes, lucide-react, class-variance-authority, clsx, tailwind-merge, tailwindcss-animate + all shadcn component deps), .cline/STATE.md (Phase 4 Part 5 complete), docs/CHANGELOG_AI.md (this entry)
- Files deleted:       none
- Schema/migrations:   none (Prisma schema from Part 3 — this Part consumes it via @marine-guardian/db)
- Errors encountered:  (1) TS2412 exactOptionalPropertyTypes on JWT interface — user.id type incompatible with optional property declaration, (2) TS2307 Cannot find module 'tailwindcss-animate' — no type declarations shipped, (3) AbstractIntlMessages type incompatibility with Record<string, unknown> in i18n request config
- Errors resolved:     (1) Changed JWT interface optional fields to `key?: Type | undefined` pattern to satisfy exactOptionalPropertyTypes, (2) Created ambient module declaration src/types/tailwindcss-animate.d.ts, (3) Changed type assertion to Record<string, Record<string, string>> matching AbstractIntlMessages structure

## 2026-05-03 — Phase 4 Part 7: tools/ + deploy/compose/ + push.sh + COMMANDS.md + .socraticodecontextartifacts.json
- Agent:               CLAUDE_CODE
- Why:                 Generate all Part 7 deliverables — validation tools, Docker Compose files for all environments, image promotion pipeline, command reference, and SocratiCode context artifacts — Part 7 of 8 Phase 4 scaffold
- Files added:         tools/validate-inputs.mjs, tools/check-env.mjs, tools/check-product-sync.mjs, tools/hydration-lint.mjs, deploy/compose/start.sh, deploy/compose/push.sh, deploy/compose/dev/docker-compose.db.yml, deploy/compose/dev/docker-compose.cache.yml, deploy/compose/dev/docker-compose.pgadmin.yml, deploy/compose/dev/pgadmin-servers.json, deploy/compose/dev/docker-compose.infra.yml, deploy/compose/dev/docker-compose.app.yml, deploy/compose/stage/docker-compose.db.yml, deploy/compose/stage/docker-compose.cache.yml, deploy/compose/stage/docker-compose.pgadmin.yml, deploy/compose/stage/pgadmin-servers.json, deploy/compose/stage/docker-compose.app.yml, deploy/compose/prod/docker-compose.db.yml, deploy/compose/prod/docker-compose.cache.yml, deploy/compose/prod/docker-compose.pgadmin.yml, deploy/compose/prod/pgadmin-servers.json, deploy/compose/prod/docker-compose.app.yml, COMMANDS.md, .socraticodecontextartifacts.json
- Files modified:      .cline/STATE.md (Phase 4 Part 7 complete), docs/CHANGELOG_AI.md (this entry)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) check-product-sync.mjs used exact section header strings that did not match PRODUCT.md's actual headings (e.g. "## App Name" vs "## App Identity"), (2) check-product-sync.mjs .env.dev gitignore check looked for literal ".env.dev" but .gitignore uses ".env.*" wildcard, (3) hydration-lint.mjs flagged new Date() in apps/web/src/app/api/health/route.ts as hydration risk (false positive — API Route Handlers are server-only)
- Errors resolved:     (1) Switched check-product-sync.mjs section checks to regex patterns matching multiple heading variants, added inline field match for "Primary users:", (2) Added wildcard pattern check — accepts ".env.*" as covering ".env.dev", (3) Added route.ts/route.tsx skip rule in hydration-lint.mjs walkDir function

## 2026-05-03 — Phase 4 Part 8: CI + governance docs + MANIFEST.txt
- Agent:               CLAUDE_CODE
- Why:                 Generate all Part 8 deliverables — GitHub Actions CI pipeline, Docker Hub publish workflow, complete file manifest, implementation map rewrite, STATE.md update — Part 8 of 8 Phase 4 scaffold (FINAL PART)
- Files added:         .github/workflows/ci.yml, .github/workflows/docker-publish.yml, MANIFEST.txt
- Files modified:      docs/IMPLEMENTATION_MAP.md (complete Phase 4 snapshot — all 8 Parts), .cline/STATE.md (Phase 4 complete — all 8 Parts done), docs/CHANGELOG_AI.md (this entry)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  Pre-tool-use security hook fired on both workflow files (GitHub Actions injection warning — confirmed safe: ci.yml uses only matrix.task and github.* context values, docker-publish.yml uses only secrets.* and steps.meta.outputs.* — no user-controlled input in run: commands)
- Errors resolved:     Confirmed both workflows safe after hook review. Proceeded with writes.
- Notes:               SocratiCode initial index (codebase_index → codebase_status → codebase_context_index) requires Docker + SocratiCode MCP running — must be triggered manually after Phase 5 passes by saying "Index this codebase" in Claude Code with Docker running.

## 2026-05-03 — Phase 4 Part 4: packages/ui + packages/jobs (storage skipped)
- Agent:               CLAUDE_CODE
- Why:                 Scaffold shared UI package (shadcn/ui foundation) and BullMQ job queue system — Part 4 of 8 Phase 4 scaffold
- Files added:         packages/ui/ (package.json, tsconfig.json, src/globals.css, src/lib/utils.ts, src/tailwind.config.ts), packages/jobs/ (package.json, tsconfig.json, src/connection.ts, src/index.ts, src/queues/types.ts, src/queues/queue-factory.ts, src/queues/er-sync.queue.ts, src/queues/alerts.queue.ts, src/queues/email.queue.ts, src/queues/maintenance.queue.ts, src/queues/index.ts, src/workers/base-worker.ts, src/workers/index.ts)
- Files modified:      pnpm-lock.yaml (added bullmq, ioredis, class-variance-authority, clsx, tailwind-merge, tailwindcss dependencies), .cline/STATE.md (Phase 4 Part 4 complete)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  packages/ui tailwind.config.ts used require("tailwindcss-animate") — CJS require() not available in ESM TypeScript module
- Errors resolved:     Replaced require("tailwindcss-animate") with empty plugins array — animate plugin will be added when apps consume the config via proper ESM import

## 2026-05-03 — Phase 5: Validation — all 9 commands pass
- Agent:               CLAUDE_CODE
- Why:                 Run all 9 Phase 5 validation commands and resolve all failures so the build gate is clean before Phase 6 Docker startup
- Files added:         apps/web/src/server/auth/auth.config.ts (edge-compatible NextAuth config for middleware — no bcrypt/prisma/node:crypto)
- Files modified:      apps/web/next.config.ts (removed orphaned webpack Configuration type annotation; added serverExternalPackages: ["bcrypt"]; added node: URI scheme webpack external handler), apps/web/src/app/login/page.tsx (extracted LoginForm component with useSearchParams, wrapped in <Suspense> in LoginPage to satisfy Next.js SSG prerender requirement), apps/web/src/middleware.ts (use edgeAuthConfig instead of full auth config), package.json (pnpm overrides: esbuild, tar, vite, postcss, next-intl — resolves 6 HIGH CVEs in tar@6.x via bcrypt→@mapbox/node-pre-gyp chain), pnpm-lock.yaml (updated lockfile after overrides applied), apps/web/tsconfig.json + apps/web/package.json + packages/* (typecheck fixes from prior sessions — tracked here for completeness), .cline/STATE.md (Phase 5 complete)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) TypeScript: "Cannot find module 'webpack'" — orphaned Configuration type annotation in next.config.ts webpack function parameter after prior session removed the import. (2) Runtime: bcrypt_lib.node native addon not found — ran npx @mapbox/node-pre-gyp install --fallback-to-build from bcrypt package dir to download prebuilt binary. (3) Next.js SSG: "useSearchParams() should be wrapped in a suspense boundary at page /login" — entire LoginPage was the useSearchParams consumer. (4) Audit: 6 HIGH CVEs in tar@6.2.1 via bcrypt>@mapbox/node-pre-gyp chain.
- Errors resolved:     (1) Removed ": Configuration" from webpack function parameter — TypeScript infers type from NextConfig.webpack signature. (2) Downloaded bcrypt native binary via node-pre-gyp. (3) Extracted LoginForm as separate module-level component with useSearchParams, made LoginPage a <Suspense> wrapper. (4) pnpm audit --fix added overrides; pnpm install applied them; re-audit confirms 0 vulnerabilities.

## 2026-05-04 — Schema Delta Fixes (feat/schema-delta-fixes)
- Agent:               CLAUDE_CODE
- Why:                 Fix 4 PRODUCT.md schema deltas discovered during pre-Phase 6 audit — PatrolType enum typo, missing Patrol.boatName, missing Tenant.currency, missing PatrolSchedule.tenantId
- Files added:         packages/db/prisma/migrations/20260504162834_schema_delta_fixes/migration.sql (manual SQL — ALTER TYPE rename enum value, ADD COLUMN ×3, ADD CONSTRAINT FK)
- Files modified:      packages/db/prisma/schema.prisma (PatrolType enum seabourn→seaborne, Patrol.boatName nullable String, Tenant.currency String(3) default IDR, PatrolSchedule.tenantId + relation), packages/shared/src/types/enums.ts (seabourn→seaborne), packages/shared/src/types/patrol.ts (boatName field), packages/shared/src/types/tenant.ts (currency field), packages/shared/src/types/patrol-schedule.ts (tenantId field), packages/shared/src/schemas/enums.ts (seabourn→seaborne), packages/shared/src/schemas/patrol.ts (boatName schema), packages/shared/src/schemas/tenant.ts (currency schema), apps/web/src/server/trpc/routers/patrol.ts (seaborne enum), apps/web/src/server/trpc/routers/patrolArea.ts (seaborne enum ×2), apps/web/src/server/trpc/routers/patrolSchedule.ts (direct tenantId scoping — replaced indirect patrolArea.tenantId join), eslint.config.mjs (added next-env.d.ts to ignores), apps/web/next.config.ts (scoped eslint-disable for webpack externals), docs/IMPLEMENTATION_MAP.md (updated status + schema delta section)
- Files deleted:       none
- Schema/migrations:   20260504162834_schema_delta_fixes — rename PatrolType.seabourn→seaborne, add Patrol.boat_name, add Tenant.currency, add PatrolSchedule.tenant_id + FK
- Errors encountered:  (1) next-env.d.ts lint error from strictTypeChecked, (2) webpack externals `any`-related lint errors in next.config.ts, (3) 4 typecheck errors in tRPC routers from stale enum values and missing tenantId
- Errors resolved:     (1) Added next-env.d.ts to eslint ignores, (2) Scoped eslint-disable block around webpack externals + single-line disable for return, (3) Updated all Zod enums to seaborne and added tenantId to patrolSchedule create mutation

## 2026-05-05 — Schema Delta Fixes Squash-Merge + Session Pause
- Agent:               CLAUDE_CODE
- Why:                 Complete squash-merge of feat/schema-delta-fixes to main (9b33fc1), write handoff for Phase 6 resume
- Files added:         .cline/handoffs/2026-05-05-schema-merge-complete-pause.md
- Files modified:      .cline/STATE.md (PAUSED status, branch field updated), docs/CHANGELOG_AI.md (this entry)
- Files deleted:       none (feat/schema-delta-fixes branch deleted after squash-merge)
- Schema/migrations:   none (migration already committed in prior session)
- Errors encountered:  none
- Errors resolved:     none

## 2026-05-05 — Phase 6 — Docker Services Startup + Visual QA
- Agent:               CLAUDE_CODE
- Why:                 Start all Docker dev services, run migrations + seed, perform Visual QA, complete Phase 6 Output Contract
- Files added:         none
- Files modified:      deploy/compose/dev/docker-compose.app.yml (healthcheck localhost→127.0.0.1 for Alpine IPv6), deploy/compose/dev/docker-compose.db.yml (PgBouncer: removed env_file, added individual env vars to avoid password-with-slash URL parsing), apps/web/Dockerfile (added Prisma engine binary copy step for Alpine standalone), .env.dev (URL-encoded special chars in DATABASE_URL and PGBOUNCER_DATABASE_URL passwords), .cline/STATE.md (Phase 6 complete)
- Files deleted:       none
- Schema/migrations:   Ran existing migrations (init + schema_delta_fixes) via pnpm db:migrate. Seed data populated (tenant, webmaster admin, event types, patrol area).
- Errors encountered:  (1) PgBouncer crash — env_file injected DATABASE_URL with `/` in password breaking URL parsing, (2) Prisma engine binary missing in Alpine standalone — query_engine .so.node not copied to runner stage, (3) App healthcheck failing — Alpine resolves `localhost` to IPv6 `::1` but Next.js binds IPv4 only, (4) Prisma CLI migrate fails — DATABASE_URL password `/` parsed as path separator, (5) Worker container restarts — worker.js not in Next.js standalone output (non-blocking)
- Errors resolved:     (1) Removed env_file from PgBouncer, added explicit DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME env vars, (2) Added find+cp step in Dockerfile builder stage to collect libquery_engine-linux-musl-openssl-3.0.x.so.node into /prisma-engines/, copied to runner .prisma/client/ and .next/server/, (3) Changed healthcheck URL to http://127.0.0.1:3000/api/health, (4) URL-encoded passwords in .env.dev: `/`→`%2F`, `+`→`%2B`, (5) Worker issue deferred to Phase 7 — non-blocking

## 2026-05-07 — Phase 7 Feature Update: EarthRanger Sync Processor Implementation
- Agent:               CLAUDE_CODE
- Why:                 Implement the er-sync processor body — 5 sync functions (event_types, subjects, events, patrols, observations) with SyncLog lifecycle, tenant validation, credential decryption, and Prisma JSON nullable handling
- Files added:         packages/jobs/src/processors/er-sync.processor.ts, packages/jobs/src/__tests__/er-sync.processor.test.ts
- Files modified:      .cline/STATE.md, docs/CHANGELOG_AI.md (this entry), docs/IMPLEMENTATION_MAP.md, .cline/memory/agent-log.md
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) Prisma JSON nullable — plain null not assignable to InputJsonValue | NullableJsonNullValueInput, (2) ESLint strict-boolean-expressions — nullable strings in conditionals need explicit != null, (3) ESLint no-unsafe-assignment — expect.objectContaining returns any, (4) ESLint no-unnecessary-type-assertion — as never casts on vi.fn().mockResolvedValue() unnecessary
- Errors resolved:     (1) Created toJsonOrNull() helper using Prisma.JsonNull + explicit type casts at call sites, (2) Changed all ternaries to use != null pattern, (3) Added <Record<string, unknown>> generic to expect.objectContaining, (4) Removed as never casts from mock setup

## 2026-05-06 — Worker Fix — Docker Internal Networking for BullMQ Workers
- Agent:               CLAUDE_CODE
- Why:                 Fix worker container crash loop — ECONNREFUSED to localhost:45196 inside Docker. Workers must connect to Valkey via Docker internal hostname, not host-mapped port.
- Files added:         none
- Files modified:      packages/jobs/src/connection.ts (rewrote from REDIS_URL parsing to individual REDIS_HOST/REDIS_PORT/REDIS_PASSWORD env vars), deploy/compose/dev/docker-compose.app.yml (added REDIS_HOST=${COMPOSE_PROJECT_NAME}_valkey + REDIS_PORT=6379 overrides to worker service), deploy/compose/stage/docker-compose.app.yml (same worker REDIS_HOST/REDIS_PORT overrides), deploy/compose/prod/docker-compose.app.yml (same worker REDIS_HOST/REDIS_PORT overrides), .cline/STATE.md (removed worker blocker, updated LAST_DONE)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) Worker ECONNREFUSED localhost:45196 — .env.dev sets REDIS_HOST=localhost and REDIS_PORT=45196 (host-mapped port), but inside Docker container, Valkey is at marine-guardian_dev_valkey:6379 on the internal network. (2) Attempted REDIS_URL override with ${REDIS_PASSWORD} in compose environment: block — Docker Compose interpolates from shell env (not env_file), causing "variable not set" warning and blank password.
- Errors resolved:     (1) Rewrote connection.ts to read REDIS_HOST/REDIS_PORT/REDIS_PASSWORD individually. Added REDIS_HOST and REDIS_PORT overrides in compose environment: block for worker service across all 3 envs. REDIS_PASSWORD flows correctly from env_file (no shell interpolation needed). (2) Dropped REDIS_URL approach entirely — individual vars avoid the Compose shell interpolation problem.

## 2026-05-08 — Phase 8 Batch 1 Item 1: Dashboard (Standard) — Command Center
- Agent:               CLAUDE_CODE
- Why:                 Implement dashboard as first Phase 8 iterative buildout item — KPI cards, event breakdown charts, recent events feed, quick stats
- Files added:         apps/web/src/server/trpc/routers/dashboard.ts (3 tenant-scoped procedures: kpis, eventBreakdown, recentEvents), apps/web/src/components/ui/chart.tsx (shadcn/ui chart component — Recharts wrapper), apps/web/components.json (shadcn/ui config)
- Files modified:      apps/web/src/app/(dashboard)/dashboard/page.tsx (full rewrite — KPI cards, bar charts, event feed, quick stats), apps/web/src/server/trpc/routers/index.ts (added dashboardRouter), apps/web/src/components/ui/card.tsx (updated shadcn card component), apps/web/package.json (added recharts dependency), pnpm-lock.yaml
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) exactOptionalPropertyTypes — passing undefined to optional number prop. (2) Date | null not assignable to new Date(). (3) strict-boolean-expressions — truthy checks on nullable strings and numbers.
- Errors resolved:     (1) Conditional rendering pattern — only pass delta/deltaLabel props when data exists. (2) Ternary null check on reportedAt. (3) Changed nullable length checks to explicit `!== undefined && .length > 0` pattern, changed nullable string checks to `!== null`.

## 2026-05-08 — Phase 8 Batch 1 Item 2: Event Kanban Board — UI Scaffold (PAUSED)
- Agent:               CLAUDE_CODE
- Why:                 Implement Event Kanban Board — drag-and-drop state transitions (New → Active → Resolved) using Kibo UI kanban component + dnd-kit
- Files added:         apps/web/src/components/kibo-ui/kanban/index.tsx (Kibo UI Kanban component), apps/web/src/components/ui/scroll-area.tsx (shadcn/ui scroll-area — kanban dependency), .cline/handoffs/2026-05-08-event-kanban-pause.md (pause handoff)
- Files modified:      apps/web/src/app/(dashboard)/events/page.tsx (full rewrite — Kanban board with 3 columns, drag-and-drop, priority badges, event cards), apps/web/package.json (added @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, @radix-ui/react-scroll-area), pnpm-lock.yaml, .cline/STATE.md (PAUSED)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  none (UI scaffold only — no typecheck/lint run yet)
- Errors resolved:     none
- Status:              PAUSED — tRPC event.updateState + event.stats procedures NOT verified. Typecheck/lint/tests NOT run. Two-stage review NOT performed.

## 2026-05-08 — Phase 8 Batch 1 Item 2: Event Kanban Board — COMPLETE
- Agent:               CLAUDE_CODE
- Why:                 Resume paused Event Kanban Board — add event.updateState + event.stats tRPC procedures, fix all TypeScript errors, write unit tests (TDD), resolve vitest/Vite 8 compatibility, run two-stage review
- Files added:         apps/web/src/server/trpc/routers/__tests__/event.test.ts (4 unit tests: happy path, tenant scoping, FORBIDDEN on missing tenantId, schema validation)
- Files modified:      apps/web/src/server/trpc/routers/event.ts (added updateState mutation: tenant-scoped updateMany, FORBIDDEN guard; added stats query), apps/web/src/app/(dashboard)/events/page.tsx (wired updateState + stats queries, optimistic UI with rollback on error), apps/web/src/components/kibo-ui/kanban/index.tsx (TypeScript fixes — ComponentRef, DragEndEvent export, strict null checks), apps/web/src/components/ui/scroll-area.tsx (React.ElementRef→React.ComponentRef for React 19 compat), apps/web/vitest.config.ts (vmForks pool, resolve.alias for @ path), apps/web/package.json (vitest ^2.1.8→^4.1.5 — Vite 8 compat fix), pnpm-lock.yaml
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) __vite_ssr_exportName__ is not defined — vitest 2.x incompatible with Vite 8.0.10 imposed by root pnpm override. 6 config-level attempts failed. (2) TypeScript strict errors in kanban component (ComponentRef, DragEndEvent, optional chaining). (3) React 19 ElementRef deprecation in scroll-area.
- Errors resolved:     (1) Upgraded vitest ^2.1.8→^4.1.5 — vitest 4.x pairs with Vite 8.x. All 4 tests pass in 307ms. (2)/(3) Fixed TypeScript and React 19 compat issues at source. Two-stage review: Stage 1 PASS (all spec behaviours implemented — columns, drag-and-drop, optimistic UI, tenant scoping, priority badges, stats), Stage 2 PASS (no any types, tests written RED before GREEN, blast-radius scope only).

## 2026-05-08 — Phase 7 Feature Update: Alert Rules CRUD UI + Notification Center
- Agent:               CLAUDE_CODE
- Why:                 Implement PRODUCT.md L177-190 Alert System + Notification Center modules — full Alert Rules CRUD on /alerts (admin-only writes via existing alertRule tRPC router), full Notification Center on /notifications (chronological list, type filter, unread state, mark-read, click-through to event), sidebar unread badge polling notification.unreadCount. Routers (alertRule.ts, notification.ts) already on main from Phase 4 — this branch adds UI + tests.
- Files added:         apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/dropdown-menu.tsx, apps/web/src/components/ui/select.tsx, apps/web/src/components/ui/separator.tsx, apps/web/src/components/ui/switch.tsx, apps/web/src/components/ui/tabs.tsx (6 shadcn primitives — vendored with React 19 ComponentRef from the outset), apps/web/src/server/trpc/routers/__tests__/alertRule.test.ts (6 unit tests: list+tenant scoping, list+isActive filter, create+RBAC, create rejects non-admin, update tenant-scoped, delete rejects non-admin), apps/web/src/server/trpc/routers/__tests__/notification.test.ts (5 unit tests: list+tenant+user scoping, list+notificationType filter, markRead, markAllRead, unreadCount+FORBIDDEN guard)
- Files modified:      apps/web/src/app/(dashboard)/alerts/page.tsx (full CRUD UI — list, create/edit dialog, severity Select, channel multi-select via checkbox, isActive Switch, delete with inline confirm; lint cleanup against actual schema — creator/isActive non-nullable per Prisma), apps/web/src/app/(dashboard)/notifications/page.tsx (full Notification Center — was 8-line stub), apps/web/src/components/layout/sidebar.tsx (added trpc.notification.unreadCount.useQuery with 30s refetch + 15s staleTime; pill badge on /notifications nav item, "99+" cap, aria-label), apps/web/src/server/trpc/routers/__tests__/event.test.ts (added typed `partial<T>` helper to satisfy no-unsafe-assignment on nested vitest matchers — same pattern applied in alertRule + notification tests), apps/web/package.json (added @radix-ui/react-{dialog,dropdown-menu,select,separator,switch,tabs} as runtime deps for the new shadcn primitives), pnpm-lock.yaml
- Files deleted:       none
- Schema/migrations:   none (alertRule + notification routers + Prisma models pre-existed on main from Phase 4)
- Errors encountered:  (1) ESLint: 53 errors after first lint pass — 28× React.ElementRef deprecated in vendored shadcn primitives (React 19), 3× strict-boolean-expressions on dropdown-menu `inset` optional prop, 14× alerts/page.tsx unnecessary conditionals/optional chains (pre-existing tech debt against Prisma's non-nullable types), 7× no-unsafe-assignment on nested expect.objectContaining matchers in 3 test files, 1× restrict-template-expressions on number→string in sidebar badge. (2) TypeScript: dropdown-menu CheckboxItem `checked: CheckedState | undefined` violated exactOptionalPropertyTypes:true; partial<T extends object>(obj: Partial<T>) didn't satisfy vitest's DeeplyAllowMatchers<T>.
- Errors resolved:     (1a) sed bulk-replace React.ElementRef→React.ComponentRef across 6 shadcn primitives. (1b) `inset && "pl-8"` → `inset === true && "pl-8"` in 3 places in dropdown-menu. (1c) Removed redundant `?.` and `?? false` in alerts/page.tsx where Prisma schema declares fields as non-null (User.fullName, AlertRule.creator, AlertRule.isActive). (1d) Defined `function partial<T>(obj: T): T { return expect.objectContaining(obj as any) as T; }` per test file (one narrow `as any` with eslint-disable + reason comment — vitest's DeeplyAllowMatchers<T> can't be expressed without importing internal vitest types). (1e) `String(unread)` for sidebar pill text. (2) Conditionally spread `checked` in dropdown-menu CheckboxItem: `{...(checked !== undefined ? { checked } : {})}`. Final: pnpm lint EXIT 0, pnpm typecheck EXIT 0, pnpm vitest run EXIT 0 — 17/17 passing in 429ms (alertRule 6, notification 5, event 6). Two-stage review: Stage 1 PASS, Stage 2 PASS.
- Spec deferrals:      (1) PRODUCT.md L182 "Alert history log" — not implemented this branch; needs separate scope (filter notifications by alertRuleId, or dedicated page). (2) PRODUCT.md L189 filter type names ("event alert, system alert, escalation, warning") differ from schema enum (critical | warning | info | system); implementation uses schema enum as locked technical contract — recommend updating PRODUCT.md to align. (3) PRODUCT.md L187 click-through to "related event or patrol" — events implemented; patrol click-through requires Notification.patrolId FK migration (current schema only has eventId).

## 2026-05-11 — Phase 8 Batch 2: Alert Rule Evaluation Engine (processor body)
- Agent:               CLAUDE_CODE (Architect-Execute: Opus planned + dispatched 2× Sonnet, both thrashed; Opus took over per memory-governance.md §4 BLOCKED clause)
- Why:                 Alert Rules CRUD UI shipped 2026-05-08 but rules did not fire — alerts BullMQ queue was wired to a stub processAlert. This task implements the processor body so events trigger notifications end-to-end.
- Files added:         packages/jobs/src/__tests__/alerts.processor.test.ts (5 vitest cases: missing tenantId rejection via validateTenantContext, no-active-rules zero-result, match+recipient creates Notification with correct fields + AuditLog, match+no-recipient yields zero notifications without opening transaction, transaction-failure throws atomically with no partial commit)
- Files modified:      packages/jobs/src/processors/alerts.processor.ts (replaced 662-byte stub with 143-line implementation: tenant-scoped event load → active rule load → ruleMatches() against conditionJson.eventTypeId + minPriority → recipient resolution via super_admin/site_admin role fallback → atomic Prisma $transaction creating Notification + AuditLog ALERT_FIRED per rule×recipient → returns {rulesEvaluated, rulesMatched, notificationsCreated}; preserved processAlert deprecated re-export for start-workers.ts compat)
- Files deleted:       none
- Schema/migrations:   none (AlertRule + Notification + AuditLog models pre-existed on main from Phase 4)
- Errors encountered:  (1) Two consecutive Sonnet subagent dispatches thrashed at "Autocompact is thrashing — context refilled to limit within 3 turns of the previous compact, 3 times in a row" — Sonnet 1 (525s, 12 tool uses) wrote test file then thrashed before processor; Sonnet 2 (378s, 15 tool uses) wrote 143-line processor then thrashed before tests/lint/commit. Auto-loaded baseline (CLAUDE.md + 5 rules files + Vercel plugin hooks + MCP server reminders + skills lists) appears to consume more than the 60K SAFE zone budgeted, leaving insufficient workspace for tool-use turns. (2) TypeScript TS2322: '"admin"' not assignable to UserRole — original Opus task spec inlined "admin" as a default-fallback recipient role, but UserRole enum is super_admin | site_admin | field_coordinator | operator (no plain "admin"). (3) ESLint: 5× no-unsafe-assignment on test file's vitest matcher chain (expect.objectContaining + expect.any + expect.stringMatching all return any per @vitest/expect typing).
- Errors resolved:     (1) Opus took over remaining work directly per memory-governance.md §4 — verified Sonnet 2's processor passed all 5 tests, made the 2 small fixes below, ran lint+typecheck clean, committed and squash-merged. Reasonable cost trade vs a 3rd dispatch attempt with low success probability. (2) Changed processor recipient query to role: { in: ["site_admin", "super_admin"] } and updated mockAdminUser.role to "site_admin" in test for consistency. (3) Wrapped vitest matcher assertion block in scoped /* eslint-disable @typescript-eslint/no-unsafe-assignment */ ... /* eslint-enable */ comment pair (alternative was duplicating the partial<T> helper pattern from the alert/notification test files; chose disable-block as more localized and idiomatic for matcher-heavy assertions). Final: 5/5 alerts.processor tests pass in 132ms, pnpm lint exit 0, pnpm typecheck exit 0, two-stage review PASS, squash-merged commit 0e62d62.
- Worker registration: alertsWorker was already wired in packages/jobs/src/start-workers.ts:12 from Phase 4 scaffold (createWorker(QUEUE_NAMES.ALERTS, processAlert, { concurrency: 3 })). Sonnet 2 preserved the processAlert named export at the bottom of alerts.processor.ts as a deprecated re-export of evaluateAlerts so start-workers.ts compiles unchanged. No edit to workers/index.ts or start-workers.ts needed.
- Deferred:            event.create enqueue integration — separate follow-up task. Without enqueue, alerts evaluation engine is fully built and tested but does not yet fire on real event creation. Next task scope: (a) inject alertsQueue into event.ts tRPC router, (b) after successful event.create, enqueue { tenantId: ctx.tenantId, eventId: created.id } with try/catch around the enqueue (mutation must still succeed if queue enqueue fails), (c) add a single integration test asserting the enqueue is called.

## 2026-05-20 — Phase 8 Batch 4 Sub-batch 4.1e: Event/Patrol area attribution + Tenant ArcGIS fields — 🎯 BATCH 4 COMPLETE
- Agent:               CLAUDE_CODE (Architect-Execute: Opus 4.7 dispatched Opus 4.7 executor subagent, executor dropped at step 7/11 after committing schema+migration+prisma generate+migrate dev, Architect salvaged partial work + completed remaining 7 files in main session per [[feedback_sonnet_thrash_inspect_before_redispatch]] doctrine)
- Why:                 Final sub-batch of Phase 8 Batch 4 v2 Foundation Tables — adds area attribution wiring on Event/Patrol (areaName + areaBoundaryId FK + areaDerivedAt — additive nullable, fields stay NULL until Batch 5+ derivation algorithm lands) + Tenant ArcGIS reference fields (arcgisBoundaryUrl + arcgisBoundaryOutfields for the Area Boundary editor reference layer, encrypted at app layer per earthrangerUrl precedent). Closes out Batch 4.
- Files added:         packages/db/prisma/migrations/20260520010000_add_area_attribution_and_tenant_arcgis/migration.sql (7-step additive DDL: ALTER tenants/events/patrols ADD COLUMN + AddForeignKey events_area_boundary_id_fkey + AddForeignKey patrols_area_boundary_id_fkey + CreateIndex 2 composites — all nullable, all SET NULL on AreaBoundary delete matching FuelEntry precedent from 4.1c), packages/db/prisma/migrations/20260520010000_add_area_attribution_and_tenant_arcgis/down.sql (7-step lossless reverse — DropIndex → DropConstraint → DropColumn in reverse dependency order), apps/web/src/server/trpc/routers/__tests__/patrol.test.ts (NEW — 6 cases covering list/getById/stats with 4.1e area-attribution assertions baked in; closes existing gap where patrol router had zero coverage)
- Files modified:      packages/db/prisma/schema.prisma (Tenant +arcgisBoundaryUrl +arcgisBoundaryOutfields, Event +areaName +areaBoundaryId +areaDerivedAt + areaBoundary AreaBoundary? relation + @@index([tenantId, areaBoundaryId]), Patrol same 3 fields + relation + index, AreaBoundary +events Event[] +patrols Patrol[] back-relations — also incidental `prisma format` column alignment on User + PatrolTrack models, cosmetic only), packages/shared/src/types/event.ts (+areaBoundaryId +areaDerivedAt — areaName already pre-existed, latent shared/Prisma drift now resolved), packages/shared/src/types/patrol.ts (+areaName +areaBoundaryId +areaDerivedAt), packages/shared/src/types/tenant.ts (+arcgisBoundaryUrl +arcgisBoundaryOutfields), packages/shared/src/schemas/event.ts (+areaBoundaryId as cuid().nullable() +areaDerivedAt as coerce.date().nullable()), packages/shared/src/schemas/patrol.ts (+3 fields matching event pattern), packages/shared/src/schemas/tenant.ts (+arcgisBoundaryUrl as url().nullable() matching earthrangerUrl pattern +arcgisBoundaryOutfields as string().nullable())
- Files deleted:       none
- Schema/migrations:   1 new migration 20260520010000_add_area_attribution_and_tenant_arcgis (applied cleanly to dev DB; `pnpm prisma migrate status` reports "Database schema is up to date" with 10 migrations total)
- Routers untouched:   apps/web/src/server/trpc/routers/event.ts + patrol.ts both use `include` (not `select`) on Event/Patrol queries — new schema fields auto-surface in responses with zero router code change. Major scope simplification vs initial STATE.md plan estimate. Event router still has list/getById/update/updateState/addAccompanyingRanger/removeAccompanyingRanger/stats unchanged. Patrol router still has list/getById/stats unchanged.
- Plan correction:    STATE.md plan listed only `area_boundary_id + area_derived_at` for Event/Patrol — v2 spec L456+L463 also requires `area_name` (free-text from ER on Event, free-text "primary area" on Patrol per v2 L463 derivation rules). Added per the same "v2 spec is authoritative" correction pattern caught in 4.1d. areaName on Event already pre-existed in packages/shared (latent drift) — 4.1e closes the gap by adding it to Prisma schema.
- Errors encountered:  (1) Opus 4.7 executor subagent dispatched as background Task reported "completed" status but actually dropped mid-work at step 7 of 11-step brief. Final result text truncated at "Client generated. Now read shared package files to match prevailing style:" — meaning schema.prisma + migration SQL + prisma generate + prisma migrate dev all completed correctly, but the 6 shared package edits + 2 test files were NOT done. (2) `pnpm lint --max-warnings 0` failed initially because turbo intercepted the `--max-warnings` flag instead of passing through to eslint. Needed `pnpm lint -- --max-warnings 0` with pass-through `--`. (3) `pnpm prisma migrate status` initially failed with "Validation Error" because DATABASE_URL not in shell env — needed `set -a && source .env.dev && set +a` before running.
- Errors resolved:     (1) Architect (main session, Opus 4.7) inspected partial work via `git status` + `git diff` — schema diff matched prescription exactly + migration SQL matched exactly + migration applied cleanly (10 migrations in DB) + prisma client regenerated. Salvageable. Per [[feedback_sonnet_thrash_inspect_before_redispatch]] doctrine, completed remaining 7 files in main session rather than re-dispatching: 6 shared package edits (additive only, ~3 fields each) + new patrol.test.ts (6 cases mirroring event.test.ts mock pattern). Net result: same final outcome as a clean executor dispatch but with 1 extra main-session hop. Squash-merged feat/area-attribution-scaffold → main (commit 6687112), branch deleted locally. Branch was never pushed to origin, so no remote branch deletion needed. (2) Used `pnpm lint -- --max-warnings 0` pass-through pattern. (3) Used `set -a && source .env.dev && set +a` to load DATABASE_URL before prisma calls.
- Verification:        typecheck 0 errors except pre-existing packages/db Node-types failures (identical baseline shipped on 4.1a/4.1b/4.1c/4.1d — Cannot find name 'process', 'console', 'Buffer', 'node:async_hooks', 'node:crypto'; api-client + shared + web all clean). lint 5/5 packages passing, 0 warnings. vitest 268 passing across 38 files (was 262 across 37 files = +6 cases +1 file, matching prediction). Migration applied cleanly. Two-stage review: Stage 1 (spec compliance against v2 L449+L456+L463) PASS. Stage 2 (code quality — no any types introduced, tests RED→GREEN, blast-radius scope only, conventional commit) PASS.
- Final ship:          Squash-merged feat/area-attribution-scaffold → main as commit 6687112 (10 files, +316 -39). Feature branch deleted locally. Origin push + governance writes pending after this CHANGELOG entry.
- 🎯 BATCH 4 COMPLETE — all 5 sub-batches shipped (4.1a AreaBoundary 56fb3fa → 4.1b PatrolTrack cfe9195 → 4.1c FuelEntry+ReportExport e972d82 → 4.1d NotificationRecipient split d32e618 → 4.1e Event/Patrol/Tenant area attribution 6687112). v2 foundation tables in place. Next milestone: Batch 5+ (breaking v2 migrations + first user-visible v2 features).

## 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1d Area B — AreaBoundary CUD fan-out via BullMQ
- Agent:               CLAUDE_CODE (Opus 4.7 — main session)
- Why:                 Wire the AreaBoundary tRPC create/update/delete mutations to enqueue area-rederive jobs for every Event + Patrol + FuelEntry row in the tenant, per v2 L545 (large blast-radius CUD path). The 5.1c worker's 50/sec rate limiter absorbs the load. Closes the consumer side of the area-rederive queue infrastructure shipped in 5.1c.
- Files added:         none
- Files modified:      apps/web/src/server/trpc/routers/areaBoundary.ts (+57 LOC — fanOutAreaRederive(tenantId, userId) private helper using Promise.all of Event/Patrol/FuelEntry findMany with explicit `where: { tenantId }` defense-in-depth scoping; Promise.all of enqueueAreaRederive per row with entity casing "event"|"patrol"|"fuelEntry" matching AreaDerivationEntity from 5.1b; userId threaded through ctx.userId per BaseJobPayload requirement; wired into create unconditionally, into update + delete gated on `result.count > 0`; return shape extended to `{boundary|result, fanOut}`), apps/web/src/server/trpc/routers/__tests__/areaBoundary.test.ts (+188 LOC — extended prisma mock with event/patrol/fuelEntry findMany; mocked @marine-guardian/jobs for enqueueAreaRederive; updated existing create test from `result.id` → `result.boundary.id`; +6 new cases covering fan-out counts per entity, payload shape with userId, tenant scoping defense-in-depth, no-op gating on count===0)
- Files deleted:       none
- Schema/migrations:   none
- Errors encountered:  (1) Two consecutive Opus 4.7 executor subagent dispatches (Agent calls with model:opus) returned interim status messages mid-stream without producing any branches, commits, or file changes. Each burned ~70s + ~10 tool uses (~287K + ~264K total_tokens). First returned "Reading the queue and area-rederive worker to verify enqueueAreaRederive signature, and verifying jobs barrel exports:" followed by ":agentId:" continuation hint; second returned "Noted — skill suggestion ignored per [[feedback_skill_injection_hooks_ignore_when_locked_stack]]. Tech stack is locked in inputs.yml (priority 6) which overrides skill suggestions (priority 7). Continuing." followed by ":agentId:" continuation hint. SendMessage tool not surfaced in inventory (ToolSearch "select:SendMessage" returned No matching deferred tools found twice), so neither agent could be continued. (2) Initial pre-flight discovered the original 5.1d brief assumed sync ingests area_name from EarthRanger but ErEvent/ErPatrol interfaces in packages/jobs/src/lib/earthranger-client.ts do not include area_name AND syncEvents/syncPatrols data spreads do not map any area_name field. Area A (v2 L546 sync inline derive) would have been a phantom feature that never fires. (3) Initial typecheck after first implementation pass flagged that AreaRederiveJobPayload (which extends BaseJobPayload) requires `userId` — interface line 3 has userId as required not optional.
- Errors resolved:     (1) User authorized main-session implementation (Opus 4.7 in the architect session) as the dispatch unblock via AskUserQuestion. Entire 5.1d-B work landed cleanly in ~10 minutes after pivot. Dispatch-mechanism issue flagged in STATE.md for investigation before next dispatch — root cause could be Agent output budget, PreToolUse skill injection hook noise interpreted as stop signal, or Opus subagent return-at-natural-sentence-boundary behavior. Main-session fallback is safe for cleanly-scoped sub-batches under ~30K main-session tokens. (2) Scope adjustment: user confirmed Ship Area B Only path via AskUserQuestion. Area A deferred to a precursor sub-batch (5.1d.A) that extends ER client + sync mapping to ingest area_name first, OR folded into a later Item that owns area_name ingestion as explicit deliverable. STATE.md DEFERRED field documents both paths for the next architect. (3) Fixed by threading ctx.userId through fanOutAreaRederive signature (added userId: string param) and updating all 5 enqueueAreaRederive calls with userId field; test assertions updated to include userId in payload shape. Comment added to helper explaining the userId source + AuditLog future use.
- Verification:        web lint 0 errors / 0 warnings, web typecheck 0 errors, web tests 274/274 PASS (+6 from 268 baseline — matches expected delta exactly), jobs lint+typecheck clean, jobs tests 57/57 unchanged. Total monorepo: 325 → 331. Trust-but-verify pass on main: re-ran web + jobs test suites after squash-merge, both confirmed clean.
- Final ship:          Branch feat/5.1d-area-boundary-cud-fanout single commit bbd1bee → squash-merged to main as commit 068d32f (2 files, +241 -4) → branch force-deleted with `git branch -D` per Rule 23 standard cleanup. NO origin push (pending user authorization with the 8 unpushed commits accumulated through Batch 5).
- Item 1 progress:     4 of 5 sub-batches done for the scope they hold (5.1a 6686042 + 5.1b c4a14a9 + 5.1c 3b4145f + 5.1d-B 068d32f). 5.1e admin manual-rebuild mutation + UI button now UNBLOCKED. Area A from 5.1d deferred (see STATE.md DEFERRED field).
