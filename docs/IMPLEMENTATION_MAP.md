# Implementation Map — Marine Guardian Command Center
# Current build state. Rewritten after every feature update.
# Last updated: 2026-05-30 — PRODUCT.md §210 backlog Item 5 SHIPPED (squash-merged to main as 4aa25b1). ✅ **Item 5 — Schedule Conflict Detection (2026-05-30, squash-merged to main as 4aa25b1).** Patrol assignment dialog now detects overlapping schedules per ranger and presents a soft-override UX: when create or update would conflict with an existing PatrolSchedule for the same ranger, the tRPC router throws BAD_REQUEST with `conflicts: PendingConflict[]` inlined into TRPCError.data; the assignment dialog catches this, surfaces a warning banner listing the conflicting windows + ranger names, requires the admin to tick an explicit override checkbox, then re-submits with `override: true` which the router accepts (audit captures via existing AuditLog write — no schema change). Soft-override over hard-block by design: legitimate cases include shift handoffs, ranger reassignment between districts, and post-hoc schedule corrections where deleting the old row would destroy audit lineage. Server detectScheduleConflicts(prisma, tenantId, rangerId, startsAt, endsAt, excludeId?) helper uses Prisma findMany with the canonical overlap predicate (startsAt < newEnd AND endsAt > newStart) plus NOT excludeId so an update never conflicts with the row being updated. Tests: web 661 → 678 (+17: 11 router conflict-detection cases covering exact-overlap / partial-overlap / touch-endpoint / same-ranger-different-window / different-ranger-same-window / update-excludes-own-row / override-bypass / tenant-scoping + 6 dialog UX cases covering render-clean / conflict-banner-on-submit / override-gates-resubmit / override-includes-flag / edit-mode-pre-populates / non-conflict-error-falls-through). Squash commit covers branch commits 046630f (server layer) + 3a1987a (UI layer). Multi-session ship spanned a power interruption mid-flight; 2 defects inherited from interrupted Sonnet UI dispatch (SelectMeta exactOptionalPropertyTypes widening + MockSelectTrigger useEffect ctx feedback loop OOM at 553s wall / 0ms test exec) fixed Opus-side in resume session — both documented as 🔴 gotcha entries in lessons.md. Visual QA NOT performed this ship — recommended follow-up: drive dev-container Playwright (APP_PORT=45204 after `docker compose build app && docker compose up -d --force-recreate`). Backlog §210 status after this ship: Item 1 ✅ + Item 3 ✅ + Item 4 ✅ + Item 4 follow-up ✅ + Item 5 ✅ + Item 6 ✅. Items 2, 7, 8 remain. ✅ **Item 4 — Super_admin tenant context switching (2026-05-30, squash-merged to main as c20190e).** PRODUCT.md §210 "Manage button to enter tenant context" shipped end-to-end. Mechanism: HttpOnly cookie `mg-impersonate-tenant` (SameSite=lax, Secure prod, 24h auto-expire, cuid-regex validated). New tRPC router `platformImpersonation` with `enter({tenantId})` (verifies super_admin + ctx.tenantId==='' + tenant exists + tenant.isActive; sets cookie via `cookies()` from `next/headers`; PLATFORM:ENTER_TENANT audit with targetTenantSlug+targetTenantName) and `exit()` (clears cookie, returns wasImpersonating boolean, PLATFORM:EXIT_TENANT audit). Both procedures use `protectedProcedure` (NOT platformAdminProcedure — exit must work after ctx.tenantId is overridden) with inline `roles.includes("super_admin")` guards. `protectedProcedure` in trpc.ts computes `effectiveTenantId`: override only when super_admin + sessionTenantId==='' + cookie present; downstream tenant-scoped procedures see the impersonated tenantId via L6 Prisma guard injection. context.ts reads cookie header on every request and exposes `impersonationTenantId` on ctx. middleware.ts reads cookie directly via `request.cookies.get()` (no import — edge-runtime safe) and short-circuits both existing redirect blocks when `isImpersonating` so super_admin can navigate between /admin/* (to switch/exit) and tenant routes simultaneously. UI: Manage button (leftmost in actions cell) added to each tenant row in /admin/tenants — disabled for inactive tenants — on click calls `enter.useMutation` then `router.push("/dashboard")`. Sticky amber `<ImpersonationBanner />` (async RSC) mounted as first child of (dashboard)/layout.tsx — reads cookie + auth + queries platformPrisma for tenant slug/name/isActive, renders only when super_admin + cookie present + tenant exists; includes `<ImpersonationBannerExitButton />` client subcomponent that calls `exit.useMutation` → `router.push("/admin/tenants") + router.refresh()`. Tests: 15 new vitest cases (10 platformImpersonation router across 6 describes covering FORBIDDEN gates, NOT_FOUND/BAD_REQUEST validation, cookie+audit happy path, stale-cookie exit; 2 admin-tenants-client for disabled-when-inactive + click→mutate→navigate; 3 impersonation-banner-exit-button for render/click/onSuccess navigation). 15 existing test fixture files extended with `impersonationTenantId: null` in ctx factories (necessary ripple from Context type change — not a logic change). Files (27 changed, +642 / -3): see commit c20190e. Verification: tsc 0 errors / lint 0 warnings / vitest 637 → 652 (+15). NO new DECISIONS_LOG entries — design choices captured in lessons.md as 🟤 decision. Backlog items 5 (schedule conflict detection), 7, 8 remain. **Visual QA on dev container (2026-05-30, port 45204 — STATE.md had stale 43000):** verified end-to-end via Playwright. Steps: login as webmaster (super_admin) → /admin landing → /admin/tenants. Manage button rendered leftmost in actions cell after `docker compose build app + up -d --force-recreate` (container had been running 7h on pre-Item-4 image — confirmed dev compose uses production multi-stage Dockerfile, no source mount, so source changes require rebuild). Click Manage on demo-site → POST trpc.platformImpersonation.enter → HttpOnly cookie set + PLATFORM:ENTER_TENANT audit row (tenant_id=NULL, user_id=super_admin, entity_id=demo-site, changes_json={targetTenantName:'Demo Site', targetTenantSlug:'demo-site'}) → router.push('/dashboard'). Banner rendered sticky amber 'Platform-admin view — Viewing tenant demo-site (Demo Site)' with Exit button. Dashboard KPIs returned demo-site data (3 active events, 1 active patrol, 4 events this month) — confirms `effectiveTenantId` override flows through tRPC + L6 Prisma guard to tenant-scoped queries. Click Exit → POST trpc.platformImpersonation.exit → cookie cleared + PLATFORM:EXIT_TENANT audit row (same shape, changes_json={targetTenantSlug:'demo-site'} — name omitted per exit lookup select) → router.push('/admin/tenants') + router.refresh(). Console clean except pre-existing favicon.ico 404. **QA follow-up identified — NOT a regression:** SSE Route Handler at apps/web/src/app/api/stream/notifications/route.ts uses `requireRouteAuth()` (apps/web/src/server/lib/route-auth.ts) which reads raw `session.user.tenantId` and returns 401 when empty — impersonating super_admin's raw tenantId is '' so SSE refuses with 4× 401s during impersonation. Same pattern likely affects all /api/exports/* Route Handlers during impersonation (they bypass tRPC and use requireRouteAuth too). Fix is small Tier 1 follow-up — extend `requireRouteAuth()` to read impersonation cookie + compute effective tenantId for super_admin in platform context, mirroring `protectedProcedure` override. Track before declaring §210 fully shipped or accept as known limitation if SSE-on-impersonated-tenant is out of scope for v1 super admin workflow.

# Prior 2026-05-22 — Defect fix bundle SHIPPED (pending commit on main). 🛡 **Per Area Report end-to-end pipeline UNBLOCKED — Batch 6 Item 2 ship-readiness restored.** Closes two compounded defects exposed by yesterday's S538/S539 ship-readiness smoke test against the dev Docker stack: (1) app container missing REDIS_HOST + REDIS_PORT environment overrides → BullMQ connection.ts fell back to localhost:45196 + ECONNREFUSED on every enqueue attempt; (2) after Defect 1 was patched in S539 (uncommitted), reportExport.create still persisted the ReportExport row but the pdf-render job was never published to Redis — root cause: queue.add() rejected the jobId with `Error: Custom Id cannot contain :`. All 7 production BullMQ queues used colon-separated jobId templates since Phase 8 Batch 5 (a systematic convention shipped with `pdf-render:${exportId}` / `patrol-track-materialize:${tenantId}:${patrolId}` / `alert:${tenantId}:${alertRuleId}:${eventId}` / etc.). None of the 12 vitest assertions caught the regression because `vi.mock("../queue-factory")` returned `{ add: vi.fn() }` — the mock bypasses BullMQ's real Redis-key validation entirely. **Forward-looking convention (binding for all future queue work)**: jobIds use `__` (double underscore) as the segment separator — NEVER `:`. The 7 production queues + 12 test asserts + 3 router/type doc comments are now consistent at the new convention. **Files changed (15 total)**: `packages/jobs/src/queues/{alerts,area-rederive,email,er-sync,maintenance,patrol-track-materialize,pdf-render}.queue.ts` (jobId templates flipped, 3 with inline JSDoc convention block addendums explaining the BullMQ-rejects-`:` cause); `packages/jobs/src/queues/types.ts` (PdfRenderJobPayload JSDoc); `apps/web/src/server/trpc/routers/{patrol,reportExport}.ts` (3 doc comments); `packages/jobs/src/queues/__tests__/{pdf-render,patrol-track-materialize,area-rederive}.queue.test.ts` (12 jobId assertion strings); `deploy/compose/dev/docker-compose.app.yml` (REDIS_HOST + REDIS_PORT app-service env overrides — bundles yesterday's S539 in-container fix into the same atomic commit). **Tests unchanged on the count axis** — assertion strings updated to new format, all 122 jobs + 459 web + 154 shared + 12 storage = 747 monorepo tests still green. **Verification pre-commit on main**: pnpm --filter @marine-guardian/jobs typecheck (0) + lint (0) + test (122/122); pnpm --filter @marine-guardian/web typecheck (0) + lint --max-warnings 0 (0) + test (459/459); grep verification of residual colon-jobId patterns = 0. **No new DECISIONS_LOG entries** (defect fix locks the `__` separator convention in lessons.md — 🔴 entry "BullMQ rejects jobIds containing `:` — unit tests that mock queue.add() never catch this"). **No new test coverage added in this commit** — future-improvement note in lessons.md proposes a real-Redis-or-ioredis-mock integration test to regression-guard the validation gap (small, fast follow-up). **Historical note**: prior IMPLEMENTATION_MAP entries below this one still reference the old colon-separated jobId convention in their narrative text (e.g. `pdf-render:${exportId}` in the 5.3b/5.3d/6.2d shipment summaries). Those references are historically accurate — they describe what was shipped at the time. The forward-looking convention is documented here + in `.cline/STATE.md` PHASE field + in lessons.md 🔴 entry. **Branch/commits**: committed directly on main as a single atomic fix() commit per the Phase 8 Batch 6 established ship pattern. 19 consecutive clean main-session commits (18 feature ships + this defect-fix bundle). Prior shipment summary follows:

# Prior 2026-05-22 — Phase 8 Batch 6 Sub-batch 6.2c SHIPPED (commit af98954 on main). 🎯 **Phase 8 Batch 6 Item 2 (Per Area Report) Sub-batch 6.2c complete — Per Area Report 3-page funder template END-TO-END OPERABLE.** Closes the Page 3 (Fuel Consumption) deliverable that completes the Per Area Report. Net new: apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page-3-fuel-consumption.tsx (~285 LOC pure RSC composer — section page-break-before:always + teal section header "Page 3 — Fuel Consumption — {area} — {dateRange}"; 3-KPI card row [Total Liters Received with entry-count caption, Total Cost currency-formatted via Intl.NumberFormat en-US emitting ₱ peso glyph for PHP, Avg L/km with seaborne-km caption — renders "N/A" when averageLitersPerKm is null]; conditional per-month breakdown table rendered ONLY when fuelConsumption.perMonthBreakdown.length ≥ 2 [single-month range shows KPI cards alone per the locked design decision], 5 columns Month/Liters/Cost/Seaborne km/L per km with tabular-nums right-aligned numeric cells + N/A token in litersPerKm column for months with zero seaborne km; methodology footer surfacing the per-area allocation caveat from PRODUCT.md §128 verbatim to funders; empty state ["No fuel entries recorded for {area} during {dateRange}"] when fuelConsumption is null [no fuel entries AND no seaborne km]; NO chart, NO Client island — pure RSC per the locked design decision). apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/__tests__/page-3-fuel-consumption.test.tsx (8 net new vitest cases via renderToStaticMarkup — empty state renders + omits KPI row; 3 KPI cards present with correct testIds + liters formatted to 1 fractional digit + entry-count pluralisation; "N/A" appears in avg-l-per-km card when averageLitersPerKm null; currency format uses Intl.NumberFormat emits ₱ peso glyph + 2,430.00 grouping; per-month table renders for multi-month ranges with March/April/May 2026 display labels + April row showing N/A for litersPerKm; per-month table hidden for single-month ranges; methodology footer mentions "per-area level" + "not tracked per individual boat" verbatim; pageBreakBefore:always regex match for fresh-page printing). Modified apps/web/src/server/per-area-report/get-per-area-report-data.ts (+~155 LOC — exported PerAreaReportFuelConsumption + PerAreaReportFuelMonthRow interfaces; fuelConsumption: PerAreaReportFuelConsumption | null field added to PerAreaReportData; new buildFuelConsumption helper with monthly bucketing via tenantLocalMonthLabel + toFiniteNumber Decimal-decoder helper [coerces Prisma Decimal | number | string into finite number, supports plain-number test fixtures]; Prisma fuelEntry.findMany scoped to tenantId + areaBoundaryId + dateReceived [gte, lt] with select on liters/totalPrice/currency/dateReceived ordered ASC by dateReceived; patrol.findMany select projection extended with startTime for monthly bucketing; SeabornePatrolBucketInput.startedAt widened to Date | null to match Patrol.startTime schema nullability; loop builds seabornePatrols array from patrols where patrolType === "seaborne"; buildFuelConsumption returns null when both fuel entries AND seaborne patrols are empty; averageLitersPerKm = totalSeabornePatrolKm > 0 ? totalLiters / totalSeabornePatrolKm : null divide-by-zero guard; per-month litersPerKm follows the same guard per bucket; defensive Date-instance check on startedAt + dateReceived skips month bucketing when missing/invalid [production: Prisma always returns valid Dates; defense covers mocked fixture compatibility without breaking pre-6.2c tests]; perMonthBreakdown sorted chronologically by YYYY-MM label; currency falls back to "PHP" when fuel-entry list is empty or first entry's currency is non-string). Modified apps/web/src/server/per-area-report/__tests__/get-per-area-report-data.test.ts (+5 net new vitest cases — fuel sum across area+range yields correct totals + currency "PHP" + entry count + L/km divides only by seaborne km [foot patrols excluded from divisor]; divide-by-zero guard returns null averageLitersPerKm when only foot patrols exist; per-month bucketing groups fuel + seaborne km into chronologically-sorted YYYY-MM buckets across 3-month range with April fuel-only month showing N/A litersPerKm; null fuelConsumption returned when only foot patrols + no fuel entries; query scope assertion matches tenantId + areaBoundaryId + dateReceived window + correct select projection; Mock registration: added prisma.fuelEntry: { findMany: vi.fn() } to mock declaration + beforeEach default of empty resolved value so pre-6.2c tests are unaffected). Modified apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/per-area-report.tsx (+8 LOC — import Page3FuelConsumption; mount <Page3FuelConsumption area + dateRange + fuelConsumption/> after Page2Heatmaps; layout JSDoc updated to reflect Page 1/2/3 coverage). NEW 🟤 decision entry in lessons.md: "FuelEntry is area-keyed, NOT patrol-keyed — L/km is always an aggregate ratio" — establishes the future contract that no Patrol↔FuelEntry join exists at the schema level; fuel-related extensions must compute L/km as sum(liters) / sum(seaborne km) within an area + period window. NO new DECISIONS_LOG entries — 6.2c is a pure data shape implementation of the locked scope correction. Tests: web 441→454 (+13: +5 loader fuel cases + +8 Page 3 RSC), shared 154 unchanged, jobs 122 unchanged, storage 12 unchanged. Monorepo total 729→742 (+13 net new for 6.2c). Verification pre-commit on main: shared typecheck 0 / shared lint 0 / shared 154 tests pass / web typecheck 0 / web lint --max-warnings 0 (after 3 inline fixes — Date|null widening for SeabornePatrolBucketInput, removed unnecessary fuelEntries cast, Intl.NumberFormat assertion updated for ₱ peso glyph) / web 454 tests pass. Committed directly to main as af98954 (5 files, +1127/-8 LOC) per the 6.2b-ii precedent — 17 consecutive clean main-session ships (5.1d-B → 5.3d → 6.1a → 6.1b → 6.1c-i → 6.1c-ii → 6.2a → 6.2b-i → 6.2b-ii → 6.2c). 6.2c consumed ~22K main-session tokens (estimate was 18-22K — landed at the upper bound of the corrected scope estimate, all within the SAFE zone). Next sub-batch 6.2d (optional) will wire ReportExport reportType:'area' into the existing PDF download path from Batch 5 Item 3 to surface Per Area Report as a funder-deliverable PDF end-to-end. 6.2b-ii prior summary preserved below for context.

# Prior 2026-05-22 — Phase 8 Batch 6 Sub-batch 6.2b-ii SHIPPED (commit ac74f5f on main). 🟣 **Phase 8 Batch 6 Item 2 (Per Area Report) Sub-batch 6.2b-ii complete — Page 2 heatmaps UI layer end-to-end operable.** Closes the UI half of the Per Area Report Page 2 deliverable that 6.2b-i set up server-side. Net new: apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/components/heat-layer.tsx (~75 LOC Client wrapper — useMap() + useEffect mount/unmount of L.heatLayer; VARIANT_OPTIONS keyed by 'events'|'tracks' with 5-stop gradient palettes [red-200→red-600 for events with radius=18 blur=22, blue-200→blue-700 for tracks with radius=14 blur=20]; cleanup on prop change avoids stacked duplicate layers across re-renders; early-return when points.length===0; `import "leaflet.heat";` for side-effect plugin registration extending L.heatLayer). components/per-area-heatmap-map.tsx (~140 LOC Client island — MapContainer + TileLayer OSM + HeatLayer×2 layered tracks-under-events + AutoFitBounds (events ∪ tracks → fitBounds with 16px padding when ≥2 points) + MapReadySignal (mirrors AreaCoverageMap's contract — double rAF paint flush, 8s safety timeout matching Puppeteer waitForFunction, window.__renderReady flip per locked "Coverage Report Page 2 Map Render Strategy" decision); default center Mindoro 13.0/121.0 zoom 9 overridden when overlays exist). page-2-heatmaps.tsx (~225 LOC RSC composer — section page-break-before:always with teal section header "Page 2 — Heatmaps — {area} — {dateRange}"; 110mm-tall heatmap-container with empty-state when both event arrays AND patrolTracks are empty; dual gradient legend rendered server-side via LegendGradientBar component as static linear-gradient CSS bars [events left = red 5-stop, tracks right = blue 5-stop] to avoid hydration drift; methodology footer explaining 250m densification + density interpretation; eventsToHeatTuples flatten LE + monitoring events to same red gradient since Page 1 surfaces the LE/monitoring split via bar charts already; tracksToHeatTuples concat all PerAreaReportPatrolTrack.sampledPoints arrays into one big HeatLatLng[]). __tests__/page-2-heatmaps.test.tsx (10 vitest cases via renderToStaticMarkup with `vi.mock("../components/per-area-heatmap-map", () => ({ PerAreaHeatmapMap: () => null }))` at the top per the react-leaflet 🔴 gotcha — leaflet imports window unconditionally; cases: section title contains area+dateRange, page-break regex match, empty-state, container mounts on events-only, container mounts on tracks-only, both legend bars always render, gradient stops are server-side hex literals, methodology footer mentions 250, mixed LE+monitoring flatten cleanly, multiple patrol tracks concat cleanly). Modified apps/web/package.json (+2 deps — leaflet.heat ^0.2.0 runtime + @types/leaflet.heat ^0.2.4 dev; pnpm-lock.yaml +2 packages; tree is tiny — no transitive deps beyond existing leaflet peer). Modified per-area-report.tsx (+8 LOC — import + mount Page2Heatmaps after Page1EventAndPatrolSummary). NO new DECISIONS_LOG entries (6.2b-ii consumes the locked "Heatmap Renderer Choice" verbatim). NO new lessons.md entries (clean pattern application — no new failure modes surfaced). Tests: web 431→441 (+10 net new for Page 2 RSC), shared 154 unchanged, jobs 122 unchanged, storage 12 unchanged. Monorepo total 719→729 (+10 net new for 6.2b-ii). Verification pre-commit on main: shared typecheck 0 / shared lint 0 / shared 154 tests pass / web typecheck 0 / web lint strict --max-warnings 0 (after a single inline nit removed — unnecessary cast `points as Array<[number, number, number]>` because @types/leaflet.heat's L.HeatLatLngTuple already matches HeatLatLng) / web 441 tests pass. Committed directly to main as ac74f5f (7 files, +726 LOC) per the 6.1c-i / 6.1c-ii / 6.2a / 6.2b-i precedent — 16 consecutive clean main-session ships (5.1d-B → 5.3d → 6.1a → 6.1b → 6.1c-i → 6.1c-ii → 6.2a → 6.2b-i → 6.2b-ii). 6.2b-ii consumed ~30K main-session tokens (estimate was 32-38K — first sub-batch in the 6.x sequence to land under the lower bound of its estimate). Next sub-batch 6.2c will ship Page 3 — Fuel Consumption (PRODUCT.md §182) in a fresh session. 6.2b-i prior summary preserved below for context.

# Prior 2026-05-22 — Phase 8 Batch 6 Sub-batch 6.2b-i SHIPPED (commit f6e9fc5 on main). 🟣 **Phase 8 Batch 6 Item 2 (Per Area Report) Sub-batch 6.2b-i complete.** Ships the pure-logic foundation for Per Area Report Page 2 heatmaps. UI (Leaflet client island + leaflet.heat dep + Page 2 RSC composer + mount) deferred to 6.2b-ii per the locked sub-batch decomposition. Net new: packages/shared/src/lib/heatmap-sample/ (4 files — types.ts with HeatLatLng tuple [lat, lon, weight] convention + SampleTrackPointsOptions; sample-track-points.ts with haversine-stepping densifier on WGS-84 mean radius 6,371,008.8 m and linear lat/lon interpolation between straddling vertices [<1cm deviation vs great-circle at 250m intervals in Philippines lat band]; index.ts barrel; __tests__/heatmap-sample.test.ts with 17 vitest cases covering haversine primitive correctness, degenerate input including <2 vertices and zero-length and sub-interval lines, output convention assertions, interval scaling, multi-segment polyline with degenerate segment skip, options defaults + custom weight propagation, and Philippines tenant-zone sanity). Extended apps/web/src/server/per-area-report/get-per-area-report-data.ts (+170 LOC): import sampleTrackPoints + HeatLatLng + extractTrackPolyline (cross-server-module, single source of truth from 6.1b); TRACK_SAMPLE_INTERVAL_METERS=250 constant per locked decision; new exported interfaces PerAreaReportEventLocation{lat, lon, eventTypeId} + PerAreaReportPatrolTrack{patrolId, patrolType, sampledPoints: HeatLatLng[]}; PerAreaReportData gains 3 fields lawEnforcementEventLocations + monitoringEventLocations + patrolTracks (parallels lawEnforcementBreakdown + monitoringBreakdown nomenclature); buildEventLocations helper filters events whose EventType.category matches the needle AND locationLat/locationLon are finite, single co-pass with breakdowns means no extra Event query; buildPatrolTracks helper extracts track polyline via extractTrackPolyline, runs sampleTrackPoints, emits PerAreaReportPatrolTrack rows, patrols with null track / non-LineString geometry / polylines<2 points are skipped (still appear in patrolSummary counts); events query SELECT extended with locationLat + locationLon; patrols query SELECT extended with id + track. Extended apps/web/src/server/per-area-report/__tests__/get-per-area-report-data.test.ts (+330 LOC): 7 net new test cases under new "Page 2 heatmap data" describe block (populate lawEnforcementEventLocations from valid events; skip null/NaN locations; skip null-eventType + unmatched-category events; build patrolTracks from materialised LineString tracks with weight=1 in Philippines band; skip Point/single-vertex/malformed tracks but still count in summary; verify patrol SELECT includes track relation AND event SELECT includes locationLat/locationLon; regression case for empty arrays). Modified packages/shared/package.json (+1 LOC exports entry "./lib/heatmap-sample"). Decision locked docs/DECISIONS_LOG.md: "Heatmap Renderer Choice (Phase 8 Batch 6 Sub-batch 6.2b) = leaflet.heat plugin + ~250m track-point densification" (REJECTS custom density-grid SVG renderer and hybrid mix; ~100% reuse of 6.1b AreaCoverageMap pattern, funder-recognized Canvas raster heatmaps, acceptable print quality at PDF DPI 96-150; governs Per Area Report + ALL future report families that need density overlays — no second-source renderer). Tests: shared 137→154 (+17 net new heatmap-sample), web 424→431 (+7 net new loader extension), jobs 122 unchanged, storage 12 unchanged. Monorepo total 695→719 (+24 net new for 6.2b-i). Verification pre-commit on main: shared typecheck 0 / shared lint 0 / shared 154 tests pass / web typecheck 0 / web lint 0 strict --max-warnings 0 / web 431 tests pass / pnpm -r test confirms all 4 workspaces clean. No inline corrections required — first iteration landed clean (15th consecutive clean main-session ship: 5.1d-B → 5.3d → 6.1a → 6.1b → 6.1c-i → 6.1c-ii → 6.2a → 6.2b-i). Committed directly to main as f6e9fc5 (7 files, +967 -10 LOC). 6.2b-i consumed ~35K main-session tokens (estimate was 28-32K — slight overage from AskUserQuestion decision-lock turn + governance overhead). Next sub-batch 6.2b-ii will mount the Leaflet client island + leaflet.heat dependency add + Page 2 RSC composer in a fresh session. 6.1c-ii prior summary preserved below for context.

# Prior 2026-05-22 — Phase 8 Batch 6 Sub-batch 6.1c-ii SHIPPED (commit e75b9cb on main). 🎯🎯🎯 **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) COMPLETE** — 3 of 3 sub-batches shipped (Page 1 — Patrol Index from 6.1a + Page 2 — Area Boundary Summary from 6.1b + Page 3 — Area Covered from 6.1c). The full funder-grade PDF deliverable now renders end-to-end: ReportExport pdf-render pipeline (Item 3 from Batch 5) feeds Puppeteer → /print-render/[tenantSlug]/coverage/[exportId] → 3-page A4/Letter/Legal landscape HTML → MinIO upload → user downloads PDF. Net new for 6.1c-ii: apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page-3-area-covered.tsx (~235 LOC RSC — page-break-before:always section frame, 5fr/4fr grid for table+chart, coverage table with per-row "Est." badge on hrsEstimatedCount>0, methodology footer notes + missingTracksCount callout, empty-state when sorted.length===0); apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/components/area-covered-chart.tsx (~135 LOC Client Component — Recharts horizontal BarChart on coverageKm domain, teal-600 bars (Page 2 uses cyan-500), "Coverage KM" axis label, tickFormatter for 1-decimal display, tooltip 2-decimal "km" suffix, top-N filter with sort tiebreak by name ASC, empty-state when no bars have coverage); apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/__tests__/page-3-area-covered.test.tsx (12 vitest cases via renderToStaticMarkup with AreaCoveredChart mocked — section frame, page-break-before:always, empty state, table row sort by coverageKm DESC, alphabetical tiebreak, Est badge presence/absence, footer Est emphasis, missing-tracks callout singular/plural/omitted, totals row, chart-column mount). Extended apps/web/src/server/coverage-report/get-coverage-report-data.ts (+18 LOC): new fields `areaCoverage: BoundaryCoverage[]` and `missingTracksCount: number` on CoverageReportData; consumes accumulateCoverageByBoundary() from packages/shared/src/lib/coverage-clip (6.1c-i library). Mapping: patrols → {id, trackLineString, totalHours} (PatrolForCoverage shape); boundariesForAttribution already in AreaBoundaryForDerivation shape (single-sourced across area-derivation + area-attribution + coverage-clip per the 6.1c-i decision lock). Polygon-only — LineString boundaries excluded from areaCoverage rows by the aggregator (clipping meaningless against unbounded line; they still appear on Page 2 as dashed reference outlines). Extended apps/web/src/server/coverage-report/__tests__/get-coverage-report-data.test.ts (+4 net new cases in new "getCoverageReportData — Page 3 area coverage" describe): per-Polygon coverage_km+hrs accumulation from a track that crosses the boundary; missingTracksCount=1 for patrols with totalHours but no track (and =0 for empty draft rows with neither); zero-row seeding for enabled Polygons with no patrol coverage; LineString boundaries excluded from areaCoverage rows but included in enabledAreas. Mounted apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/coverage-report.tsx (+5 LOC): Page3AreaCovered rendered after Page2AreaBoundarySummary with areaCoverage + missingTracksCount props. Tests: web 376→392 (+16 net new — +4 data-loader Page 3 attribution + +12 Page 3 RSC). Shared 137 unchanged. Jobs 122 unchanged. Storage 12 unchanged. Monorepo total 647→663. Verification pre-commit: web lint 0/0 strict (--max-warnings 0; one inline fix removing unused `hasAnyCoverage` const after refactor), web typecheck 0 errors, shared typecheck/lint 0. Committed directly to main as e75b9cb (no feature branch) per 6.1c-i precedent — main-session Opus 4.7 implementation continues the 5.1d-B → 5.3d → 6.1a → 6.1b → 6.1c-i clean main-session ship streak (now 13 consecutive). 6.1c-ii consumed ~30K main-session tokens (estimate was 30-35K — landed clean in the middle of the SAFE zone). 6.1c-i prior summary preserved below for context.

# Prior 2026-05-21 — Phase 8 Batch 6 Sub-batch 6.1c-i SHIPPED (squash-merge to main as 452fe30). 🎯 **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) progress: Page 3 algorithm landed; UI ships in 6.1c-ii.** Closes the analytical primitive layer for Page 3 (Area Covered). Net new: packages/shared/src/lib/coverage-clip/ (5 src files + 1 test file with 29 vitest cases — types.ts, clip-track-to-boundary.ts, compute-coverage-hours.ts, accumulate-coverage-by-boundary.ts, index.ts; pure functions exporting clipTrackToBoundary + computeCoverageHours + accumulateCoverageByBoundary). Algorithm: turf.lineSplit cuts the patrol track LineString at each polygon-edge crossing → midpoint of each piece tested via booleanPointInPolygon → sum of inside-piece lengths via @turf/length is the boundary's coverage_km. Empty FeatureCollection fallback (no crossings) tests the track's first vertex to classify fully-inside (returns trackTotalKm) vs fully-outside (returns 0). LineString boundaries return 0 km — coastlines enclose no area, filtered out by the accumulator. Hours computation pro-rates totalHours × (coverageKm / trackTotalKm) clamped to [0,1] for float drift; hrsEstimatedCount tracks per-boundary per-patrol estimation when totalHours is non-null. Aggregator filters to Polygon boundaries, seeds zero-rows, sums per patrol×boundary, tracks missingTracksCount = patrols with totalHours > 0 AND null trackLineString, sorts coverageKm DESC + areaName ASC tiebreak. Decision lock added to docs/DECISIONS_LOG.md: "Coverage Clip Library Choice = narrow @turf/* modules" — boolean-point-in-polygon + helpers + length + line-split, all ^7.1.0, ~60KB tree-shaken. Rejects full @turf/turf bundle (~200KB) and pure-math hand-rolled clipping (~250 LOC + correctness risk). Governs Page 3 + Per Area Report + future Quarterly Report (same algorithm, no second-source clip library). One inline lint correction during verification (no-unsafe-assignment on `coords[0]` after Array.isArray narrow — same precedent as 6.1a/6.1b pair[0]/pair[1] casts). One algorithm fix during initial test run (lineSplit returns empty FeatureCollection when there's no intersection; added fallback that classifies via first-vertex booleanPointInPolygon — all 137 tests pass after fix). Tests: shared 108→137 (+29 net new). Web/jobs/storage unchanged (376 / 122 / 12). Monorepo total 618→647. Verification pre-merge AND on main: shared typecheck 0 / shared lint --max-warnings 0 / shared tests 137/137 / web typecheck 0 / web lint 0 / web tests 376/376 (unchanged). Squash-merge from feat/coverage-page-3-algorithm as 452fe30 → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation per Tier-3 split decision at session start — continues the 5.1d-B → 5.3d → 6.1a → 6.1b clean main-session ship streak (now 12 consecutive). 6.1b prior summary preserved below for context.

# Prior 2026-05-21 — Phase 8 Batch 6 Sub-batch 6.1b SHIPPED (squash-merge to main as a8e44d9). 🎯 **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) IN PROGRESS** — 2 of 3 sub-batches shipped (Page 1 — Patrol Index from 6.1a + Page 2 — Area Boundary Summary from 6.1b). Closes the area-attribution + map-render infrastructure that 6.1c will reuse. Net new: packages/shared/src/lib/area-attribution/ (6 src files + 1 test file with 20 vitest cases — types.ts + nearest-start-area.ts + feature-matches-area.ts + attribute-patrol-to-area.ts + count-patrols-by-area.ts + index.ts; pure functions wrapping the 5.1 area-derivation primitives with INVERTED priority for report-time attribution: nearestStartArea geographic-first → featureMatchesArea name fallback → null per v2 §771). New apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page-2-area-boundary-summary.tsx (~250 LOC RSC with page-break-before:always + 5fr/4fr grid for table/chart + variance methodology callout + 8 vitest cases via renderToStaticMarkup with client-island mocks). New components/area-coverage-map.tsx (~230 LOC Leaflet client island with window.__renderReady flag, AutoFitBounds, OpenStreetMap tiles, cyan-filled polygons for enabled boundaries, dark polylines for patrol tracks, dashed reference outlines for ArcGIS-linked boundaries; tests via Puppeteer integration in pdf-render pipeline rather than DOM unit tests). New components/patrol-area-bar-chart.tsx (~130 LOC Recharts horizontal BarChart, Cell-per-row colors cyan/gray, sorted DESC by count then alphabetical tiebreak, Outside-enabled bar appended). New components/variance-info-callout.tsx (~40 LOC static styled methodology footer — no dialog because PDFs). Extended apps/web/src/server/coverage-report/get-coverage-report-data.ts (+159 LOC + 10 new test cases): new CoverageReportArea + CoverageReportAttribution interfaces; trackLineString field on CoverageReportPatrolRow (full polyline for map overlay); extractTrackPolyline helper for LineString/MultiLineString head-to-tail flatten (+6 vitest cases); prisma.areaBoundary.findMany scoped to tenant + isEnabled=true (defense-in-depth tenant scoping); attributePatrolToArea + countPatrolsByArea wiring populating enabledAreas[] + attributions[] + patrolCountsByArea[] + unattributedPatrolCount (+4 vitest cases). Modified coverage-report.tsx to mount Page 2 after Page 1 patrol detail table (+9 LOC). Extended deploy/pdf-renderer/src/server.js (+11 LOC): page.waitForFunction(window.__renderReady === true) after networkidle0 with 8s timeout + swallow-catch so map-less pages proceed immediately. Modified packages/shared/package.json (+1 line — new "./lib/area-attribution" exports entry). Decision locked docs/DECISIONS_LOG.md: Coverage Report Page 2 Map Render Strategy = Leaflet + OpenStreetMap tiles + Puppeteer __renderReady wait flag (REJECTS static SVG composition — governs all future report maps including 6.1c Page 3, Per Area Report, future Quarterly Report). Dependency adds apps/web: leaflet ^1.9.4 + react-leaflet ^5.0.0 (React 19 compatible — v4 had peer dep mismatch with React 19.2.5) + @types/leaflet ^1.9.21. Tests: shared 88→108 (+20 area-attribution cases), web 358→376 (+18 = 10 server helper + 8 Page 2 RSC composer). Jobs unchanged 122. Storage unchanged 12. Monorepo +38 net new vitest cases for 6.1b. Verification pre-merge AND on main: web lint 0/0 strict (--max-warnings 0), web typecheck 0 errors, shared lint 0, shared typecheck 0. Three non-blocker corrections during the verification loop: no-unsafe-assignment on pair[0]/pair[1] needing `as unknown[]` cast (precedent from 6.1a), no-unnecessary-type-conversion on String(b.source), no-unnecessary-type-assertion on `b.geometryType as ...` (Prisma enums already typed). One react-leaflet major version bump v4→v5 mid-stream to escape React 19 peer dep mismatch — pattern logged to lessons.md for future Leaflet-bearing sub-batches. Squash-merged from feat/6.1b-coverage-area-boundary-summary as a8e44d9 → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation per the 6.1a precedent — continues the 5.1d-B → 5.3d → 6.1a clean main-session ship streak (now 11 consecutive). 6.1a prior summary preserved below for context. 5.3d prior summary preserved below for context.

# Prior 2026-05-21 — Phase 8 Batch 6 Sub-batch 6.1a SHIPPED (squash-merge to main as 7de30cf). 🎯 **Phase 8 Batch 6 Item 1 (Coverage Report 3-page funder template) IN PROGRESS** — 1 of 3 sub-batches shipped (Page 1 — Patrol Index). Closes the foundational period-resolution layer + the first user-visible Coverage Report payload. ReportExport pdf-render pipeline now produces visible Page 1 PDFs instead of blank stubs. Net new: packages/shared/src/lib/coverage-period/ (12 src files — types.ts, iso-week.ts, build-period.ts, build-weekly-period.ts, get-monthly-period.ts, get-annual-period.ts, get-weekly-period.ts, get-month-week-periods.ts, get-last-completed-week.ts, patrol-starts-within-period.ts, get-selected-template-period.ts, index.ts; ~470 LOC pure functions + 37 vitest cases in 1 test file). Semantics: half-open [start, end) intervals; ISO 8601 week numbering; default UTC+8 tenant offset for v2 launch tenants (Mindoro, Banggai); spec L626 DST audit deferred until DST-observing tenant onboards. New apps/web/src/server/coverage-report/get-coverage-report-data.ts (~225 LOC + 16 vitest cases): loads tenant by slug, loads ReportExport by id with cross-tenant + reportType guards (returns null on any mismatch — caller maps to notFound), tenant-timezone → offsetMinutes resolver ("UTC"→0, else 480), period dispatch via getSelectedTemplatePeriod, prisma.patrol.findMany with startTime gte/lt period bounds + first-segment leaderName select + track.trackGeojson select, runtime paramsJson validator parseCoverageParams (returns {} for malformed input — never throws), GeoJSON LineString/MultiLineString endpoint extractor extractTrackEndpoints. Directory name coverage-report/ (not coverage/) to avoid .gitignore collision with the test-coverage glob. New apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/coverage-report.tsx (~250 LOC RSC + 8 vitest cases via renderToStaticMarkup): header block with brand line + tenant name + report title + period label + generated timestamp + paper size + tenant timezone; 3 summary cards (Foot/Seaborne/Total counts + km + hrs); patrol type subtotal table with tfoot Total; 11-column full patrol detail table (Serial / Title-or-Objective / Type / Status / Tracked By / Start Location / End Location / Start Time / End Time / Duration / KMS) with empty-state when zero patrols; embedded CSS via <style>{...} for A4/Letter/Legal landscape print; Puppeteer networkidle0 anchor img preserved. Modified: page.tsx now dispatches reportType==="coverage" → CoverageReport with notFound() on null data; other report types (per-area, ad-hoc-events, ad-hoc-patrols) keep the existing stub banner pending 6.1b/6.1c+ content. Modified: packages/shared/package.json (+1 line — new "./lib/coverage-period" exports entry). Tests: shared 51→88 (+37 period cases), web 334→358 (+24 = 16 server helper + 8 page render). Jobs unchanged 122. Storage unchanged 12. Monorepo +61 net new vitest cases for 6.1a. Verification pre-merge AND on main: web lint 0/0 strict (--max-warnings 0), web typecheck 0 errors, shared lint 0, shared typecheck 0. Five non-blocker corrections during the verification loop: number-interpolation String() wraps, June 2026 5-Mondays test assertion fix, `?.`-chain replacing non-null assertions, `as unknown[]` casts for Array.isArray narrowings, dir rename from coverage/→coverage-report/ to escape gitignore. Squash-merged from feat/6.1a-coverage-period-and-patrol-index as 7de30cf → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation per STATE.md NEXT-field brief — continues the 5.1d-B → 5.3d clean main-session ship streak (now 10 consecutive). 5.3d prior summary preserved below for context.

# Prior 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3d SHIPPED (squash-merge to main as cd9ec36). 🎯 **Phase 8 Batch 5 Item 3 COMPLETE** — 4 of 4 sub-batches shipped (5.3a infrastructure 5837821 + 5.3b queue/processor/worker 53c115e + 5.3c storage+download ad5a8d5 + 5.3d admin UI cd9ec36). Closes the full consumer-facing surface of v2 PRODUCT.md §505-506. End-to-end pipeline now operable from the UI: coordinator clicks Generate Report on /(dashboard)/patrols → reportExport.create inserts queued row + BullMQ enqueue + EXPORT_REQUESTED AuditLog → Puppeteer renders + MinIO upload → status=ready → user sees ready badge on /(dashboard)/exports (live-polled every 3s) → user clicks Download → /api/exports/reports/{id}/download Route Handler streams the PDF + writes EXPORT_DOWNLOAD AuditLog. Admin-only Retry recovers stuck/failed exports by resetting state + re-enqueuing (jobId pattern dedupes any stale in-flight job from BullMQ per 5.3b precedent). 5.3d delta: NEW PAGE /(dashboard)/exports/page.tsx (Client Component, 196 LOC) — useSession-based coordinator+ client gate with operator-friendly access-denied surface; trpc.reportExport.list.useQuery with cursor pagination + status filter dropdown; shadcn Table renders <ExportRow> per item; load-more button when nextCursor present; empty + initial-loading skeleton states. NEW COMPONENT exports/export-row.tsx (144 LOC) — Client Component wrapping shadcn TableRow; trpc.reportExport.pollStatus.useQuery with enabled + refetchInterval=3000 ONLY while status in (queued, rendering) — both flip to false for terminal states (ready, failed) so React Query drops the query entirely; initialData seeded from the parent list query to avoid the immediate re-fetch round-trip on first render; trpc.reportExport.getDownloadUrl.useQuery with enabled=true ONLY when currentStatus === "ready" so the URL fetch stays lazy; surfaces Download link on ready, inline error message + Retry button on failed. NEW COMPONENT exports/retry-button.tsx (116 LOC) — admin-only Client Component with useSession role gate (super_admin + site_admin) + adminProcedure enforcement server-side; shadcn Dialog confirm + trpc.reportExport.retry.useMutation + onSuccess invalidates trpc.reportExport.list via utils — mirrors rebuild-tracks-button.tsx 5.2c precedent. NEW COMPONENT exports/status-badge.tsx (64 LOC) — colored chip per ExportStatus: queued=slate-100, rendering=blue-100 with animated pulse dot, ready=green-100, failed=red-100 + dark-mode variants matching users/status-badge.tsx convention. NEW COMPONENT patrols/generate-report-button.tsx (210 LOC) — coordinator+ Client Component dialog with reportType + paperSize selectors, calls trpc.reportExport.create.useMutation, success surface contains a next/link to /exports for tracking the queued export. NEW MUTATION reportExport.retry adminProcedure (+44 LOC in reportExport.ts) — findFirst {id, tenantId} → NOT_FOUND for cross-tenant rows (never leaks existence), prisma.reportExport.update resetting status=queued + filePath/fileSizeBytes/errorMessage/completedAt all to null, enqueuePdfRender re-fire with exportId+tenantId+userId (BullMQ jobId pattern `pdf-render:${exportId}` dedupes stale in-flight jobs per 5.3b lock), prisma.auditLog.create EXPORT_RETRY entry; sequential awaits not $transaction because BullMQ writes outside Postgres scope — mirrors patrol.rebuildTracks (5.2c) + areaBoundary.rebuild (5.1e). NEW SHARED SCHEMA retryReportExportInputSchema (+9 LOC in packages/shared/src/schemas/report-export.ts) — z.object({id: z.string()}) with JSDoc. WIRED patrols/page.tsx (+2 LOC) — GenerateReportButton imported and rendered inside the existing flex header alongside Export CSV/PDF + RebuildTracksButton. Tests: 27 net new vitest cases (7 router retry + 4 retry-button + 7 export-row + 5 exports page + 4 generate-report-button). Web 307→334. Monorepo total 441→468. Verification pre-merge AND on main: web lint 0/0 strict (--max-warnings 0), web typecheck 0 errors, web tests 334/334, jobs tests 122/122 unchanged, storage tests 12/12 unchanged, shared typecheck 0. Main-session Opus 4.7 implementation per STATE.md NEXT-field brief — continues 9-consecutive-clean-main-session-ship streak (5.1d-B → 5.1e → 5.2a → 5.2b → 5.2c → 5.3a → 5.3b → 5.3c → 5.3d). 5.3c prior summary preserved below for context.

# Prior 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3c SHIPPED. 🎯 Item 3 progress: 3 of ~4 sub-batches shipped (5.3a infrastructure + 5.3b queue/processor/worker/wiring + 5.3c packages/storage + Route Handler). Closes the consumer side of the pdf-render pipeline (v2 PRODUCT.md §505-506). NEW PACKAGE packages/storage with `@aws-sdk/client-s3` + `@aws-sdk/lib-storage` (locked in DECISIONS_LOG "MinIO Client Library Choice" — broader S3-compat for the AWS migration arc, NOT the `minio` Node client). Surface: `uploadPdf({bucket,key,body})`, `getPdfReadStream({bucket,key})`, `deletePdf({bucket,key})`, `assertBucketExists(bucket)` (idempotent HEAD→CREATE-on-404), `getExportsBucketName()` env-derived single source of truth, `buildExportKey(tenantId, exportId, at)` env-independent key shape. Lazy S3Client with `forcePathStyle: true` for MinIO compatibility. New Route Handler at apps/web/src/app/api/exports/reports/[id]/download/route.ts (135 LOC, "// Non-tRPC: manual auth required" per security.md L11): requireRouteAuth → 401, rateLimiters.upload.check → 429, prisma.reportExport.findFirst scoped by id+tenantId → 404 on miss (NEVER 403 — no cross-tenant existence leak), status≠"ready"||filePath===null → 404, EXPORT_DOWNLOAD AuditLog written BEFORE stream opens, Readable.toWeb conversion for Web Response body, headers Content-Type application/pdf + Content-Disposition `attachment; filename="${reportType}-${YYYY-MM-DD}.pdf"` + Content-Length from row.fileSizeBytes + Cache-Control no-store. Modified: packages/jobs/src/processors/pdf-render.processor.ts (-22/+18 LOC — dropped local buildStorageFilePath + deriveAppEnv helpers, imports @marine-guardian/storage instead; success path calls `uploadPdf({bucket, key, body: pdfBuffer})` between renderer and final status=ready update; row.filePath now stores KEY only — env-derived bucket pairs back at read time via getExportsBucketName(), DECISIONS_LOG "ReportExport row.filePath Storage Convention"); apps/web/src/server/trpc/routers/reportExport.ts getDownloadUrl now returns `/api/exports/reports/${row.id}/download` (DECISIONS_LOG "ReportExport Download URL Shape" — controlled deviation from v2 §506 tenant-prefixed shape; tenant scope enforced server-side at Route Handler boundary via session.tenantId); inputs.yml storage.enabled flipped false→true + provider=minio (Rule 9 bidirectional governance sync). Tests: storage 0→12 (entirely new package), jobs 119→122 (+3 — upload-call assertion + 2 storage-failure cases), web 298→307 (+9 route handler cases). Monorepo total 417→441. Squash-merge to main pending — feat/5.3c-pdf-storage-download-route holds the single implementation commit + this governance commit. 5.3b prior summary preserved below for context.

# Last updated: 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3b SHIPPED (squash-merge to main as 53c115e). 🎯 Item 3 progress: 2 of ~4 sub-batches shipped. pdf-render BullMQ queue + processor + worker + reportExport.create enqueue wiring + EXPORT_REQUESTED AuditLog all delivered — closes the loop from v2 PRODUCT.md L505-506 where 4.1c left reportExport.create writing status=queued rows without enqueueing. Net new: packages/jobs/src/queues/pdf-render.queue.ts (44 LOC, jobId `pdf-render:${exportId}` NOT scoped by tenant because exportId is globally unique cuid); packages/jobs/src/processors/pdf-render.processor.ts (149 LOC — loads ReportExport by (id, tenantId) via platformPrisma cast same as 5.1c+5.2b, loads Tenant for slug, queued→rendering atomic transition BEFORE renderer call, constructs printUrl from WEB_APP_INTERNAL_URL+tenant.slug+row.reportType+row.id, landscape=true for coverage report only per v2 §771, success path status=ready+filePath+fileSizeBytes+completedAt, failure path status=failed only on LAST attempt via (attemptsMade+1)>=opts.attempts check); packages/jobs/src/workers/pdf-render.worker.ts (54 LOC, concurrency=2 + limiter={max:5,duration:1000} per DECISIONS_LOG "Puppeteer Concurrency + Rate Limiter" lock); packages/jobs/src/queues/types.ts +18 LOC (PdfRenderJobPayload + QUEUE_NAMES.PDF_RENDER + JobPayloadMap entry); packages/jobs/src/{queues,processors,workers}/index.ts barrels updated + root packages/jobs/src/index.ts +17 LOC re-exports the full pdf-render surface plus relocated pdf-renderer-client surface; packages/jobs/src/start-workers.ts +9 LOC registers startPdfRenderWorker as the 7th worker. Wired: apps/web/src/server/trpc/routers/reportExport.ts createReportExport mutation now calls enqueuePdfRender({exportId, tenantId, userId}) AFTER prisma.reportExport.create then writes prisma.auditLog.create with action="EXPORT_REQUESTED" + entityType="ReportExport" + entityId=created.id + changesJson={reportType, paperSize}; sequential await pattern (NOT $transaction-wrapped) matches patrol.rebuildTracks + areaBoundary.rebuild precedent — $transaction can't span Postgres + Valkey. Cross-package relocation per 5.1c+5.2a arc: apps/web/src/server/lib/pdf-renderer-client.ts → packages/jobs/src/lib/pdf-renderer-client.ts (git mv, content unchanged) + test file relocated alongside — required because the new 5.3b processor in packages/jobs cannot import from apps/web (would create circular dep). Client has no app-specific dependencies (only process.env + fetch + Buffer) so the move is mechanically clean. Env var addition: .env.example + .env.dev gain WEB_APP_INTERNAL_URL=http://marine-guardian_dev_app:3000 (internal Docker hostname the renderer container resolves when Puppeteer navigates to /print-render/[tenantSlug]/[reportType]/[exportId]; NOT secret — not in CREDENTIALS.md). Tests: 5 new queue cases + 12 new processor cases + 6 new worker cases + 3 new reportExport.create wiring cases = ~26 net new descriptors authored, minus 1 dummy teardown test removed during afterAll-hook refactor = 25 net new for 5.3b. Verification: web lint 0/0 errors+warnings (strict --max-warnings 0), web typecheck 0 errors, web tests 300 → 298 (-5 relocated pdf-renderer-client moved to jobs + 3 new wiring cases = net -2), jobs lint 0/0 errors+warnings, jobs typecheck 0 errors, jobs tests 92 → 119 (+27 net new in jobs after relocation tally), monorepo total 392 → 417 (+25). 17 files +1037/-5 in feat/5.3b-pdf-render-queue-processor-worker single commit 3e676d7 → squash-merged to main as 53c115e → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation per STATE.md NEXT-field brief — continues 5.1d-B/5.1e/5.2a/5.2b/5.2c/5.3a precedent (7 consecutive clean main-session ships). **Item 3 progress: 2 of ~4 sub-batches shipped (5.3a infrastructure + 5.3b queue/processor/worker/wiring). Remaining: 5.3c (packages/storage MinIO surface + /api/exports/reports/[id]/download streaming Route Handler — also replaces this processor's storage stub with real upload), 5.3d (/[tenant]/exports admin UI list + status polling + retry mutation).** Prior shipment summary follows:

# Prior 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.3a SHIPPED (squash-merged to main as 5837821). OPENS Phase 8 Batch 5 Item 3 (ReportExport pdf-render pipeline). Dev infrastructure only: new deploy/pdf-renderer/ standalone Node + Chromium HTTP service (Dockerfile + package.json + src/server.js 194 LOC native http listener with POST /render guarded by X-PDF-Renderer-Token constant-time compare + puppeteer.launch with --no-sandbox + --disable-dev-shm-usage + --disable-gpu, page.pdf with paperSize/landscape/printBackground/preferCSSPageSize, GET /health). New deploy/compose/dev/docker-compose.pdf-renderer.yml joins external app_network with no host ports (internal Docker network only); start.sh wired dev-only (stage/prod compose deferred to follow-up sub-batch). New apps/web/src/server/lib/pdf-renderer-client.ts typed HTTP wrapper (RenderPdfInput {printUrl, paperSize, landscape, exportId} → Promise<Buffer> or PdfRendererError; consumed by 5.3b BullMQ processor). New apps/web/src/server/lib/service-token-guard.ts Edge-compatible constant-time string compare (no node:crypto.timingSafeEqual — manual diff |= charCodeAt XOR loop). New apps/web/src/app/print-render/[tenantSlug]/[reportType]/[exportId]/page.tsx stub render target (real Coverage Report + Per Area Report content deferred to Batch 6+; URL deviates from v2 L724 /_print/ path because Next.js excludes _-prefixed folders from routing — locked in DECISIONS_LOG "PDF Renderer Internal Route Path"). middleware.ts: service-token guard added for /print-render/* BEFORE the auth chain — returns 401 without valid X-PDF-Renderer-Token header, bypasses login redirect for service-to-service calls only. env.ts: PDF_RENDERER_SERVICE_URL + PDF_RENDERER_SERVICE_TOKEN added to Zod schema as optional (will tighten in 5.3b). .env.{dev,staging,prod,example} + CREDENTIALS.md: PDF_RENDERER_SERVICE_URL + 48-char unique PDF_RENDERER_SERVICE_TOKEN per env (internal Docker hostname pattern http://marine-guardian_{env}_pdf_renderer:4000). DECISIONS_LOG: 4 locked decisions for Item 3 — (1) ReportExport PDF Storage Backend = MinIO bucket marine-guardian-{env}-exports per-tenant prefix, disk fallback rejected; (2) PDF Renderer Service Auth = X-PDF-Renderer-Token header + 48-char shared secret + constant-time compare matching v2 L774-775 spec; (3) Puppeteer Concurrency + Rate Limiter = 5.3b worker concurrency=2 limiter={max:5,duration:1000} (Chromium ~300-500MB resident, scale via container replicas not in-process concurrency); (4) PDF Renderer Internal Route Path = /print-render/ deviating from v2 L724 /_print/ (Next.js private folder exclusion). Verification (on feat branch AND on main after squash-merge): web lint 0/0 errors+warnings (strict --max-warnings 0 · two strict-mode iterations), web typecheck 0 errors, web tests 288 → 300 PASS (+12 net new: 5 pdf-renderer-client + 7 service-token-guard), jobs tests 92/92 unchanged (5.3a is web + standalone-deploy-only — zero @marine-guardian/jobs changes). Total monorepo: 380 → 392 tests (+12). 15 files +739/-0 in feat/5.3a-puppeteer-docker-renderer-client single commit 066ac07 → squash-merged to main as 5837821 → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation per STATE.md NEXT field brief — continues 5.1d-B/5.1e/5.2a/5.2b/5.2c precedent. **Item 3 progress: 1 of ~4 sub-batches shipped (5.3a infrastructure). Remaining: 5.3b (BullMQ pdf-render queue + processor + worker + reportExport.create enqueue wiring), 5.3c (packages/storage MinIO surface + /api/exports/reports/[id]/download streaming Route Handler), 5.3d (/[tenant]/exports admin UI list + polling + retry).** Prior shipment summary follows:

# 2026-05-21 — Phase 8 Batch 5 Sub-batch 5.2c SHIPPED (squash-merge to main pending user authorization). patrol.rebuildTracks adminProcedure tRPC mutation on apps/web/src/server/trpc/routers/patrol.ts — mirrors 5.1e areaBoundary.rebuild shape: optional tenantId input, super_admin cross-tenant gets PLATFORM:PATROL_TRACK_REBUILD action label per security.md superadmin convention, site_admin restricted to own tenant (TRPCError FORBIDDEN on cross-tenant attempt), enqueues one materializePatrolTrack job per state==='open' Patrol via `enqueuePatrolTrackMaterialize` from @marine-guardian/jobs (5.2b queue), writes AuditLog with action/userId/tenantId/entityType='Patrol'/entityId=targetTenantId/changesJson={enqueued, scope}, returns {tenantId, enqueued, action}. Closed patrols (state='done' or 'cancelled') skipped intentionally — immutable post-close and re-fetching wastes ER API quota. 5.2b queue's jobId pattern `patrol-track-materialize:${tenantId}:${patrolId}` (userId excluded) dedupes races between this admin path and any future enqueue path so two operators clicking Rebuild collapse to one ER fetch per patrol. UI: new apps/web/src/app/(dashboard)/patrols/rebuild-tracks-button.tsx Client Component (115 LOC) — useSession-based role gating (hidden for non-admin), shadcn/ui Dialog confirm warning about minutes-long completion for large tenants, trpc.patrol.rebuildTracks.useMutation with inline success/error state, wired into existing patrols page header alongside Export CSV/PDF buttons. 7 net new test cases in `describe('patrol.rebuildTracks (5.2c)')` mirroring 5.1e areaBoundary.rebuild test shape exactly. Required ancillary mock fix in apps/web/src/app/api/exports/patrols/__tests__/route.test.ts — added `platformPrisma: {}` stub to @marine-guardian/db mock because the patrol.ts new import of @marine-guardian/jobs cascades the jobs barrel's eager worker loads (including area-rederive.processor.ts's platformPrisma cast) into the export route's module graph via the existing route.ts → patrol.ts `patrolListFilters` cross-import. Verification (on feat-equivalent local edits AND verified after every step): web lint 0/0 errors+warnings, web typecheck 0 errors, web tests 281→288 PASS (+7 net new exactly matching expected delta), jobs tests 92/92 unchanged (5.2c is web-only — no @marine-guardian/jobs changes). Total monorepo: 373 → 380 tests (+7). 5 files modified/added: patrol.ts router (+57), patrol.test.ts (+157), rebuild-tracks-button.tsx (new, 115), patrols/page.tsx (+2), route.test.ts (+9 mock stub). Main-session Opus 4.7 implementation per STATE.md NEXT field brief — pattern mirrors 5.1e/5.1d/5.2a/5.2b precedent. **Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job) COMPLETE upon merge: 3 of 3 sub-batches shipped (5.2a d2a1dd1 ✅ + 5.2b db50f6c ✅ + 5.2c PENDING_USER_AUTH).** Next: Phase 8 Batch 5 Item 3 — ReportExport pdf-render queue + Puppeteer Docker service + streaming download Route Handler. Origin push state: 10 commits at origin/main (last af7cfd7), 2 commits ahead (d2a1dd1 5.2a + db50f6c 5.2b) + 1 governance commit (203f251) + 5.2c implementation + governance commits all pending user authorization. Prior shipment summary follows:

# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.2b SHIPPED (squash-merged to main as db50f6c). BullMQ `patrol-track-materialize` queue + processor + worker registered in @marine-guardian/jobs, wrapping the 5.2a materializePatrolTrack helper for async invocation from any context. Queue name `'patrol-track-materialize'`; jobId pattern `patrol-track-materialize:${tenantId}:${patrolId}` for BullMQ-layer dedupe at enqueue time (userId deliberately excluded so two rapid enqueues from different operators on the same patrol collapse to one ER fetch). Processor uses same `platformPrisma as unknown as ExtendedPrismaClient` cast pattern as 5.1c area-rederive.processor.ts per [[2026-05-20 🟡 platformPrisma → ExtendedPrismaClient cast pattern for BullMQ processors]]; returns MaterializationResult for BullMQ result storage so 5.2c admin UI can surface skipReason. Worker registered with limiter `{max:20, duration:1000}` and concurrency PATROL_TRACK_MATERIALIZE_CONCURRENCY=5 — intentionally tighter than 5.1c's 50/sec because the ER `/subject/{id}/tracks/` endpoint is network-bound, carries higher per-job cost (potential GeoJSON with thousands of coordinates), and typically has stricter vendor-side rate limits than the events/patrols endpoints. Registered as 6th worker in start-workers.ts alongside er-sync/alerts/email/maintenance/area-rederive. Type registry updated: PatrolTrackMaterializeJobPayload + QUEUE_NAMES.PATROL_TRACK_MATERIALIZE + JobPayloadMap entry. Wired into all barrels (queues/processors/workers/start-workers/root index.ts). NO AuditLog write at processor level — automatic materialization has no user context; 5.2c admin manual-rebuild owns AuditLog per Option A scope split. Verification: jobs lint 0/0, jobs typecheck 0, jobs tests 71→92 PASS (+21 net new: 5 queue + 8 processor + 8 worker), web lint 0/0, web typecheck 0, web tests 281/281 unchanged. Total monorepo: 352 → 373 tests (+21). 12 files changed, +646 / -0 in feat/5.2b-patrol-track-materialize-queue single commit → squash-merged to main as db50f6c → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation continues per STATE.md 5.1e/5.1d/5.2a post-mortem recommendation. **Phase 8 Batch 5 Item 2 progress: 2 of 3 sub-batches shipped (5.2a d2a1dd1 ✅ + 5.2b db50f6c ✅); 1 remains (5.2c patrol.rebuildTracks admin tRPC mutation + UI stub button).** **DEFERRED**: er-sync.processor inline enqueue after patrol upsert — same precondition as 5.1d Area A (no tests exist for er-sync.processor.ts yet); pickup path bootstraps the test harness in a precursor sub-batch then adds the inline enqueue cleanly. Both 5.1d Area A and 5.2b inline enqueue can share that same test-infra investment. Origin push state: 10 commits pushed earlier this session bringing origin/main current to af7cfd7; 2 new commits (d2a1dd1 5.2a + db50f6c 5.2b) + this governance commit await user authorization. Prior shipment summary follows:

# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.2a SHIPPED (squash-merged to main as d2a1dd1; 9 prior commits also pushed to origin/main earlier this session). Real implementation of `materializePatrolTrack(prisma, patrolId): Promise<MaterializationResult>` in `packages/jobs/src/lib/patrol-track-materialization.ts` — closes the loop on the 4.1b PatrolTrack scaffold stub. Loads patrol + first segment, resolves tenant ER credentials (decrypted), prefers `earthrangerTrackToken` when present, fetches GPS track from EarthRanger `/subject/{leaderErId}/tracks/?since=&until=` via new `EarthRangerClient.fetchSubjectTracks(subjectId, since, until)` method (constructor extended with optional `trackToken?: string` arg — defaults to main token, backward compatible), summarises features into `pointCount` (sum across LineStrings) + `hasTimestamps` (defensive: any feature with missing or mismatched `coordinateProperties.times` flips whole track to false) + `lastTrackTime` (max parseable timestamp), atomically upserts `PatrolTrack` on patrolId (unique) with `source="er_api"` + `fetchedAt=now`. Idempotent per v2 PRODUCT.md §1043 refresh predicate. Three skip paths return `{skipped: true, skipReason}` without throwing: `no_segment` (patrol has no segments OR no usable time references), `no_leader` (segment[0].leaderErId is null), `no_credentials` (tenant missing earthrangerUrl or earthrangerDasToken). Time range fallback order: `segment.actualStart/End → scheduledStart/End → patrol.startTime/endTime → new Date()` for `until`. NO AuditLog write — automatic materialization has no user; deferred to 5.2c admin manual-rebuild where ctx.session.userId is available. Helper relocated via `git mv` from `apps/web/src/server/sync/patrol-track-materialization.ts` (35 LOC stub) → `packages/jobs/src/lib/patrol-track-materialization.ts` (301 LOC real impl) — same architectural arc as 5.1c area-derivation relocation. 14 vitest cases (happy path, all 3 skip paths, both token paths, multi-feature aggregation, timestamp edge cases, time-range fallback). Verification: jobs lint 0/0, jobs typecheck 0, jobs tests 71/71 PASS (57 baseline + 14 net), web typecheck 0 (cross-package import surface intact via @marine-guardian/jobs root barrel), web lint 0/0 with --max-warnings 0, web tests 281/281 PASS unchanged (5.2a is jobs-only). Total monorepo: 338 → 352 tests (+14). 6 files changed, +910/-39 in feat/5.2a-patrol-track-materialization single commit b38d951 → squash-merged to main as d2a1dd1 → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation continues per STATE.md 5.1e/5.1d post-mortem recommendation while dispatch-mechanism issue remains unresolved. Two errors caught + fixed before merge: (1) ESLint no-unnecessary-type-assertion on cast that became redundant once binding was typed explicitly; (2) test helper `makeTenant` was using nullish coalescing `??` for override-vs-default selection — `{ earthrangerUrl: null }` override silently resolved to default because `null ?? "enc:..."` evaluates to `"enc:..."`. Fixed by switching to `in` operator pattern (`"earthrangerUrl" in overrides ? overrides.earthrangerUrl : default`). Logged 🟡 fix entry to lessons.md as recurring pattern for any test factory accepting nullable Partial<T> overrides. **Phase 8 Batch 5 Item 2 progress: 1 of 3 sub-batches shipped (5.2a); 2 remain (5.2b BullMQ queue/processor/worker scaffold; 5.2c patrol.rebuildTracks admin tRPC mutation + UI stub).** **DEFERRED**: er-sync.processor inline enqueue after patrol upsert — same precondition as 5.1d Area A (no tests exist for er-sync.processor.ts yet); pickup path bootstraps the test harness in a precursor sub-batch then adds the inline enqueue cleanly. User selected this deferral via AskUserQuestion at session start. Origin push state: 9 commits pushed earlier this session bringing origin/main current to af7cfd7 (the 5.1e governance commit); 1 new commit (d2a1dd1 5.2a) + 1 pending governance commit await user authorization. Prior shipment summary follows:

# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1e SHIPPED (squash-merged to main as 88744ed). Item 1 (AreaBoundary derivation algorithm) COMPLETE for the scope under its remit. areaBoundary.rebuild adminProcedure tRPC mutation reuses 5.1d fanOutAreaRederive helper to fan jobs into the 5.1c area-rederive BullMQ queue (50/sec rate limiter) for every Event + Patrol + FuelEntry in the target tenant, writes an immutable AuditLog row with action PLATFORM:AREA_REBUILD (super_admin cross-tenant per security.md superadmin convention) or AREA_REBUILD (own-tenant), and returns {tenantId, enqueued, action}. RBAC: super_admin may target any tenantId; site_admin must omit or match own (cross-tenant attempt → FORBIDDEN). UI: minimal stub button on the existing /(dashboard)/patrol-areas placeholder page with shadcn/ui Dialog confirm + inline success/error feedback + useSession-based role gating (no toast lib in stack — used create-user-dialog.tsx pattern). Verification (on feat branch AND on main after squash-merge): web lint 0/0, web typecheck 0, web tests 281/281 (was 274 baseline + 7 net new in new `describe('areaBoundary.rebuild (5.1e)')`: rejects operator / rejects field_coordinator / site_admin own-tenant happy path with AuditLog write + tenant-scoped fan-out / site_admin cross-tenant FORBIDDEN / super_admin own-tenant AREA_REBUILD / super_admin cross-tenant PLATFORM:AREA_REBUILD with fan-out targeting specified tenant / empty-tenant still writes AuditLog with enqueued=0). jobs tests 57/57 unchanged (web-only sub-batch). 4 files changed +309/-1 in feat/5.1e-area-boundary-rebuild-admin branch (single commit 9b382a1 → squash-merge as 88744ed → branch force-deleted per Rule 23). Scope adjustment from STATE.md NEXT field brief: brief called for "UI button on /admin/area-boundaries" but that page does not exist (no admin area-boundary CRUD page has been built yet — patrol-areas is a stub). User chose Backend-only + minimal stub via AskUserQuestion — same precondition-deferral pattern as 5.1d Area A. Dispatch path skipped: 5.1e was small enough (~50K actual incl. orientation) for main-session safely and skipping dispatch avoided the 5.1d dispatch-stall risk. Main-session Opus 4.7 implementation took ~25 minutes including governance. Phase 8 Batch 5 Item 1 shipment ledger: 5.1a 6686042 + 5.1b c4a14a9 + 5.1c 3b4145f + 5.1d-B 068d32f + 5.1e 88744ed = Item 1 COMPLETE for scope under remit. 5.1d Area A (sync inline re-derive on areaName change — v2 L546) remains DEFERRED — area_name not yet ingested from ER. Origin push of 9 commits pending user authorization. Next: Phase 8 Batch 5 Item 2 (PatrolTrack Materialization job). Prior shipment summary follows:
#
# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1c SHIPPED (squash-merged to main as 3b4145f; origin push pending). BullMQ area-rederive queue + processor + worker registered in packages/jobs with concurrency limiter `{max:50, duration:1000}` per v2 spec L545. Three new files in packages/jobs (queue + processor + worker) + JobData type `{entity: 'event'|'patrol'|'fuelEntry'; id: string; tenantId: string}` registered in queues/types.ts + AREA_REDERIVE_CONCURRENCY=10 exported constant for production tuning. Architectural relocation: applyAreaDerivation helper moved from apps/web/src/server/sync/area-derivation.ts → packages/jobs/src/lib/area-derivation.ts (97% git rename similarity, blame preserved) because 5.1d sync engine processor lives in @marine-guardian/jobs and apps→packages dependency direction is one-way. 5.1e admin tRPC will import via `@marine-guardian/jobs` root barrel re-export. base-worker.ts extended with backward-compatible optional limiter WorkerOption (existing 4 workers unaffected). Processor uses `platformPrisma as unknown as ExtendedPrismaClient` cast with explanatory comment — worker passes explicit tenantId so L6 tenant-guard is a no-op + encryption extension unused for plaintext columns. Verification: jobs lint 0/0, jobs typecheck 0, jobs tests 57/57 PASS (was 35 + 22 net: 8 relocated + 14 new), web typecheck 0, web tests 268/268 PASS (was 276; -8 relocated). Total monorepo test count unchanged at 325. Third consecutive clean Opus 4.7 dispatch via Agent(model: "opus") — doctrine [[feedback_marine_guardian_opus_executor_default]] holds. 17 files changed, +617 / -1 in feat/5.1c-area-rederive-queue branch (squash-merged as 3b4145f + force-deleted per Rule 23 standard squash-merge cleanup). Phase 8 Batch 5 Item 1 progress: 3 of 5 shipped (5.1a 6686042 + 5.1b c4a14a9 + 5.1c 3b4145f); 2 remain (5.1d sync inline re-derive + AreaBoundary CUD enqueue, 5.1e admin manual-rebuild). 5.1d unblocked. Prior shipment summary follows:
#
# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1b SHIPPED (squash-merged to main as c4a14a9; origin push pending). applyAreaDerivation persistence helper INITIALLY shipped at apps/web/src/server/sync/area-derivation.ts — RELOCATED to packages/jobs/src/lib/area-derivation.ts during 5.1c shipment for correct workspace dependency direction (apps→packages, not inverse). Helper wraps 5.1a pure deriveArea with DB I/O — per-entity branching (Event has coords for nearest-fallback; Patrol+FuelEntry name-only because schema has no coords; FuelEntry write omits areaDerivedAt because schema has no column). Architect-confirmed Option A: no AuditLog write in 5.1b (deferred to 5.1e where ctx.session.userId is available). Single callable used by 5.1c BullMQ processor + 5.1d sync engine + 5.1e admin manual-rebuild — eliminates risk of three downstream callers re-implementing load→derive→write divergently. 8 vitest cases + lint 0/0 + typecheck 0 errors + web tests 276/276 at 5.1b ship time; tests + impl relocated to packages/jobs at 5.1c ship time with all 8 cases still passing. Prior shipment summary follows:
#
# 2026-05-20 — Phase 8 Batch 5 Sub-batch 5.1a SHIPPED (squash-merged to main as 6686042). Area-derivation pure functions in packages/shared/src/lib/area-derivation/: matchByName (exact name + alias lookup, case-insensitive, trimmed, filters isEnabled=true), findNearestBoundary (edge-distance to nearest Polygon outer ring OR LineString segment via equirectangular projection + haversine great-circle, default threshold 5km per NEAREST_BOUNDARY_THRESHOLD_KM, returns null on no-match/invalid/empty), deriveArea composite (name first → nearest fallback → NULL preserves area_name verbatim) per v2 spec L531-L561. AreaBoundaryForDerivation structural subset type decouples pure functions from Prisma client; DeriveAreaResult.matchedVia exposes name/nearest provenance for 5.1b AuditLog observability per L561. Zero external geo deps — haversineKm + pointToSegmentDistanceKm helpers implemented by hand and exported for testability. Vitest bootstrap added to packages/shared (previously had no test runner — copied jobs package convention). 55 new tests across 3 files cover all v2 spec L561 mandate cases (exact match, alias match, case difference, trimming, threshold within/at/beyond, malformed geometry skip with console.warn, invalid lat/lon, LineString geometry, name-wins-over-nearest precedence). Verification: typecheck 0 (packages/shared) / lint 5/5 0 warnings / vitest 350 passing across 38+3 files (workspace was 295; +55 new in shared). 11 files changed, +1019 -2 in feat/area-derivation-functions branch (squash-merged + deleted). Opus 4.7 executor reported clean DONE on first dispatch — no salvage needed, contrasting with 4.1e executor's silent mid-brief drop. 🎯 BATCH 4 COMPLETE shipment ledger remains: 4.1a 56fb3fa → 4.1b cfe9195 → 4.1c e972d82 → 4.1d d32e618 → 4.1e 6687112. Phase 8 Batch 5 Item 1 progress: 1 of 5 sub-batches shipped (5.1a); 4 remain (5.1b applyAreaDerivation persistence helper, 5.1c BullMQ area-rederive queue+processor+worker, 5.1d sync-engine inline re-derive + AreaBoundary CUD enqueue, 5.1e admin manual-rebuild tRPC mutation + UI button).
# Prior 2026-05-19 — Phase 8 Batch 4 Sub-batch 4.1c SHIPPED to main (commit e972d82, squash-merged from feat/fuel-entry-report-export-scaffold which was branched off f1b188a). Adds FuelEntry + ReportExport v2 foundation tables (CC-native, additive, non-breaking). Schema: 3 new enums (ReportType{coverage,area,consolidated,detailed,rangers,patrol_filtered} + PaperSize{A4,Letter,Legal} + ReportExportStatus{queued,rendering,ready,failed}) + 2 new models (FuelEntry with cuid PK, tenant_id FK RESTRICT, area_name string, area_boundary_id nullable FK to AreaBoundary with SET NULL preserving area_name snapshot, date_received DATE, liters DECIMAL(12,3), total_price DECIMAL(14,2), currency string, receipt_photo_url nullable, notes nullable, logged_by_user_id FK to users RESTRICT, 3 indexes; ReportExport with cuid PK, tenant_id FK RESTRICT, requested_by_user_id FK to users RESTRICT, report_type + paper_size + status enums, params_json JSONB, file_path/file_size_bytes/error_message nullable, completed_at nullable, 3 indexes) + back-relations on Tenant (fuelEntries + reportExports), User (loggedFuelEntries + requestedReportExports with named relations), AreaBoundary (fuelEntries). Migrations (2 separate per task spec): 20260519231300_add_fuel_entry/{migration,down}.sql (DROP TABLE only — no enums) and 20260519231301_add_report_export/{migration,down}.sql (DROP TABLE then drop 3 enum types in dependency order). Routers: fuelEntry (list/getById/create [operator+]/update [ownership-checked]/updateAny [coordinator+ no ownership check]/delete [site_admin+] per RBAC matrix spec §405-408 — split into two update procedures because single procedure would conflate ownership + role checks) + reportExport (list/getById/pollStatus/create [coordinator+ inserts queued row, DOES NOT enqueue BullMQ yet]/getDownloadUrl [returns null when not ready, canonical /[tenantId]/exports/[id]/download URL when status=ready, throws NOT_FOUND for missing rows — no cross-tenant existence leak]). Shared schemas: fuel-entry.ts + report-export.ts + 3 enum additions to enums.ts; both exported through index.ts. FuelEntry decimal handling: stored as Postgres DECIMAL, serialized by Prisma as string, Zod input validates positive decimal regex + Number()>0 refinement (no float precision loss round-tripping from UI). Currency snapshot per spec §196: read from prisma.tenant.findFirst at create time, not from session (session doesn't carry currency). 21 new tests across 2 files (12 fuelEntry + 9 reportExport using the partial<T> helper pattern from 2026-05-08 lesson). Verification: typecheck 0 / lint 0 / vitest 260 passing (was 239 baseline + 21 new; zero regressions across 37 test files). NOTE: packages/db Node-types failures pre-existing — identical baseline shipped on 4.1a/4.1b. Two new lessons added: (1) 🔴 "Prisma migrate dev sweeps multiple new models into one migration" — when both new models are in schema.prisma at migrate-dev time, --name flag does NOT control which model lands in which migration; agent split by hand after sweep; (2) 🟡 "Zod cuid() strict format check rejects synthetic short strings" — test fixtures need 25-char cuid-shaped strings starting with 'c', not bare 'ab-1'. Phase 8 Batch 4 progress: 3 of 5 sub-batches shipped (4.1a + 4.1b + 4.1c); 2 remain (4.1d NotificationRecipient split with data migration, 4.1e Event/Patrol attribution FKs + Tenant ArcGIS fields).
# Prior 2026-05-17 — Phase 8 Batch 2 Item 5 (Realtime SSE) CODE COMPLETE on feat/sse-events-and-hardening (3 commits awaiting squash-merge). SSE-1 + SSE-2 already shipped to main commit 3710472 on 2026-05-17 morning. SSE-3 splits the UI/hardening work across the open branch: 3a (d15631f) mounts RealtimeProvider in the dashboard layout + adds NotificationBell to the header + invalidates the /notifications page tRPC queries on every new realtime event; 3b (dac6bfd) drops the 30s tRPC `refetchInterval` from the sidebar's unreadCount query and replaces it with useEffect-driven invalidation keyed on the notification-store's notifications.length — zero background polling between events, near-instant badge updates on event arrival; 3c (b0b768a) hardens the SSE Route Handler with two additions: (a) per-userId rate-limit via existing `rateLimiters.api` 120/min tier (try/check after requireRouteAuth → 429 NextResponse on throw, skips subscribe entirely on rate-limit), (b) new module apps/web/src/server/lib/sse-metrics.ts with process-local active-connection counter + reconnect counter (5 getters/mutators + __resetMetricsForTests, monotonic with clamp-at-0 decrement), wired into route.ts via incrementConnection() after successful subscribeToChannel + decrementConnection() in cancel() gated on connectionTracked flag to keep increment/decrement paired across subscribe-failure path. Test deltas: 128 → 140 (+12: 6 sse-metrics module tests + 6 new route-handler tests covering increment, decrement, no-increment-on-subscribe-rejection, rate-limit-token-is-userId, 429-skips-subscribe, auth-failure-short-circuits-rate-limit). All gates green: typecheck clean, lint clean (zero in-session fixups for 3c), test 140/140, build registers /api/stream/notifications as 161 B ƒ Dynamic Route (unchanged). Deferred per task scope: server-side reconnect attribution (needs client session-correlation header), multi-instance metric aggregation (needs Redis-backed counter), metric exposure endpoint (no public surface yet — getters are importable today). Phase 8 Batch 2 backlog after 3c squash-merges: 0 items — Batch 2 COMPLETE. Prior 2026-05-13: Item 4 SS-4 — Notifications + Alert History Export Route Handlers shipped on feat/exports-audit-views (squash-merge pending). Two routes in one branch: apps/web/src/app/api/exports/notifications/route.ts (GET handler — tenant+user-scoped findMany with isRead + notificationType filters; 9-column NotificationRow with flattened alertRuleName/eventTitle/patrolTitle relations) AND apps/web/src/app/api/exports/alert-history/route.ts (GET handler — tenant-scoped findMany with alertRuleId filter; 8-column AlertHistoryRow with both immutable snapshot columns AND current relation values falling back to "(deleted)"). 6 tests each (12 new). Modified: notification.ts + alertHistory.ts routers exported `notificationListFilters` + `alertHistoryListFilters` z.objects — single source of truth. /notifications/page.tsx + /alerts/history/page.tsx headers gained Export CSV + Export PDF buttons (same `<Button asChild><a download href={buildExportUrl(entity, filters, format)}>` shape; notifications passes current typeFilter so export matches view). Full web suite: 84/84 (was 72 — added 12), typecheck 6/6 clean, lint 5/5 clean (zero fixups — SS-3 lint patterns carried forward). Build registers all 5 export Dynamic Routes (/api/exports/events|patrols|alert-rules|notifications|alert-history at 159 B / 103 kB each). No new lessons. After SS-4 fully ships, Item 4 (PDF/CSV Exports) is COMPLETE. Only Real-time SSE remains in Phase 8 Batch 2 (Tier 3, mandatory split ≥3 sub-sessions per memory-governance.md §1). Prior 2026-05-13: SS-3 Alert Rules Export shipped on feat/exports-alert-rules (squash-merge pending). New: apps/web/src/app/api/exports/alert-rules/route.ts (GET handler — same shape as SS-1 events + SS-2 patrols handlers with prisma.alertRule.findMany + creator include + 9-column AlertRuleRow: id, name, condition=JSON.stringify(conditionJson), channels=notificationChannels.join(", "), isActive="true"/"false", creatorName, creatorId, createdAt ISO, updatedAt ISO), 6 new route tests (CSV byte-level BOM + tenant scoping + channels-joined-comma + escaped-JSON conditionJson assertions, PDF content-type, 401, 413 overflow + no audit row, DATA_EXPORT audit shape with 64-hex filterHash + entityType="alert-rules", isActive filter boolean-coercion propagation). Modified: alertRule.ts router exported `alertRuleListFilters` z.object ({isActive}) — single source of truth; alertRuleRouter.list now uses `alertRuleListFilters.extend({cursor, limit})`. alerts/page.tsx: inserted Export CSV + Export PDF buttons (same `<Button asChild><a download href={buildExportUrl("alert-rules", {}, format)}>` shape) inside the existing header flex-gap-2 group before View History + New Rule. Full web suite: 72/72 (was 66 — added 6), typecheck 6/6 clean, lint 5/5 clean (one trivial fixup — removed unnecessary `?.` + `??` on r.creator.fullName since Prisma include with required FK narrows to non-null), build registers /api/exports/alert-rules alongside /api/exports/events and /api/exports/patrols as ƒ Dynamic Route. No new lessons — SS-1's BOM-via-arrayBuffer 🟡 fix and SS-2's lint patterns carried forward verbatim. Plan deviation (1, minor): used JSON.stringify(conditionJson) for the condition column over the plan's "priority>=200" example because conditionJson is an opaque Json column whose structure varies; raw JSON is the honest representation. Column count 9 (one fewer than SS-1/SS-2's 10) — AlertRule is a leaner entity, padding would be artificial. SS-4 (Notifications + Alert History combined) remains as the final Item 4 sub-session (~22K, branch feat/exports-audit-views). Phase 8 Batch 2 backlog after Item 4 fully ships: 1 item — real-time SSE (Tier 3, mandatory split per memory-governance.md §1).
# ---

## Status: Phase 8 Batch 1 complete. Phase 8 Batch 2 — Alert Rule Evaluation Engine processor body + er-sync create-path enqueue integration shipped. Engine fires on every newly-synced EarthRanger event. All Docker services healthy.

### Schema Delta Fixes (feat/schema-delta-fixes — merged to main)
- [x] PatrolType enum: seabourn → seaborne (Prisma, shared types/schemas, seed, tRPC routers)
- [x] Patrol.boatName: nullable String field added
- [x] Tenant.currency: String(3) with default 'IDR' added
- [x] PatrolSchedule.tenantId: direct tenant_id column added, tRPC router uses direct scoping
- [x] Migration: 20260504162834_schema_delta_fixes (manual SQL — rename enum, add columns, add FK)
- [x] ESLint: next-env.d.ts ignored, webpack externals scoped eslint-disable

---

### Root Config (Part 1)
- [x] pnpm-workspace.yaml
- [x] turbo.json
- [x] tsconfig.base.json (strict: true, noUncheckedIndexedAccess, exactOptionalPropertyTypes)
- [x] .editorconfig, .prettierrc, eslint.config.mjs (ESLint 9.x flat config)
- [x] .gitignore (final — includes CREDENTIALS.md, .env.*, .specstory/, .code-review-graph/)
- [x] .nvmrc (Node 22)
- [x] package.json (turbo scripts + devDependencies)
- [x] pnpm-lock.yaml

### Spec Files (Phase 3)
- [x] inputs.yml (version 3 — 18 entities, 19 modules, 4 roles)
- [x] inputs.schema.json
- [x] .env.dev (gitignored — generated credentials, non-standard ports)
- [x] .env.staging (gitignored — standard ports, Traefik vars)
- [x] .env.prod (gitignored — standard ports, Traefik vars)
- [x] .env.example (committed — placeholder template only)
- [x] scripts/sync-credentials-to-env.sh
- [x] CREDENTIALS.md (gitignored — master credential file with all service passwords)

### packages/shared (Part 2)
- [x] 19 TypeScript interfaces: Tenant, User, Patrol, PatrolArea, PatrolSegment, PatrolSchedule,
      AccompanyingRanger, Event, EventType, Observation, Subject, SubjectGroup, KnownRanger,
      AlertRule, Notification, SyncLog, AuditLog, Enums, index barrel
- [x] 19 Zod schemas with create/update variants (matching TypeScript interfaces)
- [x] All schemas re-export from packages/shared/src/schemas/index.ts

### packages/api-client (Part 2)
- [x] Typed tRPC client factory with superjson transformer
- [x] Exports createTRPCClient() for all apps to consume

### packages/db (Part 3)
- [x] 18 Prisma models (all entities from inputs.yml)
- [x] 13 enums (PatrolStatus, EventCategory, ObservationType, SubjectStatus, AlertSeverity,
      AlertStatus, NotificationType, NotificationStatus, SyncStatus, UserRole, TenantStatus,
      PatrolScheduleFrequency, SubjectSex)
- [x] Init migration (up + down SQL) — tenantId NOT NULL on all entities (multi-tenant)
- [x] PostgreSQL RLS enabled on all tenant-scoped tables (L2 — multi mode)
- [x] seed.ts — webmaster super_admin account + demo tenant + sample data
- [x] src/client.ts — Prisma client with L6 tenant-guard extension
- [x] src/middleware/tenant-guard.ts — L6 Prisma $allOperations auto-inject tenantId
- [x] src/middleware/encryption.ts — AES-256-GCM field encryption for sensitive columns
- [x] src/helpers/audit.ts — L5 writeAuditLog() immutable audit trail helper
- [x] src/helpers/rls.ts — L2 withTenant() PostgreSQL RLS transaction wrapper

### packages/ui (Part 4)
- [x] Tailwind CSS configuration (tailwind.config.ts)
- [x] Shared globals.css (CSS custom properties — shadcn/ui color tokens)
- [x] src/lib/utils.ts (cn() helper)
- [x] shadcn/ui components.json present in apps/web (New York style, CSS variables)

### packages/jobs (Part 4 + Phase 7)
- [x] BullMQ connection factory (src/connection.ts)
- [x] Queue factory with standard job options (src/queues/queue-factory.ts)
- [x] 5 typed queues: alerts, email, er-sync (EarthRanger sync), maintenance, index barrel
- [x] Base worker class with tenantId validation + error handling (src/workers/base-worker.ts)
- [x] Workers index barrel (src/workers/index.ts)
- [x] er-sync processor (src/processors/er-sync.processor.ts) — 5 sync functions: syncEventTypes, syncSubjects, syncEvents, syncPatrols, syncObservations. SyncLog lifecycle (running→success/failed), tenant validation, credential decryption, Prisma JSON nullable via toJsonOrNull() helper
- [x] er-sync processor tests (src/__tests__/er-sync.processor.test.ts) — 7 tests: tenant not configured, sync event_types, sync subjects, sync events, sync patrols, sync observations, API error → failed SyncLog

### apps/web (Part 5 — Next.js 15, App Router)
- [x] next.config.ts (output: standalone, 7 HTTP security headers, Content-Security-Policy,
      serverExternalPackages: ["bcrypt"], node: URI scheme webpack external handler)
- [x] tailwind.config.ts + postcss.config.js
- [x] apps/web/src/env.ts (Zod-validated env vars, server + client split)
- [x] Auth.js v5 config (Credentials provider + bcrypt), session with roles + tenantId
- [x] apps/web/src/server/auth/auth.config.ts (edge-compatible auth — JWT only, no bcrypt/prisma)
- [x] tRPC handler at /api/trpc/[trpc]
- [x] Health endpoint at /api/health (returns 200 + build metadata)
- [x] src/middleware.ts (tenant resolution + auth guard + rate limiting)
- [x] src/server/lib/rate-limit.ts (LRU-cache, 4 tiers: public/auth/api/upload)
- [x] src/server/lib/sanitize.ts (DOMPurify XSS sanitizer — sanitize() + sanitizePlainText())
- [x] L3 RBAC middleware (src/server/trpc/middleware/rbac.ts)
- [x] L1 tenant scoping middleware (src/server/trpc/middleware/tenant.ts)
- [x] tRPC context (userId, roles, tenantId from session)
- [x] 13 tRPC routers: alertRule, event, eventType, knownRanger, notification, observation,
      patrol, patrolArea, patrolSchedule, subject, syncLog, user, index barrel
- [x] 13 dashboard pages (App Router):
      /dashboard, /map, /patrols, /patrol-areas, /events, /observations,
      /subjects, /alerts, /notifications, /users, /settings, /sync, /login
- [x] Dashboard layout with sidebar + header components
- [x] shadcn/ui base components: button, card, badge, input, label
- [x] i18n messages: en.json, id.json (Bahasa Indonesia), ms.json (Malay)
- [x] Dockerfile (multi-stage: deps → builder → runner, node:22-alpine)
- [x] .dockerignore

### deploy/compose (Part 7)
- [x] deploy/compose/start.sh (all-in-one dev/stage/prod startup, --build for dev)
- [x] deploy/compose/push.sh (dev→hub→staging→prod promotion pipeline)

- [x] deploy/compose/dev/docker-compose.db.yml (PostgreSQL 16 + PgBouncer)
- [x] deploy/compose/dev/docker-compose.cache.yml (Valkey 7)
- [x] deploy/compose/dev/docker-compose.infra.yml (MailHog dev email)
- [x] deploy/compose/dev/docker-compose.pgadmin.yml (pgAdmin 4)
- [x] deploy/compose/dev/docker-compose.app.yml (builds from source — has build: key)
- [x] deploy/compose/dev/pgadmin-servers.json (pre-configured server registration)

- [x] deploy/compose/stage/docker-compose.db.yml
- [x] deploy/compose/stage/docker-compose.cache.yml
- [x] deploy/compose/stage/docker-compose.pgadmin.yml
- [x] deploy/compose/stage/docker-compose.app.yml (pulls from Docker Hub, NO build:, Traefik labels)
- [x] deploy/compose/stage/pgadmin-servers.json

- [x] deploy/compose/prod/docker-compose.db.yml
- [x] deploy/compose/prod/docker-compose.cache.yml
- [x] deploy/compose/prod/docker-compose.pgadmin.yml
- [x] deploy/compose/prod/docker-compose.app.yml (pulls from Docker Hub, NO build:, Traefik labels)
- [x] deploy/compose/prod/pgadmin-servers.json

- [x] .socraticodecontextartifacts.json (4 entries: database-schema, implementation-map,
      decisions-log, product-definition)

### tools/ (Part 7)
- [x] tools/validate-inputs.mjs (validates inputs.yml against inputs.schema.json)
- [x] tools/check-env.mjs (validates required env vars present in .env.dev)
- [x] tools/check-product-sync.mjs (PRODUCT.md ↔ inputs.yml alignment + private tag check)
- [x] tools/hydration-lint.mjs (SSR hydration mismatch detection)

### scripts/ (Part 7)
- [x] scripts/log-lesson.sh (human quick-log to lessons.md in Rule 18 typed format)
- [x] scripts/sync-credentials-to-env.sh (propagate CREDENTIALS.md → .env files)
- [x] COMMANDS.md (master dev command reference)

### CI / GitHub Actions (Part 8)
- [x] .github/workflows/ci.yml
      - governance job: validate-inputs + check-env + check-product-sync
      - quality matrix: lint, typecheck, test, build (via pnpm turbo run)
      - security job: pnpm audit --audit-level=high (blocks on HIGH/CRITICAL CVEs)
- [x] .github/workflows/docker-publish.yml
      - triggers on push to main + workflow_dispatch
      - multi-platform build: linux/amd64 + linux/arm64
      - pushes tags: latest, staging-latest, sha-{short}, branch ref
      - image: secrets.DOCKERHUB_USERNAME/marine-guardian
      - Dockerfile: ./apps/web/Dockerfile

### Governance docs (all Parts)
- [x] MANIFEST.txt (complete file inventory across all 8 Parts)
- [x] docs/PRODUCT.md (human-owned — V31 spec)
- [x] docs/DESIGN.md (extracted design aesthetic — Sentry/dark palette)
- [x] docs/CHANGELOG_AI.md (agent attribution log — Parts 1-8)
- [x] docs/DECISIONS_LOG.md (13 locked decisions)
- [x] docs/IMPLEMENTATION_MAP.md (this file)
- [x] .cline/STATE.md (phase position — updated after every task)
- [x] .cline/memory/lessons.md (typed entries — 🔴 gotchas and 🟤 decisions)
- [x] .cline/memory/agent-log.md (running action log)
- [x] project.memory.md
- [x] .github/skills/spec-driven-core/SKILL.md
- [x] .vscode/mcp.json (SocratiCode + Context7 + shadcn MCP servers)

---

### Security Layers Active
- [x] L1 — tRPC tenantId scoping (multi-tenant — required in all resolvers)
- [x] L2 — PostgreSQL RLS (active — SET LOCAL app.current_tenant_id via withTenant())
- [x] L3 — RBAC middleware (requireRole() on all protected tRPC procedures)
- [x] L4 — PgBouncer (connection pooling — pool limits per COMPOSE_PROJECT_NAME)
- [x] L5 — Immutable AuditLog (writeAuditLog() on all mutations)
- [x] L6 — Prisma $allOperations tenant-guard (auto-injects tenantId, all operations)
- [x] HTTP Security Headers (7 headers in next.config.ts — all routes)
- [x] Rate limiting (4 tiers: public 30/min, auth 10/min, api 120/min, upload 20/min)
- [x] DOMPurify XSS sanitizer (sanitize() + sanitizePlainText() in sanitize.ts)
- [x] AES-256-GCM field encryption (sensitive columns via Prisma extension)

---

### Ports (dev — all non-standard, from inputs.yml ports.dev)
- App:            45204
- PostgreSQL:     44027
- PgBouncer:      44028
- Valkey:         44029
- MinIO API:      44030
- MinIO Console:  44031
- MailHog SMTP:   44032
- MailHog UI:     44033
- pgAdmin:        44034

---

### Phase 6 — Docker Services + Visual QA (2026-05-05)
- [x] All Docker services started via deploy/compose/start.sh dev up -d
- [x] PostgreSQL 16: port 45194 ✅ healthy
- [x] PgBouncer: port 45195 ✅ healthy
- [x] Valkey 7: port 45196 ✅ healthy
- [x] MinIO: port 45197 (API), 45198 (console) ✅ healthy
- [x] MailHog: port 45199 (SMTP), 45200 (UI) ✅ running
- [x] pgAdmin 4: port 45201 ✅ healthy
- [x] App (Next.js): port 45204 ✅ healthy
- [x] Worker: 4 BullMQ workers stable (er-sync, alerts, email, maintenance) — Docker internal networking fix applied
- [x] 2 migrations applied: init + schema_delta_fixes
- [x] Seed data: 1 tenant, webmaster super_admin, admin user, 3 event types, 1 patrol area
- [x] Visual QA: /api/health → 200, /login → 200, /dashboard → 302 redirect (auth working)

Docker fixes applied (6 total):
1. PgBouncer env_file removal — individual env vars instead of full .env.dev
2. Prisma engine binary copy — find+cp in Dockerfile builder stage
3. Healthcheck localhost→127.0.0.1 — Alpine IPv6 resolution fix
4. DATABASE_URL password URL-encoding — %2F and %2B for special chars
5. Prisma CLI env sourcing — set -a && source .env.dev prefix for host commands
6. Worker Docker internal networking — REDIS_HOST/REDIS_PORT overrides in compose environment: block pointing to ${COMPOSE_PROJECT_NAME}_valkey:6379 (not host-mapped localhost:45196)

---

### Not yet built (deferred to Phase 7/8)
- [x] ~~EarthRanger API sync implementation (er-sync worker body)~~ — DONE (Phase 7)
- [x] ~~Dashboard: KPI cards, event breakdown charts, recent events feed, quick stats~~ — DONE (Phase 8 Batch 1 Item 1)
- [x] ~~Event Kanban Board: drag-and-drop state transitions, tenant-scoped updateState, optimistic UI, unit tests~~ — DONE (Phase 8 Batch 1 Item 2)
- [x] ~~Alert rule evaluation engine (alerts worker body)~~ — DONE (Phase 8 Batch 2; processor + tests shipped 2026-05-11; er-sync create-path enqueue integration shipped 2026-05-11 — closes the deferred follow-up)
- [x] ~~Interactive map (MapLibre/mapcn on /map page)~~ — DONE (Phase 8 Batch 2; geo router + vendor primitive + InteractiveMap wrapper + page route shipped 2026-05-11 across 4 sub-sessions 1.1/1.2a/1.2b/1.2c)
- [ ] Real-time notifications (WebSocket/SSE) — Phase 8 Batch 2 final item; Tier 3 split into SSE-1/SSE-2/SSE-3. **SSE-1 + SSE-2 SHIPPED to main 2026-05-17 in squash-merge commit 3710472 (pushed to origin/main). feat/sse-foundation branch deleted locally (never pushed to remote).** SSE-1: Valkey pub/sub publisher in packages/jobs + subscriber in apps/web + per-user SSE endpoint at /api/stream/notifications + alerts.processor publishes after tx commit. SSE-2: client SSE pipeline — EventSource wrapper (apps/web/src/lib/realtime/event-source-client.ts) + Zustand notification store (notification-store.ts) + REST polling fallback (notification-poller.ts) + orchestration hook (useNotificationStream.ts) with exponential backoff [1,2,4,8,16]s and polling fallback after 5 failed reconnects. SSE-3 (events stream endpoint + UI integration + War Room hook + securityVersion hardening + E2E) remaining on fresh branch feat/sse-events-and-hardening.
- [x] ~~PDF/CSV export endpoints~~ — DONE (Phase 8 Batch 2 Item 4 — SS-1 events, SS-2 patrols, SS-3 alert-rules, SS-4 notifications + alert-history; all 5 endpoints live under /api/exports/* with identical shape)
- [ ] Mobile app (not in scope for V1)
- [ ] README.md (generated by Phase 8 when PRODUCT.md fully implemented)

### Dashboard (Phase 8 Batch 1 Item 1)
- [x] tRPC router: `apps/web/src/server/trpc/routers/dashboard.ts` — 3 tenant-scoped procedures (kpis, eventBreakdown, recentEvents)
- [x] Router registration: added to `apps/web/src/server/trpc/routers/index.ts`
- [x] Dashboard page: `apps/web/src/app/(dashboard)/dashboard/page.tsx` — KPI cards, horizontal bar charts (law enforcement + monitoring), recent events feed, quick stats sidebar
- [x] shadcn/ui chart component: `apps/web/src/components/ui/chart.tsx` + recharts dependency
- [x] shadcn/ui config: `apps/web/components.json` (new-york style, CSS variables)

### Event Kanban Board (Phase 8 Batch 1 Item 2 — COMPLETE)
- [x] Kibo UI Kanban component: `apps/web/src/components/kibo-ui/kanban/index.tsx` — KanbanProvider, KanbanBoard, KanbanCards, KanbanCard, KanbanHeader (TypeScript strict fixes applied)
- [x] shadcn/ui scroll-area: `apps/web/src/components/ui/scroll-area.tsx` (React 19 ComponentRef compat)
- [x] Events page rewrite: `apps/web/src/app/(dashboard)/events/page.tsx` — 3-column Kanban (New/Active/Resolved), drag-and-drop state transitions, optimistic UI with rollback on error, priority badges, stats bar
- [x] Dependencies: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, @radix-ui/react-scroll-area
- [x] tRPC `event.updateState` mutation — tenant-scoped updateMany with FORBIDDEN guard when tenantId absent
- [x] tRPC `event.stats` query — tenant-scoped counts by state (total, newEvents, active, resolved)
- [x] Unit tests: `apps/web/src/server/trpc/routers/__tests__/event.test.ts` — 4 tests (happy path, tenant scoping, FORBIDDEN, schema validation), all passing
- [x] vitest upgraded ^2.1.8→^4.1.5 — resolves Vite 8 incompatibility from root pnpm override
- [x] Two-stage review: Stage 1 PASS (spec compliance), Stage 2 PASS (code quality)

### Alert Rules + Notification Center (Phase 7 Feature Update — COMPLETE)
- [x] Alert Rules page: `apps/web/src/app/(dashboard)/alerts/page.tsx` — list, create/edit dialog, severity Select, channel multi-select (in_app + email), isActive Switch toggle, inline delete confirm. Backed by `alertRule` tRPC router (already on main: list/create/update/delete with tenant scoping + adminProcedure for writes)
- [x] Notification Center page: `apps/web/src/app/(dashboard)/notifications/page.tsx` — full rewrite from 8-line stub. Chronological list (newest first), type filter (critical/warning/info/system + "all"), priority color indicators (red/orange/blue/gray dot + badge), unread state (border-left + "New" pill), mark-individual-read (Mark read button on unread items), mark-all-read button, click-through to `/events/{eventId}` when notification has eventId
- [x] Sidebar unread badge: `apps/web/src/components/layout/sidebar.tsx` — `notification.unreadCount` query with 30s refetchInterval + 15s staleTime, pill badge on /notifications nav item with "99+" cap and aria-label
- [x] 6 shadcn primitives added: `apps/web/src/components/ui/{dialog,dropdown-menu,select,separator,switch,tabs}.tsx` — React 19 ComponentRef from the outset, dropdown-menu CheckboxItem `checked` conditionally spread for exactOptionalPropertyTypes:true
- [x] Dependencies: @radix-ui/react-{dialog,dropdown-menu,select,separator,switch,tabs}
- [x] Unit tests: `apps/web/src/server/trpc/routers/__tests__/alertRule.test.ts` (6 tests — list+tenant, list+filter, create+RBAC, create non-admin rejection, update tenant-scoped, delete non-admin rejection); `apps/web/src/server/trpc/routers/__tests__/notification.test.ts` (5 tests — list+tenant+user, list+filter, markRead, markAllRead, unreadCount+FORBIDDEN). Plus typed `partial<T>` helper added to alertRule/notification/event test files for vitest matcher type safety
- [x] Lint cleanup: alerts/page.tsx tightened against Prisma schema (creator + isActive non-nullable), bulk React.ElementRef→React.ComponentRef across vendored shadcn primitives (28 errors fixed in one sed), dropdown-menu strict-boolean for optional `inset` prop
- [x] Two-stage review: Stage 1 PASS (spec compliance with 3 deferrals logged), Stage 2 PASS (code quality)
- [x] Final: lint 0, typecheck 0, 17/17 tests passing in 429ms

#### Spec deferrals from this branch (logged in CHANGELOG_AI 2026-05-08)
- [ ] **Alert history log** (PRODUCT.md L182) — not implemented; needs separate scope decision (notifications view filtered by alertRuleId, or dedicated page)
- [ ] **Filter type spec/schema alignment** (PRODUCT.md L189 "event alert, system alert, escalation, warning" vs schema enum `critical | warning | info | system`) — implementation uses schema enum; recommend updating PRODUCT.md L189 (1-line edit) to resolve drift
- [ ] **Notification → patrol click-through** (PRODUCT.md L187) — events implemented; patrol requires `Notification.patrolId` FK migration (current schema has only `eventId`)

---

### Interactive Map (Phase 8 Batch 2 — COMPLETE)
- [x] Geo tRPC router: `apps/web/src/server/trpc/routers/map.ts` — 4 tenant-scoped read-only procedures (getBounds, getSubjects, getPatrolAreas, getEvents) with L6 guardrails. Registered on appRouter.
- [x] Router tests: `apps/web/src/server/trpc/routers/__tests__/map.test.ts` — 10 unit tests covering tenant isolation, role-based access, geo filtering. All pass.
- [x] mapcn vendor primitive: `apps/web/src/components/ui/map.tsx` — 1844-line MapLibre GL primitive (MIT) installed via `npx shadcn@latest add @mapcn/map`. File-level `/* eslint-disable */` + `// @ts-nocheck` headers applied — registry-managed, see DECISIONS_LOG ("mapcn Vendor File Lint/TS Suppression").
- [x] InteractiveMap wrapper: `apps/web/src/components/map/InteractiveMap.tsx` — 25-line `"use client"` component, centered on Banda Sea `[127.5, -2.5]` zoom 6, props surface `className?: string` only. No data layers, no tRPC calls, no markers (deferred to follow-up).
- [x] Map page route: `apps/web/src/app/(dashboard)/map/page.tsx` — server component renders `<InteractiveMap />` inside full-viewport rounded container.
- [x] Coordinate convention locked: `[lon, lat]` (mapcn primitive contract) — see DECISIONS_LOG from sub-session 1.2a.
- [x] Two-stage review: Stage 1 PASS (PRODUCT.md L191 map view served, tenant-scoped data API ready). Stage 2 PASS for map files; pre-existing user-dialog lint errors documented + deferred to `fix/user-dialogs-strict-mode`.
- [x] Sub-session decomposition: 1.1 (router, Sonnet) → 1.2a (mapcn install, Sonnet) → 1.2b (wrapper, Opus direct) → 1.2c (page route + suppression + governance, Opus direct). Applied per memory-governance.md §5 Thrashing Recovery.
- [x] **Data layer wiring — subjects + events markers (2026-05-12, commit f041215).** Subjects render as emerald-500 circular markers (gray-400 if stale); events render as 45°-rotated diamonds colored by EarthRanger priority tier (red-600/orange-500/amber-400/sky-400). Null lat/lon filtered client-side for type narrowing. Tooltips via `MarkerTooltip`.
- [x] **Patrol tracks wiring (2026-05-12, commit e1e2d8a).** PatrolSelector shadcn `<Select>` overlay (top-left, `absolute z-10 max-w-xs`) queries `trpc.patrol.list({ state: "open", limit: 200 })`. Selected patrol id triggers `trpc.map.patrolTracks.byPatrolId` query; result mapped to `[lon, lat]` tuples and rendered as `<MapRoute color="#2563eb" width={3} opacity={0.85} />` when ≥2 valid points. MapRoute cleans up its own MapLibre source/layer on patrol change via existing useEffect cleanup. Sentinel `"__none__"` value used for clear-selection option.
- [x] **Patrol-area polygons (2026-05-12, commit 7250039).** New `apps/web/src/components/map/MapPolygon.tsx` (119 lines) — client component using `useMap()` to imperatively `addSource` (geojson Feature wrapping the stored Polygon/MultiPolygon) + two `addLayer` calls (fill at 0.2 opacity, line outline at 0.8 opacity 1.5px width), both colored by per-area `colorHex`. InteractiveMap queries `trpc.map.patrolAreas.list({ activeOnly: true })` and maps each result to `<MapPolygon>` rendered BEFORE `<MapRoute>` for correct z-order (track line on top). Prisma `Json` → `GeoJSON.Polygon | GeoJSON.MultiPolygon` cast routed through `unknown`. **MAP FEATURE GROUP COMPLETE.**

### Alert Rule Evaluation Engine (Phase 8 Batch 2 — COMPLETE)
- [x] Processor: `packages/jobs/src/processors/alerts.processor.ts` — `evaluateAlerts(job)` exported. Tenant-scoped event load → active rule load → match on `conditionJson.eventTypeId + minPriority` → recipient fallback to super_admin/site_admin → atomic Prisma `$transaction` creates Notification + AuditLog (action `ALERT_FIRED`) per rule×recipient. Returns `{rulesEvaluated, rulesMatched, notificationsCreated}`.
- [x] Tests: `packages/jobs/src/__tests__/alerts.processor.test.ts` — 5 vitest cases (missing tenantId rejection, no-active-rules, match+recipient happy path with notification + audit log assertions, match+no-recipient yields zero without opening transaction, transaction-failure atomic no-partial-commit). All pass in 132ms.
- [x] Worker registration: pre-existed in `packages/jobs/src/start-workers.ts:12` from Phase 4 scaffold. `processAlert` deprecated re-export preserved at end of processor file for compat.
- [x] **Enqueue integration shipped 2026-05-11** — actual integration site is `er-sync.processor.ts syncEvents`, not the (non-existent) `event.create` tRPC mutation. Refactored `syncEvents` to split `upsert` into `findUnique` + `create`/`update` so create-vs-update is distinguishable. `enqueueAlert({tenantId, userId:"system", alertRuleId:"", eventId, priority})` is called on the create path only, wrapped in try/catch so a queue outage never fails the sync. 5 new er-sync tests cover create-path, update-path, enqueue-on-create-only, no-enqueue-on-update, sync-succeeds-on-enqueue-failure.

### Exports (Phase 8 Batch 2 Item 4 — IN PROGRESS)
- [x] **SS-0: Foundation primitives (2026-05-12, commit 4caa449).** 5 shared helpers in apps/web/src/server/lib/: route-auth.ts (Auth.js v5 session verifier — `requireRouteAuth()` returns `{userId, tenantId, roles}`, throws RouteAuthError with 401 NextResponse), export-csv.ts (~30-LOC RFC-4180 renderer with UTF-8 BOM + CRLF endings), export-pdf.tsx (React-PDF Document component + `renderExportPdf()` async Buffer wrapper), export-audit.ts (writeExportAudit → DATA_EXPORT AuditLog row with filterHash entityId + changesJson={format, rowCount}), export-filename.ts (`{entity}-{tenantSlug}-{YYYYMMDD-HHmmss}.{ext}` UTC). 15 unit tests across 3 test files (10 csv + 2 pdf + 3 audit). @react-pdf/renderer added to deps. vitest.config.ts gained @vitejs/plugin-react to support JSX in `.tsx` test files (Next.js tsconfig sets jsx:preserve).
- [x] **SS-1: Events export (2026-05-12, branch feat/exports-events — squash-merge pending).** Route Handler at apps/web/src/app/api/exports/events/route.ts: manual auth via `requireRouteAuth()`, rate limit via `rateLimiters.upload.check(userId)` (20/min), validates state+priority via reused `eventListFilters` (exported from event router), fetches Tenant.slug+name for filename + PDF header, prisma.event.findMany with `take: 10001` for overflow detection (HTTP 413 on >10000), sha256 filterHash from sorted-keys JSON, flattens to 10-column EventRow (id, serial, title, state, priority, eventType.display+category, reportedByName, reportedAt ISO, createdAt ISO), branches on `format=csv|pdf`, writes DATA_EXPORT audit log AFTER success. New helper apps/web/src/lib/exports.ts `buildExportUrl(entity, filters, format)`. /events page header gains Export CSV + Export PDF outline buttons (`<Button asChild><a download href=...>`). 6 new route-handler tests (CSV byte-level BOM + tenant scoping, PDF content-type, 401 missing session, 413 overflow, audit shape with 64-hex filterHash, filter propagation). Full web suite: 60/60 (was 54). Lessons: 🟡 fix — Response.text() strips UTF-8 BOM during decoding; assert via arrayBuffer() byte-level check.
- [x] **SS-2: Patrols export (2026-05-12, branch feat/exports-patrols — squash-merge pending).** Route Handler at apps/web/src/app/api/exports/patrols/route.ts: same shape as SS-1 events handler, substituting `patrol` for `event`. Manual auth via `requireRouteAuth()`, rate limit via `rateLimiters.upload.check(userId)` (20/min), validates state+patrolType via reused `patrolListFilters` (exported from patrol router), fetches Tenant.slug+name, prisma.patrol.findMany with `take: 10001` for overflow detection (HTTP 413 on >10000), sha256 filterHash from sorted-keys JSON, flattens to 10-column PatrolRow (id, serialNumber, title, patrolType, state, boatName, startTime ISO, endTime ISO, totalDistanceKm, createdAt ISO), branches on `format=csv|pdf`, writes DATA_EXPORT audit log AFTER success. /patrols page header gains Export CSV + Export PDF outline buttons (`<Button asChild><a download href={buildExportUrl("patrols", {}, format)}>`); page no longer a stub. 6 new route-handler tests (CSV byte-level BOM + tenant scoping, PDF content-type, 401 missing session, 413 overflow + no audit row, audit shape with 64-hex filterHash, state+patrolType filter propagation). Full web suite: 66/66 (was 60). No new lessons — SS-1's BOM-via-arrayBuffer 🟡 fix carried forward verbatim.
- [x] **SS-3: Alert Rules export (2026-05-13, branch feat/exports-alert-rules — squash-merge pending).** Route Handler at apps/web/src/app/api/exports/alert-rules/route.ts: same shape as SS-1/SS-2 handlers, substituting `alertRule` for the entity. Manual auth via `requireRouteAuth()`, rate limit via `rateLimiters.upload.check(userId)` (20/min), validates query-string `isActive` (coerced from string to boolean before safeParse) via reused `alertRuleListFilters` (exported from alertRule router), fetches Tenant.slug+name, prisma.alertRule.findMany with `include: { creator: { select: { id, fullName } } }` + `take: 10001` for overflow detection (HTTP 413 on >10000), sha256 filterHash from sorted-keys JSON, flattens to 9-column AlertRuleRow (id, name, condition=JSON.stringify(conditionJson), channels=notificationChannels.join(", "), isActive="true"/"false", creatorName=creator.fullName, creatorId=createdBy, createdAt ISO, updatedAt ISO), branches on `format=csv|pdf`, writes DATA_EXPORT audit log AFTER success. /alerts page header gains Export CSV + Export PDF outline buttons positioned before View History + New Rule (`<Button asChild variant="outline" size="sm"><a download href={buildExportUrl("alert-rules", {}, format)}>`). 6 new route-handler tests (CSV byte-level BOM + tenant scoping + channels-joined-comma + escaped-JSON conditionJson body assertions, PDF content-type, 401 missing session, 413 overflow + no audit row, audit shape with 64-hex filterHash + entityType="alert-rules", isActive filter propagation with boolean coercion verified). Full web suite: 72/72 (was 66). No new lessons — SS-1's BOM-via-arrayBuffer 🟡 fix and SS-2's lint patterns carried forward. One trivial lint fixup: removed unnecessary `?.` + `??` on `r.creator.fullName` (Prisma include with required FK narrows to non-null).
- [x] **SS-4: Notifications + Alert History (2026-05-13, branch feat/exports-audit-views — squash-merge pending).** Two Route Handlers in one branch. (1) `apps/web/src/app/api/exports/notifications/route.ts`: same shape as SS-1/2/3 with TENANT+USER scoping (notifications are per-user), reuses `notificationListFilters` (exported from notification router) for isRead boolean + notificationType enum validation; query-string isRead coerced "true"/"false"/undefined → boolean before safeParse; prisma.notification.findMany with `include: {alertRule, event, patrol}` + take 10001; flattens to 9-column NotificationRow (id, title, message, type, isRead, alertRuleName, eventTitle, patrolTitle, createdAt ISO) with relation columns using nullish coalescing to "" since alertRule/event/patrol are optional FKs. (2) `apps/web/src/app/api/exports/alert-history/route.ts`: same shape with tenant-only scoping, reuses `alertHistoryListFilters` (exported from alertHistory router) for alertRuleId filter; prisma.alertHistory.findMany with `include: {alertRule, event}` + take 10001 + `orderBy: {firedAt: "desc"}`; flattens to 8-column AlertHistoryRow (id, ruleNameSnapshot, eventTitleSnapshot, matchedPriority as string, recipientCount as string, alertRuleCurrent=alertRule?.name ?? "(deleted)", eventCurrent=event?.title ?? "(deleted)", firedAt ISO) — exposes both the immutable snapshot fields AND the current relation values since alertHistory rows survive deletion of FK targets via SetNull onDelete. Both handlers: manual auth + rateLimiters.upload (20/min) + sha256 filterHash + CSV/PDF branches + DATA_EXPORT audit AFTER success. /notifications page header: Export CSV + Export PDF buttons inserted before the existing Select + Mark all as read, pass `notificationType: typeFilter==="all"?undefined:typeFilter` so the export matches the user's filtered view. /alerts/history page header: wrapped existing Back to Alert Rules in flex group, prepended Export CSV + Export PDF (no filter — page does not yet expose alertRuleId filter UI). 12 new route-handler tests (6 per route — CSV byte-level BOM + tenant scoping + flattened relation columns assertion for notifications, snapshot column assertions for alert-history; PDF content-type, 401 missing session, 413 overflow + no audit row, DATA_EXPORT audit shape with 64-hex filterHash + entityType, filter propagation into prisma.where with boolean coercion for notifications and alertRuleId string for alert-history). Full web suite: 84/84 (was 72). No new lessons — SS-1's BOM-via-arrayBuffer 🟡 fix and SS-3's lint patterns carried forward verbatim (zero fixups this session).

### Phase 8 Batch 5 — v2 Loop-Closers (IN PROGRESS)
- [x] **Sub-batch 5.1a: Area-derivation pure functions (2026-05-20, squash-merged to main as 6686042).** First sub-batch of Phase 8 Batch 5 Item 1 (AreaBoundary derivation algorithm). Ships the pure-logic LEAF layer in `packages/shared/src/lib/area-derivation/`: `matchByName(areaName, boundaries)` (exact name + alias lookup, case-insensitive, trimmed, filters isEnabled=true; name match wins over alias match; alias order is tiebreaker), `findNearestBoundary(point, boundaries, thresholdKm?=5)` (edge-distance to nearest Polygon outer ring OR LineString segment; equirectangular local projection for foot-of-perpendicular + haversine great-circle; defensive null on invalid lat/lon, empty boundaries, malformed geometry via console.warn), `deriveArea({areaName?, point?}, boundaries)` composite returning `{areaBoundaryId, matchedVia: 'name' | 'nearest' | null}` per v2 spec L531-L561 algorithm (name first → nearest fallback → NULL preserves area_name verbatim). Exported helpers `haversineKm` + `pointToSegmentDistanceKm` for testability. `AreaBoundaryForDerivation` structural subset type decouples pure functions from Prisma client + full v2 schema fields — callers in 5.1b–5.1e project full AreaBoundary rows down. Zero external geo deps (no turf/geolib/mapbox) — math implemented by hand, accurate to <0.1% within 5km threshold per spherical earth approximation. `matchedVia` exposed for 5.1b AuditLog observability per v2 L561 ("derivation job is a load-bearing piece of data quality"). Vitest bootstrap added to packages/shared (previously had no test runner — minimum-viable config matching packages/jobs style: node env + src/**/__tests__/**/*.test.ts pattern + --passWithNoTests). 55 new vitest tests across 3 files cover all v2 L561 mandate cases: match-by-name (17 — exact, case difference, alias, whitespace, disabled skip, empty/null, name-priority-over-alias); find-nearest-boundary (25 — haversine Manila→Cebu ±1%, perpendicular/clamped/past-endpoint, interior polygon returns nearest-edge NOT zero, within/at-edge/beyond threshold, custom threshold, LineString geometry, multiple boundaries returns nearest not first, disabled skip, malformed geometry skip, invalid lat/lon, empty boundaries); derive-area (13 — name-match path, nearest-fallback path, name precedence over nearest, no inputs, partial inputs, beyond threshold). Verification: typecheck 0 (packages/shared) / lint 5/5 0 warnings / vitest 350 passing across 41 files (workspace was 295/38; +55 new in shared). 11 files in feat/area-derivation-functions (squash-merged + deleted): 3 implementation + 1 barrel + 1 types subset + 3 test files + vitest.config.ts + package.json (added test script + vitest devDep) + pnpm-lock.yaml. TDD discipline: RED→GREEN→REFACTOR strictly followed per brief; no `any` types introduced; only `console.warn` in production code (defensive geometry skip). Two-stage review: Stage 1 spec compliance against v2 L531-L561 PASS, Stage 2 code quality PASS. Dispatch experience: Opus 4.7 executor reported STATUS: DONE accurately on first dispatch — no salvage required (contrasting with 4.1e executor's silent mid-brief drop at step 7/11). Confirms [[feedback_marine_guardian_opus_executor_default]] doctrine: brief format with pre-inlined v2 spec + explicit file list + TDD ordering + verification checklist proved sufficient. Deferred to subsequent sub-batches: applyAreaDerivation persistence helper that writes back areaBoundaryId + areaDerivedAt + logs matchedVia to AuditLog (5.1b); BullMQ `area-rederive` queue + processor + worker registration + concurrency cap 50/sec per v2 L545 (5.1c); sync-engine inline re-derive on `area_name` change per v2 L546 + AreaBoundary CUD enqueue (create/update/delete/enable/disable fans out rederive jobs to ALL Event+Patrol+FuelEntry rows in tenant) per v2 L545 (5.1d); super-admin manual-rebuild tRPC mutation + UI button on /admin/area-boundaries (5.1e).
- [x] **Sub-batch 5.1b: applyAreaDerivation persistence helper (2026-05-20, squash-merged to main as c4a14a9).** Second sub-batch of Phase 8 Batch 5 Item 1. Wraps 5.1a pure deriveArea with DB I/O. New file `apps/web/src/server/sync/area-derivation.ts` (158 LOC, single export `applyAreaDerivation(prisma, entity: 'event'|'patrol'|'fuelEntry', id: string): Promise<{areaBoundaryId, matchedVia}>`). Per-entity branching: Event loads tenantId + areaName + locationLat + locationLon (only entity with coordinates) — supports both name-match AND nearest-fallback paths; Patrol loads tenantId + areaName only (schema has NO coordinate fields) — name-match only path, deriveArea called with point=null; FuelEntry loads tenantId + areaName only (schema has areaName NOT NULL + NO areaDerivedAt column) — name-match only, write-back omits areaDerivedAt key entirely. Boundary load: `prisma.areaBoundary.findMany({where: {tenantId, isEnabled: true}, select: minimal})` — scoped to row's tenantId for isolation. Architect-confirmed Option A: NO AuditLog write in 5.1b (deferred to 5.1e where ctx.session.userId is available) — reason: AuditLog.userId is NOT NULL but sync engine + worker contexts have no user; areaDerivedAt timestamp + matchedVia return value provide sufficient observability for automatic derivation. NO transaction wrapping (load+write are idempotent, last write wins for same input). NO try/catch (findUniqueOrThrow on invalid id is programmer bug, exceptions propagate). Type contract: `PrismaClientLike = ExtendedPrismaClient` alias exported for callers in 5.1c/5.1d/5.1e + test mocks. Vitest file `apps/web/src/server/sync/__tests__/area-derivation.test.ts` (288 LOC, 8 cases) covers: event+name-match writes both fields; event+nearest-fallback returns matchedVia='nearest' at point ~4.4km from polygon south edge within 5km threshold; event+no-match still writes areaBoundaryId=null+areaDerivedAt=Date (timestamp records "we tried"); patrol+name-match verifies findUniqueOrThrow select call does NOT include locationLat/Lon; fuelEntry+name-match verifies update payload does NOT include areaDerivedAt key (`'areaDerivedAt' in updateCall.data === false`); tenant isolation verifies findMany where clause carries row's tenantId not constant; alias match (TRNP → Tubbataha Reefs Natural Park); isEnabled filter on findMany. All mocks via vi.fn() with mockResolvedValue + structural cast to PrismaClientLike. Modified packages/shared/package.json: +1 export path "./lib/area-derivation" closes 5.1a gap where files shipped but no package export declared (minimal additive fix, no new deps). Verification: lint 0/0 warnings, typecheck 0 errors, web tests 276/276 PASS (was 268 + 8 new). Branch feat/area-derivation-persistence squash-merged to main as c4a14a9 + deleted per Rule 23. 5.1c unblocked.
- [x] **Sub-batch 5.1c: BullMQ area-rederive queue + processor + worker (2026-05-20, squash-merged to main as 3b4145f).** Third sub-batch of Phase 8 Batch 5 Item 1. Wraps 5.1b applyAreaDerivation with BullMQ infrastructure so async re-derivation can be triggered from sync engine (5.1d), admin manual-rebuild (5.1e), and future bulk operations. New files: `packages/jobs/src/queues/area-rederive.queue.ts` (36 LOC — `areaRederiveQueue` Queue instance, `AreaRederiveJobData` type, `enqueueAreaRederive(data): Promise<Job>` helper; queue name `'area-rederive'`); `packages/jobs/src/processors/area-rederive.processor.ts` (63 LOC — `processAreaRederive(job): Promise<DerivationResult>`; instantiates platformPrisma + casts to ExtendedPrismaClient with explanatory comment; calls applyAreaDerivation(prisma, entity, id) + returns result for BullMQ result storage; no AuditLog write per Option A); `packages/jobs/src/workers/area-rederive.worker.ts` (46 LOC — `createAreaRederiveWorker(connection)` factory using base-worker.createWorker with limiter `{max:50, duration:1000}` per v2 L545 + concurrency AREA_REDERIVE_CONCURRENCY=10 exported constant). Three vitest files (queue 6 cases + processor 9 cases + worker 7 cases = 22 net new tests). Modified barrels: queues/types.ts (+AreaRederiveJobPayload + QUEUE_NAMES.AREA_REDERIVE), queues/index.ts (+5 LOC exports), processors/index.ts (+1 LOC export), workers/index.ts (+5 LOC worker/factory/constant exports), workers/base-worker.ts (+9 LOC optional limiter field, conditional spread, backward-compatible — existing 4 workers unaffected), start-workers.ts (+5 LOC — startAreaRederiveWorker registered as 5th worker), packages/jobs/src/index.ts (+17 LOC barrel re-exports incl. applyAreaDerivation for 5.1e admin tRPC consumption). Modified packages/jobs/package.json: +@marine-guardian/shared workspace dep (required by relocated helper which imports deriveArea), +`./lib/area-derivation` subpath export entry; pnpm-lock.yaml +3 lines. **Helper relocation**: applyAreaDerivation moved from apps/web/src/server/sync/ → packages/jobs/src/lib/ (97% git rename similarity on impl + 94% on test). Reason: 5.1d sync engine lives in @marine-guardian/jobs and apps→packages dependency direction is one-way; relocation preserves blame. Architect Option A from 5.1b continues: NO AuditLog write in worker context (no userId; deferred to 5.1e). Verification (both on feat branch AND independently re-verified on main after squash-merge): jobs lint 0/0, jobs typecheck 0, jobs tests 57/57 PASS (was 35 baseline + 22 net), web typecheck 0, web tests 268/268 PASS (was 276; -8 relocated to jobs; total monorepo test count unchanged at 325). Third consecutive clean Opus 4.7 executor dispatch via Agent(model: "opus") — doctrine [[feedback_marine_guardian_opus_executor_default]] holds. Executor reported DONE_WITH_CONCERNS with 6 sound concerns (helper relocation, shared workspace dep add, ExtendedPrismaClient cast, base-worker limiter extension, two-commit branch collapsed via squash, concurrency=10 conservative default) — all accepted by architect. 17 files changed, +617 / -1 in branch (squash-merged as 3b4145f + force-deleted per Rule 23). 5.1d unblocked.
- [x] **Sub-batch 5.1d Area B: AreaBoundary CUD fan-out (2026-05-20, squash-merged to main as 068d32f).** Fourth sub-batch (B-side) of Phase 8 Batch 5 Item 1. v2 L545 large-blast-radius path. Adds `fanOutAreaRederive(tenantId, userId): Promise<{enqueued: number}>` private helper in apps/web/src/server/trpc/routers/areaBoundary.ts that queries Event + Patrol + FuelEntry findMany via Promise.all with explicit `where: { tenantId }` defense-in-depth scoping per security.md L5 reports/exports rule, then enqueues area-rederive per row via the 5.1c queue with entity casing "event"|"patrol"|"fuelEntry" matching the 5.1b AreaDerivationEntity contract. Wired into create unconditionally, into update + delete gated on `result.count > 0` to avoid spurious fan-outs on no-op writes. Return shape extended from raw Prisma result to `{boundary|result, fanOut}`. userId threaded from ctx.userId since adminProcedure guarantees it and BaseJobPayload requires it. 2 files changed, +241 / -4 in feat/5.1d-area-boundary-cud-fanout branch (squash-merged as 068d32f + force-deleted per Rule 23). +6 new test cases (web 268→274). Area A (sync inline re-derive on areaName change — v2 L546) DEFERRED to a future sub-batch: precondition unmet — ErEvent/ErPatrol payload types don't include area_name and syncEvents/syncPatrols data spreads don't map it, so the "incoming vs persisted areaName" comparison the original brief specified has no source.
- [x] **Sub-batch 5.1e: Admin manual-rebuild tRPC mutation + minimal UI stub (2026-05-20, squash-merged to main as 88744ed).** Fifth and final sub-batch of Phase 8 Batch 5 Item 1. `areaBoundary.rebuild` adminProcedure mutation reuses 5.1d fanOutAreaRederive helper to enqueue area-rederive for every Event + Patrol + FuelEntry in the target tenant via the 5.1c BullMQ queue (50/sec rate limiter). RBAC: super_admin may target any tenantId (cross-tenant rebuild = action `PLATFORM:AREA_REBUILD` per security.md superadmin convention); site_admin must omit tenantId or match own (cross-tenant attempt = FORBIDDEN; own-tenant = action `AREA_REBUILD`). Writes immutable AuditLog row with userId from ctx.userId, tenantId = targetTenantId, entityType="AreaBoundary", entityId=targetTenantId (operation targets tenant universe), changesJson={enqueued, scope: platform|tenant}. Returns {tenantId, enqueued, action}. UI: minimal stub button on existing patrol-areas placeholder page (no /admin/area-boundaries CRUD page exists yet — deferred to a future Item) using shadcn/ui Dialog confirm + inline success/error feedback + useSession-based role gating. 4 files changed, +309/-1 in feat/5.1e-area-boundary-rebuild-admin (single commit 9b382a1 → squash-merge as 88744ed). +7 new test cases (web 274→281). Main-session Opus 4.7 implementation — dispatch path skipped because 5.1d post-mortem flagged Opus subagent stall risk and 5.1e scope was small (~50K actual including orientation) so dispatching offered no benefit. 🎯 Item 1 COMPLETE for scope under remit. 5.1d Area A remains deferred pending area_name ingestion in ER sync engine.

### Phase 8 Batch 5 Item 2 — PatrolTrack Materialization job (🎯 IN PROGRESS — 1 of 3 sub-batches shipped)
- [x] **Sub-batch 5.2a: materializePatrolTrack helper + ER client fetchSubjectTracks (2026-05-20, squash-merged to main as d2a1dd1).** First sub-batch of Phase 8 Batch 5 Item 2. Closes the loop on the 4.1b PatrolTrack scaffold stub. Real implementation of `materializePatrolTrack(prisma: ExtendedPrismaClient, patrolId: string): Promise<MaterializationResult>` in `packages/jobs/src/lib/patrol-track-materialization.ts`. Loads patrol + first segment via `prisma.patrol.findUniqueOrThrow({select:{id,tenantId,startTime,endTime,segments:{select:{leaderErId,actualStart,actualEnd,scheduledStart,scheduledEnd},orderBy:{scheduledStart:"asc"},take:1}}})`. Three early-return skip paths (no exception, observable via `result.skipped + skipReason`): `no_segment` (patrol has no segments OR no resolvable time range), `no_leader` (segment[0].leaderErId is null), `no_credentials` (tenant missing earthrangerUrl or earthrangerDasToken). Tenant credentials decrypted via `@marine-guardian/db.decrypt`; `earthrangerTrackToken` preferred when present (passes through to `EarthRangerClient` constructor as optional 3rd arg). Time range fallback: `segment.actualStart/End → scheduledStart/End → patrol.startTime/endTime → new Date()` (open patrol → fetch up to now). Calls new `EarthRangerClient.fetchSubjectTracks(subjectId, since, until): Promise<ErTrackResponse>` → GET `/api/v1.0/subject/{id}/tracks/?since=&until=` using `trackToken` (or main `token` when no separate track token configured). Summarises features into `pointCount` (sum of coords across all LineString features) + `hasTimestamps` (defensive: ANY feature with missing or length-mismatched `coordinateProperties.times` flips the whole track to false) + `lastTrackTime` (max parseable timestamp across all features, null when `hasTimestamps=false`). Atomically `upsert`s `PatrolTrack` on `patrolId` (unique constraint) with `source="er_api"`, `fetchedAt=new Date()`, and the full GeoJSON FeatureCollection in `trackGeojson` (Prisma Json column). Idempotent per v2 PRODUCT.md §1043 — re-running for the same patrol overwrites the existing row. NO AuditLog write per Option A scope split (automatic materialization has no user; deferred to 5.2c admin manual-rebuild). **ER client extension**: `earthranger-client.ts` gains `ErTrackCoordinate3 + ErTrackFeature + ErTrackResponse` interfaces (exported); constructor signature extended with optional `trackToken?: string` arg defaulting to main `token` (backward compatible — all existing callers including er-sync.processor.ts at line 37 work unchanged); private `request<T>(path, bearer?)` refactored to accept override bearer. **Helper relocation** via `git mv` from `apps/web/src/server/sync/patrol-track-materialization.ts` (35 LOC stub) → `packages/jobs/src/lib/patrol-track-materialization.ts` (301 LOC real impl). Same architectural arc as 5.1c area-derivation relocation — 5.2b BullMQ processor lives in @marine-guardian/jobs and apps→packages dependency direction is one-way per Rule 13. `packages/jobs/src/index.ts` re-exports `materializePatrolTrack + MaterializationResult + MaterializationSkipReason` so 5.2b processor + 5.2c admin tRPC mutation can consume through the package boundary. `packages/jobs/package.json` adds `./lib/patrol-track-materialization` subpath export. 14 vitest cases in `__tests__/patrol-track-materialization.test.ts`: happy path (3-point single feature, summary correct, upsert payload correct), all 3 skip paths (no_segment, no_leader, no_credentials × 2 cases for URL vs token), trackToken+fallback paths (constructor called with correct args including undefined fallback), patrolEnded mirrors patrol.endTime, hasTimestamps=false when feature lacks `coordinateProperties.times`, hasTimestamps=false when times.length ≠ coords.length, multi-feature pointCount aggregation + max lastTrackTime, empty-features still upserts with pointCount=0, time-range fallback prefers actual over scheduled over patrol-level. All mocks via vi.mock — no real DB or HTTP. Verification: jobs lint 0/0, jobs typecheck 0, jobs tests 71/71 PASS (57 baseline + 14 net), web typecheck 0 (cross-package import surface intact via @marine-guardian/jobs root barrel), web lint 0/0 with --max-warnings 0, web tests 281/281 PASS unchanged (jobs-only sub-batch). Total monorepo: 338 → 352 (+14). 6 files changed +910/-39 in feat/5.2a-patrol-track-materialization single commit b38d951 → squash-merged to main as d2a1dd1 → branch force-deleted per Rule 23. Main-session Opus 4.7 implementation continues per STATE.md 5.1e/5.1d post-mortem recommendation — dispatch-mechanism issue from 5.1d (2 consecutive Opus subagent stalls with `:agentId:` continuation hint) remains unresolved, so main-session is the safe fallback for cleanly-scoped sub-batches. Two errors caught + fixed before merge: (1) ESLint @typescript-eslint/no-unnecessary-type-assertion on `as PatrolForMaterialization` after Prisma select narrowing — fixed by switching to typed `const patrol: PatrolForMaterialization = await ...` binding; (2) `makeTenant` test helper used nullish coalescing for override-vs-default which silently overrode explicit `null` to default (two `no_credentials` cases failed because `{ earthrangerUrl: null } → "enc:..."` not null) — fixed by `in`-operator pattern. Logged 🟡 fix entry to lessons.md (applies to any future test factory accepting nullable Partial<T> overrides). 5.2b unblocked.

### Phase 8 Batch 4 — v2 Foundation Tables (🎯 COMPLETE 2026-05-20)
- [x] **Sub-batch 4.1a: AreaBoundary scaffold (2026-05-19, branch feat/area-boundary-scaffold — squash-merge pending).** First sub-batch of the additive non-breaking v2 foundation work. Adds Prisma model `AreaBoundary` + 2 new enums (`BoundarySource{official,custom}`, `GeometryType{Polygon,LineString}`) + back-relations on Tenant (`areaBoundaries`) and User (`createdAreaBoundaries`). Migration 20260519183900_add_area_boundary creates the `area_boundaries` table with cuid PK, tenant FK (RESTRICT), name, aliases TEXT[], region, source default `custom`, geometry_type enum, geometry_geojson JSONB, is_enabled default true, override_official default false, nullable arcgis_reference_id, created_by_user_id FK (RESTRICT), timestamps, and indexes on tenant_id + (tenant_id, is_enabled). Shared Zod schemas: `areaBoundarySchema` + `createAreaBoundarySchema` (omits id/tenantId/createdByUserId/timestamps) + `updateAreaBoundarySchema` (`create.partial()`). New tRPC router `areaBoundary` mounted on appRouter with `list`/`getById` via tenantProcedure (paginated, filter by isEnabled + region) and `create`/`update`/`delete` via adminProcedure (super_admin + site_admin only). 6 router tests across 2 describes — list tenant scoping + filter assertion, create tenant+creator scoping, RBAC rejection for operator, update + delete tenant scoping. Full web suite 232/232 (was 226). Derivation algorithm, ArcGIS reference load, override_official auto-flip, audit wiring, Area Boundary Editor UI, and seed data all explicitly deferred to Batch 5+.
- [x] **Sub-batch 4.1b: PatrolTrack scaffold (2026-05-19, branch feat/patrol-track-scaffold — squash-merge pending).** Second sub-batch of the additive non-breaking v2 foundation work. Adds Prisma model `PatrolTrack` + 1 new enum (`TrackSource{er_api,cache}`) + back-relation `patrolTracks` on Tenant + one-to-one back-relation `track PatrolTrack?` on Patrol. Migration 20260519223000_add_patrol_track creates the `patrol_tracks` table with cuid PK, tenant_id FK (RESTRICT), patrol_id FK (CASCADE) with UNIQUE constraint enforcing the one-track-per-patrol invariant, nullable subject_id, since/until timestamps, track_geojson JSONB, has_timestamps default false, point_count default 0, nullable last_track_time, patrol_ended default false, source enum non-null, fetched_at timestamp, timestamps, indexes on tenant_id + (tenant_id, patrol_ended). Shared Zod schemas: `patrolTrackSchema` + 3 input schemas (`listPatrolTracksInputSchema` with optional patrolId/patrolEnded filters + cursor pagination, `getPatrolTrackByIdInputSchema`, `getPatrolTrackByPatrolIdInputSchema`). New tRPC router `patrolTrack` mounted on appRouter is READ-ONLY by design: `list`/`getById`/`getByPatrolId` all via tenantProcedure — no create/update/delete (writes flow exclusively through the Patrol Track Materialization job per PRODUCT.md §501-502 to keep the upsert invariant centralized). Stub `apps/web/src/server/sync/patrol-track-materialization.ts` added with `materializePatrolTrack(patrolId)` throwing "Not implemented — Batch 5 wiring"; header docblock documents the `needs_refetch` predicate per PRODUCT.md §1043, the per-tenant async-pool concurrency cap of 4, and planned integration with packages/jobs/src/processors/er-sync.processor.ts in Batch 5. 7 router tests across 3 describes (list tenant scoping, list patrolId filter, list patrolEnded filter, getById success path, getById cross-tenant returns null, getByPatrolId success path, getByPatrolId returns null when no materialized track). Full web suite 239/239 (was 232). Patrol Track Materialization real impl + Valkey cache layer + area-covered aggregation + Patrol Track Viewer Modal UI + track time math + Douglas-Peucker simplification + computed_distance_km/computed_duration_hours on Patrol + is_test_patrol/is_deleted/sync_needed flags all explicitly deferred to Batch 5+.
- [x] **Sub-batch 4.1c: FuelEntry + ReportExport scaffolds (2026-05-19, squash-merged to main as e972d82, pushed to origin).** See header (line 3) for full prose entry.
- [x] **Sub-batch 4.1d: NotificationRecipient split with data migration (2026-05-20, branch feat/notification-recipient-split — squash-merge pending).** Fourth sub-batch of the additive v2 foundation work — and the ONLY one with real data-migration semantics. Splits per-user read state OFF Notification ONTO NotificationRecipient per v2 spec L480-484: one Notification row, N NotificationRecipient rows. Schema delta on Notification: removed userId+isRead, added subjectId nullable FK to Subject (SET NULL); removed indexes user_id+is_read; added index subject_id. New NotificationRecipient model: cuid PK, notification_id FK CASCADE, user_id FK NO ACTION, is_read default false, read_at nullable, email_sent_at nullable, email_status enum default pending, created_at; 3 indexes (notification_id, (user_id, is_read), (user_id, created_at DESC)). New enum NotificationEmailStatus with 5 literal values (pending, sent, suppressed_by_cooldown, digested, failed). Back-relations updated: User.notifications → User.notificationRecipients; Subject.notifications added; Notification.recipients added. Migration 20260519233500_add_notification_recipient_split: 14-step DDL — CREATE enum + table + 3 indexes + 2 FKs FIRST, then backfill INSERT (1 NotificationRecipient row per existing Notification preserving user_id+is_read+created_at, using deterministic md5-derived ID for idempotency on re-run), THEN drop indexes + FK + columns on notifications + add subject_id + FK + index. Hand-written down.sql reverses in 10 steps with lossy restore (picks earliest recipient per notification when N>1). Pre-flight: SELECT COUNT(*) FROM notifications → 0 rows in dev → backfill ran as no-op but the SQL is correct for staging/prod safety. Plan corrections caught during main-session review against v2 spec L480-484: (1) notificationType STAYS on Notification (not moved per stale STATE.md plan text), (2) read_at field added to NotificationRecipient (omitted in plan), (3) email_status is a 5-value enum not a default-string. Router rewrite of apps/web/src/server/trpc/routers/notification.ts: all 4 procedures (list/markRead/markAllRead/unreadCount) now query prisma.notificationRecipient.* with nested join filter `notification: {tenantId: ctx.tenantId}` for tenant scoping + top-level `userId: ctx.userId` for user scoping; markRead+markAllRead set BOTH isRead=true AND readAt=new Date() per v2 spec L483; list returns FLATTENED shape so item.id surfaces as recipient.id and UI's markRead({id: n.id}) naturally targets the recipient. Tests: notification.test.ts went 6 → 10 cases with 3 NEW pathways (cross-user markRead silent reject — proves userId filter in WHERE is load-bearing, user-scoped markAllRead, flattened shape mapping). Consumers: exports/notifications/route.ts rewritten to query notificationRecipient.findMany with same nested-filter pattern; CSV's ID column still uses notification.id (not recipient.id) for backward-compat with older CSV consumers. UI page apps/web/src/app/(dashboard)/notifications/page.tsx needed ZERO edits — router's flattened return shape matches what the page consumed. SSE Route Handler + sidebar + realtime-store + poller all no-op (shape decoupled from DB column shape). 262 tests passing across 37 files (was 260; +2 net). typecheck 0 / lint 0. Migration applied cleanly to dev DB. 3 new lessons: 🔴 "prisma migrate dev hangs on stale advisory locks" + 🟤 "Backfill SQL should use deterministic md5 IDs for idempotency" + 🟤 "v2 spec is authoritative — verify against PRODUCT.md before trusting STATE.md plan text". Deferred: actual Notification creation flow (fan-out to N recipients per alert) — Batch 5+ AlertRule restructure. Email dispatch worker that flips emailStatus pending→sent/suppressed_by_cooldown/digested/failed — Batch 5+. Read-at rendering in UI ("Read 2 hours ago") — Batch 6+ UI polish. NotificationRecipient retention policy (1y per spec L645) — Batch 7+.
- [x] **Sub-batch 4.1e: Event/Patrol area attribution + Tenant ArcGIS fields (2026-05-20, squash-merged to main as 6687112).** Fifth and final sub-batch of the additive non-breaking v2 foundation work — **closes Batch 4**. Adds 3 columns to Event + 3 to Patrol (areaName + areaBoundaryId FK to AreaBoundary SET NULL + areaDerivedAt, all nullable) + 2 columns to Tenant (arcgisBoundaryUrl + arcgisBoundaryOutfields, both nullable; encrypted at app layer per existing earthrangerUrl precedent, plain TEXT in DB) + 2 back-relations on AreaBoundary (events + patrols) + 2 composite indexes (events_tenant_id_area_boundary_id_idx + patrols_tenant_id_area_boundary_id_idx for "items in this area" Per Area Report queries — Batch 6+). Migration 20260520010000_add_area_attribution_and_tenant_arcgis: 7-step additive DDL (ALTER TABLE tenants/events/patrols ADD COLUMN + AddForeignKey + CreateIndex) + lossless 7-step down.sql. All additive nullable — no backfill needed, no data-migration risk; fields stay NULL until the AreaBoundary derivation algorithm (`derive_area(row)` per v2 spec L516+L540) lands in Batch 5+. Plan correction caught against v2 spec L456+L463: STATE.md plan listed only `area_boundary_id + area_derived_at` — v2 spec also includes `area_name` (free-text from ER on Event, free-text "primary area" on Patrol, derived from objective field or start_location's nearest AreaBoundary). Added area_name to both per the same "v2 spec is authoritative" correction pattern caught in 4.1d. Note: areaName already pre-existed in packages/shared/src/types/event.ts + schemas/event.ts (latent shared/Prisma drift) — 4.1e resolves the drift by adding it to the Prisma schema. Shared types + schemas updated: types/{event,patrol,tenant}.ts + schemas/{event,patrol,tenant}.ts — 6 files, additive only (areaBoundaryId as `z.string().cuid().nullable()`, areaDerivedAt as `z.coerce.date().nullable()`, arcgisBoundaryUrl as `z.string().url().nullable()` matching earthrangerUrl pattern). Routers UNTOUCHED: apps/web/src/server/trpc/routers/event.ts + patrol.ts both use `include` (not `select`) on their Event/Patrol queries, so new schema fields auto-surface in responses with zero router code change. Tests: NEW apps/web/src/server/trpc/routers/__tests__/patrol.test.ts (6 cases — closes existing gap as patrol router previously had zero coverage; covers list/getById/stats with 4.1e area-attribution assertions baked in: fields surface as null when un-derived, fields surface populated when derivation has run, cross-tenant isolation, FORBIDDEN guard, tenant-scoped count grouping). Verification: typecheck 0 (except pre-existing packages/db Node-types baseline — identical to 4.1a-4.1d) / lint 5/5 0 warnings / vitest 268 passing across 38 files (was 262/37; +6 cases +1 file) / migration applied cleanly to dev DB. Dispatch experience: Opus 4.7 executor subagent reported "completed" but actually dropped at step 7 of 11 after writing schema.prisma + migration SQL + running pnpm prisma migrate dev. Salvageable per [[feedback_sonnet_thrash_inspect_before_redispatch]] doctrine — Architect verified partial work matched brief exactly + completed remaining 7 files (6 shared + patrol.test.ts) in main session. **🎯 BATCH 4 COMPLETE** — all 5 sub-batches shipped (4.1a AreaBoundary → 4.1b PatrolTrack → 4.1c FuelEntry+ReportExport → 4.1d NotificationRecipient split → 4.1e Event/Patrol/Tenant area attribution). v2 foundation tables in place. Deferred to Batch 5+: AreaBoundary derivation algorithm (Event area_boundary_id+area_derived_at set by sync job from area_name match OR nearest-boundary lookup; Patrol area_boundary_id+area_derived_at set from start_location nearestBoundary) — re-derive BullMQ job + manual rebuild button. Patrol Track Materialization job (per-patrol GPS track fetch from ER). ReportExport pdf-render queue + marine-guardian-pdf-renderer Docker service. Notification fan-out flow + email dispatch worker. Tenant sync-engine fields (15 fields). Event/Patrol enum changes (priority Int→enum, state new_event→new, Patrol.state expansion). AlertRule restructure. AuditLog impersonation fields.

### Next Step
SSE-1 + SSE-2 Real-time pipeline SHIPPED to main 2026-05-17 via squash-merge commit 3710472 (pushed to origin). Three feat-branch commits (a426c95 SSE-1 + 0cf0b49 SSE-2 RED + f64d88b SSE-2 GREEN) collapsed into one atomic feature commit. Server foundation + client pipeline live. Phase 8 Batch 2 remaining backlog:

1. **SSE-3: Events stream + UI integration + War Room hook + security hardening + E2E** — Tier 2-3, ~28K estimate (slightly larger than originally specced because SSE-2 deliberately scoped down to the 4 stub modules from RED; UI integration moved here). Scope:
   - `apps/web/src/app/api/stream/events/route.ts` — second SSE endpoint, tenant-scoped (no user filter — all operators see all events). Uses eventChannel(tenantId) from SSE-1 + same subscribeToChannel pattern as the notifications route.
   - Publisher integration at event-creation seam (er-sync processor or event router create) — uses eventChannel(tenantId) from SSE-1.
   - `apps/web/src/hooks/useEventStream.ts` — events SSE channel hook with tRPC query invalidation pattern mirroring useNotificationStream.
   - Sidebar UI wiring: `apps/web/src/components/layout/sidebar.tsx` replaces 30s refetchInterval with SSE-driven invalidate (built-in polling fallback covers reliability).
   - `/notifications` page UI wiring: invoke useNotificationStream + optional audio chime for `type === "critical"`.
   - `/events` page UI wiring: invoke useEventStream + live toast on new event.
   - Security: `securityVersion` check on heartbeat to close stream on role/tenant change (security.md L255).
   - Playwright E2E: trigger alert via API → assert notification appears <2s without refresh.
   - 4 new test files: events route + events publisher integration + securityVersion invalidation + E2E happy-path.
   - Branch options: `feat/sse-events-and-hardening` (squash-merge SSE-1+SSE-2 first; smaller PRs) OR extend current `feat/sse-foundation` (single Phase 8 Batch 2 completion PR for all three sub-sessions).

2. Open spec deferral: filter type drift (PRODUCT.md L189 enum mismatch) — 1-line human edit, not agent-side.

Shipped since the last "Next Step" rewrite (2026-05-16):
- ✅ `feat/exports-foundation` (4caa449) — SS-0 Export Foundation primitives merged
- ✅ `feat/exports-events` (SS-1, commit 0171309 + final-ship 1c783cd) — Events Export Route Handler + UI buttons merged + pushed to origin
- ✅ `feat/exports-patrols` (SS-2, commit 52869ae + final-ship e735686) — Patrols Export Route Handler + UI buttons merged + pushed to origin
- ✅ `feat/exports-alert-rules` (SS-3, commit 1e03188 + final-ship 0070eeb) — Alert Rules Export Route Handler + UI buttons merged + pushed to origin
- ✅ `feat/exports-audit-views` (SS-4, commit ab5a782 + final-ship 0b0d421) — Notifications + Alert History Export Route Handlers + UI buttons merged + pushed to origin; Item 4 (PDF/CSV Exports) COMPLETE
- ✅ `feat/sse-foundation` SSE-1 + SSE-2 — squash-merged to main 2026-05-17 as commit 3710472, pushed to origin/main, local branch deleted. Combined three feat commits (a426c95 SSE-1 server foundation + 0cf0b49 SSE-2 RED stubs + f64d88b SSE-2 GREEN client pipeline). Files: realtime-publisher.ts, realtime-subscriber.ts, /api/stream/notifications/route.ts, alerts.processor.ts publish hook, event-source-client.ts, notification-store.ts, notification-poller.ts, useNotificationStream.ts + their test files. 20 new SSE-2 client tests + 13 SSE-1 server tests, all GREEN.

No known blockers. All services operational. Main currently at b0b36c8 (chore scan recording 2026-05-17 re-scan verifying Foundation Bundle 3 plugins active) on top of 3710472 (SSE-1+SSE-2 squash-merge), both pushed to origin/main. 144 tests passing across all packages (117 web — +20 SSE-2 client + 27 jobs unchanged).
